---
aliases: [Creation Operator, Raising Operator, 产生算子, 升算符]
english_name: Creation Operator
tags: [物理学, 量子力学, 算符代数, 弦理论先修, 弦理论]
prerequisites:
  - "[[量子谐振子]]"
  - "[[量子算符]]"
  - "[[对易关系]]"
used_by:
  - "[[湮灭算符]]"
  - "[[Fock 空间]]"
  - "[[零点能]]"
  - "[[场的正则量子化]]"
  - "[[振子代数]]"
evidence_status: 教材事实
---

# 产生算符（Creation Operator）

## 一句话解释

产生算符是把量子谐振子的激发数提高一级的线性算符；在量子场论中，它还能表示向某个模式加入一个粒子量子。

## 为什么要学它

直接求解每个能级的微分方程很费力。产生算符把已知状态变成更高一级的状态，使整个谐振子能谱可由基态逐级构造。

自由量子场的每个 Fourier 模都像一个量子谐振子。场的产生算符用于构造多粒子态；弦的负频振子也扮演类似角色，用来构造弦的激发态。

## 直观图景

可以把谐振子的能级想成一架梯子。产生算符 $a^\dagger$ 像“向上一步”的操作：

$$
|0\rangle
\xrightarrow{a^\dagger}
|1\rangle
\xrightarrow{a^\dagger}
|2\rangle
\xrightarrow{a^\dagger}
\cdots
$$

这个比喻只表示能级和激发数的变化。它不表示空间里真的存在一架梯子，也不表示每次作用都一定创造一个新的基本粒子。

## 正式定义

一维量子谐振子的产生算符定义为：

$$
a^\dagger=
\sqrt{\frac{m\omega}{2\hbar}}
\left(
\hat x-
\frac{i}{m\omega}\hat p
\right).
$$

其中：

- $a^\dagger$：产生算符，也是湮灭算符 $a$ 的伴随算符；
- $\hat x$：位置算符；
- $\hat p$：动量算符；
- $m$：振子的质量；
- $\omega$：角频率；
- $\hbar$：约化 Planck 常数；
- $i$：虚数单位，满足 $i^2=-1$；
- $\dagger$：取伴随，包含复共轭和转置的推广。

根号前因子与括号中的量共同保证 $a^\dagger$ 无量纲。该定义采用标准的一维谐振子归一化约定。

## 与湮灭算符的对易关系

对应的湮灭算符为：

$$
a=
\sqrt{\frac{m\omega}{2\hbar}}
\left(
\hat x+
\frac{i}{m\omega}\hat p
\right).
$$

由正则对易关系：

$$
[\hat x,\hat p]=i\hbar I
$$

可以推出：

$$
[a,a^\dagger]=I.
$$

这里 $[A,B]=AB-BA$，$I$ 是恒等算符。很多教材在不引起混淆时把右边简写成 $1$。

这条关系表示先降低再升高与先升高再降低并不相同，两种顺序相差一次恒等操作。

## 数算符

定义数算符：

$$
N=a^\dagger a.
$$

它满足：

$$
[N,a^\dagger]=a^\dagger.
$$

符号含义：

- $N$：激发数算符；
- $[N,a^\dagger]$：交换二者作用顺序所得的差；
- 右边的 $a^\dagger$：说明产生算符会把数算符本征值提高一。

推导如下：

$$
[N,a^\dagger]
=[a^\dagger a,a^\dagger]
=a^\dagger[a,a^\dagger]
=a^\dagger.
$$

第二步使用了对易子的乘积法则，最后一步使用 $[a,a^\dagger]=I$。

## 为什么它把激发数提高一

设 $|n\rangle$ 是数算符本征态：

$$
N|n\rangle=n|n\rangle,
\qquad n=0,1,2,\ldots
$$

由：

$$
Na^\dagger
=a^\dagger N+[N,a^\dagger]
$$

得到：

$$
Na^\dagger|n\rangle
=(n+1)a^\dagger|n\rangle.
$$

因此，只要 $a^\dagger|n\rangle$ 不为零，它就是本征值为 $n+1$ 的数算符本征态。

这不是额外假设，而是由对易关系直接推出的结论。

## 归一化系数

产生算符的精确作用是：

$$
a^\dagger|n\rangle
=
\sqrt{n+1}\,|n+1\rangle.
$$

平方根系数可由范数求出：

$$
\|a^\dagger|n\rangle\|^2
=
\langle n|aa^\dagger|n\rangle.
$$

利用：

$$
aa^\dagger=a^\dagger a+I=N+I,
$$

可得：

$$
\|a^\dagger|n\rangle\|^2=n+1.
$$

所以向量长度是 $\sqrt{n+1}$。把这个长度提出来，剩余的 $|n+1\rangle$ 仍保持归一化。

## 对能量的作用

谐振子 Hamiltonian 为：

$$
H=\hbar\omega\left(N+\frac12\right).
$$

于是：

$$
[H,a^\dagger]
=\hbar\omega a^\dagger.
$$

若：

$$
H|n\rangle=E_n|n\rangle,
$$

则：

$$
H(a^\dagger|n\rangle)
=(E_n+\hbar\omega)(a^\dagger|n\rangle).
$$

所以每作用一次 $a^\dagger$，能量增加：

$$
\Delta E=\hbar\omega.
$$

$\Delta E$ 是能量变化量。$\hbar$ 的量纲是能量乘时间，$\omega$ 的量纲是时间的倒数，所以 $\hbar\omega$ 具有能量量纲。

## 从基态构造全部能量本征态

基态满足：

$$
a|0\rangle=0.
$$

第 $n$ 个归一化激发态可写成：

$$
|n\rangle
=
\frac{(a^\dagger)^n}{\sqrt{n!}}|0\rangle.
$$

其中：

- $(a^\dagger)^n$：连续作用 $n$ 次产生算符；
- $n!$：阶乘，$n!=n(n-1)\cdots2\cdot1$；
- $1/\sqrt{n!}$：保证最终状态归一化。

例如：

$$
|1\rangle=a^\dagger|0\rangle,
$$

$$
|2\rangle=\frac{(a^\dagger)^2}{\sqrt2}|0\rangle.
$$

## 位置表象中的作用

在位置表象中：

$$
\hat p=-i\hbar\frac{d}{dx}.
$$

因此：

$$
a^\dagger
=
\sqrt{\frac{m\omega}{2\hbar}}x
-
\sqrt{\frac{\hbar}{2m\omega}}
\frac{d}{dx}.
$$

第一项乘以 $x$，第二项对波函数求导。它们的组合把第 $n$ 个 Hermite–Gaussian 波函数变为与第 $n+1$ 个波函数成比例的函数。

这里的负号来自 $-i\hat p$ 与位置表象动量算符中 $-i$ 的乘积。计算时不能只凭“产生是加号”猜符号。

## 它是否真的“产生粒子”

要区分三种语境。

### 单个机械谐振子

$a^\dagger$ 只把同一个系统的激发数提高一级。例如原子在近似二次势阱中的振动从第一激发态升到第二激发态，不表示多出一个原子。

### 量子场论

自由场按动量分解后，每个模式都是一个谐振子。带模式标签的算符：

$$
a_{\mathbf k}^\dagger
$$

可把动量为 $\mathbf k$ 的粒子占据数增加一：

$$
a_{\mathbf k}^\dagger|n_{\mathbf k}\rangle
=
\sqrt{n_{\mathbf k}+1}
|n_{\mathbf k}+1\rangle.
$$

此时“产生一个粒子”是标准解释，但它依赖自由场或渐近自由粒子的 Fock 空间描述。强相互作用背景中，什么算一个粒子可能与观察者、时空和近似有关。

### 弦理论

弦有无穷多个振动模式。负模式振子通常充当产生方向，例如玻色弦中的 $\alpha_{-n}^\mu$ 可作用在基态上构造激发态。

它增加的是指定模式和目标时空方向的弦振动激发。所得 Fock 态还必须满足 Virasoro 或 BRST 约束，才可能代表物理态。

## 多模式产生算符

若系统有多个独立模式，用标签 $r$ 区分：

$$
[a_r,a_s^\dagger]=\delta_{rs}I.
$$

$\delta_{rs}$ 是 Kronecker delta：$r=s$ 时为 $1$，否则为 $0$。不同独立模式的玻色产生算符彼此对易：

$$
[a_r^\dagger,a_s^\dagger]=0.
$$

因此对玻色模式，交换加入两个不同模式量子的顺序不会改变状态。

## 玻色产生算符与费米产生算符

本文件主要讨论谐振子的玻色产生算符。玻色模式使用对易关系，并允许单个模式具有任意非负整数占据数。

费米模式使用反对易关系：

$$
\{b_r,b_s^\dagger\}
=\delta_{rs}I.
$$

并满足：

$$
(b_r^\dagger)^2=0.
$$

因此同一费米模式不能被同样的产生算符连续占据两次。不能把玻色公式不加修改地用于费米模式。

## 具体例子

设系统处于 $|2\rangle$。作用一次产生算符：

$$
a^\dagger|2\rangle
=\sqrt3|3\rangle.
$$

若再次作用：

$$
(a^\dagger)^2|2\rangle
=\sqrt3\sqrt4|4\rangle
=2\sqrt3|4\rangle.
$$

每一步都使激发数增加一，但系数随着原有激发数改变。

若 $\omega=5\,\mathrm{s^{-1}}$，作用一次后能量增加：

$$
\Delta E=5\hbar\,\mathrm{s^{-1}}.
$$

由于 $\hbar$ 的单位是 $\mathrm{J\,s}$，结果单位是焦耳。

## 适用条件与事实边界

1. $a^\dagger$ 的具体公式依赖 Hamiltonian、归一化和单位约定。
2. 标准升降关系针对理想二次势的量子谐振子；非谐系统一般没有完全相同的等间隔梯子。
3. 在量子场论中，“产生粒子”的解释最直接适用于自由场或散射理论中的渐近粒子态。
4. 在曲时空、非平衡背景或强相互作用体系中，粒子概念可能不是唯一的。
5. 弦的振子产生方向构造的是候选 Fock 态，仍需施加世界面约束。
6. 对无界算符，公式应在共同适当定义域上理解。

## 常见误区

1. **$a^\dagger$ 的匕首表示普通倒数。** $\dagger$ 表示伴随，不是 $1/a$。
2. **作用一次产生算符只把状态改成 $|n+1\rangle$，没有系数。** 正确系数是 $\sqrt{n+1}$。
3. **产生算符在任何系统中都创造一颗新粒子。** 在机械振子中，它只提高激发级。
4. **产生算符可以降低能量。** 对标准谐振子，它使能量增加 $\hbar\omega$。
5. **玻色和费米产生算符满足相同代数。** 前者使用对易关系，后者使用反对易关系。
6. **弦振子产生的每个态都是物理态。** 还需满足 Virasoro、BRST 等约束。
7. **产生算符本身是可观测量。** $a^\dagger$ 通常不是自伴算符，不能直接当作普通可观测量。

## 练习

1. 用自己的话解释产生算符为何像能级梯子的“向上一步”。
2. 已知 $[N,a^\dagger]=a^\dagger$ 和 $N|n\rangle=n|n\rangle$，推导 $a^\dagger|n\rangle$ 的数算符本征值。
3. 计算 $a^\dagger|4\rangle$。
4. 从 $[a,a^\dagger]=I$ 推导 $aa^\dagger=N+I$。
5. 计算 $(a^\dagger)^3|0\rangle$，并说明如何归一化。
6. 为什么在单个机械谐振子中不能把 $a^\dagger$ 一概解释成产生新粒子？
7. 玻色产生算符与费米产生算符最重要的代数差别是什么？

## 答案与提示

1. 它把数算符本征值从 $n$ 变成 $n+1$，同时把能量提高 $\hbar\omega$；“梯子”只是能级结构的比喻。
2. $Na^\dagger|n\rangle=(a^\dagger N+a^\dagger)|n\rangle=(n+1)a^\dagger|n\rangle$。
3. $a^\dagger|4\rangle=\sqrt5|5\rangle$。
4. $[a,a^\dagger]=aa^\dagger-a^\dagger a=I$，而 $a^\dagger a=N$，所以 $aa^\dagger=N+I$。
5. 逐次作用得到 $(a^\dagger)^3|0\rangle=\sqrt{1\cdot2\cdot3}|3\rangle=\sqrt{3!}|3\rangle$；乘 $1/\sqrt{3!}$ 后归一化。
6. 系统仍是同一个振子，只是内部振动能级改变；粒子解释来自量子场模式的占据数表示。
7. 玻色模式通常满足对易关系并允许任意占据数；费米模式满足反对易关系，同一模式至多占据一次。

## 与其他概念的关系

- [[量子谐振子]] 给出产生算符的来源和能谱；
- [[湮灭算符]] 是它的伴随并降低激发数；
- [[对易关系]] 决定升降代数；
- [[Fock 空间]] 用产生算符从真空构造多模式状态；
- [[场的正则量子化]] 把每个场模变成一对产生与湮灭算符；
- [[振子代数]] 将这一结构推广到弦的无穷多个模式；
- [[玻色子]] 与 [[费米子]] 使用不同的产生算符代数。

## 参考资料

- [MIT OCW 8.04, Quantum Physics I](https://ocw.mit.edu/courses/8-04-quantum-physics-i-spring-2013/)
- Barton Zwiebach, *Mastering Quantum Mechanics*, MIT Press, 2022.
- J. J. Sakurai and Jim Napolitano, *Modern Quantum Mechanics*, 3rd ed., Cambridge University Press, 2020.
- Claude Cohen-Tannoudji, Bernard Diu and Franck Laloë, *Quantum Mechanics*, Wiley.
- Michael E. Peskin and Daniel V. Schroeder, *An Introduction to Quantum Field Theory*, Westview Press, 1995.
- Joseph Polchinski, *String Theory, Vol. 1*, Cambridge University Press, 1998.
- [David Tong, Lectures on String Theory](https://arxiv.org/abs/0908.0333)
