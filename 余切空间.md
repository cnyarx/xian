---
aliases:
  - Cotangent Space
  - 余切向量
english_name: Cotangent Space
tags:
  - 数学
  - 微分几何
  - 弦理论先修
  - 弦理论
prerequisites:
  - "[[切空间]]"
  - "[[向量空间]]"
  - "[[线性映射]]"
  - "[[偏导数]]"
  - "[[流形]]"
used_by:
  - "[[切空间与余切空间的对偶]]"
  - "[[微分形式]]"
  - "[[外微分]]"
  - "[[度量]]"
  - "[[联络]]"
  - "[[协变导数]]"
evidence_status: 教材事实
---

# 余切空间（Cotangent Space）

## 一句话解释

余切空间是切空间的对偶向量空间，其中的余切向量是把切向量映射成实数的线性函数。

## 为什么要学它

切向量描述方向，余切向量描述“沿某个方向测量函数变化多少，或把一个向量映射成数值”。

函数的全微分 $df$ 是余切向量的基本例子。微分形式、梯度、动量、相空间的正则形式和世界面上的共形结构都依赖余切空间。

## 直观图景

切向量像一个附在点上的小箭头，余切向量则像一个“箭头测度仪”：给它一个箭头，它返回一个数值。这个数值可以理解为沿该方向测量某个量时得到的读数。

如果你站在流形上的一点，你面前有无数方向可以走。每个方向都由一个切向量表示。余切向量会告诉你，在某个方向上的变化率是多少。

## 对偶向量空间

设 $V$ 是域 $\mathbb F$ 上的向量空间。$V$ 的对偶空间：

$$
V^*=\operatorname{Hom}(V,\mathbb F)
$$

是所有从 $V$ 到 $\mathbb F$ 的线性映射的集合。$V^*$ 自身也是向量空间，其维数等于：

$$
\dim V^*=\dim V.
$$

对偶空间中的元素叫对偶向量或线性泛函。

## 余切空间的定义

流形 $M$ 在点 $p$ 处的余切空间定义为切空间的对偶：

$$
T_p^*M=(T_pM)^*.
$$

其元素叫余切向量，记作：

$$
\omega\in T_p^*M.
$$

## 每个符号是什么意思

- $T_pM$：$p$ 处的切空间；
- $T_p^*M$：$p$ 处的余切空间；
- $\omega$：一个余切向量；
- $\omega(v)$：余切向量 $\omega$ 作用在切向量 $v$ 上的结果，是一个实数。

## 余切空间的基

在坐标图 $(U,\varphi)$ 中，切空间有自然基：

$$
\left\{\frac{\partial}{\partial x^i}\Big|_p\right\}.
$$

余切空间的对偶基记作：

$$
\{dx^i|_p\}.
$$

它对切空间基向量的作用为：

$$
dx^i|_p\!\left(\frac{\partial}{\partial x^j}\Big|_p\right)
=\delta^i_j.
$$

其中 $\delta^i_j$ 是 Kronecker delta：$i=j$ 时为 $1$，不等时为 $0$。

## 余切向量的分量

一个余切向量可写成：

$$
\omega=\sum_{i=1}^{n}\omega_i\,dx^i|_p.
$$

其中 $\omega_i$ 是实数分量，下标 $i$ 表示余切向量的分量位置与切向量分量相反。

## 函数的微分

光滑函数 $f:M\to\mathbb R$ 在 $p$ 处的微分 $df|_p$ 是余切空间中最重要的例子。它定义为：

$$
df|_p(v)=v[f],
$$

即把切向量 $v$ 映射为函数 $f$ 沿 $v$ 方向的方向导数。

在坐标中：

$$
df|_p
=\sum_{i=1}^{n}\frac{\partial f}{\partial x^i}\Big|_p\,dx^i|_p.
$$

所以函数微分的分量为偏导数。

## 微分记号 $df$ 与 $dx^i$

$dx^i$ 首先是坐标函数 $x^i$ 自身的微分。在 $p$ 处：

$$
dx^i|_p\!\left(\frac{\partial}{\partial x^j}\Big|_p\right)
=\delta^i_j.
$$

所以 $\{dx^i|_p\}$ 确实与 $\{\partial/\partial x^i|_p\}$ 对偶。

$dx^i$ 不是无穷小量，而是余切空间中明确定义的线性泛函。

## 余切向量在坐标变换下如何变化

若两套坐标 $x^i$ 和 $y^j$ 在 $p$ 处重叠，余切向量分量变换规律为：

$$
\omega_{(y)j}
=\sum_{i=1}^{n}\frac{\partial x^i}{\partial y^j}\Big|_p
\omega_{(x)i}.
$$

这与切向量分量的变换矩阵互为逆。因此余切向量称为“协变向量”，而切向量称为“反变向量”。

## 为什么余切向量分量位置在下标

切向量基 $\partial/\partial x^i$ 和余切向量基 $dx^i$ 通过对偶配对。切向量分量的变换矩阵是 Jacobi 矩阵 $\partial y^j/\partial x^i$，余切向量分量则用其逆矩阵 $\partial x^i/\partial y^j$ 变换。

根据 Einstein 求和约定，通常把反变指标写成上标、协变指标写成下标，使指标配对自动体现变换规律。

## 余切丛

把所有点处的余切空间并在一起，得到余切丛：

$$
T^*M=\bigcup_{p\in M}T_p^*M.
$$

它是 $2n$ 维光滑流形。每个元素是 $(p,\omega)$，其中 $p\in M$、$\omega\in T_p^*M$。

余切丛上可定义自然辛结构，使它成为经典力学中相空间的原型。

## 光滑映射的拉回

设 $F:M\to N$ 是光滑映射。它的拉回：

$$
F^*_{(p)}:T_{F(p)}^*N\to T_p^*M
$$

把 $N$ 在 $F(p)$ 处的余切向量线性地拉回到 $M$ 在 $p$ 处的余切向量。

对函数 $g:N\to\mathbb R$，其拉回定义为：

$$
F^*_{(p)}(dg|_{F(p)})
=d(g\circ F)|_p.
$$

拉回是切映射的对偶操作。

## 拉回与切映射的关系

切映射把切向量从定义域推到值域，拉回把余切向量从值域拉回定义域。二者满足：

$$
(F^*\omega)(v)
=\omega(F_*v).
$$

这体现了“对偶”的含义：推前在切向量上定义，拉回通过自然配对在余切向量上定义。

## 余切向量与梯度

在 Euclidean 空间中，函数 $f$ 的梯度通常写成向量 $\nabla f$。标准 Euclidean 内积已经被默认使用，因此人们容易忽略梯度依赖度量这一点。

在一般流形上，不加额外结构就能自然定义的是微分 $df$，它是一个余切向量。仅凭光滑流形结构，还没有一个自然的梯度向量。

选定度量 $g$ 后，梯度向量 $\operatorname{grad}f$ 由下式唯一确定：

$$
g(\operatorname{grad}f,v)=df(v)
$$

对任意切向量 $v$ 成立。也就是说，度量把余切向量 $df$ 转换成切向量 $\operatorname{grad}f$。

## 余切向量与动量

在经典力学中，位置 $q^i$ 的共轭动量 $p_i$ 是余切向量分量。相空间局部可看作余切丛 $T^*Q$，其中 $Q$ 是位形空间。

正则一形式：

$$
\theta=\sum_i p_i\,dq^i
$$

是余切丛上的自然构造。

## 世界面共形结构中的余切向量

弦世界面 $\Sigma$ 上的局部复坐标 $z$ 可引出余切基：

$$
dz,\qquad d\bar z.
$$

它们是构造世界面微分形式、共形场和全纯/反全纯 Ward 恒等式的基础。

## 一个计算例子

设 $M=\mathbb R^2$，$p=(0,0)$，函数：

$$
f(x,y)=x^3+xy.
$$

坐标基为 $\{dx,dy\}$。微分：

$$
df|_p
=\frac{\partial f}{\partial x}\Big|_p dx
+\frac{\partial f}{\partial y}\Big|_p dy.
$$

计算偏导数：

$$
\frac{\partial f}{\partial x}=3x^2+y,
\qquad
\frac{\partial f}{\partial y}=x.
$$

在 $p$ 处：

$$
df|_p=0\cdot dx+0\cdot dy=0.
$$

这表示在原点处所有方向的方向导数均为零，对应驻点。

## 适用条件与事实边界

1. 余切空间是切空间的对偶向量空间，维数等于流形维数。
2. 余切向量自然拥有上标还是下标取决于变换性质；在通常约定中余切向量分量用下标。
3. 坐标基 $dx^i$ 是在选定坐标图后定义的，换坐标后基向量的变换需对偶核对。
4. 函数的微分 $df$ 是余切向量，不依赖额外度量。
5. 把余切向量变成切向量需要度量等额外结构。
6. 拉回 $F^*$ 是切映射的对偶操作，方向相反。
7. 余切丛 $T^*M$ 是光滑流形，具有自然辛结构。
8. 正则动量 $p_i$ 以余切向量分量的形式出现，这是相空间几何的基础。
9. 世界面共形理论中，$dz$ 和 $d\bar z$ 直接进入全纯与反全纯微分的定义。

## 常见误区

1. **余切向量就是导数。** 它是把切向量映射成实数的线性函数；$df$ 是一个例子，但不是所有余切向量都来自某个函数的微分。
2. **$dx^i$ 是无穷小量。** 它是余切空间中的基向量，作为线性泛函定义。
3. **余切向量与切向量只是下标上标不同。** 它们属于不同向量空间，变换规律不同。
4. **梯度在任何空间都是向量。** 在一般流形上，梯度是度量把余切向量 $df$ 变成切向量后得到的结果。
5. **余切空间可独立于切空间定义。** 余切空间通过取对偶从切空间得到。
6. **拉回就是把函数值拉回来。** 拉回作用于余切向量，与函数的复合是不同的运算层级。

## 练习

1. 写出余切空间 $T_p^*M$ 的定义。
2. 写出 $dx^i|_p$ 对 $\partial/\partial x^j|_p$ 的作用结果。
3. 函数 $f$ 在 $p$ 处的微分 $df|_p$ 如何用坐标基和偏导数表示？
4. 余切向量分量在坐标变换下按什么规律变化？
5. 拉回 $F^*$ 的定义域和值域是什么？
6. 为什么一般流形上梯度不能直接定义为向量？
7. 经典力学中相空间与余切丛有什么关系？
8. 设 $f(x,y)=x^2y$，求 $df$ 在 $(1,2)$ 处的表达式。

## 答案与提示

1. $T_p^*M$ 是切空间 $T_pM$ 的对偶向量空间，由所有从 $T_pM$ 到 $\mathbb R$ 的线性映射组成。
2. $dx^i|_p(\partial/\partial x^j|_p)=\delta^i_j$。
3. $df|_p=\sum_i(\partial f/\partial x^i)|_p\,dx^i|_p$。
4. 逆 Jacobi 变换：$\omega_{(y)j}=\sum_i(\partial x^i/\partial y^j)|_p\,\omega_{(x)i}$。
5. $F^*_{(p)}:T_{F(p)}^*N\to T_p^*M$。
6. 梯度向量需要内积把余切向量 $df$ 映射成切向量；余切向量 $df$ 本身不需要度量。
7. 位形空间 $Q$ 的余切丛 $T^*Q$ 局部给出相空间，正则动量就是余切向量分量。
8. $\partial f/\partial x=2xy$，$\partial f/\partial y=x^2$；在 $(1,2)$ 处为 $df=4dx+dy$。

## 与其他概念的关系

- [[切空间]] 是余切空间的对偶空间；
- [[切空间与余切空间的对偶]] 详细解释对偶配对与分量变换；
- [[向量空间]] 提供对偶空间的一般定义；
- [[线性映射]] 给出线性泛函和拉回的基本语言；
- [[微分形式]] 是余切向量的反对称张量积；
- [[外微分]] 把函数微分的概念推广到高阶微分形式；
- [[度量]] 将余切向量与切向量相互转换；
- [[联络]] 使用余切方向定义邻域之间的比较。

## 参考资料

- John M. Lee, *Introduction to Smooth Manifolds*, 2nd ed., Springer, 2013.
- Loring W. Tu, *An Introduction to Manifolds*, 2nd ed., Springer, 2011.
- Michael Spivak, *Calculus on Manifolds*, Westview Press, 1998.
- Mikio Nakahara, *Geometry, Topology and Physics*, 2nd ed., CRC Press, 2003.
- Theodore Frankel, *The Geometry of Physics*, 3rd ed., Cambridge University Press, 2012.
- Ralph Abraham and Jerrold E. Marsden, *Foundations of Mechanics*, 2nd ed., AMS Chelsea, 2008.