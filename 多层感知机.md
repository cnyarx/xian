---
aliases: [多层感知机, MLP, 前向传播, Hidden Layer]
tags: [神经网络, 教程, 大模型, 学习教程]
prerequisites: ["[[神经元与感知机]]", "[[激活函数]]"]
next: ["[[反向传播]]"]
---
# 多层感知机

## 一句话定义

多层感知机（Multi-Layer Perceptron，MLP）把许多神经元排成多层；前一层输出作为后一层输入。

## 结构

```text
输入层 → 隐藏层1 → 隐藏层2 → 输出层
```

隐藏层不是看不见，而是它位于输入和输出之间、没有直接标签。

一层计算：

$$h=\phi(Wx+b)$$

两层 MLP：

$$h=\phi(W_1x+b_1)$$
$$y=W_2h+b_2$$

- $x$：输入。
- $W_1,b_1,W_2,b_2$：训练得到的参数。
- $\phi$：激活函数。
- $h$：隐藏表示。
- $y$：输出。

## 前向传播

前向传播（Forward Pass）就是从输入开始逐层计算到输出。

数值例子：

$$x=[1,2],\quad W=\begin{bmatrix}1&0\\-1&2\end{bmatrix},\quad b=[0,1]$$

先算：

$$Wx=\begin{bmatrix}1\\3\end{bmatrix},\qquad Wx+b=\begin{bmatrix}1\\4\end{bmatrix}$$

两个分量都为正，所以经过 ReLU 后结果仍为 $[1,4]$。

## 表达能力

只要隐藏单元足够多，具有适当非线性激活的前馈网络能在有限区域上任意逼近连续函数。这叫通用近似定理。但它只保证“存在某组参数”，不保证容易训练，也不说明需要多少神经元。

## Transformer 中的 MLP

每个 Transformer 块都有前馈网络，对每个 token 的向量独立进行变换。注意力负责 token 之间交换信息，MLP 负责每个位置内部的特征加工。

## PyTorch 示例

```python
import torch.nn as nn

model = nn.Sequential(
    nn.Linear(4, 8),
    nn.ReLU(),
    nn.Linear(8, 2),
)
```

输入 4 维，隐藏层 8 维，输出 2 维。

## 概念与公式导航

- [[通用近似定理|通用近似定理]]

## 练习

1. 设 $x=[1,2]$，$W_1=\begin{bmatrix}1&-1\\0&2\end{bmatrix}$，$b_1=[0,1]$，激活函数为 ReLU，$W_2=[2,-1]$，$b_2=1$。手算这个 `2→2→1` 网络的输出。
2. 没有激活函数时，两层 MLP 为什么可合并？
3. 隐藏层越宽一定越好吗？列出两个代价。
4. 解释注意力和 MLP 在 Transformer 中的分工。

## 来源

- Cybenko, G. (1989). *Approximation by Superpositions of a Sigmoidal Function*. Mathematics of Control, Signals and Systems. https://doi.org/10.1007/BF02551274 `[A]`
- Goodfellow et al. (2016). *Deep Learning*. 第6章 `[A]`