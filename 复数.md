---
aliases:
  - Complex Number
  - Complex Numbers
english_name: Complex Number
tags:
  - 数学
  - 高中基础
  - 弦理论先修
  - 弦理论
prerequisites:
  - "[[实数]]"
used_by:
  - "[[Euler 公式]]"
  - "[[复分析]]"
  - "[[Fourier 变换]]"
evidence_status: 教材事实
---

# 复数（Complex Number）

## 一句话解释

复数把实数轴扩展成一个平面，使负数也能开平方，并能用长度和方向统一描述振动与旋转。

## 为什么要学它

波和振动本来可以只用正弦、余弦计算，但复数能把许多三角运算变成更简单的指数和乘法运算。量子力学、共形场论和弦的模展开都大量使用复数。

## 虚数单位

定义虚数单位 $i$ 满足：

$$
i^2=-1.
$$

因此，方程

$$
x^2+1=0
$$

在复数中有两个解：

$$
x=i\quad\text{或}\quad x=-i.
$$

$i$ 不是“虚假”的数，只是不能放在一维实数轴上表示。

## 复数的标准形式

复数写成：

$$
z=a+bi,
$$

其中 $a,b$ 都是实数：

- $a=\operatorname{Re}z$ 是实部；
- $b=\operatorname{Im}z$ 是虚部；
- 当 $b=0$ 时，复数就是普通实数；
- 当 $a=0$ 时，称为纯虚数。

因此实数是复数的一部分：

$$
\mathbb R\subset\mathbb C.
$$

## 复平面

复数 $z=a+bi$ 可表示成平面上的点 $(a,b)$：

- 横轴表示实部；
- 纵轴表示虚部。

这样，复数既可看成一个点，也可看成从原点指向该点的二维向量。

## 加法和减法

实部与实部相加，虚部与虚部相加：

$$
(a+bi)+(c+di)=(a+c)+(b+d)i.
$$

例如：

$$
(2+3i)+(1-5i)=3-2i.
$$

## 乘法

按照普通代数展开，再使用 $i^2=-1$：

$$
(a+bi)(c+di)=(ac-bd)+(ad+bc)i.
$$

例如：

$$
(1+i)^2=1+2i+i^2=2i.
$$

复数乘法不仅改变长度，也会改变方向。

## 复共轭

$z=a+bi$ 的复共轭（complex conjugate）是：

$$
\bar z=a-bi.
$$

它在复平面上把点关于实轴翻转。复数与其共轭的乘积是实数：

$$
z\bar z=(a+bi)(a-bi)=a^2+b^2.
$$

## 模

复数的模（modulus）表示它到原点的距离：

$$
|z|=\sqrt{a^2+b^2}.
$$

因此：

$$
|z|^2=z\bar z.
$$

这就是平面直角三角形的勾股关系。

## 除法

为了让分母变成实数，可以同时乘分母的复共轭：

$$
\frac{a+bi}{c+di}
=
\frac{(a+bi)(c-di)}{c^2+d^2},
$$

前提是 $c+di\ne0$。

例如：

$$
\frac1{1+i}
=
\frac{1-i}{(1+i)(1-i)}
=
\frac{1-i}{2}.
$$

## 极坐标形式

若 $z$ 到原点的距离为 $r$，与正实轴夹角为 $\theta$，则：

$$
z=r(\cos\theta+i\sin\theta).
$$

这里 $r=|z|\ge0$。结合 [[Euler 公式]]，可进一步写成：

$$
z=re^{i\theta}.
$$

这种形式使复数乘法变得直观：长度相乘，角度相加。

## 一个旋转例子

因为

$$
i=e^{i\pi/2},
$$

把一个复数乘以 $i$，会把它在复平面中逆时针旋转 $90^\circ$。例如：

$$
1\times i=i,
\qquad
i\times i=-1.
$$

点从 $1$ 转到 $i$，再转到 $-1$。

## 常见误区

1. **虚数是不真实或没有用途的数。** 错；它是现代数学和物理的标准工具。
2. **$i^2=1$。** 错，定义是 $i^2=-1$。
3. **虚部是 $bi$。** 通常 $\operatorname{Im}z=b$，不是 $bi$。
4. **$|a+bi|=a+b$。** 错，应为 $\sqrt{a^2+b^2}$。
5. **复数没有大小就不能表示长度。** 复数没有与实数相同的全序，但它有非负实数模 $|z|$。

## 练习

1. 写出 $z=3-4i$ 的实部和虚部。
2. 计算 $(2+i)(3-2i)$。
3. 求 $3-4i$ 的复共轭和模。
4. 化简 $1/i$。
5. 说明为什么 $z\bar z$ 一定是非负实数。

## 答案与提示

1. 实部 $3$，虚部 $-4$。
2. $8-i$。
3. 共轭为 $3+4i$，模为 $5$。
4. $1/i=-i$，因为同时乘 $-i$ 后得到 $-i/1$。
5. $z\bar z=a^2+b^2\ge0$。

## 与其他概念的关系

- [[Euler 公式]] 把复数、指数函数和三角函数连接起来；
- [[复分析]] 研究复变量函数；
- Fourier 分析用复指数分解波；
- 量子态的振幅通常是复数，实际概率由模平方得到。

## 参考资料

- [OpenStax, Precalculus 2e, Section 3.1](https://openstax.org/books/precalculus-2e/pages/3-1-complex-numbers)
- [MIT OCW 18.03SC, Complex Numbers and Exponentials](https://ocw.mit.edu/courses/18-03sc-differential-equations-fall-2011/)
