---
aliases: [Fundamental Theorem of Calculus, FTC]
english_name: Fundamental Theorem of Calculus
tags: [数学, 微积分, 弦理论先修, 弦理论]
prerequisites:
  - "[[导数]]"
  - "[[积分]]"
used_by:
  - "[[变分法]]"
  - "[[作用量]]"
evidence_status: 教材事实
---

# 微积分基本定理（Fundamental Theorem of Calculus）

## 一句话解释

微积分基本定理说明：导数研究局部变化，积分累积局部变化，它们在适当条件下互相撤销。

## 为什么要学它

没有这一定理，求定积分通常只能不断细分和逼近。有了它，可以先找原函数再代入端点。物理中也能从变化率恢复总变化，从局部规律得到整体结果。

## 第一部分：累积函数的导数

若 $f$ 在所研究区间连续，定义：

$$
F(x)=\int_a^x f(t)\,dt.
$$

那么：

$$
F'(x)=f(x).
$$

解释：$F(x)$ 记录从固定起点 $a$ 到当前位置 $x$ 的累计量。让上限增加很小的 $\Delta x$，新增量近似为 $f(x)\Delta x$，所以每单位 $x$ 的新增速率就是 $f(x)$。

积分中的变量写成 $t$，只是为了不与上限 $x$ 混淆；$t$ 是哑变量，换成其他符号不会改变积分值。

## 第二部分：用原函数计算定积分

若 $F'(x)=f(x)$，则：

$$
\int_a^b f(x)\,dx=F(b)-F(a).
$$

常简写为：

$$
\int_a^b f(x)\,dx=[F(x)]_a^b.
$$

这把“无限细分后求和”转换成“找原函数并计算两个端点”。

## 一个例子

计算：

$$
\int_0^2 x^2\,dx.
$$

$x^2$ 的一个原函数是 $F(x)=x^3/3$，所以：

$$
\int_0^2x^2\,dx
=\frac{2^3}{3}-\frac{0^3}{3}
=\frac83.
$$

## 从速度恢复位移

若：

$$
v(t)=\frac{dx}{dt},
$$

则：

$$
x(t_2)-x(t_1)=\int_{t_1}^{t_2}v(t)\,dt.
$$

导数把位置变成瞬时速度，积分把整段速度累积回位置变化。

## 从力恢复势能差

一维保守力满足：

$$
F(x)=-\frac{dU}{dx}.
$$

所以：

$$
U(b)-U(a)=-\int_a^bF(x)\,dx.
$$

局部的力与整体的势能差由同一套导数—积分关系连接。

## 上限是函数时

若：

$$
G(x)=\int_a^{g(x)}f(t)\,dt,
$$

结合基本定理和链式法则：

$$
G'(x)=f(g(x))g'(x).
$$

上限变化多快，会额外乘上 $g'(x)$。

## 成立条件

入门版本通常假设 $f$ 连续。更高等的分析会把条件放宽到更一般的可积函数，但不能完全忽略条件。

如果函数有严重奇点或积分不收敛，不能机械套用端点公式。

## 它不等于“求导和积分永远完全抵消”

不定积分后再求导：

$$
\frac d{dx}\left(\int f(x)\,dx\right)=f(x)
$$

在适当条件下成立。

先求导再不定积分：

$$
\int f'(x)\,dx=f(x)+C,
$$

会留下任意常数，因为求导丢失了常数信息。

## 常见误区

1. **定积分等于 $F(a)-F(b)$。** 顺序反了，应为 $F(b)-F(a)$。
2. **积分和导数无条件互逆。** 错，需要适当连续性或可积性条件。
3. **先求导再积分能恢复唯一原函数。** 错，会留下常数 $C$。
4. **积分变量名称影响结果。** 哑变量改名不改变结果。
5. **基本定理只是在说面积。** 它连接所有局部变化率与累计量，范围远大于几何面积。

## 练习

1. 用基本定理计算 $\int_1^3 2x\,dx$。
2. 若 $F(x)=\int_0^x\cos t\,dt$，求 $F'(x)$。
3. 若 $G(x)=\int_0^{x^2}e^t\,dt$，求 $G'(x)$。
4. 已知 $v(t)=4t$，求 $0$ 到 $3$ 秒的位移。
5. 为什么 $\int f'(x)dx$ 会多出常数？

## 答案与提示

1. $[x^2]_1^3=8$。
2. $\cos x$。
3. $2xe^{x^2}$。
4. $\int_0^3 4t\,dt=18$。
5. 因为求导无法区分相差常数的函数。

## 与其他概念的关系

- 它直接连接 [[导数]] 与 [[积分]]；
- [[作用量]] 的变分需要积分与分部积分；
- [[变分法]] 从整体积分量推导局部微分方程；
- 物理中从局部密度得到总量、从变化率得到总变化都依靠这一思想。

## 参考资料

- [OpenStax, Calculus Volume 1, Section 5.3](https://openstax.org/books/calculus-volume-1/pages/5-3-the-fundamental-theorem-of-calculus)
- [MIT OCW 18.01SC, Fundamental Theorem of Calculus](https://ocw.mit.edu/courses/18-01sc-single-variable-calculus-fall-2010/)
