---
aliases: [Python 变量, 列表, 字典, 循环, 条件]
tags: [Python, 编程, 教程, 大模型, 学习教程]
prerequisites: ["[[安装与环境]]"]
next: ["[[NumPy 入门]]"]
---
# 变量与基本运算

## 变量是什么

**变量（Variable）**是给一个值起的名字，像给盒子贴标签：

```python
age = 16
name = "小明"
```

`=` 是赋值，不是数学里的“恒等”。它表示把右边的值交给左边名字。

## 常见数据类型

```python
count = 10          # int，整数
loss = 2.31         # float，小数
text = "你好"       # str，字符串
ready = True        # bool，布尔值，只有 True/False
nothing = None      # 没有值
```

`#` 后面是注释，解释代码但不会执行。

可用 `type` 查看类型：

```python
print(type(loss))
```

## 算术和比较

```python
3 + 2   # 5
3 - 2   # 1
3 * 2   # 6
3 / 2   # 1.5
3 ** 2  # 9，乘方
7 % 3   # 1，余数

3 > 2   # True
3 == 3  # True，比较是否相等
3 != 4  # True，比较是否不相等
```

## 列表

**列表（List）**按顺序保存多个值：

```python
tokens = ["我", "爱", "学习"]
print(tokens[0])      # 我
print(len(tokens))    # 3
tokens.append("AI")
```

Python 从 0 开始编号，所以第一个元素是 `[0]`。

## 字典

**字典（Dictionary）**保存“键 → 值”的对应关系：

```python
vocab = {"猫": 0, "狗": 1, "鸟": 2}
print(vocab["狗"])   # 1
```

大模型的词表就可以理解为一个把 token 映射到整数编号的巨大字典。

## 条件判断

```python
score = 85

if score >= 90:
    print("优秀")
elif score >= 60:
    print("及格")
else:
    print("需要复习")
```

缩进表示哪些语句属于同一个代码块。通常使用 4 个空格。

## 循环

```python
for token in ["我", "爱", "AI"]:
    print(token)
```

输出：

```text
我
爱
AI
```

用 `range` 重复指定次数：

```python
for step in range(3):
    print(step)
```

输出 `0、1、2`，不包含 3。

## 一个完整例子：计算平均损失

```python
losses = [2.4, 2.0, 1.7, 1.5]
total = 0.0

for loss in losses:
    total = total + loss

average = total / len(losses)
print(average)
```

这段代码把每一步训练损失相加，再除以步数。输出 `1.9`。

## 概念与公式导航

- [[变量与函数|变量与函数]]

## 练习

1. 创建一个包含 5 个词的列表，逐个输出。
2. 创建词表字典，把三个词映射为 `0、1、2`。
3. 写循环计算 `[1, 2, 3, 4, 5]` 的平方和。
4. 输入一个损失值：小于 1 输出“较低”，否则输出“仍需训练”。
5. 解释 `=` 和 `==` 的区别。

## 来源

- Python Software Foundation, *The Python Tutorial*：https://docs.python.org/3/tutorial/ `[A]`
- Python Software Foundation, *Built-in Types*：https://docs.python.org/3/library/stdtypes.html `[A]`
