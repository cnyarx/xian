---
aliases: [损失缩放公式, 浮点范围公式, Loss Scaling Formula, Floating-Point Range]
tags: [公式, 训练系统, 数值稳定, 大模型, 学习教程]
prerequisites: ["[[浮点数与数值稳定]]", "[[混合精度与损失缩放]]", "[[导数梯度与链式法则公式]]"]
---
# 损失缩放与浮点范围公式

## 1. 浮点数的结构

规格化二进制浮点数可概括为：

$$
x=(-1)^s\times(1.f)_2\times2^{e-bias}
$$

- $s$：符号位；
- $f$：有效数字字段；
- $e$：指数编码；
- $bias$：指数偏置。

IEEE 754 还定义了零、非规格化数、无穷和 NaN；上式不覆盖全部特殊值。

## 2. 静态损失缩放

原损失为 $L(\theta)$，选择正数缩放因子 $S$：

$$
L'(\theta)=S L(\theta)
$$

根据导数的线性性质：

$$
\nabla_\theta L'=S\nabla_\theta L
$$

更新前反缩放：

$$
g=\frac{\nabla_\theta L'}{S}
$$

若所有运算精确，上式与直接计算原梯度相同；它的工程价值在于改变低精度反向过程中的中间数值范围。

## 3. 数值例子

若原梯度为：

$$
g=2\times10^{-8}
$$

取 $S=1024$：

$$
g'=Sg=2.048\times10^{-5}
$$

反缩放后：

$$
\frac{g'}{S}=2\times10^{-8}
$$

例子只展示数学尺度变化；真实硬件是否保留这个数还取决于格式、非规格化数支持和具体内核。

## 4. 动态损失缩放

一种概念性更新规则是：

$$
S_{t+1}=
\begin{cases}
S_t/r,&\text{检测到溢出}\
rS_t,&\text{连续若干步未溢出}\
S_t,&\text{其他情况}
\end{cases}
$$

其中 $r>1$。框架实现会规定增长间隔、回退因子和更新是否跳过，不能把该概括式当作统一标准。

## 5. 精度与范围的取舍

指数位主要决定可表示数量级范围，有效数字位主要决定相邻可表示数的间距。BF16 保留 FP32 的 8 位指数字段但有效数字较少；FP16 指数位更少，因此更容易在训练中遇到范围问题。

## 边界

- 损失缩放不改变理想数学目标，但有限精度下的舍入路径会变化；
- 若梯度在缩放前已因其他操作变成 NaN，缩放不能修复；
- 优化器必须在反缩放后做梯度裁剪，否则裁剪阈值的含义会改变；
- 不同设备对非规格化数和累积精度的处理可能不同。

## 最小自测

1. $S=256$、缩放后梯度为 $0.512$，反缩放梯度是多少？
2. 为什么损失缩放因子不能无限增大？

答案：

1. $0.512/256=0.002$；
2. 因为过大的中间值可能溢出为无穷或 NaN。

## 正文

- [[混合精度训练|混合精度训练]]

## 来源

- IEEE 754-2019, *IEEE Standard for Floating-Point Arithmetic*：https://doi.org/10.1109/IEEESTD.2019.8766229 `[A]`
- Micikevicius et al. (2018), *Mixed Precision Training*：https://arxiv.org/abs/1710.03740 `[A]`
- PyTorch AMP Documentation：https://pytorch.org/docs/stable/amp.html `[A]`
