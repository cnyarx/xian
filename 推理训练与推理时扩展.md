---
aliases: [推理训练, 推理时扩展, 思维链, 过程奖励, 自洽性, Reasoning Training, Test-Time Scaling, Chain of Thought, Process Reward, Self-Consistency]
tags: [概念, 后训练, 推理, 大模型, 学习教程]
level: L11
prerequisites: ["[[后训练与指令微调]]", "[[偏好数据与RLHF]]", "[[智能体状态与控制循环]]"]
---
# 推理训练与推理时扩展

## 一句话定义

推理训练（Reasoning Training）在训练阶段教模型生成中间推理步骤；推理时扩展（Test-Time Scaling）在推理阶段通过多次采样、搜索或验证投入更多计算来提高答案质量。

## 直觉理解

普通回答像心算直接写答案；推理训练教学生列出步骤。推理时扩展像给学生更多草稿纸和检查时间，让他尝试多种方法再选择最好答案。

## 严格定义

思维链（Chain-of-Thought, CoT）让模型生成中间步骤 $z$ 再得到答案 $y$：$P(y,z\mid x)=P(z\mid x)P(y\mid x,z)$。

自洽性（Self-Consistency）采样多条推理链 $z_1,\ldots,z_k$，用多数投票或验证器选择最终答案。过程奖励（Process Reward）对每个中间步骤评分，区别于只给最终答案打分的结果奖励。

## 例子

数学题"24×17"：模型生成 8 种解法，其中 7 种得到 408，1 种得到 418。自洽性选择 408。

## 边界

- 更多计算不保证答案正确；
- 错误推理可能通过多数投票被放大；
- 过程奖励模型本身可能有偏差；
- 增加采样、搜索或验证步骤会提高计算和延迟开销，具体增幅取决于方法、模型和硬件，不能简单概化为“线性或更快”；

## 最小自测

1. 训练时推理和推理时扩展有什么区别？
2. 自洽性为什么可能失败？

答案：1. 前者改变模型参数，后者固定参数增加推理计算；2. 多数推理链可能共享同一错误模式。

## 详细教程

- [[推理训练|推理训练]]
- [[推理时扩展|推理时扩展]]

## 来源

- Wei et al. (2022), *Chain-of-Thought Prompting Elicits Reasoning in Large Language Models*：https://arxiv.org/abs/2201.11903 `[A]`
- Wang et al. (2023), *Self-Consistency Improves Chain of Thought Reasoning*：https://arxiv.org/abs/2203.11171 `[A]`
- Lightman et al. (2023), *Let's Verify Step by Step*：https://arxiv.org/abs/2305.20050 `[A]`