---
aliases: [SFT, 监督微调, Instruction Tuning]
tags: [大模型, 训练, 教程, 学习教程]
prerequisites: ["[[后训练概览]]"]
next: ["[[人类反馈强化学习]]"]
---
# 监督微调

## 一句话定义

SFT 用指令—回答示范训练模型，使模型学会以对话格式响应，并遵循指令风格。

## 训练格式

```text
<|user|>解释什么是梯度下降。</s>
<|assistant|>梯度下降是……</s>
```

不同模型使用不同聊天模板和特殊 token。

## 损失

SFT 通常仍使用交叉熵，仅对回答部分计算损失：

$$L=-\frac{1}{|\text{answer}|}\sum_{t\in\text{answer}}\log p_\theta(x_t\mid x_{<t})$$

指令或提示部分的 token 通常不贡献损失（masked），模型只学习生成回答。

## 数据来源

- 人工标注指令和回答。
- 从更强模型蒸馏合成数据。
- 从已有文本转换格式。
- 代码、数学、对话等多领域混合。

数据质量、多样性、格式和许可都需要慎重。

## 能做什么与不能做什么

- 能大幅改善对话格式和指令遵循。
- 不能保证事实正确，也不能从根本上消除有害行为。
- 数据过于单一会让模型偏向特定风格。

## 特殊格式

数学推理、代码生成、工具调用需要特定格式，如 JSON 工具模式、`<think>` 推理块等。格式错误可能导致输出无法解析。

## 概念与公式导航

- [[后训练与指令微调|后训练与指令微调]]
- [[语言模型损失与困惑度公式|语言模型损失与困惑度公式]]

## 练习

1. 写一条简单 SFT 样本，包含指令和理想回答。
2. 为什么通常只对回答部分计算损失？
3. 为什么 SFT 数据需要多样性？
4. 蒸馏数据和人工标注数据各有什么优缺点？

## 来源

- Wei, J. et al. (2022). *Finetuned Language Models Are Zero-Shot Learners*. ICLR. https://arxiv.org/abs/2109.01652 `[A]`
- Chung, H. W. et al. (2022). *Scaling Instruction-Finetuned Language Models*. https://arxiv.org/abs/2210.11416 `[A]`