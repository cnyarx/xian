---
aliases: [向量数据库, Vector Database, Dense Retrieval, Embedding Search]
tags: [大模型, RAG, 教程, 学习教程]
prerequisites: ["[[RAG 基础]]", "[[高维空间直觉]]"]
next: ["[[工具调用]]"]
---
# 嵌入与向量数据库

## 语义检索

嵌入模型把查询和文档块编码为向量。含义相近的向量通常更接近，再用余弦相似度或点积检索。

$$\cos(q,d)=\frac{q\cdot d}{\|q\|\|d\|}$$

## 一个可手算的检索例子

假设查询向量与两个文档向量是：

$$
q=[1,1],\qquad d_1=[1,0],\qquad d_2=[1,1]
$$

则：

$$
\cos(q,d_1)=\frac{1}{\sqrt2\times1}\approx0.707
$$

$$
\cos(q,d_2)=\frac{2}{\sqrt2\times\sqrt2}=1
$$

所以按余弦分数，$d_2$ 排在 $d_1$ 前面。这个分数只说明嵌入空间中的方向接近，不证明 $d_2$ 内容真实，也不保证它完整回答查询。

若系统先加元数据条件“仅搜索产品 A 且用户有权限的文档”，不满足条件的候选应在可信服务端被排除，再进行向量排序。

## 向量数据库

向量数据库保存向量、原文和元数据，提供最近邻搜索、过滤和更新。它不是语言模型，也不会自己理解答案。

## 精确与近似搜索

遍历全部向量最准确但数据大时慢。近似最近邻（ANN）索引牺牲少量召回换速度。HNSW 用多层邻接图快速导航；IVF 先把向量分组，再搜索最相关组。

## 混合检索

向量检索擅长语义近似，关键词检索擅长专有名词、编号和精确词。混合检索把两者结果融合，通常比单一方法更稳，但必须在目标数据上验证。

## 元数据过滤

先按权限、日期、产品、语言过滤，再向量搜索。权限过滤必须在可信服务端执行，不能仅让模型“不要泄露”。

## 概念与公式导航

- [[向量检索与ANN|向量检索与 ANN]]
- [[点积范数与余弦公式|点积、范数与余弦公式]]

## 练习

1. 向量数据库和普通关系数据库有什么区别？
2. ANN 为什么不是精确搜索？
3. 产品编号查询为何需要关键词检索？
4. 权限过滤应放在哪里？

## 来源

- Karpukhin, V. et al. (2020). *Dense Passage Retrieval*. EMNLP. https://arxiv.org/abs/2004.04906 `[A]`
- Malkov, Y. & Yashunin, D. (2020). *Efficient and Robust Approximate Nearest Neighbor Search Using Hierarchical Navigable Small World Graphs*. TPAMI https://arxiv.org/abs/1603.09320 `[A]`
- FAISS Documentation：https://faiss.ai/ `[A]`