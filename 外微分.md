---
aliases:
  - Exterior Derivative
  - Exterior Differentiation
english_name: Exterior Derivative
tags:
  - 数学
  - 微分几何
  - 微分形式
  - 弦理论先修
  - 弦理论
prerequisites:
  - "[[微分形式]]"
  - "[[偏导数]]"
  - "[[复合函数]]"
used_by:
  - "[[Stokes 定理]]"
  - "[[上同调]]"
  - "[[闭形式与恰当形式]]"
  - "[[辛结构]]"
  - "[[规范场强]]"
  - "[[Bianchi 恒等式]]"
  - "[[Kalb-Ramond 场强]]"
evidence_status: 教材事实
---

# 外微分（Exterior Derivative）

## 一句话解释

外微分是把 $p$ 形式变成 $(p+1)$ 形式的坐标无关微分运算，它测量一个微分形式的局部变化，并且连续做两次永远得到零。

## 为什么要学它

普通导数主要处理函数。外微分把“求导”推广到一形式、二形式和任意次数的微分形式，并保持反对称结构。

它用同一个符号 $d$ 统一表达：

- 函数的全微分；
- 三维向量分析中的旋度型结构；
- Stokes 定理中的内部变化；
- 闭形式、恰当形式和 de Rham 上同调；
- Abelian 规范场强 $F=dA$；
- Kalb–Ramond 场强 $H=dB$；
- Bianchi 恒等式 $dF=0$、$dH=0$。

弦理论中的规范场、反对称张量场、拓扑通量和 D-brane 世界体耦合都依赖外微分。

## 直观图景

函数 $f$ 给每个点一个数。$df$ 接收一个方向，告诉你沿该方向走时 $f$ 变化多快。

一形式 $\alpha$ 给每个方向一个读数。$d\alpha$ 接收两个方向，测量沿一个小回路走一圈时，$\alpha$ 的读数是否有净环流。

二形式 $\beta$ 测量有向面积。$d\beta$ 接收三个方向，测量一个小体积边界上的面积通量是否有净流出。

这个图景由 Stokes 定理精确表达：区域内部的 $d\omega$ 积分，等于边界上的 $\omega$ 积分。

## 正式定义

在光滑流形 $M$ 上，外微分是一族线性映射：

$$
d:\Omega^p(M)\to\Omega^{p+1}(M),
$$

它由以下性质唯一确定：

1. 对零形式，也就是光滑函数 $f$，$df$ 是通常的函数微分；
2. 满足分次 Leibniz 法则；
3. 满足 $d^2=0$；
4. 与光滑映射的拉回相容。

在局部坐标中，可直接给出计算公式。

## 每个符号是什么意思

- $M$：光滑流形；
- $\Omega^p(M)$：$M$ 上所有光滑 $p$ 形式；
- $d$：外微分算符；
- $p$：输入形式的次数；
- $d\omega$：$\omega$ 的外微分，是一个 $(p+1)$ 形式；
- $d^2$：连续施加两次外微分；
- $\wedge$：微分形式的楔积；
- $F^*$：光滑映射 $F$ 对微分形式的拉回。

## 从函数开始

对光滑函数：

$$
f:M\to\mathbb R,
$$

它的外微分就是通常的微分：

$$
df|_p(v)=v[f].
$$

在局部坐标 $x^1,\ldots,x^n$ 中：

$$
df=\frac{\partial f}{\partial x^i}dx^i.
$$

所以 $d$ 把零形式变成一形式：

$$
d:\Omega^0(M)\to\Omega^1(M).
$$

## 一般坐标公式

若 $p$ 形式写成：

$$
\omega
=\frac{1}{p!}\omega_{i_1\cdots i_p}(x)
\,dx^{i_1}\wedge\cdots\wedge dx^{i_p},
$$

则：

$$
d\omega
=\frac{1}{p!}
\frac{\partial\omega_{i_1\cdots i_p}}{\partial x^j}
\,dx^j\wedge dx^{i_1}\wedge\cdots\wedge dx^{i_p}.
$$

外微分只对系数函数求偏导，再在前面楔上相应的坐标一形式。

因为：

$$
d(dx^i)=d^2x^i=0,
$$

坐标基一形式本身不会产生额外项。

## 分量反对称化公式

同一个结果也可写成：

$$
(d\omega)_{i_0i_1\cdots i_p}
=(p+1)\partial_{[i_0}
\omega_{i_1\cdots i_p]}.
$$

方括号表示对其中所有指标完全反对称化。若反对称化定义中是否包含阶乘因子不同，公式前面的数值因子也会随约定变化。

几何结论不依赖这类记号约定，但实际计算必须保持一致。

## 一形式的外微分

设：

$$
\alpha=\alpha_i dx^i.
$$

则：

$$
d\alpha
=\partial_j\alpha_i\,dx^j\wedge dx^i.
$$

整理成标准反对称形式：

$$
d\alpha
=\frac12
(\partial_i\alpha_j-\partial_j\alpha_i)
\,dx^i\wedge dx^j.
$$

因此 $d\alpha$ 自动只保留偏导数的反对称部分。

## 二维一形式例子

在 $\mathbb R^2$ 中取：

$$
\alpha=P(x,y)dx+Q(x,y)dy.
$$

逐项求外微分：

$$
d\alpha=dP\wedge dx+dQ\wedge dy.
$$

其中：

$$
dP=P_xdx+P_ydy,
\qquad
dQ=Q_xdx+Q_ydy.
$$

利用 $dx\wedge dx=dy\wedge dy=0$ 和 $dy\wedge dx=-dx\wedge dy$，得到：

$$
d\alpha
=(Q_x-P_y)dx\wedge dy.
$$

这正是 Green 定理中出现的局部旋度型组合。

## 一个具体计算

令：

$$
\alpha=x^2y\,dx+(x+y^3)\,dy.
$$

这里：

$$
P=x^2y,
\qquad
Q=x+y^3.
$$

所以：

$$
Q_x=1,
\qquad
P_y=x^2.
$$

因此：

$$
d\alpha=(1-x^2)dx\wedge dy.
$$

该二形式在 $x=1$ 或 $x=-1$ 的点上为零；在其他位置，其系数一般非零，表示非零的局部环流密度。

## 二形式的外微分

设：

$$
\beta=\frac12\beta_{ij}dx^i\wedge dx^j.
$$

则：

$$
d\beta
=\frac12\partial_k\beta_{ij}
\,dx^k\wedge dx^i\wedge dx^j.
$$

分量形式为：

$$
(d\beta)_{kij}
=\partial_k\beta_{ij}
+\partial_i\beta_{jk}
+\partial_j\beta_{ki}.
$$

这是一个三形式，完全反对称。

## 三维二形式例子

在 $\mathbb R^3$ 中写：

$$
\beta
=P\,dy\wedge dz
+Q\,dz\wedge dx
+R\,dx\wedge dy.
$$

则：

$$
d\beta
=\left(
\frac{\partial P}{\partial x}
+\frac{\partial Q}{\partial y}
+\frac{\partial R}{\partial z}
\right)
 dx\wedge dy\wedge dz.
$$

括号里的组合与向量场 $(P,Q,R)$ 的散度相同。不过把二形式对应到向量场时，已经隐含使用了 Euclidean 体积形式或度量。外微分本身不需要度量。

## 线性

外微分是线性的。对常数 $a,b$ 和同次数形式 $\alpha,\beta$：

$$
d(a\alpha+b\beta)
=a\,d\alpha+b\,d\beta.
$$

所以可以逐项求导，再把结果相加。

这里 $a,b$ 必须是标量常数。若乘子是函数 $f$，还要使用 Leibniz 法则。

## 分次 Leibniz 法则

若：

$$
\alpha\in\Omega^p(M),
\qquad
\beta\in\Omega^q(M),
$$

则：

$$
d(\alpha\wedge\beta)
=d\alpha\wedge\beta
+(-1)^p\alpha\wedge d\beta.
$$

符号 $(-1)^p$ 来自 $d$ 是次数为 $1$ 的分次导子。

特别地，函数 $f$ 是零形式，因此：

$$
d(f\beta)=df\wedge\beta+f\,d\beta.
$$

## Leibniz 符号例子

若 $\alpha$ 是一形式，则 $p=1$：

$$
d(\alpha\wedge\beta)
=d\alpha\wedge\beta
-\alpha\wedge d\beta.
$$

若 $\alpha$ 是二形式，则 $p=2$：

$$
d(\alpha\wedge\beta)
=d\alpha\wedge\beta
+\alpha\wedge d\beta.
$$

所以符号取决于左边形式的次数，而不是右边形式的次数。

## 为什么 $d^2=0$

对函数 $f$：

$$
df=\partial_i f\,dx^i.
$$

再求一次外微分：

$$
d(df)
=\partial_j\partial_i f
\,dx^j\wedge dx^i.
$$

若 $f$ 足够光滑，混合偏导数对称：

$$
\partial_j\partial_i f
=\partial_i\partial_j f.
$$

但楔积反对称：

$$
dx^j\wedge dx^i
=-dx^i\wedge dx^j.
$$

对称系数与反对称基相乘后完全抵消，因此：

$$
d^2f=0.
$$

同样的对称—反对称抵消对任意微分形式成立：

$$
d^2\omega=0.
$$

## $d^2=0$ 不等于“什么都没变化”

第一次外微分通常不为零。$d^2=0$ 只表示对第一次外微分的结果再做外微分会得到零。

例如：

$$
f(x,y)=x^2y
$$

有：

$$
df=2xy\,dx+x^2dy,
$$

这一般不为零；但：

$$
d(df)=0.
$$

这与“梯度的旋度为零”是同一类结构。

## 闭形式

若 $p$ 形式 $\omega$ 满足：

$$
d\omega=0,
$$

则称它为闭形式。

闭表示它没有被外微分探测到的局部边界变化。例如一形式闭意味着其局部旋度型量为零。

“闭”不是说形式的定义域是闭集，也不是说积分路径闭合。

## 恰当形式

若存在 $(p-1)$ 形式 $\eta$，使：

$$
\omega=d\eta,
$$

则称 $\omega$ 为恰当形式。

因为：

$$
d\omega=d(d\eta)=0,
$$

所以每个恰当形式都是闭形式。

但反过来只在局部或满足额外拓扑条件时成立。

## Poincaré 引理

在星形开集等可缩的局部区域中，正次数闭形式都是恰当形式。这是 Poincaré 引理的典型表述。

因此闭形式在足够小的局部区域通常能写成某个势的外微分。但在整个流形上，拓扑可能阻止这种势被全局一致定义。

例如圆周上的某些闭一形式具有非零周期积分，因而不是全局恰当形式。

## 一个全局不恰当的闭形式例子

在去掉原点的平面：

$$
M=\mathbb R^2\setminus\{0\}
$$

上考虑：

$$
\alpha
=\frac{-y\,dx+x\,dy}{x^2+y^2}.
$$

直接计算可得：

$$
d\alpha=0.
$$

但沿绕原点一圈的单位圆积分：

$$
\int_{S^1}\alpha=2\pi.
$$

若 $\alpha=df$ 且 $f$ 是全局单值光滑函数，则闭曲线积分应为零。因此 $\alpha$ 闭但不全局恰当。

障碍来自被挖掉的原点造成的非平凡拓扑。

## 与拉回相容

若：

$$
F:M\to N
$$

是光滑映射，$\omega\in\Omega^p(N)$，则：

$$
d(F^*\omega)=F^*(d\omega).
$$

这意味着“先拉回再求外微分”与“先求外微分再拉回”结果相同。

该性质保证外微分不依赖特殊坐标，并能在参数化曲线、曲面和世界面上自然工作。

## 链式法则的推广

对函数 $f:N\to\mathbb R$：

$$
F^*(df)=d(f\circ F).
$$

这就是普通链式法则的微分形式版本。

若在坐标中：

$$
f=f(y),
\qquad
y^i=F^i(x),
$$

则：

$$
d(f\circ F)
=\frac{\partial f}{\partial y^i}(F(x))
\frac{\partial F^i}{\partial x^a}dx^a.
$$

## 外微分不需要联络

对一般张量场求协变导数通常需要选择联络。外微分作用在微分形式上时，不需要联络。

原因是坐标变换产生的相关附加项在完全反对称化后抵消。因此 $d\omega$ 能仅由光滑结构自然定义。

若选取无挠联络 $\nabla$，外微分分量可写成协变导数的反对称化：

$$
(d\omega)_{i_0\cdots i_p}
=(p+1)\nabla_{[i_0}
\omega_{i_1\cdots i_p]}.
$$

若联络有挠率，则需额外谨慎，不能直接无条件套用这一简单公式。

## 与 Stokes 定理的关系

对适当定向的带边界 $n$ 维流形 $M$ 和满足条件的 $(n-1)$ 形式 $\omega$：

$$
\int_M d\omega
=\int_{\partial M}\omega.
$$

左边积分区域内部的外微分，右边积分边界上的原形式。

这说明外微分是与“取边界”对偶的微分运算。因为边界的边界为空：

$$
\partial^2=0,
$$

对应形式一侧也有：

$$
d^2=0.
$$

## 一维基本定理

在区间 $[a,b]$ 上，函数 $f$ 是零形式：

$$
df=f'(x)dx.
$$

Stokes 定理给出：

$$
\int_{[a,b]}df
=\int_{\partial[a,b]}f
=f(b)-f(a).
$$

这就是微积分基本定理。

## Abelian 规范场强

电磁规范势是一形式：

$$
A=A_\mu dx^\mu.
$$

场强定义为：

$$
F=dA.
$$

分量为：

$$
F_{\mu\nu}
=\partial_\mu A_\nu-
\partial_\nu A_\mu.
$$

所以：

$$
F=\frac12F_{\mu\nu}
\,dx^\mu\wedge dx^\nu.
$$

由 $d^2=0$ 自动得到：

$$
dF=d^2A=0.
$$

这是 Abelian Bianchi 恒等式。

## 规范变换为何保持场强

Abelian 规范变换为：

$$
A\longmapsto A+d\lambda,
$$

其中 $\lambda$ 是零形式。

变换后的场强：

$$
F' =d(A+d\lambda)
=dA+d^2\lambda
=F.
$$

所以规范势可以改变，而场强不变。关键正是 $d^2=0$。

## 非 Abelian 情形的边界

对非 Abelian 规范场，场强不是简单的 $dA$，而是：

$$
F=dA+A\wedge A
$$

在一种常见矩阵值形式约定下成立，耦合常数和因子 $i$ 可能被放在不同位置。

相应 Bianchi 恒等式是协变形式：

$$
DF=0,
$$

而不是通常的 $dF=0$。因此不能把 Abelian 公式无条件推广到非 Abelian 理论。

## Kalb–Ramond 场强

弦理论中的 Kalb–Ramond 势是二形式：

$$
B=\frac12B_{\mu\nu}dx^\mu\wedge dx^\nu.
$$

在最简单的局部描述中，其三形式场强为：

$$
H=dB.
$$

分量为：

$$
H_{\mu\nu\rho}
=3\partial_{[\mu}B_{\nu\rho]}.
$$

由 $d^2=0$ 得：

$$
dH=0
$$

在没有磁源、奇点或修正项的局部区域成立。

## 二形式规范变换

二形式势的局部规范变换为：

$$
B\longmapsto B+d\Lambda,
$$

其中 $\Lambda$ 是一形式。

于是：

$$
H' =d(B+d\Lambda)
=dB+d^2\Lambda
=H.
$$

这与电磁势 $A\mapsto A+d\lambda$ 的结构平行，只是形式次数整体提高了一阶。

## 弦理论中的修正场强

在异质弦和某些超引力理论中，$H$ 可能包含 Chern–Simons 修正，Bianchi 恒等式也可能变成：

$$
dH\ne0
$$

并由规范与引力曲率项决定。

因此“$H=dB$ 所以 $dH=0$”是最简单局部模型中的结论，不是所有弦背景中的无条件事实。磁源、NS5-brane 和全局 gerbe 结构也会改变全局描述。

## 外微分与世界面拉回

设弦嵌入：

$$
X:\Sigma\to M.
$$

对目标时空形式 $\omega$：

$$
d(X^*\omega)=X^*(d\omega).
$$

因此若目标时空二形式 $B$ 的场强为 $H=dB$，则世界面上：

$$
d(X^*B)=X^*H.
$$

不过二维世界面上的三形式恒为零，所以在普通二维世界面内部，$X^*H$ 作为三形式为零。$H$ 的物理影响仍可通过目标空间背景、延拓三维体和全局拓扑体现。

## de Rham 复形

把所有次数的微分形式和外微分串联：

$$
0\longrightarrow\Omega^0(M)
\xrightarrow{d}\Omega^1(M)
\xrightarrow{d}\Omega^2(M)
\xrightarrow{d}\cdots
\xrightarrow{d}\Omega^n(M)
\longrightarrow0.
$$

因为 $d^2=0$，每一步的像都包含在下一步的核中：

$$
\operatorname{im}d_{p-1}
\subseteq\ker d_p.
$$

这个链称为 de Rham 复形，是定义 de Rham 上同调的基础。

## 上同调的入口

第 $p$ 个 de Rham 上同调群定义为：

$$
H^p_{\mathrm{dR}}(M)
=\frac{\ker(d:\Omega^p\to\Omega^{p+1})}
{\operatorname{im}(d:\Omega^{p-1}\to\Omega^p)}.
$$

分子是闭 $p$ 形式，分母是恰当 $p$ 形式。商空间记录“闭但无法全局写成外微分”的部分。

外微分是局部微分运算，却能通过上同调探测流形的全局拓扑。

## 适用条件与事实边界

1. 外微分把 $p$ 形式变成 $(p+1)$ 形式，并且由光滑结构自然定义。
2. 对函数，外微分就是通常的全微分；对一形式，它提取偏导数的反对称部分。
3. 外微分满足线性、分次 Leibniz 法则和 $d^2=0$。
4. $d^2=0$ 依赖足够光滑时混合偏导数的对称性与楔积的反对称性。
5. 外微分与拉回相容：$dF^*=F^*d$。
6. 定义外微分不需要度量或联络；用 Hodge 星表达散度等操作时才需要额外结构。
7. 每个恰当形式都是闭形式；闭形式是否全局恰当取决于拓扑。
8. Poincaré 引理是局部或可缩区域结论，不能直接推出任意流形上的闭形式都全局恰当。
9. Abelian 场强满足 $F=dA$ 和 $dF=0$；非 Abelian 场强还含 $A\wedge A$，使用协变 Bianchi 恒等式。
10. 简单局部模型中 $H=dB$ 且 $dH=0$；弦理论中的源、全局结构和 Chern–Simons 修正可改变这一表述。
11. $d(X^*\omega)=X^*(d\omega)$ 对光滑映射成立，并保证世界面拉回与外微分相容。
12. 不同资料的反对称化权重、耦合常数和因子 $i$ 约定可能不同，比较分量公式时必须核对。

## 常见误区

1. **外微分只能作用在函数上。** 它作用于任意次数的微分形式。
2. **$d^2=0$ 表示第一次求导已经为零。** 第一次通常非零，只是再求一次外微分为零。
3. **外微分就是对每个系数求普通导数，完全不管楔积。** 还必须按反对称楔积和分次 Leibniz 法则组织结果。
4. **$d(\alpha\wedge\beta)=d\alpha\wedge\beta+\alpha\wedge d\beta$ 永远成立。** 左因子为奇数次形式时第二项有负号。
5. **每个闭形式都是恰当形式。** 局部常成立，全局可能被拓扑阻止。
6. **外微分需要先选择度量。** 不需要；Hodge 星、余微分等才依赖度量。
7. **外微分与协变导数完全相同。** 外微分只增加一个完全反对称余切指标，不需要一般联络。
8. **非 Abelian 场强也是 $F=dA$。** 还含非线性项 $A\wedge A$。
9. **任何弦背景都严格满足 $H=dB$ 与 $dH=0$。** 源、全局拓扑和 Chern–Simons 修正会改变简单公式。
10. **$dF=0$ 与场方程是同一件事。** 在 Abelian 电磁学中它是 Bianchi 恒等式；动力学方程通常还涉及 Hodge 星，例如 $d{*F}=J$。

## 练习

1. 外微分把 $p$ 形式映到什么次数的形式？
2. 写出函数 $f$ 的外微分坐标公式。
3. 设 $\alpha=Pdx+Qdy$，写出 $d\alpha$。
4. 求 $\alpha=x^2y\,dx+(x+y^3)dy$ 的外微分。
5. 写出分次 Leibniz 法则。
6. 若 $\alpha$ 是一形式，$d(\alpha\wedge\beta)$ 的第二项为什么带负号？
7. 用混合偏导数和楔积说明为什么 $d^2f=0$。
8. “恰当形式必闭”如何由 $d^2=0$ 推出？
9. 为什么闭形式不一定全局恰当？
10. 写出外微分与拉回的相容关系。
11. Abelian 规范变换 $A\mapsto A+d\lambda$ 为什么不改变 $F=dA$？
12. 写出 Kalb–Ramond 二形式势的简单局部场强和规范变换。

## 答案与提示

1. $d:\Omega^p(M)\to\Omega^{p+1}(M)$。
2. $df=(\partial f/\partial x^i)dx^i$。
3. $d\alpha=(\partial_xQ-\partial_yP)dx\wedge dy$。
4. $d\alpha=(1-x^2)dx\wedge dy$。
5. $d(\alpha\wedge\beta)=d\alpha\wedge\beta+(-1)^p\alpha\wedge d\beta$，其中 $p=\deg\alpha$。
6. 因为 $(-1)^1=-1$；$d$ 是次数为 $1$ 的分次导子。
7. 二阶偏导系数对指标对称，$dx^j\wedge dx^i$ 对指标反对称，求和后抵消。
8. 若 $\omega=d\eta$，则 $d\omega=d^2\eta=0$。
9. 非平凡拓扑可使闭形式具有非零周期积分，从而不存在全局单值势。
10. $d(F^*\omega)=F^*(d\omega)$。
11. $F'=d(A+d\lambda)=dA+d^2\lambda=F$。
12. $H=dB$，$B\mapsto B+d\Lambda$；在简单无源局部模型中 $H$ 不变且 $dH=0$。

## 与其他概念的关系

- [[微分形式]] 是外微分作用的对象；
- [[偏导数]] 提供局部坐标中的系数求导；
- [[复合函数]] 和链式法则解释 $F^*(df)=d(f\circ F)$；
- [[Stokes 定理]] 将内部的 $d\omega$ 积分与边界上的 $\omega$ 积分联系；
- [[上同调]] 用 $d$ 的核与像刻画全局拓扑；
- [[辛结构]] 是闭且非退化的二形式；
- [[联络]] 与 [[协变导数]] 用于一般张量微分，而外微分无需选择联络；
- [[规范场强]] 在 Abelian 情形写成 $F=dA$；
- [[Bianchi 恒等式]] 在 Abelian 情形由 $d^2=0$ 直接产生；
- [[Kalb-Ramond 场]] 的简单局部场强为 $H=dB$；
- [[NS5-brane]] 可作为 $H$ 通量的磁源，使全局结构不同于无源局部模型；
- [[Chern-Simons 形式]] 可进入弦有效理论中的修正场强。

## 参考资料

- John M. Lee, *Introduction to Smooth Manifolds*, 2nd ed., Springer, 2013.
- Loring W. Tu, *An Introduction to Manifolds*, 2nd ed., Springer, 2011.
- Michael Spivak, *Calculus on Manifolds*, Westview Press, 1998.
- Harley Flanders, *Differential Forms with Applications to the Physical Sciences*, Dover, 1989.
- Raoul Bott and Loring W. Tu, *Differential Forms in Algebraic Topology*, Springer, 1982.
- Mikio Nakahara, *Geometry, Topology and Physics*, 2nd ed., CRC Press, 2003.
- Theodore Frankel, *The Geometry of Physics*, 3rd ed., Cambridge University Press, 2012.
- Michael E. Peskin and Daniel V. Schroeder, *An Introduction to Quantum Field Theory*, Westview Press, 1995.
- Joseph Polchinski, *String Theory, Vol. 1*, Cambridge University Press, 1998.
- Joseph Polchinski, *String Theory, Vol. 2*, Cambridge University Press, 1998.
- David Tong, *Lectures on String Theory*, [arXiv:0908.0333](https://arxiv.org/abs/0908.0333)
