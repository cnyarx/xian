---
aliases: [Derivative, Differentiation]
english_name: Derivative
tags: [数学, 微积分, 弦理论先修, 弦理论]
prerequisites:
  - "[[函数]]"
  - "[[极限]]"
used_by:
  - "[[积分]]"
  - "[[偏导数]]"
  - "[[Lagrangian]]"
evidence_status: 教材事实
---

# 导数（Derivative）

## 一句话解释

导数描述函数在某一点附近变化得有多快，也就是图像在该点的切线斜率。

## 为什么要学它

速度是位置的导数，加速度是速度的导数；物理方程几乎都描述量如何变化。弦理论的运动方程和世界面场论会同时使用时间、空间方向的导数。

## 从平均变化率到瞬时变化率

函数从 $x$ 变到 $x+\Delta x$ 时，平均变化率是：

$$
\frac{f(x+\Delta x)-f(x)}{\Delta x}.
$$

让非零的 $\Delta x$ 趋近零，得到导数：

$$
f'(x)=\lim_{\Delta x\to0}
\frac{f(x+\Delta x)-f(x)}{\Delta x}.
$$

它没有直接除以零，而是研究差商的极限。

## 几何意义

差商是连接图像两点的割线斜率。两点越来越接近时，割线趋近切线，所以导数是切线斜率。

- $f'(x)>0$：函数局部上升；
- $f'(x)<0$：函数局部下降；
- $f'(x)=0$：切线水平，但不一定是最大或最小值。

## 用定义求 $x^2$ 的导数

设 $f(x)=x^2$：

$$
f'(x)=\lim_{h\to0}\frac{(x+h)^2-x^2}{h}.
$$

展开：

$$
\frac{x^2+2xh+h^2-x^2}{h}=2x+h.
$$

在 $h\ne0$ 时约去 $h$，再取极限：

$$
f'(x)=\lim_{h\to0}(2x+h)=2x.
$$

## 常见求导规则

$$
\frac{d}{dx}c=0,
$$

$$
\frac{d}{dx}x^n=nx^{n-1},
$$

$$
\frac{d}{dx}[f+g]=f'+g',
$$

$$
\frac{d}{dx}[fg]=f'g+fg',
$$

$$
\frac{d}{dx}\frac fg=\frac{f'g-fg'}{g^2}\quad(g\ne0).
$$

这些分别是常数、幂、和、积与商的求导规则。

## 链式法则

复合函数的导数为：

$$
\frac{d}{dx}f(g(x))=f'(g(x))g'(x).
$$

它表示总变化率等于“外层对中间量的变化率”乘“中间量对输入的变化率”。

例如：

$$
\frac{d}{dx}(x^2+1)^3
=3(x^2+1)^2\cdot2x.
$$

## 指数与三角函数导数

使用弧度时：

$$
\frac{d}{dx}e^x=e^x,
$$

$$
\frac{d}{dx}\sin x=\cos x,
$$

$$
\frac{d}{dx}\cos x=-\sin x.
$$

三角函数这些简洁形式依赖角度使用弧度。

## 高阶导数

连续求导可写成：

$$
f''(x)=\frac{d^2f}{dx^2}.
$$

位置的一阶时间导数是速度，二阶时间导数是加速度。二阶导数也描述图像弯曲方向。

## 单位和量纲

若 $y$ 的单位为米、$x$ 的单位为秒，则 $dy/dx$ 的单位是米每秒。导数的量纲等于输出量纲除以输入量纲。

因此不能忽略导数变量；$d/dt$ 与 $d/dx$ 描述不同方向的变化。

## 近似

当 $\Delta x$ 很小时：

$$
f(x+\Delta x)\approx f(x)+f'(x)\Delta x.
$$

这叫线性近似。它表示足够小的局部范围内，光滑曲线看起来像直线。

## 物理中的例子

$$
v(t)=\frac{dx}{dt},
\qquad
a(t)=\frac{d^2x}{dt^2}.
$$

若 $x(t)=At^2$，则 $v=2At$、$a=2A$。

弦坐标依赖两个变量 $X^\mu(\tau,\sigma)$，需要 [[偏导数]] 分别描述沿 $\tau$ 和 $\sigma$ 的变化。

## 常见误区

1. **导数就是函数值。** 错，它是变化率。
2. **$f'(x)=0$ 必定是极值。** 错，例如 $f(x)=x^3$ 在零处导数为零但不是极值。
3. **积的导数等于 $f'g'$。** 错，应为 $f'g+fg'$。
4. **链式法则可以漏掉内层导数。** 错。
5. **$d^2x/dt^2$ 是 $(dx/dt)^2$。** 错，前者是二阶导数。

## 练习

1. 求 $d(x^3)/dx$。
2. 求 $d(3x^2+2x-1)/dx$。
3. 求 $d[\sin(2x)]/dx$。
4. 若 $x(t)=5t^2$，求速度和加速度。
5. 说明导数的几何意义。

## 答案与提示

1. $3x^2$。
2. $6x+2$。
3. $2\cos(2x)$。
4. $v=10t$，$a=10$。
5. 函数图像在该点的切线斜率。

## 与其他概念的关系

- 导数由 [[极限]] 定义；
- [[积分]] 在微积分基本定理中与导数互为逆过程；
- [[偏导数]] 处理多变量函数；
- 运动方程、场方程和弦方程都由导数组成。

## 参考资料

- [OpenStax, Calculus Volume 1, Chapter 3](https://openstax.org/books/calculus-volume-1/pages/3-introduction)
- [MIT OCW 18.01SC, Differentiation](https://ocw.mit.edu/courses/18-01sc-single-variable-calculus-fall-2010/)
