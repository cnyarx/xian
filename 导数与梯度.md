---
aliases:
  - 导数
  - 梯度
  - 链式法则
  - Derivative
  - Gradient
  - Chain Rule
tags:
  - 数学
  - 微积分
  - 教程
  - 大模型
  - 学习教程
prerequisites:
  - 简单方程与函数图像
next:
  - "[[梯度下降]]"
  - "[[反向传播]]"
---

# 导数与梯度

## 一句话定义

**导数**（Derivative）描述函数在某一点的变化速度。**梯度**（Gradient）是导数在多变量函数上的推广——它告诉你朝哪个方向走，函数值上升最快。

---

## 为什么需要它

大模型的训练本质上就是求梯度。训练过程可以概括为：

1. 计算当前模型预测的"错误程度"（损失函数）
2. 计算损失函数对每个参数的梯度
3. 沿着梯度的反方向调整参数（让损失下降）

这就像一个盲人在山上想下山——他用脚感受各个方向的坡度，然后朝最陡的下坡方向走。梯度就是"坡度"。

---

## 导数

### 直觉理解

**导数 = 函数在某一点的斜率 = 变化速度**

```
位置    →    速度（导数）
钱      →    赚钱速度（导数）
温度    →    升温速度（导数）
损失    →    损失下降速度（导数）
```

### 严格定义

函数 $f(x)$ 在点 $x$ 处的导数 $f'(x)$ 定义为：

$$
f'(x) = \lim_{h \to 0} \frac{f(x + h) - f(x)}{h}
$$

**每个符号在说什么：**
- $f'(x)$：函数 $f(x)$ 在 $x$ 处的导数
- $f(x+h) - f(x)$：$x$ 从 $x$ 变到 $x+h$ 时，函数值的变化量
- $h$：$x$ 的微小变化量
- $\lim_{h \to 0}$：让 $h$ 趋近于 0，求出"瞬间"的变化速度

**这条公式在说：** 导数 = 函数值的变化量 ÷ 自变量的变化量，当变化量趋近于 0 时的极限。

### 数值例子

$f(x) = x^2$ 在 $x=3$ 处的导数：

```
f'(3) = 2 × 3 = 6
```

这意味着：在 $x=3$ 附近，$x$ 每增加 1，$f(x)$ 大约增加 6。

验证：
```
f(3) = 9
f(3.1) = 9.61
变化量 = 9.61 - 9 = 0.61
x的变化量 = 0.1
变化速度 ≈ 0.61 / 0.1 = 6.1 ≈ 6 ✓
```

### 常见函数的导数

| 函数 $f(x)$ | 导数 $f'(x)$ | 说明 |
|---|---:|---|
| $c$（常数） | $0$ | 常数不变 |
| $x$ | $1$ | 变化速度恒定 |
| $x^2$ | $2x$ | 变化速度随 $x$ 增大 |
| $x^n$ | $nx^{n-1}$ | 幂函数规则 |
| $e^x$ | $e^x$ | 指数函数导数等于自身 |
| $\ln x$ | $1/x$ | 对数函数的导数 |

---

## 偏导数

### 直觉理解

当函数有多个变量时，**偏导数**（Partial Derivative）是"固定其他变量不变，只看这一个变量对函数的影响"。

**生活例子：**

```
温度 = f(时间, 位置)

偏导数 ∂f/∂时间：在同一个位置，温度随时间的变化速度
偏导数 ∂f/∂位置：在同一个时间，温度随位置的变化速度
```

### 严格定义

对 $f(x_1, x_2, \ldots, x_n)$，关于 $x_1$ 的偏导数记作：

$$
\frac{\partial f}{\partial x_1} = \lim_{h \to 0} \frac{f(x_1 + h, x_2, \ldots, x_n) - f(x_1, x_2, \ldots, x_n)}{h}
$$

注意：符号从 $d$ 变成了 $\partial$（读作 "partial"），表示这是偏导数。

---

## 梯度

### 一句话定义

**梯度**（Gradient）是把所有偏导数放在一起组成的向量，记作 $\nabla f$。

$$
\nabla f = \begin{bmatrix}
\frac{\partial f}{\partial x_1} \\[4pt]
\frac{\partial f}{\partial x_2} \\[4pt]
\vdots \\[4pt]
\frac{\partial f}{\partial x_n}
\end{bmatrix}
$$

**每个符号在说什么：**
- $\nabla f$（读作 "nabla f" 或 "grad f"）：函数 $f$ 的梯度
- 梯度的每个分量是 $f$ 对对应变量的偏导数
- 梯度是一个向量，指向函数值上升最快的方向

### 直觉理解

```
想象你站在一座山上：

梯度 → 指向最陡的上坡方向
梯度的大小 → 那个方向的坡度有多陡

要下山（最小化损失）：
朝梯度的反方向走 → -∇f

这就是梯度下降（Gradient Descent）
```

### 数值例子

$f(x, y) = x^2 + y^2$

```
∂f/∂x = 2x
∂f/∂y = 2y

在点 (3, 4) 处：
梯度 ∇f = [2×3, 2×4] = [6, 8]
```

梯度指向 $(6, 8)$，表示从 $(3, 4)$ 出发，朝 $(6, 8)$ 方向走，函数值上升最快。

---

## 链式法则

### 一句话定义

**链式法则**（Chain Rule）是计算复合函数导数的方法。这是反向传播的数学基础。

### 直觉理解

如果你想知道"调高温度旋钮会让最终产品温度变化多少"，你需要知道：

1. 旋钮转 1 格 → 电流变化多少
2. 电流变化 1 安 → 加热器功率变化多少
3. 功率变化 1 瓦 → 温度变化多少

最终 = 这三个变化率的乘积。

这就是链式法则。

### 严格定义

如果 $z = g(y)$ 且 $y = f(x)$，那么：

$$
\frac{dz}{dx} = \frac{dz}{dy} \cdot \frac{dy}{dx}
$$

**多变量版本：**

如果 $L = f(g(h(x)))$，那么：

$$
\frac{dL}{dx} = \frac{dL}{dg} \cdot \frac{dg}{dh} \cdot \frac{dh}{dx}
$$

### 在大模型中的核心角色

**反向传播（Backpropagation）就是链式法则。**

一个神经网络是很多层函数的复合：

```
输出 = 第N层(第N-1层(...第1层(输入)...))
```

计算损失对第一层参数的梯度时，需要把后面所有层的梯度连乘起来：

```
∂L/∂W₁ = ∂L/∂y_N × ∂y_N/∂y_{N-1} × ... × ∂y₂/∂y₁ × ∂y₁/∂W₁
```

这就是链式法则在神经网络中的应用。

---

## 在大模型中的核心角色

### 1. 训练

```
for 每一步:
    1. 前向传播：计算预测
    2. 计算损失 L
    3. 反向传播：计算 ∂L/∂θ（每个参数的梯度）
    4. 更新参数：θ = θ - 学习率 × 梯度
```

### 2. 梯度下降

梯度下降是机器学习中最基础的优化算法，见 `[[梯度下降]]`。

### 3. 自动求导

PyTorch 等框架会自动计算梯度，你不需要手动推导。但它背后的原理就是链式法则。

---

## 常见误区

1. **导数不是"切线"**
   导数是切线斜率，不是切线本身。

2. **梯度向上，不是向下**
   梯度指向函数值**上升**最快的方向。梯度下降是朝梯度的**反方向**走。

3. **梯度为零不一定是最小值**
   梯度为零可能是最大值、最小值或鞍点。需要额外判断。

4. **链式法则不是简单相加**
   有些初学者以为 $d(f(g(x)))/dx = f'(g(x)) + g'(x)$，这是错的。应该是乘积。

---

## 概念与公式导航

- [[参数梯度与优化器|参数、梯度与优化器]]
- [[导数梯度与链式法则公式|导数、梯度与链式法则公式]]

## 练习

1. 求导：$f(x) = 3x^2 + 2x + 1$ 在 $x=2$ 处的导数。

2. 求偏导数：$f(x, y) = x^2 y + 3xy^2$，求 $\partial f/\partial x$ 和 $\partial f/\partial y$。

3. 求梯度：$f(x, y, z) = x^2 + y^2 + z^2$ 在点 $(1, 2, 3)$ 处的梯度。

4. 用链式法则求导：$f(x) = (x^2 + 1)^3$。（提示：设 $u = x^2 + 1$，$f = u^3$）

5. （思考题）为什么梯度下降要朝梯度的反方向走，而不是朝梯度方向走？

---

## 本篇引入的新概念

| 中文 | 英文 | 简要说明 |
|---|---:|---|
| 导数 | Derivative | 函数在某一点的变化速度 |
| 偏导数 | Partial Derivative | 多变量函数对一个变量的导数 |
| 梯度 | Gradient | 所有偏导数组成的向量 |
| 链式法则 | Chain Rule | 复合函数求导的规则 |
| 反向传播 | Backpropagation | 链式法则在神经网络中的应用 |

---

## 下一步

- 数学基础继续：`[[指数与对数]]`
- 看导数在机器学习中的应用：`[[梯度下降]]`
- 看链式法则在神经网络中的应用：`[[反向传播]]`

---

## 来源

- 微积分基础：Stewart, J. (2015). *Calculus: Early Transcendentals* (8th ed.). Cengage Learning. 第2-3章, 第14章 `[A]`
- 梯度与优化：Goodfellow, I., Bengio, Y., & Courville, A. (2016). *Deep Learning*. MIT Press. 第4章 `[A]`

---

> 创建日期：2026-09-04 | 最后修订：2026-09-04