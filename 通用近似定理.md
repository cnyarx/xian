---
aliases: [通用近似定理, Universal Approximation Theorem, UAT]
tags: [概念, 神经网络, 理论, 大模型, 学习教程]
level: L8
prerequisites: ["[[线性变换与仿射变换]]", "[[变量与函数]]", "[[极限与趋近]]", "[[激活函数公式|激活函数公式]]"]
---
# 通用近似定理

## 一句话定义

通用近似定理（Universal Approximation Theorem）是一族有明确条件的定理。以 Cybenko 的经典形式为例：在紧致区域上，使用连续、非恒定且具有特定“sigmoidal”性质的激活函数时，足够宽的单隐层前馈网络可以任意精确地逼近连续函数。后续定理把结论扩展到 ReLU 等其他激活函数，但不能把条件简写为“任意非线性激活都可以”。

## 直觉理解

用足够多的直线段（激活函数）可以拼出任意曲线。隐层神经元越多，每个"线段"越细，逼近效果越好。但定理只保证"存在"这样一组参数，不保证训练能找到。

## 严格定义

对于 $[0,1]^n$ 上的任意连续函数 $f$ 和任意 $\epsilon>0$，存在单隐层网络 $g$ 使得：

$$\sup_{x\in[0,1]^n}|f(x)-g(x)|<\epsilon$$

其中 $g(x)=\sum_{i=1}^N v_i\sigma(w_i^Tx+b_i)$，$\sigma$ 是非线性有界单调递增连续函数。

## 例子

用 ReLU 网络逼近 $f(x)=x^2$：在多个点放置不同斜率的 ReLU，分段线性近似曲线。随着片段增多，逼近越来越精确。

## 边界

- 定理只保证存在性，不保证训练算法能找到所需参数；
- 所需隐藏层宽度可能随目标函数和精度要求快速增长；
- 经典形式要求紧致定义域和连续目标函数，不直接证明离散任务或任意非线性激活都可近似；
- 深度网络在某些函数族上能比浅层网络更高效，但这不是对所有任务、数据和训练过程都成立的无条件结论。

## 最小自测

1. 通用近似定理保证我们能训练出完美模型吗？
2. 为什么实践中深度网络比宽网络更常用？

答案：1. 不保证，它只保证存在一组参数，不保证训练能找到；2. 深度网络便于分层组合特征，并且对某些特定函数族，在给定网络类别、激活函数和误差标准时，深层网络可以比浅层网络用更少参数达到同等逼近精度；这不是对所有任务都成立的普遍优势。

## 详细教程

- [[多层感知机|多层感知机]]

## 来源

- Cybenko (1989), *Approximation by Superpositions of a Sigmoidal Function*：https://doi.org/10.1007/BF02551274 `[A]`
- Hornik, Stinchcombe & White (1989), *Multilayer Feedforward Networks are Universal Approximators*：https://doi.org/10.1016/0893-6080(89)90020-8 `[A]`