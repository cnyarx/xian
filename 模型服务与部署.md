---
aliases: [模型服务, 部署, 背压, 流式输出, 服务生命周期, Model Serving, Deployment, Backpressure, Streaming]
tags: [概念, 部署, 推理, 大模型, 学习教程]
level: L10
prerequisites: ["[[连续批处理与分页KV缓存]]", "[[推理阶段与延迟指标]]", "[[KV-Cache基础]]"]
---
# 模型服务与部署

## 一句话定义

模型服务（Model Serving）把训练好的模型部署为可调用的 API，处理请求调度、批处理、资源管理和监控；本地部署在用户自己的硬件上运行完整服务。

## 直觉理解

餐厅厨房（模型服务）接收订单，调度厨师（GPU）、准备食材（KV Cache），确保每道菜按时出餐的同时不浪费资源。本地部署是自己在家做饭。

## 关键概念

- 服务生命周期：加载、预热、服务、健康检查、优雅关闭；
- 背压（Backpressure）：请求超过处理能力时拒绝或排队；
- 流式输出：逐 token 返回，降低感知延迟；
- 监控：吞吐量、延迟、错误率、显存使用。

## 例子

服务启动时加载模型到 GPU（冷启动），预热后监听端口。收到请求后，调度器将请求加入连续批处理，返回流式 token。请求过多时返回 503 或排队。

## 边界

- 本地部署的数据安全取决于模型、工具、插件和遥测都被控制；
- 服务暴露到公网需要认证、加密和限流；
- 单 GPU 能服务的并发数和 batch size 有上限。

## 最小自测

1. 背压是什么？为什么需要它？
2. 流式输出能减少总延迟吗？

答案：1. 请求过多时主动拒绝或排队，防止系统崩溃；2. 不能减少总延迟，但能降低感知延迟。

## 详细教程

- [[模型服务|模型服务]]
- [[本地部署|本地部署]]

## 来源

- Kwon et al. (2023), *Efficient Memory Management for Large Language Model Serving with PagedAttention*：https://arxiv.org/abs/2309.06180 `[A]`
- IETF, *HTTP Semantics*, RFC 9110（状态码与服务语义）：https://www.rfc-editor.org/rfc/rfc9110 `[A]`
- NIST, *Zero Trust Architecture*, SP 800-207（认证、授权与最小信任边界）：https://csrc.nist.gov/pubs/sp/800/207/final `[A]`
- OpenTelemetry, *Documentation*（指标、日志和追踪的工程规范）：https://opentelemetry.io/docs/ `[A]`