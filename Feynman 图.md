---
aliases:
  - Feynman Diagram
  - Feynman Graph
  - 费曼图
english_name: Feynman Diagram
tags:
  - 物理学
  - 量子场论
  - 微扰论
  - 弦理论先修
  - 弦理论
prerequisites:
  - "[[场的正则量子化]]"
  - "[[路径积分]]"
  - "[[传播子]]"
  - "[[时序乘积]]"
  - "[[Wick 定理]]"
  - "[[生成泛函]]"
  - "[[微扰论]]"
  - "[[S 矩阵]]"
used_by:
  - "[[圈展开]]"
  - "[[有效场论]]"
  - "[[弦理论完整学习路线图]]"
evidence_status: 教材事实
---

# Feynman 图（Feynman Diagram）

## 一句话解释

Feynman 图是把量子场论微扰展开中的传播子、相互作用顶点、外部场插入和积分变量画成图的记账语言；每张图代表一个明确的数学表达式，而不是粒子在时空中真实走过的轨迹。

## 为什么要学它

Dyson 级数（Dyson series）是把时间演化算符或 $S$ 矩阵按相互作用次数展开的微扰级数；生成泛函展开则用源导数组织同一批微扰项。[[Wick 定理]] 再把其中的场算符分解成所有允许的收缩。直接逐项书写这些收缩很快会失去可读性，而 Feynman 图把相同信息压缩为：

- 线表示自由传播子；
- 顶点表示相互作用项；
- 外线标记被研究的外部场或渐近粒子；
- 圈对应未被顶点动量守恒完全固定的内部动量积分；
- 对称因子修正同一数学项被重复计数的次数。

它使我们能从 Lagrangian 系统地写出关联函数和散射振幅的微扰项，也是理解树级、圈修正（loop correction）、发散、重整化与 [[有效场论]]（effective field theory）的基础。圈修正是含闭合内部动量回路的量子修正；有效场论是在限定能标内按允许算符及其抑制尺度组织物理效应的理论框架。

对弦理论而言，点粒子 Feynman 图还提供重要对照：点粒子相互作用集中在顶点，弦相互作用则由光滑世界面的分裂与合并描述。两者都按拓扑组织微扰展开，但不能把时空中的粒子图与弦的世界面当成同一个对象。

## 本篇约定

采用自然单位 $\hbar=c=1$ 和 mostly-minus 度规：

$$
\eta_{\mu\nu}=\operatorname{diag}(+1,-1,-1,-1).
$$

以实标量 $\phi^4$ 理论为主要例子：

$$
\mathcal L
=\frac12\partial_\mu\phi\,\partial^\mu\phi
-\frac12m^2\phi^2
-\frac{\lambda}{4!}\phi^4.
$$

动量空间 Feynman 传播子采用：

$$
\widetilde\Delta_F(p)
=\frac{i}{p^2-m^2+i\varepsilon}.
$$

相互作用项 $\mathcal L_I=-\lambda\phi^4/4!$ 对应顶点因子 $-i\lambda$。Fourier 变换、外态归一化和整体动量守恒 delta 的写法可能因文献约定而异；比较公式时必须整套核对。

## 直观图景

可以把微扰计算想成拼装网络：

- 每个顶点有若干接口，接口数由相互作用中的场次数决定；
- 两个接口相连形成内部线，对应一个传播子；
- 没有连接到另一顶点、而是通向外部标记的接口形成外线；
- 对所有满足场类型、量子数和动量守恒的连接方式求和。

这个类比只描述组合结构。图上的线不是可被相机追踪的经典路径，内部线也不代表一个能被单独探测的短命粒子。它们是积分表达式中的传播子因子。

## 正式定义

在给定自由理论和相互作用的微扰展开中，一张 Feynman 图由以下数据组成：

1. **顶点（vertex）**：对应作用量或相互作用 Hamiltonian 中的一项；
2. **内部线（internal line）**：连接两个顶点或同一顶点的两个半边，对应一次 Wick 收缩和一个传播子；
3. **外线（external line）**：连接外部场插入或渐近态与图的其余部分；
4. **场与量子数标签**：说明每条线是哪种场，并携带动量、旋量、Lorentz、规范群等指标；
5. **积分与求和**：对未固定的内部位置、内部动量和离散指标积分或求和；
6. **组合系数**：包括展开阶数、顶点阶乘、费米符号和图的对称因子。

图与公式之间的对应关系依赖理论。不能只凭图形外观计算，必须先从具体 Lagrangian 和 convention 导出 Feynman 规则。

## 从 Dyson 展开到图

相互作用图景中的 $S$ 矩阵为：

$$
S
=T\exp\left[i\int d^4x\,\mathcal L_I(x)\right]
$$

其中该写法适用于已正确处理相互作用 Hamiltonian 与 Lagrangian 关系的情形。展开为：

$$
S
=\sum_{V=0}^{\infty}
\frac{i^V}{V!}
\prod_{a=1}^{V}\int d^4z_a\,
T\left\{\mathcal L_I(z_1)\cdots\mathcal L_I(z_V)\right\}.
$$

对 $\phi^4$ 理论，每个 $\mathcal L_I(z_a)$ 含四个场。Wick 定理枚举这些场之间以及它们与外部场之间的全部收缩：

- 每条收缩画成一条线；
- 每个 $\phi^4(z_a)$ 插入画成四价顶点；
- 没有被内部收缩消耗的外部场形成外线；
- 等价收缩合并后产生图的组合系数。

因此 Feynman 图不是额外假设，而是 Dyson–Wick 展开的图形化索引。

## 从生成泛函到图

对 $\phi^4$ 理论：

$$
Z[J]
=\exp\left[
-i\frac{\lambda}{4!}
\int d^4z\,
\left(\frac1i\frac{\delta}{\delta J(z)}\right)^4
\right]Z_0[J].
$$

自由生成泛函为：

$$
Z_0[J]
=Z_0[0]\exp\left[
-\frac12\int d^4x\,d^4y\,
J(x)\Delta_F(x-y)J(y)
\right].
$$

相互作用算符的四次源导数产生四价顶点；$Z_0[J]$ 的二次源结构使端点两两配对成传播子。展开耦合常数并对外源求导，就得到全部允许图。

[[生成泛函]] 的三层结构对应：

- $Z[J]$：生成包括不连通图在内的全部图；
- $W[J]=-i\ln Z[J]$：其源导数生成连通图；
- $\Gamma[\varphi]$：其场导数生成单粒子不可约顶点函数。

与源无关的真空图仍需通过 $Z[J]/Z[0]$ 或 $W[J]-W[0]$ 消去。

## 核心公式

### 标量传播子与顶点

对本篇的 $\phi^4$ convention：

$$
\boxed{
\text{内部标量线：}
\frac{i}{p^2-m^2+i\varepsilon}
}
$$

这条公式表示自由标量场从一端传播到另一端时，每条动量空间内部线贡献的核。$p$ 是流过该线的四动量，$m$ 是质量参数，$i\varepsilon$ 规定极点处方。在四维自然单位中，$p^2$、$m^2$ 和 $\varepsilon$ 都具有质量平方量纲，因此传播子具有质量的负二次方量纲。它要求使用本篇的自由 Klein–Gordon 二次作用量、mostly-minus 度规和 Feynman 边界条件。例如取一条 Euclidean 型离壳动量使 $p^2-m^2=-3m^2$，并暂略去极小的 $\varepsilon$，传播子约为 $-i/(3m^2)$。推导依赖 [[传播子]] 中对二次动力学算符求逆的过程。

$$
\boxed{
\phi^4\text{ 顶点：}-i\lambda
}
$$

这条公式表示相互作用 $-\lambda\phi^4/4!$ 的一次插入。$\lambda$ 是四维 $\phi^4$ 理论中的无量纲耦合；因而顶点因子本身无质量量纲。该结论依赖作用量指数 $e^{iS}$、四个场完全相同以及 Lagrangian 中已经包含 $1/4!$。例如一个四外线树图只有一个顶点时，其截肢后的核心因子就是 $-i\lambda$。推导依赖 Dyson 级数、Wick 收缩和四个相同场的 $4!$ 种连接计数。

每个顶点还带四维动量守恒：

$$
(2\pi)^4\delta^{(4)}
\left(\sum_{r\in v}p_r\right),
$$

其中所有 $p_r$ 统一约定为流入顶点。该 delta 表示顶点处总四动量为零；$r$ 枚举接到顶点 $v$ 的线。在自然单位中 $\delta^{(4)}(p)$ 具有质量的负四次方量纲，恰好补偿位置积分 $d^4x$ Fourier 变换后的量纲。它要求平移不变背景；例如四点顶点有 $p_1+p_2+p_3+p_4=0$。其推导依赖对顶点位置积分产生 Fourier delta。

### 圈数公式

设图有：

- $I$ 条内部线；
- $V$ 个顶点；
- $C$ 个连通分量。

独立圈数为：

$$
\boxed{L=I-V+C}.
$$

对连通图 $C=1$：

$$
\boxed{L=I-V+1}.
$$

圈数公式表示顶点动量守恒约束后仍独立的内部动量数。$L$、$I$、$V$、$C$ 都是无量纲整数，因此量纲一致。公式适用于把每条内部线和顶点视为图论边与顶点的有限图，并需按每个连通分量分别处理。例如连通图 $I=4,V=3$ 时 $L=2$。推导可从“每条内部线先给一个动量、每个顶点给一个守恒约束，而每个连通分量有一个冗余总动量约束”得到，依赖图的连通性和顶点动量守恒。

每个独立圈通常对应一个独立的四动量积分：

$$
\int\frac{d^4\ell}{(2\pi)^4}.
$$

这里 $\ell$ 是圈动量，$d^4\ell$ 在四维具有质量四次方量纲，$(2\pi)^4$ 来自 Fourier convention。该积分表示对所有未被外动量和顶点守恒固定的内部动量求和；具体收敛性还取决于传播子与顶点。一个一圈 tadpole 只有一个这样的积分。其来源正是上面的 $L=1$ 独立动量计数。

### $\phi^4$ 图的半边计数

若一张 $\phi^4$ 图有 $V$ 个四价顶点、$I$ 条内部线和 $E$ 条外线，则：

$$
\boxed{4V=2I+E}.
$$

左边数全部四价顶点提供的半边，右边把每条内部线的两个端点和每条外线的一个端点相加。$V$、$I$、$E$ 都是无量纲非负整数。公式只适用于所有相互作用顶点均为四价的图；若还含二价反项、三价或更高价顶点，必须分别计数。例如 $V=2,E=4$ 给出 $I=2$，再结合连通圈数公式得到 $L=1$。推导只依赖逐个清点每个顶点的四个半边。

## 公式中每个符号的意思

- $\phi(x)$：实标量场；
- $\mathcal L_I$：相互作用 Lagrangian 密度；
- $\lambda$：$\phi^4$ 耦合常数；
- $m$：标量粒子质量参数；
- $p$、$k$、$\ell$：线所携带的四动量；
- $i\varepsilon$：规定积分极点绕行方式并选出 Feynman 边界条件；
- $V$：相互作用顶点数；
- $I$：内部线数；
- $E$：外线数；
- $C$：图的连通分量数；
- $L$：独立圈数；
- $S_G$：图 $G$ 的对称因子；
- $Z[J]$：全部图的生成泛函；
- $W[J]$：连通图的生成泛函；
- $\Gamma[\varphi]$：1PI 顶点函数的生成泛函。

## 怎样写出动量空间 Feynman 规则

对给定理论，不能背一套规则到处使用。标准流程是：

1. 把 Lagrangian 分成自由二次部分和相互作用部分；
2. 求自由二次算符的逆，得到带正确指标结构和 $i\varepsilon$ 的传播子；
3. 从每个相互作用单项式读出顶点的场类型、指标和动量依赖；
4. 计入作用量指数中的 $i$，得到顶点因子；
5. 在每个顶点施加动量守恒；
6. 对每个独立圈动量积分；
7. 收缩所有内部指标，并计入闭合费米回路负号等统计符号；
8. 除以图的对称因子；
9. 对所有满足给定外线与微扰阶数的非等价图求和。

若相互作用含导数，顶点因子会依赖动量；若有费米场或规范场，还必须保留箭头、旋量矩阵、Lorentz 指标、群指标和鬼场贡献。

## 具体例子一：$\phi^4$ 四点函数的树图

考虑一阶连通四点关联函数：

$$
G_c^{(4)}(x_1,x_2,x_3,x_4)
=\langle0|T\{\phi(x_1)\phi(x_2)\phi(x_3)\phi(x_4)\}|0\rangle_c.
$$

在耦合常数一阶：

$$
G_{c,\,\lambda}^{(4)}
=\frac{-i\lambda}{4!}
\int d^4z\,
\left\langle0\left|
T\{\phi_1\phi_2\phi_3\phi_4\phi(z)^4\}
\right|0\right\rangle_{0,c}.
$$

要得到一个四外线连通图，四个外部场必须分别与顶点 $z$ 的四个场收缩。共有 $4!$ 种双射，恰好抵消相互作用中的 $1/4!$：

$$
\boxed{
G_{c,\,\lambda}^{(4)}
=(-i\lambda)
\int d^4z\,
\prod_{a=1}^{4}\Delta_F(x_a-z)
}.
$$

这张图有 $V=1$、$I=0$、$E=4$，所以：

$$
L=I-V+1=0.
$$

它是一张树图。把四条外部传播子截去，并 Fourier 变换到动量空间后，核心四点顶点为：

$$
-i\lambda
$$

并伴随：

$$
(2\pi)^4\delta^{(4)}(p_1+p_2+p_3+p_4),
$$

这里统一把四个动量取为流入顶点。

## 具体例子二：$\phi^4$ 二点函数的一圈蝌蚪图

蝌蚪图（tadpole diagram）是含有一条从同一顶点出发并回到该顶点的内部线的图，外形像带尾巴的蝌蚪。一阶二点函数包含：

$$
\frac{-i\lambda}{4!}
\int d^4z\,
\langle0|T\{\phi(x)\phi(y)\phi(z)^4\}|0\rangle_0.
$$

取连通收缩：

1. $\phi(x)$ 可与顶点的四个场之一收缩，有 $4$ 种；
2. $\phi(y)$ 再与剩余三个场之一收缩，有 $3$ 种；
3. 顶点剩余两个场彼此收缩，只有 $1$ 种。

因此有 $4\times3=12$ 个等价收缩。与 $4!=24$ 合并得到 $1/2$：

$$
\boxed{
G_{c,\,\lambda}^{(2)}(x,y)
=-\frac{i\lambda}{2}
\int d^4z\,
\Delta_F(x-z)\Delta_F(0)\Delta_F(z-y)
}.
$$

内部的自收缩形成 tadpole 圈。按图拓扑计数，截去两条外部传播子后有 $V=1$、$I=1$：

$$
L=1-1+1=1.
$$

动量空间中的 1PI 自能核含：

$$
\boxed{
-i\Sigma_{\mathrm{tad}}
=\frac{-i\lambda}{2}
\int\frac{d^4\ell}{(2\pi)^4}
\frac{i}{\ell^2-m^2+i\varepsilon}
}.
$$

该圈积分在四维通常紫外发散，需要正规化（regularization）和质量反项。正规化是先用截断、维数延拓等受控方法让发散表达式暂时成为可操作对象，再由重整化吸收允许的发散；Feynman 图负责组织积分，不会自动使发散消失。

## 对称因子

同一未标记图可能由许多不同 Wick 收缩产生。原始展开中的 $1/V!$、相互作用单项式中的阶乘和等价收缩数合并后，常把一张图的贡献写成：

$$
\frac{1}{S_G}\times
(\text{顶点因子})
(\text{传播子因子})
(\text{积分与 delta}).
$$

$S_G$ 是保持外部标记和所有场类型不变、却不改变图连接结构的内部置换所造成的重复计数因子。它必须结合具体理论与外线标记计算，不能只看图像大致形状猜测。

在上面的 tadpole 二点图中，顶点剩余两个相同场互换不改变收缩结构，最终给出 $S_G=2$，所以系数为 $1/2$。

对称因子不等于圈数，也不等于顶点价数；不同图即使有相同的 $V$、$I$ 和 $L$，也可能有不同对称因子。

## 外线、内线与截肢

在位置空间关联函数中，每个外部插入点通常通过一条传播子连接到图的内部。这样的表达式称为带外腿（external legs）的 Green 函数。

散射振幅需要把外部粒子解释为渐近在壳态。LSZ 约化（Lehmann–Symanzik–Zimmermann reduction）是把时间有序 Green 函数转换为渐近粒子散射振幅的公式。它的作用包括：

- 截去外部传播子；
- 取外部动量到质量壳 $p^2=m^2$；
- 计入场强重整化留数；
- 与指定的入射、出射态归一化匹配。

因此“画出了某个相关函数的图”不等于“已经得到物理散射振幅”。内部线一般离壳并被积分；外部渐近粒子才满足相应的在壳条件。

## 连通图、1PI 图与可约图

### 连通图

若任意两个顶点或外部插入之间都存在图上的路径，图就是连通的。连通散射过程由 $W[J]$ 的源导数组织。

### 单粒子不可约图

一张连通图若剪断任意一条内部线后仍保持连通，称为单粒子不可约图（one-particle irreducible, 1PI）。$\Gamma[\varphi]$ 生成 1PI 顶点函数。

### 单粒子可约图

若剪断某一条内部线会把图分成两个不连通部分，则该图是单粒子可约的（one-particle reducible, 1PR）。完整连通 Green 函数可由 1PI 顶点和完整传播子拼接得到。

“1PI”只检查剪断一条内部线，不表示剪掉顶点或多条线后也不能断开。

## 树图与圈图

对连通图：

$$
L=I-V+1.
$$

- $L=0$：树图，没有独立内部动量圈；
- $L=1$：一圈图；
- $L=2$：两圈图；
- 依此类推。

树级通常给半经典展开的最低量子阶，但“树级”等于“经典”需要结合所计算对象和有效作用量语境理解。圈图体现量子涨落并引入独立动量积分，但耦合常数阶数与圈数不是普遍相同的概念。

例如在 $\phi^4$ 理论中，顶点数控制 $\lambda^V$，而圈数还依赖内部线和外线。相同的 $\lambda$ 阶数可因外线数不同而出现不同拓扑。

## 真空图与归一化

没有外线的图称为真空图。它们贡献真空—真空振幅 $Z[0]$，但不会作为普通归一化关联函数中的外部过程保留下来。

对关联函数使用：

$$
\frac{Z[J]}{Z[0]}
$$

或对连通生成泛函使用：

$$
W[J]-W[0]
$$

可消去与源无关的真空图。

要区分：

- **连通真空图**自身没有分裂成多个图块，但仍无外线；
- **不连通图**可能含一个带外线的部分乘一个真空泡；
- 取 $\ln Z$ 选出连通分量，却不会自动令 $W[0]=0$。

## 费米场与规范场中的附加规则

### 费米场

费米传播子带旋量指标和箭头方向。每个独立闭合费米回路在标准 Grassmann 微扰规则中贡献额外因子：

$$
-1.
$$

此外还必须保留顶点处费米场顺序、外部费米态交换符号和 gamma 矩阵乘法顺序。

### 规范场

规范场传播子依赖 [[规范固定]]；闭合图中通常还需 [[Faddeev-Popov 鬼场]]。单张离壳图一般可能依赖规范参数，只有适当组合后的物理可观测量才应满足相应的规范独立性。

因此不能把标量理论的“线和顶点”规则不加修改地套到 Yang–Mills 理论。

## 与 S 矩阵的关系

[[S 矩阵]] 把渐近入射态映射到渐近出射态。Feynman 图用于组织给定微扰阶数下的矩阵元：

1. 指定外部粒子种类、动量和极化；
2. 枚举满足守恒律的连通图；
3. 按 Feynman 规则写出每张图；
4. 对内部动量积分并对图求和；
5. 进行重整化，并在需要时取适当的在壳极限；
6. 从振幅进一步计算截面或衰变率。

单张图通常不是独立可观测量。对称性和幺正性（unitarity，即概率守恒）往往只在一组图及反项的总和中正确体现。

## 与弦世界面的关系

点粒子量子场论中，相互作用被画成时空中的点状顶点，内部线连接这些顶点。固定圈阶的振幅是对内部动量的积分。

微扰弦论中，弦的分裂与合并形成光滑二维世界面：

- 闭弦树级对应球面；
- 闭弦一圈对应环面；
- 更高圈对应更高亏格的 Riemann 曲面；
- 外部弦态由世界面上的顶点算符插入表示；
- 振幅需要对插入位置和世界面模空间积分。

细长管极限（thin tube limit，即世界面窄长区域趋近一条细线的极限）可以表现出点粒子传播子的因子化（factorization）结构，但世界面不是"把每条粒子 Feynman 线加粗"得到的。弦论把许多点粒子场论图及其高能行为统一到世界面几何中。

## 适用条件与事实边界

1. Feynman 图是微扰展开的记账工具；若理论处于强耦合且没有受控展开，小数量图通常不足以描述完整物理。
2. 微扰级数在许多量子场论中是渐近级数（asymptotic series）：截断到适当阶数可以改善近似，但无穷级数本身不保证收敛。
3. 图的传播子和顶点规则依赖自由作用量、相互作用、规范固定、边界条件与 convention。
4. 内部线通常离壳，不能被解释成可单独探测的粒子。
5. 单张图可能依赖规范、重整化方案或场变量选择；物理量来自满足要求的图与反项之和。
6. 对称因子必须从等价收缩或图自同构结合外部标记计算，不能凭外形猜测。
7. $Z$、$W$、$\Gamma$ 分别组织全部图、连通图和 1PI 图；三者不能混用。
8. 真空图需通过归一化消去；取对数本身不等于把 $W[0]$ 设为零。
9. 每圈一个独立动量积分的说法针对通常的平移不变微扰理论；边界、背景场或离散时空会改变具体表示。
10. 弦世界面按拓扑和模空间组织微扰振幅，与点粒子时空 Feynman 图相关但不相同。

## 常见误区

1. **Feynman 图是粒子真实运动路线的照片。** 它是积分项的图形记账，内部线通常离壳。
2. **画出所有看起来可能的图就完成了计算。** 还必须满足场类型、守恒律、微扰阶数、指标收缩和对称因子。
3. **每条线都代表一个能被探测器看到的粒子。** 只有外部渐近态可这样关联；内部线是传播子。
4. **顶点因子永远是 $-i\lambda$。** 这只属于本篇约定的 $-\lambda\phi^4/4!$；其他相互作用有不同规则。
5. **圈数等于顶点数。** 正确拓扑关系是 $L=I-V+C$。
6. **同阶耦合常数的图一定有相同圈数。** 圈数还取决于内部线、外线和连通性。
7. **对称因子就是 Lagrangian 中的 $4!$。** $4!$ 会与 Wick 收缩数合并，最终因子由整张图的等价计数决定。
8. **$W[J]$ 消除了所有真空图。** $W[0]$ 仍可含连通真空图，需再减去。
9. **连通图都是 1PI 图。** 一张连通图可能剪断一条内部线后断开，因此是 1PR。
10. **相关函数的外线无需处理就等于散射振幅。** 散射振幅还需 LSZ 截肢、在壳极限和外态归一化。
11. **单张规范理论图必须是规范独立的。** 规范独立性通常只对适当总和与物理量成立。
12. **弦世界面就是粒子 Feynman 图的二维画法。** 世界面按亏格和模空间组织，物理与几何结构更丰富。

## 练习

1. 用一句话解释 Feynman 图与 Wick 收缩的关系。
2. 在 $\phi^4$ 理论中，一个顶点为什么给出 $-i\lambda$ 而不是 $-i\lambda/4!$？
3. 对连通图 $V=3$、$I=4$，计算圈数。
4. 一张 $\phi^4$ 连通图有 $V=2$、$E=4$。求内部线数 $I$ 和圈数 $L$。
5. 解释 tadpole 二点图的对称因子为什么是 $2$。
6. 写出 $\phi^4$ 四点树图的位置空间表达式。
7. 为什么除以 $Z[0]$ 会消去附着在带外线图旁边的真空泡？
8. 连通图与 1PI 图有什么区别？
9. 为什么一个闭合费米回路有额外负号？
10. “内部线表示粒子借用能量后短暂存在”有什么问题？
11. 相关函数图怎样转化为散射振幅？
12. 点粒子 Feynman 图和弦世界面在微扰展开中分别按什么结构组织？

## 答案与提示

1. 每次自由场 Wick 收缩对应一条传播子线；相互作用插入对应顶点，全部允许收缩由图枚举。
2. 展开的一阶系数含 $-i\lambda/4!$，而四条有标记外线连接到顶点四个相同场有 $4!$ 种方式，两者相消，留下 $-i\lambda$。
3. 对连通图 $L=I-V+1=4-3+1=2$，所以是两圈图。
4. 由 $4V=2I+E$ 得 $8=2I+4$，所以 $I=2$；连通图 $L=2-2+1=1$。
5. 两条外线连接顶点后，剩余两个相同场彼此收缩；交换这两个内部半边不改变图。等价收缩数与 $4!$ 合并后留下系数 $1/2$，即 $S_G=2$。
6. $G_{c,\lambda}^{(4)}=(-i\lambda)\int d^4z\,\prod_{a=1}^{4}\Delta_F(x_a-z)$。
7. 真空泡与含外线部分因不连通而因子化；分子和 $Z[0]$ 中出现同一真空因子，归一化后相消。
8. 连通只要求整张图有路径相连；1PI 还要求剪断任意一条内部线都不能把图断开。
9. 闭合费米回路的额外 $-1$ 来自 Grassmann 变量或费米算符反对易关系下的完整 Wick 收缩符号计数：把费米收缩按顶点既定次序重排并闭合成一条独立回路后，相对于对应的玻色连接结构多出一个负号。不能把它简单归结为某个固定长度的“一次奇循环置换”；外部费米态交换等其他符号仍需另计。
10. 这把积分核误解成经典过程。内部动量通常离壳并在所有允许值上积分，内部线不是可单独观测的中间粒子轨迹。
11. 对连通 Green 函数进行 LSZ 约化：截去外部传播子、取外部动量在壳并计入场强留数和外态归一化。
12. 点粒子场论按顶点、传播子和独立圈动量组织；微扰弦论按世界面拓扑、顶点算符插入与模空间积分组织。

## 与其他概念的关系

- [[场的正则量子化]] 提供自由场、粒子态和算符基础；
- [[路径积分]] 把相互作用展开为自由 Gaussian 期望值；
- [[传播子]] 给出每条内部线的数学因子；
- [[时序乘积]] 规定相互作用图景算符的时间排序；
- [[Wick 定理]] 把时序乘积枚举为收缩，并产生图的组合结构；
- [[生成泛函]] 用源导数系统地产生全部图、连通图和 1PI 图；
- [[微扰论]] 按耦合常数和半经典阶次组织图；
- [[S 矩阵]] 把图计算连接到渐近粒子散射；
- [[圈展开]] 按 $L$ 系统组织树级、一圈与更高圈图；
- [[有效场论]] 用 UV 理论与 EFT 两边的树图和圈图执行 matching，并按幂次计数选择需要计算的图；
- [[计算图自动微分与梯度累加|计算图、自动微分与梯度累加]] 也使用节点和边记录复杂表达式，因此两者都可视为图形化记账语言。但计算图描述一次确定计算及链式法则依赖，Feynman 图枚举微扰项、传播子、顶点和圈积分；Feynman 图不是计算图，传播子也不是反向传播；
- [[弦理论完整学习路线图]] 将本节点放在生成泛函之后、圈展开之前。

## 参考资料

- Michael E. Peskin and Daniel V. Schroeder, *An Introduction to Quantum Field Theory*, Westview Press, 1995, Chapters 4, 6 and 9.
- Mark Srednicki, *Quantum Field Theory*, Cambridge University Press, 2007, Chapters 8–11.
- Michael D. Schwartz, *Quantum Field Theory and the Standard Model*, Cambridge University Press, 2014, Chapters 7–13.
- Steven Weinberg, *The Quantum Theory of Fields, Vol. 1*, Cambridge University Press, 1995, Chapters 6 and 10.
- Claude Itzykson and Jean-Bernard Zuber, *Quantum Field Theory*, McGraw-Hill, 1980.
- [David Tong, Quantum Field Theory](https://www.damtp.cam.ac.uk/user/tong/qft.html)
- Joseph Polchinski, *String Theory, Vol. 1*, Cambridge University Press, 1998, Chapters 2, 6 and 7.
- David Tong, *String Theory*, arXiv:0908.0333, [arXiv](https://arxiv.org/abs/0908.0333)
