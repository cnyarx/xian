---
aliases: [量化, Quantization, INT8, INT4, GPTQ, AWQ, GGUF]
tags: [大模型, 部署, 教程, 学习教程]
prerequisites: ["[[向量与矩阵]]"]
next: ["[[模型服务]]"]
---
# 模型量化

## 一句话定义

量化用较少比特近似表示模型权重或激活，从而减少内存和计算成本。

## 基本公式

把浮点数 $x$ 映射为整数 $q$。若目标整数格式允许的范围是 $[q_{min},q_{max}]$：

$$q=\operatorname{clamp}\left(\operatorname{round}(x/s)+z,\ q_{min},\ q_{max}\right)$$
$$\hat x=s(q-z)$$

- $s$：缩放因子。
- $z$：零点。
- $q_{min},q_{max}$：目标整数格式允许的最小值和最大值。
- $\operatorname{clamp}$：把超出范围的结果截到边界内。
- $\hat x$：反量化后的近似值。

多个原值映射到同一个整数会产生量化误差。

## 常见精度

- FP16/BF16：半精度浮点。
- INT8：8 位整数。
- INT4：4 位整数，压缩更强。

权重 4-bit 的理论存储约为 16-bit 的四分之一，但元数据、分组尺度和运行时缓存会增加额外占用。

## PTQ 与 QAT

- 训练后量化（PTQ）：训练完成再量化，成本低。
- 量化感知训练（QAT）：训练时模拟量化误差，通常精度更好但成本高。

## GPTQ 与 AWQ

GPTQ 使用校准数据和二阶近似逐步量化权重。AWQ 观察激活，保护对输出更重要的权重通道。两者都是具体 PTQ 方法，实际效果依赖模型、数据、内核和硬件。

## GGUF

GGUF 是 llama.cpp 生态的模型文件格式，可包含多种量化类型和元数据。它不是一种单独量化算法。

## 跨学科关系：与有效场论共享“受控近似”结构

[[有效场论|有效场论]] 与模型量化都要求先指定使用范围和保真目标，再压缩当前任务不能分辨的细节，并以误差验证近似是否可接受：EFT 按 $E/\Lambda$ 幂次截断算符，量化则按位宽近似权重或激活并实测任务质量、延迟和显存。

两者不等价。模型量化没有 EFT 的 Wilson 系数、matching、算符基或 RG running；量化的舍入、截断和校准误差也不等于 EFT 的低能截断误差。完整边界见 [[弦理论与大模型跨学科关系图谱]]。

## 概念与公式导航

- [[模型量化方法|模型量化方法]]
- [[均匀仿射量化公式|均匀仿射量化公式]]
- [[有效场论|有效场论]]
- [[弦理论与大模型跨学科关系图谱]]

## 练习

1. 4-bit 理论上比 16-bit 少多少权重存储？
2. $s=0.1,z=0,x=0.26$，量化和反量化结果是什么？
3. PTQ 与 QAT 有何区别？
4. 为什么 GGUF 不能等同于量化算法？

## 来源

- Jacob, B. et al. (2018). *Quantization and Training of Neural Networks for Efficient Integer-Arithmetic-Only Inference*. CVPR https://arxiv.org/abs/1712.05877 `[A]`
- Frantar, E. et al. (2023). *GPTQ: Accurate Post-Training Quantization for Generative Pre-trained Transformers*. ICLR https://arxiv.org/abs/2210.17323 `[A]`
- Lin, J. et al. (2024). *AWQ: Activation-aware Weight Quantization for LLM Compression and Acceleration*. MLSys https://arxiv.org/abs/2306.00978 `[A]`
- llama.cpp GGUF specification：https://github.com/ggml-org/ggml/blob/master/docs/gguf.md `[A]`