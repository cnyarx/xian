---
aliases:
  - Interval
  - Intervals
english_name: Interval
tags:
  - 数学
  - 高中基础
  - 弦理论先修
  - 弦理论
prerequisites:
  - "[[实数]]"
used_by:
  - "[[不等式]]"
  - "[[函数]]"
  - "[[积分]]"
  - "[[开弦边界条件]]"
evidence_status: 教材事实
---

# 区间（Interval）

## 一句话解释

区间是数轴上连续的一段实数；两个数只要属于这段区间，它们中间的所有实数也都属于这段区间。

## 为什么要学它

数学和物理不仅要说明一个量“是什么数”，还要说明它“允许在哪个范围内变化”。例如：

- 时间可以只研究从 $0$ 秒到 $10$ 秒；
- 函数可能只在正数范围内有定义；
- 积分要指定起点和终点；
- 开弦上的位置参数常取 $0$ 到 $\pi$ 之间的所有值。

这些范围都可以用区间简洁地表示。

## 区间建立在实数上

本篇用到的所有端点和区间内元素都属于 [[实数]]。若 $a<b$，从 $a$ 到 $b$ 的连续一段数可以根据是否包含端点写成不同区间。

## 闭区间

闭区间包含左右两个端点：

$$
[a,b]=\{x\in\mathbb R\mid a\le x\le b\}.
$$

逐个解释符号：

- $[a,b]$ 是从 $a$ 到 $b$ 的闭区间；
- 花括号表示一个集合；
- $x\in\mathbb R$ 表示 $x$ 是实数；
- $\mid$ 表示“满足”；
- $a\le x\le b$ 表示 $x$ 不小于 $a$，也不大于 $b$。

例如：

$$
[0,1]
$$

包含 $0$、$1$ 和它们之间的所有实数，如 $0.2$、$1/2$、$\sqrt2/2$。

## 开区间

开区间不包含左右两个端点：

$$
(a,b)=\{x\in\mathbb R\mid a<x<b\}.
$$

例如 $(0,1)$ 包含 $1/2$，但不包含 $0$ 和 $1$。

## 半开区间

半开区间只包含一个端点：

$$
[a,b)=\{x\in\mathbb R\mid a\le x<b\},
$$

$$
(a,b]=\{x\in\mathbb R\mid a<x\le b\}.
$$

方括号靠近哪一端，就表示包含哪一端；圆括号靠近哪一端，就表示不包含哪一端。

## 无限区间

若范围向某一方向无限延伸，可以写成：

$$
(-\infty,a),\qquad (-\infty,a],
$$

$$
(a,\infty),\qquad [a,\infty).
$$

其中 $\infty$ 表示正方向没有上界，$-\infty$ 表示负方向没有下界。它们不是普通实数，所以靠近无穷的一端永远使用圆括号。

例如：

$$
[0,\infty)
$$

表示所有不小于 $0$ 的实数。

## 单点和空区间

若左右端点相同，闭区间只包含一个数：

$$
[a,a]=\{a\}.
$$

而开区间 $(a,a)$ 不可能包含满足 $a<x<a$ 的实数，因此它是空集：

$$
(a,a)=\varnothing.
$$

$\varnothing$ 表示没有任何元素的集合。

## 用不等式和区间表达同一个范围

下面两种写法表达相同意思：

$$
-2\le x<3
$$

和

$$
x\in[-2,3).
$$

第一种是 [[不等式]] 写法，第二种是区间写法。左端允许等于 $-2$，所以用方括号；右端不允许等于 $3$，所以用圆括号。

## 区间长度

有限区间从 $a$ 延伸到 $b$，其长度为：

$$
b-a.
$$

开、闭与否不改变长度。例如 $(0,1)$ 和 $[0,1]$ 的长度都是 $1$。它们是否包含端点不同，但从左端到右端的距离相同。

## 弦理论中的例子

开弦常把弦上位置参数写成：

$$
\sigma\in[0,\pi].
$$

它表示：

- $\sigma$ 可以取 $0$ 到 $\pi$ 之间的任意实数；
- $\sigma=0$ 和 $\sigma=\pi$ 也允许取到；
- 两个端点对应开弦的两端。

闭弦则常把 $\sigma$ 看作首尾相接的周期坐标。虽然也可以先取某个有限区间作为代表范围，但两个端点代表同一个位置；这会在 [[闭弦周期条件]] 中详细解释。

## 常见误区

1. **圆括号一定表示坐标点。** 错。$(a,b)$ 在不同上下文中可能是坐标点，也可能是开区间。
2. **$[a,b]$ 只包含端点。** 错，它还包含两个端点之间的所有实数。
3. **$[0,\infty]$ 是标准实数区间。** 错，$\infty$ 不是实数，应写成 $[0,\infty)$。
4. **开区间的长度比闭区间短。** 错，$(a,b)$ 与 $[a,b]$ 的长度都为 $b-a$。
5. **$(a,a)$ 包含 $a$。** 错，严格不等式 $a<x<a$ 无解，所以它是空集。

## 练习

1. 把 $-2\le x<3$ 写成区间。
2. 把 $x\in(-1,4]$ 写成不等式。
3. 判断 $0$、$1/2$、$1$ 是否属于 $(0,1]$。
4. 写出所有负实数组成的区间。
5. 求区间 $[-3,5)$ 的长度。
6. 解释为什么无穷端不能使用方括号。

## 答案与提示

1. $[-2,3)$。
2. $-1<x\le4$。
3. $0$ 不属于；$1/2$ 属于；$1$ 属于。
4. $(-\infty,0)$。
5. $5-(-3)=8$。
6. 方括号表示包含端点，但 $\infty$ 和 $-\infty$ 不是实数端点，不能被包含。

## 与其他概念的关系

- 区间的元素来自 [[实数]]；
- 区间可以表示 [[不等式]] 的解集；
- [[函数]] 需要用区间说明定义域和值域；
- [[积分]] 用区间说明累积计算的范围；
- 弦的参数区间与 [[开弦边界条件]]、[[闭弦周期条件]] 有直接关系。

## 参考资料

- [OpenStax, Precalculus 2e, Section 1.1](https://openstax.org/books/precalculus-2e/pages/1-1-real-numbers-algebra-essentials)
- [MIT OCW 18.01SC, Single Variable Calculus](https://ocw.mit.edu/courses/18-01sc-single-variable-calculus-fall-2010/)
