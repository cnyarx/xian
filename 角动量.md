---
aliases: [Angular Momentum, Orbital Angular Momentum, 角动量]
english_name: Angular Momentum
tags: [物理学, 经典力学, 量子力学, 对称性, 弦理论先修, 弦理论]
prerequisites:
  - "[[向量]]"
  - "[[动量]]"
  - "[[对易关系]]"
  - "[[量子算符]]"
used_by:
  - "[[自旋]]"
  - "[[SO 群]]"
  - "[[Lie 代数]]"
  - "[[球谐函数]]"
  - "[[弦的量子态]]"
evidence_status: 教材事实
---

# 角动量（Angular Momentum）

## 一句话解释

角动量是描述物体或量子态“绕某点旋转多少”的物理量，也是空间旋转变换的生成元。

## 为什么要学它

经典力学中，角动量守恒解释行星轨道、陀螺和旋转物体。量子力学中，角动量只能取特定离散值，并决定原子能级、选择定则和粒子的旋转性质。

自旋是一种不依赖经典空间轨道的内禀角动量。弦的激发态也按目标时空旋转群的角动量或自旋表示分类，因此角动量代数是连接量子力学、群论和弦谱的入口。

## 直观图景

同样质量和速度的物体，离旋转中心越远，越难改变其绕中心的运动；同样距离下，横向速度越大，旋转趋势越强。

这由经典公式：

$$
\mathbf L=\mathbf r\times\mathbf p
$$

表达。叉积只保留垂直于位置方向的动量成分，所以径直朝向或离开原点的运动不产生关于该原点的轨道角动量。

量子角动量不能完全想成小球沿确定圆轨道转动。量子态通常没有同时确定的位置和动量，角动量由算符和旋转对称性定义。

## 经典轨道角动量

相对于选定原点，质点的轨道角动量为：

$$
\mathbf L=\mathbf r\times\mathbf p.
$$

若 $\mathbf p=m\mathbf v$，则：

$$
\mathbf L=m\mathbf r\times\mathbf v.
$$

其中：

- $\mathbf L$：轨道角动量；
- $\mathbf r$：从原点指向质点的位置向量；
- $\mathbf p$：线动量；
- $m$：质量；
- $\mathbf v$：速度；
- $\times$：三维向量叉积。

角动量方向由右手定则确定，垂直于 $\mathbf r$ 与 $\mathbf p$ 张成的平面。

量纲为：

$$
[\mathbf L]
=\mathrm{kg\,m^2/s},
$$

与作用量和 $\hbar$ 的量纲相同。

## 大小公式

角动量大小为：

$$
L=rp\sin\theta,
$$

其中 $\theta$ 是 $\mathbf r$ 与 $\mathbf p$ 的夹角。

- 若 $\theta=90^\circ$，角动量最大，为 $rp$；
- 若 $\theta=0$ 或 $180^\circ$，角动量为零；
- 改变参考原点会改变 $\mathbf r$，因此轨道角动量通常依赖原点。

## 力矩与角动量变化

力矩定义为：

$$
\boldsymbol\tau
=\mathbf r\times\mathbf F.
$$

对质点可推出：

$$
\frac{d\mathbf L}{dt}
=\boldsymbol\tau.
$$

推导：

$$
\frac{d}{dt}(\mathbf r\times\mathbf p)
=
\mathbf v\times\mathbf p
+
\mathbf r\times\frac{d\mathbf p}{dt}.
$$

由于 $\mathbf p=m\mathbf v$，第一项 $\mathbf v\times m\mathbf v=0$；第二项利用 $d\mathbf p/dt=\mathbf F$，得到力矩。

若总外力矩为零：

$$
\boldsymbol\tau=0,
$$

则：

$$
\frac{d\mathbf L}{dt}=0,
$$

角动量守恒。

## 旋转对称性与守恒

若系统的 Lagrangian 在空间旋转下不变，Noether 定理给出角动量守恒。

对中心势：

$$
V(\mathbf r)=V(r),
$$

力沿径向，因此：

$$
\mathbf r\times\mathbf F=0.
$$

角动量守恒使运动限制在垂直于固定 $\mathbf L$ 的平面内。这是行星轨道和中心力问题的重要简化。

“旋转对称性导致角动量守恒”比只背力矩公式更一般，也能推广到场论和量子理论。

## 量子轨道角动量算符

量子力学把位置和动量提升为算符：

$$
\hat{\mathbf L}
=\hat{\mathbf r}\times\hat{\mathbf p}.
$$

在位置表象中：

$$
\hat{\mathbf p}=-i\hbar\boldsymbol\nabla,
$$

所以：

$$
\hat{\mathbf L}
=-i\hbar
\mathbf r\times\boldsymbol\nabla.
$$

三个 Cartesian 分量为：

$$
L_x=yp_z-zp_y,
$$

$$
L_y=zp_x-xp_z,
$$

$$
L_z=xp_y-yp_x.
$$

算符帽常在上下文清楚时省略。

## 角动量对易关系

量子角动量分量满足：

$$
[L_i,L_j]
=i\hbar\epsilon_{ijk}L_k.
$$

具体地：

$$
[L_x,L_y]=i\hbar L_z,
$$

并循环得到另外两式。

这里：

- $i,j,k$：取 $x,y,z$ 三个方向；
- $\epsilon_{ijk}$：Levi-Civita 符号；
- 重复指标 $k$：按约定求和；
- $\hbar$：给出角动量的量子尺度。

不同分量不对易，因此一般不能同时具有三个分量的精确值。

## 总角动量平方

定义：

$$
L^2=L_x^2+L_y^2+L_z^2.
$$

它与每个分量对易：

$$
[L^2,L_i]=0.
$$

因此可以同时选择 $L^2$ 和一个分量，通常是 $L_z$，作为共同可观测量。

不能同时指定 $L_x,L_y,L_z$ 的精确值，但可以同时指定总大小平方和一个选定方向的投影。

## 本征值

共同本征态记为：

$$
|\ell,m\rangle.
$$

满足：

$$
L^2|\ell,m\rangle
=\hbar^2\ell(\ell+1)|\ell,m\rangle,
$$

$$
L_z|\ell,m\rangle
=\hbar m|\ell,m\rangle.
$$

对三维空间中的轨道角动量：

$$
\ell=0,1,2,\ldots
$$

并且：

$$
m=-\ell,-\ell+1,\ldots,\ell.
$$

因此固定 $\ell$ 时共有：

$$
2\ell+1
$$

个不同 $m$ 值。

注意总角动量大小不是 $\hbar\ell$，而是：

$$
|\mathbf L|
=\hbar\sqrt{\ell(\ell+1)}.
$$

## 为什么出现 $\ell(\ell+1)$

角动量分量不对易，所以量子态不能像经典向量那样同时给出三个确定分量。角动量升降代数要求存在最高和最低 $m$ 值，并由范数非负性限制表示。

这些条件共同给出：

$$
m=-\ell,\ldots,\ell
$$

以及：

$$
L^2=\hbar^2\ell(\ell+1).
$$

额外的 $+1$ 是量子角动量代数的结果，不是测量误差。

## 角动量升降算符

定义：

$$
L_+=L_x+iL_y,
$$

$$
L_-=L_x-iL_y.
$$

它们满足：

$$
[L_z,L_\pm]=\pm\hbar L_\pm.
$$

作用为：

$$
L_+|\ell,m\rangle
=
\hbar
\sqrt{\ell(\ell+1)-m(m+1)}
|\ell,m+1\rangle,
$$

$$
L_-|\ell,m\rangle
=
\hbar
\sqrt{\ell(\ell+1)-m(m-1)}
|\ell,m-1\rangle.
$$

它们改变 $m$，但不改变 $\ell$。这与谐振子产生、湮灭算符改变能级不同，不能把两套升降算符完全等同。

最高态满足：

$$
L_+|\ell,\ell\rangle=0,
$$

最低态满足：

$$
L_-|\ell,-\ell\rangle=0.
$$

## 角动量是旋转生成元

绕单位向量 $\hat{\mathbf n}$ 旋转小角度 $\delta\theta$ 的量子变换为：

$$
U(\delta\theta)
\approx
I-
\frac{i}{\hbar}
\delta\theta
\hat{\mathbf n}\cdot\mathbf J.
$$

有限旋转为：

$$
U(\theta)
=
\exp\left(
-\frac{i}{\hbar}
\theta\hat{\mathbf n}\cdot\mathbf J
\right).
$$

这里用 $\mathbf J$ 表示一般总角动量生成元，它可以包含轨道角动量和自旋。

公式整体说明：角动量决定量子态在空间旋转下怎样变化。这是角动量比“物体绕圈程度”更深的定义。

## 轨道角动量与自旋

一般单粒子的总角动量可写成：

$$
\mathbf J=\mathbf L+\mathbf S.
$$

其中：

- $\mathbf L$：由空间位置和动量产生的轨道角动量；
- $\mathbf S$：不依赖经典空间轨道的内禀角动量，即自旋；
- $\mathbf J$：总角动量。

轨道角动量在普通三维单值波函数中取整数 $\ell$。自旋可以取整数或半整数，例如电子自旋为 $1/2$。

自旋不是粒子表面真的以超光速转动。它由量子态在旋转群下的表示定义。

## 角动量相加

两个角动量 $\mathbf J_1$ 和 $\mathbf J_2$ 的总和为：

$$
\mathbf J=\mathbf J_1+\mathbf J_2.
$$

若量子数分别为 $j_1,j_2$，总角动量允许值为：

$$
j=|j_1-j_2|,
|j_1-j_2|+1,
\ldots,
j_1+j_2.
$$

例如两个自旋 $1/2$ 相加：

$$
\frac12\otimes\frac12
=0\oplus1.
$$

即得到一个 $j=0$ 单态和一个 $j=1$ 三重态。具体基底变换系数称为 Clebsch–Gordan 系数。

## 轨道角动量波函数

在球坐标中，$L^2$ 和 $L_z$ 的共同本征函数是球谐函数：

$$
Y_{\ell m}(\theta,\phi).
$$

满足：

$$
L^2Y_{\ell m}
=\hbar^2\ell(\ell+1)Y_{\ell m},
$$

$$
L_zY_{\ell m}
=\hbar mY_{\ell m}.
$$

球谐函数描述波函数的角向形状。原子轨道中常说的 $s,p,d$ 轨道分别对应 $\ell=0,1,2$。

## 具体例子：圆周运动

质量 $m=2\,\mathrm{kg}$ 的质点以速度 $v=3\,\mathrm{m/s}$ 在半径 $r=4\,\mathrm m$ 的圆周上运动。速度与半径垂直，因此：

$$
L=mrv.
$$

代入：

$$
L=2\times4\times3
=24\,\mathrm{kg\,m^2/s}.
$$

若速度方向改为沿半径方向，即使 $r$ 和 $v$ 大小不变，也有 $\sin\theta=0$，所以关于圆心的角动量为零。

## 具体例子：$\ell=1$ 量子态

若：

$$
\ell=1,
$$

则：

$$
L^2=2\hbar^2,
$$

总大小为：

$$
|\mathbf L|=\sqrt2\hbar.
$$

允许的磁量子数为：

$$
m=-1,0,1.
$$

对应的 $L_z$ 测量值为：

$$
-\hbar,0,+\hbar.
$$

共有 $2\ell+1=3$ 个方向投影状态。

## 与场和弦的关系

场的角动量来自空间旋转的 Noether 荷，通常同时包含轨道部分和由场指标产生的自旋部分。

弦激发态带有目标时空指标。例如多个振子作用在弦基态上会产生标量、向量或更高阶张量态。它们按照目标时空 Lorentz 群或其小群的表示分类，对应不同自旋内容。

弦世界面模式编号也常被口语化称为“振动角动量”之外的量子数，但不能与目标时空角动量混淆。世界面共形权重、模式数和目标时空自旋是相关却不同的概念。

## 适用条件与事实边界

1. $\mathbf L=\mathbf r\times\mathbf p$ 是相对于选定原点的经典轨道角动量。
2. 三维叉积写法是三维空间特有的简洁表示；高维中旋转生成元自然写成反对称张量 $J^{ij}$。
3. 轨道角动量的整数 $\ell$ 来自普通三维单值空间波函数；一般角动量表示还允许半整数 $j$。
4. 不同分量不对易，不能把量子角动量当作三个同时确定的经典数字。
5. 总角动量可以包含轨道和自旋部分；自旋不是经典刚体自转。
6. 在相对论理论中，粒子自旋由 Poincaré 群小群表示更精确地定义。
7. 弦态的目标时空自旋必须在施加物理态约束后判断。

## 常见误区

1. **只要物体运动就一定有非零角动量。** 若动量沿径向，关于所选原点的角动量为零。
2. **轨道角动量不依赖参考原点。** 改变原点通常会改变 $\mathbf r\times\mathbf p$。
3. **量子角动量三个分量能同时精确确定。** 它们彼此不对易。
4. **角动量大小是 $\hbar\ell$。** 正确值是 $\hbar\sqrt{\ell(\ell+1)}$。
5. **升降算符 $L_\pm$ 改变 $\ell$。** 它们改变 $m$，保持 $\ell$ 不变。
6. **自旋就是粒子表面绕轴旋转。** 自旋是内禀量子角动量和旋转群表示。
7. **世界面模式编号就是目标时空自旋。** 二者是不同类型的量子数。

## 练习

1. 用自己的话解释为什么径向运动关于原点的轨道角动量为零。
2. 质量 $1\,\mathrm{kg}$ 的质点在半径 $2\,\mathrm m$ 处以垂直半径的 $5\,\mathrm{m/s}$ 速度运动，求角动量大小。
3. 写出量子角动量三个分量的对易关系。
4. 对 $\ell=2$，列出全部 $m$ 值，并求 $L^2$ 本征值。
5. $L_+$ 作用在 $|\ell,m\rangle$ 上改变哪个量子数？
6. 为什么 $L_x,L_y,L_z$ 不能同时具有确定值，而 $L^2,L_z$ 可以？
7. 说明轨道角动量与自旋的区别。

## 答案与提示

1. $L=rp\sin\theta$，径向运动时 $\theta=0$ 或 $\pi$，所以叉积为零。
2. $L=mrv=1\times2\times5=10\,\mathrm{kg\,m^2/s}$。
3. $[L_i,L_j]=i\hbar\epsilon_{ijk}L_k$，例如 $[L_x,L_y]=i\hbar L_z$。
4. $m=-2,-1,0,1,2$；$L^2=\hbar^2\ell(\ell+1)=6\hbar^2$。
5. 把 $m$ 提高一，不改变 $\ell$；最高态处结果为零。
6. 三个分量两两不对易，而 $[L^2,L_z]=0$，所以后两者可选共同本征态。
7. 轨道角动量来自位置和动量，依赖空间运动；自旋是粒子或场表示中的内禀角动量。

## 与其他概念的关系

- [[动量]] 与位置通过叉积形成经典轨道角动量；
- [[Noether 定理]] 把旋转对称性与角动量守恒联系起来；
- [[对易关系]] 给出量子角动量代数；
- [[自旋]] 是内禀角动量；
- [[Lie 代数]] 把角动量对易关系解释为旋转代数；
- [[SO 群]] 描述空间旋转；
- [[球谐函数]] 给出轨道角动量的角向本征函数；
- [[弦的量子态]] 按目标时空旋转或 Lorentz 表示分类。

## 参考资料

- [MIT OCW 8.04, Quantum Physics I](https://ocw.mit.edu/courses/8-04-quantum-physics-i-spring-2013/)
- [MIT OCW 8.05, Quantum Physics II](https://ocw.mit.edu/courses/8-05-quantum-physics-ii-fall-2013/)
- J. J. Sakurai and Jim Napolitano, *Modern Quantum Mechanics*, 3rd ed., Cambridge University Press, 2020.
- Claude Cohen-Tannoudji, Bernard Diu and Franck Laloë, *Quantum Mechanics*, Wiley.
- Herbert Goldstein, Charles Poole and John Safko, *Classical Mechanics*, 3rd ed., Addison-Wesley, 2001.
- Howard Georgi, *Lie Algebras in Particle Physics*, 2nd ed., Westview Press, 1999.
- Joseph Polchinski, *String Theory, Vol. 1*, Cambridge University Press, 1998.
