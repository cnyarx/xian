---
aliases: [残差连接, LayerNorm, RMSNorm, Pre-Norm, Post-Norm]
tags: [Transformer, 神经网络, 教程, 大模型, 学习教程]
prerequisites: ["[[多头注意力]]", "[[反向传播]]"]
next: ["[[前馈网络]]"]
---
# 残差连接与层归一化

## 残差连接

残差连接（Residual Connection）把一个模块的输入直接加到输出：

$$y=x+F(x)$$

模块只需学习“在原输入基础上改多少”，即残差 $F(x)$。它还为梯度提供直接通路，使深层网络更容易训练。

## LayerNorm

层归一化（Layer Normalization）对单个 token 的隐藏维度做标准化。

给定向量 $x\in\mathbb{R}^d$：

$$\mu=\frac1d\sum_{i=1}^d x_i$$
$$\sigma^2=\frac1d\sum_{i=1}^d(x_i-\mu)^2$$
$$\operatorname{LayerNorm}(x)_i=\gamma_i\frac{x_i-\mu}{\sqrt{\sigma^2+\epsilon}}+\beta_i$$

- $\mu$：该 token 各维均值。
- $\sigma^2$：方差。
- $\epsilon$：防止除以零的小正数。
- $\gamma,\beta$：可训练缩放和平移参数。

归一化让不同层数值尺度更稳定。

### LayerNorm 数值例子

对 $x=[1,2,3]$，先算：

$$
\mu=2,
\qquad
\sigma^2=\frac{(1-2)^2+(2-2)^2+(3-2)^2}{3}=\frac23
$$

为了看清主过程，暂取 $\gamma=[1,1,1]$、$\beta=[0,0,0]$，并忽略很小的 $\epsilon$：

$$
\operatorname{LayerNorm}(x)
\approx
\left[
\frac{-1}{\sqrt{2/3}},0,\frac{1}{\sqrt{2/3}}
\right]
\approx[-1.225,0,1.225]
$$

输出均值约为 0，方差约为 1。真实实现保留 $\epsilon$，并通过 $\gamma,\beta$ 学习合适的缩放和平移。

## RMSNorm

RMSNorm 不减均值，只按均方根缩放：

$$\operatorname{RMSNorm}(x)_i=\gamma_i\frac{x_i}{\sqrt{\frac1d\sum_jx_j^2+\epsilon}}$$

计算略简，现代语言模型中很常见。

## Pre-Norm 与 Post-Norm

**Post-Norm（原始 Transformer）**：

$$y=\operatorname{Norm}(x+F(x))$$

**Pre-Norm**：

$$y=x+F(\operatorname{Norm}(x))$$

Pre-Norm 的残差主路更直接，通常更容易稳定训练很深的网络；但最终质量与具体架构、初始化和训练设置有关。

## 一个 Transformer 块

```text
x
├─ Norm → Attention ─┐
└────────────────────+→ h
                       ├─ Norm → FFN ─┐
                       └──────────────+→ y
```

两个子层各有一条残差连接。

## 常见误区

- 归一化不是把所有数据缩到 0 到 1。
- LayerNorm 与 BatchNorm 统计方向不同；Transformer 通常使用 LayerNorm 或 RMSNorm。
- 残差连接不意味着模块可省略，它只是保留原输入并学习增量。

## 概念与公式导航

- [[残差LayerNorm与RMSNorm公式|残差、LayerNorm 与 RMSNorm 公式]]

## 练习

1. 对 `[1,2,3]` 计算均值和方差。
2. 为什么残差连接有助于梯度传播？
3. 比较 LayerNorm 与 RMSNorm。
4. 画出 Pre-Norm Transformer 块。

## 来源

- He, K. et al. (2016). *Deep Residual Learning for Image Recognition*. CVPR https://doi.org/10.1109/CVPR.2016.90 `[A]`
- Ba, J. L. et al. (2016). *Layer Normalization*. https://arxiv.org/abs/1607.06450 `[A]`
- Zhang, B. & Sennrich, R. (2019). *Root Mean Square Layer Normalization*. NeurIPS https://arxiv.org/abs/1910.07467 `[A]`
- Xiong, R. et al. (2020). *On Layer Normalization in the Transformer Architecture*. ICML https://arxiv.org/abs/2002.04745 `[B]`