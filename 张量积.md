---
aliases: [Tensor Product]
english_name: Tensor Product
tags: [数学, 线性代数, 弦理论先修, 弦理论]
prerequisites:
  - "[[向量空间]]"
  - "[[线性映射]]"
used_by:
  - "[[量子态]]"
  - "[[张量]]"
  - "[[闭弦周期条件]]"
evidence_status: 教材事实
---

# 张量积（Tensor Product）

## 一句话解释

张量积是一种把两个向量空间组合成更大空间的方法，使两个系统的独立自由度可以同时被记录。

## 为什么要学它

两个量子系统的联合状态位于两个状态空间的张量积中。闭弦的左行与右行振动部分也会组合成完整状态。张量积还是理解一般张量的入口。

## 记号

向量 $v\in V$ 与 $w\in W$ 的简单张量写成：

$$
v\otimes w\in V\otimes W.
$$

$\otimes$ 读作张量积。$V\otimes W$ 中的一般元素是有限个简单张量的线性组合：

$$
\sum_i v_i\otimes w_i.
$$

并非每个一般元素都能写成单独一个 $v\otimes w$。

## 双线性规则

张量积满足：

$$
(v_1+v_2)\otimes w
=v_1\otimes w+v_2\otimes w,
$$

$$
v\otimes(w_1+w_2)
=v\otimes w_1+v\otimes w_2,
$$

$$
(\lambda v)\otimes w
=v\otimes(\lambda w)
=\lambda(v\otimes w).
$$

它对两个输入分别线性。

## 基与维数

若 $V$ 的基为 $\{e_i\}$，$W$ 的基为 $\{f_j\}$，则 $V\otimes W$ 的一组基是：

$$
\{e_i\otimes f_j\}.
$$

若：

$$
\dim V=m,
\qquad
\dim W=n,
$$

那么：

$$
\dim(V\otimes W)=mn.
$$

例如两个二维空间组合后得到四维空间。

## 一个具体例子

设 $V=W=\mathbb R^2$，基分别是 $e_1,e_2$ 和 $f_1,f_2$。张量积空间的基为：

$$
e_1\otimes f_1,
e_1\otimes f_2,
e_2\otimes f_1,
e_2\otimes f_2.
$$

每个基元素同时记录第一个空间和第二个空间的一个方向。

## 与 Cartesian 积的区别

Cartesian 积 $V\times W$ 中元素是有序对 $(v,w)$，维数相加；张量积 $V\otimes W$ 的维数相乘，并能表达两个自由度之间的相关系数。

例如两个二维空间：

$$
\dim(V\times W)=2+2=4,
$$

$$
\dim(V\otimes W)=2\times2=4.
$$

此例数值碰巧相同，但结构不同；其他维数下也可能数值不同。

## 量子联合系统

若系统 A 状态空间为 $\mathcal H_A$，系统 B 为 $\mathcal H_B$，联合空间为：

$$
\mathcal H_A\otimes\mathcal H_B.
$$

简单乘积态写成 $|a\rangle\otimes|b\rangle$。一般状态可能不能分解成一个乘积态，这类状态称为纠缠态。

例如：

$$
\frac1{\sqrt2}
(|0\rangle\otimes|0\rangle
+|1\rangle\otimes|1\rangle)
$$

不能写成单独两个状态的张量积。

## 线性映射的张量积

若 $A:V\to V'$、$B:W\to W'$，则：

$$
(A\otimes B)(v\otimes w)
=Av\otimes Bw.
$$

它分别作用于两个因子。

## 弦理论中的例子

闭弦具有左行与右行振动。量子态常由左行 Fock 空间和右行 Fock 空间的张量积构成，再施加能级匹配等物理条件。

这不是说任意左右态组合都自动成为物理闭弦态；还需要约束和投影。

## 常见误区

1. **$v\otimes w$ 是普通数字乘法。** 错，它产生组合空间中的元素。
2. **张量积空间的一切向量都是简单乘积态。** 错，一般是多个简单张量的和。
3. **张量积与 Cartesian 积相同。** 错，构造和用途不同。
4. **维数在张量积时相加。** 有限维时相乘。
5. **两个量子系统的联合状态总能拆成各自状态。** 纠缠态不能。

## 练习

1. 二维与三维向量空间的张量积维数是多少？
2. 写出两组二维基的张量积基。
3. 展开 $(v_1+v_2)\otimes w$。
4. 一般张量是否总能写成单个简单张量？
5. 闭弦左右行状态组合后为何还要施加约束？

## 答案与提示

1. $6$。
2. $e_i\otimes f_j$，其中 $i,j=1,2$。
3. $v_1\otimes w+v_2\otimes w$。
4. 不能。
5. 联合空间包含数学上可能但不满足能级匹配或其他物理条件的状态。

## 与其他概念的关系

- 张量积组合两个 [[向量空间]]；
- [[张量]] 可通过多重线性映射或张量积理解；
- 量子联合系统与纠缠依赖张量积；
- 闭弦左右行振动空间通过张量积组合。

## 参考资料

- [MIT OCW 18.06, Linear Algebra](https://ocw.mit.edu/courses/18-06-linear-algebra-spring-2010/)
- Sheldon Axler, *Linear Algebra Done Right*, 3rd ed.
- Michael Nielsen and Isaac Chuang, *Quantum Computation and Quantum Information*
