---
aliases:
  - Topological Space
  - Topology
english_name: Topological Space
tags:
  - 数学
  - 拓扑学
  - 几何
  - 弦理论先修
  - 弦理论
prerequisites:
  - "[[集合]]"
  - "[[函数]]"
  - "[[区间]]"
used_by:
  - "[[流形]]"
  - "[[连续映射]]"
  - "[[亏格]]"
  - "[[群作用]]"
  - "[[模空间]]"
evidence_status: 教材事实
---

# 拓扑空间（Topological Space）

## 一句话解释

拓扑空间是在一个集合上规定哪些子集叫开集，从而定义“附近”“连续”“连通”和“有没有洞”等不依赖具体长度的性质。

## 为什么要学它

几何不只有长度和角度。圆和椭圆虽然度量不同，却能通过连续变形互相转换；圆周和线段则具有不同的整体连接方式。

流形、黎曼曲面、紧致空间、连续对称、亏格和模空间都建立在拓扑空间之上。弦世界面的整体形状也首先按拓扑分类，再讨论度量与共形结构。

## 直观图景

拓扑研究在连续拉伸、弯曲和压缩下仍保持的性质，但不允许撕裂、粘合或让不同点突然合并。

常见比喻是：带一个洞的咖啡杯与圆环面可以连续变形，因为它们都只有一个“把手”。这个比喻帮助理解，但严格拓扑不靠橡皮材料，而靠开集和连续映射定义。

## 正式定义

设 $X$ 是集合。若它的一族子集 $\mathcal T\subseteq\mathcal P(X)$ 满足：

1. $\varnothing\in\mathcal T$ 且 $X\in\mathcal T$；
2. $\mathcal T$ 中任意多个集合的并仍属于 $\mathcal T$；
3. $\mathcal T$ 中有限多个集合的交仍属于 $\mathcal T$；

则 $\mathcal T$ 叫 $X$ 上的一个拓扑，二元组：

$$
(X,\mathcal T)
$$

叫拓扑空间。

$\mathcal T$ 中的集合叫开集。

## 每个符号是什么意思

- $X$：底层集合；
- $\mathcal P(X)$：$X$ 的幂集，即所有子集组成的集合；
- $\mathcal T$：被指定为开集的子集族；
- $\varnothing$：空集；
- “任意并”：可以合并有限、可数甚至更一般的一族开集；
- “有限交”：只保证有限多个开集的交仍开。

拓扑不是单个集合，而是一族满足规则的子集。

## 为什么空集和全集必须开

全集 $X$ 表示整个空间，本身当然可以作为一个没有外部边界限制的开放区域。

空集没有任何点，因此“其中每点都有开邻域”一类条件会真空成立。把二者纳入开集还能使任意并和有限交规则保持统一。

## 为什么允许任意并

若每个开集都表示某些点周围的开放区域，把任意多个开放区域合在一起，合并后每个点仍来自其中某个开放区域，因此仍应是开集。

形式上，若 $U_\alpha\in\mathcal T$，则：

$$
\bigcup_{\alpha\in I}U_\alpha\in\mathcal T.
$$

指标集 $I$ 可以无限。

## 为什么只保证有限交

在实数通常拓扑中，每个区间：

$$
U_n=\left(-\frac1n,\frac1n\right)
$$

都是开集，但无限交为：

$$
\bigcap_{n=1}^{\infty}U_n=\{0\},
$$

单点集 $\{0\}$ 不是实数通常拓扑中的开集。

所以拓扑公理不能要求任意交都开，只要求有限交。

## 实数上的通常拓扑

实数轴上，所有开区间：

$$
(a,b)
$$

以及它们的任意并组成通常拓扑。

一个集合 $U\subseteq\mathbb R$ 开，当且仅当对每个 $x\in U$，都存在 $\varepsilon>0$，使：

$$
(x-\varepsilon,x+\varepsilon)\subseteq U.
$$

也就是说，每个内部点周围都有一小段仍完全位于集合中。

## 开集不等于开区间

开区间是实数轴上的一种开集，但开集可以是多个不相交开区间的并，例如：

$$
(-2,-1)\cup(3,5).
$$

在一般拓扑空间中甚至未必有数轴和区间，开集由拓扑直接指定。

## 离散拓扑

对任意集合 $X$，令：

$$
\mathcal T=\mathcal P(X).
$$

即每个子集都开。这叫离散拓扑。

在离散拓扑中，每个单点集都是开集，每个函数从离散空间出发都是连续的。

## 平凡拓扑

令：

$$
\mathcal T=\{\varnothing,X\}.
$$

这叫平凡拓扑或不可分拓扑。

它只有最少的开集。从任意拓扑空间到平凡拓扑空间的函数都连续，因为目标中只有空集和全集需要检查原像。

## 同一集合可有不同拓扑

底层集合相同，不代表拓扑空间相同。

例如集合 $X=\{a,b\}$ 可取：

$$
\{\varnothing,X\}
$$

或：

$$
\{\varnothing,\{a\},\{b\},X\}.
$$

前者是平凡拓扑，后者是离散拓扑。它们允许的连续函数和点的可区分性不同。

## 闭集

若 $F\subseteq X$ 的补集是开集：

$$
X\setminus F\in\mathcal T,
$$

则 $F$ 叫闭集。

闭集满足：

1. $\varnothing$ 与 $X$ 都闭；
2. 任意多个闭集的交闭；
3. 有限多个闭集的并闭。

这是开集公理经 De Morgan 律得到的对偶版本。

## 集合可以既开又闭

空集和全集总是既开又闭。

在不连通空间中，还可能有其他既开又闭的集合，英文称 clopen。

所以“开”和“闭”不是日常语言中的互斥反义词。一个集合可以两者都是，也可以两者都不是。

## 邻域

若存在开集 $U$ 满足：

$$
x\in U\subseteq N,
$$

则 $N$ 叫点 $x$ 的邻域。

邻域本身不一定开；关键是它包含一个围绕 $x$ 的开集。

若直接要求 $N$ 是含 $x$ 的开集，则称开邻域。

## 内点与内部

若存在开集 $U$ 满足：

$$
x\in U\subseteq A,
$$

则 $x$ 是 $A$ 的内点。

$A$ 的所有内点组成内部：

$$
\operatorname{int}(A).
$$

内部是包含于 $A$ 的最大开集。集合 $A$ 开当且仅当：

$$
A=\operatorname{int}(A).
$$

## 闭包

集合 $A$ 的闭包 $\overline A$ 是包含 $A$ 的最小闭集。

等价地，点 $x$ 属于 $\overline A$，当且仅当 $x$ 的每个开邻域都与 $A$ 相交。

例如在实数通常拓扑中：

$$
\overline{(0,1)}=[0,1].
$$

闭包加入了所有极限边界点。

## 边界

集合 $A$ 的边界为：

$$
\partial A
=\overline A\setminus\operatorname{int}(A).
$$

边界点的每个邻域都同时碰到 $A$ 和其补集。

例如：

$$
\partial(0,1)=\{0,1\}.
$$

拓扑边界与“流形是否有边界”相关但不是完全相同的定义，后者需要局部半空间模型。

## 基

拓扑的一组基 $\mathcal B$ 是一族开集，使每个开集都能写成基元素的并。

更具体地，对每个开集 $U$ 和每个 $x\in U$，都存在 $B\in\mathcal B$ 满足：

$$
x\in B\subseteq U.
$$

实数通常拓扑可由所有开区间作为基，也可只用有理端点开区间作为可数基。

基像一套最小“局部积木”，但基不必是唯一的。

## 开覆盖

若一族开集 $\{U_\alpha\}$ 满足：

$$
X\subseteq\bigcup_{\alpha\in I}U_\alpha,
$$

通常等价写为并集覆盖整个 $X$，就称它是 $X$ 的开覆盖。

对集合 $A\subseteq X$，只需：

$$
A\subseteq\bigcup_{\alpha\in I}U_\alpha.
$$

流形用坐标图的开集覆盖；紧致性则询问开覆盖能否缩成有限子覆盖。

## 连续映射

设 $f:X\to Y$ 是两个拓扑空间之间的函数。若对 $Y$ 中每个开集 $V$，其原像：

$$
f^{-1}(V)
$$

都是 $X$ 中的开集，则 $f$ 连续。

这是适用于任意拓扑空间的连续性定义，不依赖距离或 $\varepsilon$-$\delta$。

注意这里是开集的原像，不要求 $f$ 存在反函数。

## 为什么使用原像

原像保持集合运算：

$$
f^{-1}\left(\bigcup_\alpha V_\alpha\right)
=\bigcup_\alpha f^{-1}(V_\alpha),
$$

$$
f^{-1}\left(\bigcap_\alpha V_\alpha\right)
=\bigcap_\alpha f^{-1}(V_\alpha).
$$

因此它与拓扑公理中的并和有限交自然兼容。

函数的直接像一般不保交，也不保证把开集送到开集，所以不能用“开集的像总开”定义普通连续性。

## 同胚

若函数 $f:X\to Y$：

1. 是双射；
2. 连续；
3. 逆函数 $f^{-1}:Y\to X$ 也连续；

则称 $f$ 是同胚，记作：

$$
X\cong Y
$$

在拓扑语境中。

同胚空间具有相同拓扑性质，可视作连续变形意义下同一种空间。

## 连续双射不一定是同胚

即使 $f$ 连续且双射，逆函数也可能不连续，所以仍需单独检查。

在紧空间到 Hausdorff 空间的连续双射等常见条件下，逆连续可自动推出，但这需要额外定理，不能无条件省略。

## 连通性

若空间 $X$ 不能写成两个不相交、非空开集的并：

$$
X=U\cup V,
\qquad U\cap V=\varnothing,
$$

则称 $X$ 连通。

等价地，连通空间只有 $\varnothing$ 和 $X$ 两个既开又闭的子集。

区间在实数通常拓扑中连通；两个分离开区间的并不连通。

## 道路连通

若任意两点 $x_0,x_1\in X$ 都可由连续路径：

$$
\gamma:[0,1]\to X
$$

连接，并满足：

$$
\gamma(0)=x_0,
\qquad
\gamma(1)=x_1,
$$

则 $X$ 道路连通。

道路连通推出连通，但一般拓扑空间中连通不一定推出道路连通。对许多常见流形，两者关系更友好。

## 紧致性

拓扑空间 $X$ 紧致，若它的每个开覆盖都有有限子覆盖。

若：

$$
X\subseteq\bigcup_{\alpha\in I}U_\alpha,
$$

则能找到有限多个指标 $\alpha_1,\ldots,\alpha_n$，使：

$$
X\subseteq U_{\alpha_1}\cup\cdots\cup U_{\alpha_n}.
$$

紧致性把可能无限的局部信息压缩成有限信息，是分析和几何中的重要有限性条件。

## 实数中的 Heine–Borel 定理

在 $\mathbb R^n$ 的通常拓扑中，集合紧致当且仅当它闭且有界。

但“闭且有界等于紧致”不是任意拓扑空间中的定义，也不对所有度量空间成立。一般定义仍是开覆盖定义。

例如闭区间 $[0,1]$ 紧致，而开区间 $(0,1)$ 不紧致。

## Hausdorff 条件

若任意两个不同点 $x\ne y$ 都有不相交开邻域 $U,V$：

$$
x\in U,
\qquad y\in V,
\qquad U\cap V=\varnothing,
$$

则空间称为 Hausdorff 空间。

这保证不同点可由开集分离，并使序列或网的极限在适当意义下唯一。

流形通常要求 Hausdorff，避免两个点局部无法区分的异常空间。

## 第二可数条件

若拓扑有一组可数基，就称空间第二可数。

这意味着所有开集都可由一套可数局部积木生成。常见流形通常要求第二可数，从而排除过大的病态空间，并保证许多分析工具可用。

第二可数不等于集合本身可数；例如 $\mathbb R$ 不可数，但有由有理端点区间组成的可数基。

## 子空间拓扑

若 $A\subseteq X$，定义：

$$
\mathcal T_A
=\{U\cap A:U\in\mathcal T_X\}.
$$

这叫 $A$ 从 $X$ 继承的子空间拓扑。

例如圆周作为平面子集，其开集是平面开集与圆周的交，而不是要求它在平面中本身包含二维小圆盘。

## 积拓扑

两个拓扑空间 $X,Y$ 的笛卡尔积：

$$
X\times Y
$$

可赋予积拓扑，其基由：

$$
U\times V
$$

组成，其中 $U\subseteq X$、$V\subseteq Y$ 开。

这使投影映射：

$$
\pi_X:X\times Y\to X,
\qquad
\pi_Y:X\times Y\to Y
$$

连续。

## 商拓扑

若对集合 $X$ 施加等价关系 $\sim$，商集为：

$$
X/{\sim}.
$$

令自然投影：

$$
q:X\to X/{\sim}
$$

连续，并规定 $U\subseteq X/{\sim}$ 开当且仅当：

$$
q^{-1}(U)
$$

在 $X$ 中开，就得到商拓扑。

把线段两端粘合成圆、把正方形边粘合成圆环面，都可用商拓扑严格描述。

## 圆周作为商空间

取闭区间 $[0,1]$，把端点识别：

$$
0\sim1.
$$

商空间：

$$
[0,1]/(0\sim1)
$$

与圆周同胚。

这里“粘合”不是把端点距离简单设小，而是把它们变成同一个等价类，并给商集适当拓扑。

## 拓扑不变量

若两个空间同胚，则任何真正的拓扑不变量必须相同。常见例子包括：

- 连通分支数；
- 紧致性；
- 基本群；
- 同调群与上同调群；
- 对闭可定向曲面而言的亏格。

长度、角度和曲率通常不是纯拓扑不变量，因为它们依赖额外度量或微分结构。

## 局部与整体

两个空间可以在每个小区域看起来相同，却有不同整体拓扑。

直线与圆周局部都像开区间，但圆周闭合，直线不闭合。圆柱与平面局部都像二维平面，但圆柱有一个周期方向。

流形定义主要控制局部 Euclidean 结构，而拓扑不变量区分整体拼接方式。

## 在弦理论中的作用

弦扫出的二维世界面先按拓扑分类：

- 闭弦树级对应球面；
- 开弦树级常对应圆盘；
- 闭弦一圈对应环面；
- 更高圈对应更高亏格曲面。

微扰展开对不同世界面拓扑求和。固定某个拓扑后，还要积分其复结构或共形结构的模参数。

拓扑分类不等于完整量子振幅；还需世界面场论、规范固定、鬼场、顶点算符和模空间测度。

## 一个有限集合例子

设：

$$
X=\{a,b,c\}
$$

并取：

$$
\mathcal T
=\{\varnothing,\{a\},\{a,b\},X\}.
$$

检查：

- 空集和全集都在；
- 任意并仍在列表中；
- 有限交仍在列表中。

所以它是一个拓扑。

集合 $\{b\}$ 不是开集，因为不在 $\mathcal T$ 中；但 $\{b,c\}$ 是闭集，因为其补集 $\{a\}$ 开。

## 适用条件与事实边界

1. 拓扑空间必须同时指定底层集合和拓扑；只给集合不足以确定连续性。
2. 开集是由拓扑指定的，不要求空间先有距离。
3. 任意并保持开，但一般只保证有限交保持开。
4. 开与闭不是互斥概念；集合可既开又闭，也可两者都不是。
5. 连续性定义使用目标开集的原像，不要求函数可逆。
6. 连续双射不一定是同胚，还需逆映射连续或附加足够条件。
7. 道路连通推出连通，但一般情况下反向不成立。
8. “紧致等于闭且有界”只是在 $\mathbb R^n$ 等特定空间中的定理，不是一般定义。
9. 拓扑只描述连续结构，不自动给出距离、角度、面积或曲率。
10. 世界面拓扑只给弦微扰阶的整体分类，不单独决定完整振幅。

## 常见误区

1. **开集一定是不含边界点的区间。** 一般空间没有区间；开集由拓扑定义。
2. **无限多个开集的交仍开。** 拓扑只保证有限交。
3. **闭集就是不开的集合。** 一个集合可以既开又闭，或既不开也不闭。
4. **同一集合只有一种拓扑。** 同一底层集合可赋予许多不同拓扑。
5. **连续函数把开集的像都变成开集。** 连续性要求开集的原像开；把开集映成开集叫开映射，是另一性质。
6. **连续双射自动是同胚。** 逆函数可能不连续。
7. **连通与道路连通完全等价。** 一般拓扑空间中并不等价。
8. **紧致永远等于闭且有界。** 这是 Euclidean 空间中的特殊结论。
9. **拓扑相同意味着长度和曲率相同。** 这些需要度量等额外结构。
10. **咖啡杯和圆环面的比喻就是严格证明。** 严格等价要构造同胚或使用拓扑不变量。

## 练习

1. 写出拓扑的三条公理。
2. 为什么通常拓扑中的无限个开集之交不一定开？给出例子。
3. 在集合 $X=\{a,b\}$ 上写出离散拓扑和平凡拓扑。
4. 闭集如何通过开集定义？
5. 写出拓扑连续映射的定义。
6. 连续双射还缺少什么条件才是同胚？
7. 连通与道路连通有何关系？
8. 紧致性的开覆盖定义是什么？
9. 为什么圆周在平面中的子空间拓扑下仍有开邻域？
10. 弦微扰论为什么要区分世界面亏格？

## 答案与提示

1. 空集和全集开；任意开集并开；有限开集交开。
2. $(-1/n,1/n)$ 都开，但其无限交是 $\{0\}$，在实数通常拓扑中不开。
3. 离散拓扑为 $\{\varnothing,\{a\},\{b\},X\}$；平凡拓扑为 $\{\varnothing,X\}$。
4. $F$ 闭当且仅当补集 $X\setminus F$ 开。
5. 对目标空间每个开集 $V$，原像 $f^{-1}(V)$ 在定义域中开。
6. 逆函数也必须连续。
7. 道路连通必连通；一般连通未必道路连通。
8. 每个开覆盖都能选出有限子覆盖。
9. 子空间开集是平面开集与圆周的交，不需要包含二维圆盘。
10. 不同亏格对应不同圈阶和弦耦合幂次，是微扰展开的不同拓扑扇区。

## 与其他概念的关系

- [[集合]] 提供底层集合、子集族、并、交和原像；
- [[函数]] 在拓扑之间通过开集原像定义连续性；
- [[流形]] 是局部同胚于 Euclidean 空间并满足附加条件的拓扑空间；
- [[群作用]] 可在拓扑空间上连续作用并构造轨道与商空间；
- [[上同调]] 提供区分空间整体拓扑的代数不变量；
- [[亏格]] 分类闭可定向曲面的把手数；
- [[模空间]] 把几何结构按等价关系组成带拓扑的参数空间。

## 参考资料

- James R. Munkres, *Topology*, 2nd ed., Pearson, 2000.
- John M. Lee, *Introduction to Topological Manifolds*, 2nd ed., Springer, 2011.
- Allen Hatcher, *Algebraic Topology*, Cambridge University Press, 2002; [free author edition](https://pi.math.cornell.edu/~hatcher/AT/AT.pdf)
- [MIT OpenCourseWare 18.901, Introduction to Topology](https://ocw.mit.edu/courses/18-901-introduction-to-topology-fall-2004/)
- Loring W. Tu, *An Introduction to Manifolds*, 2nd ed., Springer, 2011.
- Joseph Polchinski, *String Theory, Vol. 1*, Cambridge University Press, 1998.
