---
aliases: [Quantum Operator, Operator in Quantum Mechanics]
english_name: Quantum Operator
tags: [物理学, 量子力学, 线性代数, 弦理论先修, 弦理论]
prerequisites:
  - "[[量子态]]"
  - "[[Born 规则]]"
  - "[[Hilbert 空间]]"
  - "[[线性映射]]"
  - "[[特征值]]"
  - "[[特征向量]]"
used_by:
  - "[[对易关系]]"
  - "[[不确定性原理]]"
  - "[[量子谐振子]]"
  - "[[产生算符]]"
  - "[[湮灭算符]]"
  - "[[场的正则量子化]]"
evidence_status: 教材事实
---

# 量子算符（Quantum Operator）

## 一句话解释

量子算符是作用在量子态上的线性变换；自伴算符用于表示可观测量，酉算符用于表示保持概率的状态变换。

## 为什么要学它

量子态说明系统处于什么状态，量子算符则说明怎样测量、怎样演化以及怎样改变状态。位置、动量、能量、角动量和弦振动模式都用算符表示。

弦量子化后，经典模系数变成算符。它们的对易关系、正规排序和约束决定允许的弦态及质量谱。

## 从线性映射到算符

算符 $A$ 把 Hilbert 空间中的状态映射到另一个向量：

$$
A:\mathcal D(A)\subseteq\mathcal H\to\mathcal H.
$$

这里：

- $\mathcal H$：Hilbert 空间；
- $\mathcal D(A)$：算符的定义域；
- $A|\psi\rangle$：$A$ 作用于状态后的向量。

线性算符满足：

$$
A\left(c_1|\psi\rangle+c_2|\phi\rangle\right)
=
c_1A|\psi\rangle+c_2A|\phi\rangle.
$$

线性保证叠加态的各分量能按同一规则演化和变换。

## 矩阵表示

在有限维正交归一基 $\{|e_n\rangle\}$ 中，算符的矩阵元为：

$$
A_{mn}=\langle e_m|A|e_n\rangle.
$$

若：

$$
|\psi\rangle=
\sum_n\psi_n|e_n\rangle,
$$

则：

$$
(A\psi)_m=
\sum_nA_{mn}\psi_n.
$$

所以有限维量子算符可用矩阵计算，但算符本身不依赖所选基。换基会改变矩阵元素，不改变抽象物理变换。

## 恒等算符与零算符

恒等算符 $I$ 满足：

$$
I|\psi\rangle=|\psi\rangle.
$$

零算符 $0$ 满足：

$$
0|\psi\rangle=|0\rangle.
$$

这里右边的 $|0\rangle$ 是零向量，不应与某个编号为零的归一化基态混淆。

正交归一完备基满足：

$$
I=\sum_n|e_n\rangle\langle e_n|.
$$

连续基中，求和相应变成积分。

## 算符乘积与作用顺序

算符乘积 $AB$ 表示先作用 $B$，再作用 $A$：

$$
AB|\psi\rangle
=A(B|\psi\rangle).
$$

一般情况下：

$$
AB\ne BA.
$$

因此作用顺序可能改变结果。两种顺序的差由对易子描述：

$$
[A,B]=AB-BA.
$$

量子理论许多非经典性质都来自算符不对易。

## 伴随算符

算符 $A$ 的伴随算符 $A^\dagger$ 由：

$$
\langle\phi|A\psi\rangle
=
\langle A^\dagger\phi|\psi\rangle
$$

定义，其中所有向量和定义域条件必须适当。

有限维正交归一基中：

$$
A^\dagger=(A^*)^T,
$$

即先对矩阵元素复共轭，再转置。

乘积的伴随会反转顺序：

$$
(AB)^\dagger=B^\dagger A^\dagger.
$$

## 自伴算符

若算符与其伴随相等：

$$
A=A^\dagger,
$$

则称 $A$ 为自伴算符（self-adjoint operator）。

自伴算符用于表示可观测量，关键原因包括：

- 谱值为实数；
- 不同本征值对应的本征态正交；
- 在合适条件下存在谱分解；
- Born 规则可把谱投影连接到测量概率。

无限维情况下，“Hermitian”或“对称”与严格自伴并不总等价；必须同时检查定义域。初等有限维课程中两者常被当作同义词。

## 本征值与本征态

若非零向量 $|a\rangle$ 满足：

$$
A|a\rangle=a|a\rangle,
$$

则：

- $a$：算符 $A$ 的本征值；
- $|a\rangle$：对应本征态。

若系统处于归一化本征态 $|a\rangle$，理想测量 $A$ 会以概率 $1$ 得到 $a$。

若状态是多个本征态的叠加：

$$
|\psi\rangle=
\sum_a c_a|a\rangle,
$$

测得 $a$ 的概率由 Born 规则给出：

$$
P(a)=|c_a|^2.
$$

## 谱分解

有限维自伴算符可写成：

$$
A=
\sum_a aP_a,
$$

其中 $P_a$ 投影到本征值 $a$ 的本征子空间。

这些投影满足：

$$
P_aP_b=
\delta_{ab}P_a,
\qquad
\sum_aP_a=I.
$$

如果本征值不退化：

$$
P_a=|a\rangle\langle a|.
$$

连续谱需要用积分和投影值测度严格表述；形式上常写成对连续本征值的积分。

## 期望值

归一化纯态中，可观测量 $A$ 的期望值为：

$$
\langle A\rangle_\psi
=
\langle\psi|A|\psi\rangle.
$$

若 $A$ 自伴，该期望值为实数，因为：

$$
\langle\psi|A|\psi\rangle^*
=
\langle\psi|A^\dagger|\psi\rangle
=
\langle\psi|A|\psi\rangle.
$$

期望值是重复测量的统计平均，不表示单次测量一定得到这个数。

## 方差与不确定度

可观测量 $A$ 的方差为：

$$
(\Delta A)^2
=
\langle(A-\langle A\rangle)^2\rangle.
$$

展开后：

$$
(\Delta A)^2
=
\langle A^2\rangle-
\langle A\rangle^2.
$$

若 $|\psi\rangle$ 是 $A$ 的本征态，则 $\Delta A=0$。反过来，在适当定义域条件下，归一化态中 $\Delta A=0$ 表示该态位于某个确定本征值的本征子空间。

## 投影算符

投影算符满足：

$$
P^2=P.
$$

正交投影还满足：

$$
P^\dagger=P.
$$

一维投影：

$$
P=|u\rangle\langle u|,
\qquad
\langle u|u\rangle=1.
$$

它把任意状态投影到 $|u\rangle$ 方向：

$$
P|\psi\rangle
=|u\rangle\langle u|\psi\rangle.
$$

投影算符既描述测量事件，也用于定义物理约束子空间。

## 酉算符

若：

$$
U^\dagger U=UU^\dagger=I,
$$

则 $U$ 是酉算符（unitary operator）。

它保持内积：

$$
\langle U\phi|U\psi\rangle
=
\langle\phi|\psi\rangle.
$$

因此保持范数和 Born 概率结构。闭合量子系统的时间演化由酉算符表示：

$$
|\psi(t)\rangle
=U(t,t_0)|\psi(t_0)\rangle.
$$

若 Hamiltonian 与时间无关：

$$
U(t,t_0)=
\exp\left[-\frac{i}{\hbar}H(t-t_0)\right].
$$

## 无穷小变换与生成元

连续酉变换在小参数 $\varepsilon$ 下可写为：

$$
U(\varepsilon)
\approx I-i\varepsilon G,
$$

其中 $G$ 是自伴生成元。不同单位约定可能把 $\hbar$ 放入 $G$ 或参数中。

例如：

- Hamiltonian 生成时间平移；
- 动量生成空间平移；
- 角动量生成旋转。

这连接了对称变换、守恒量和量子算符。

## 位置算符

一维位置表象中，位置算符通过乘法作用：

$$
(\hat x\psi)(x)=x\psi(x).
$$

这里：

- $\psi(x)$：位置波函数；
- $\hat x$：位置算符；
- $(\hat x\psi)(x)$：作用后新函数在 $x$ 处的值。

位置期望值为：

$$
\langle x\rangle
=
\int_{-\infty}^{\infty}
\psi^*(x)x\psi(x)dx.
$$

积分收敛且状态属于相应定义域时，该表达才有意义。

## 动量算符

一维位置表象中，动量算符为：

$$
\hat p=-i\hbar\frac{d}{dx}.
$$

它作用在波函数上：

$$
(\hat p\psi)(x)
=-i\hbar\frac{d\psi}{dx}.
$$

平面波：

$$
\psi_p(x)=e^{ipx/\hbar}
$$

满足：

$$
\hat p\psi_p=p\psi_p.
$$

所以它是动量本征函数。平面波在整个实线上不可平方归一化，应理解为广义本征态或波包展开的基元。

## 位置与动量不对易

对足够光滑的测试波函数 $\psi(x)$：

$$
[\hat x,\hat p]\psi
=
\hat x\hat p\psi-
\hat p\hat x\psi.
$$

逐项计算：

$$
\hat x\hat p\psi
=-i\hbar x\psi',
$$

$$
\hat p\hat x\psi
=-i\hbar(x\psi)'
=-i\hbar(\psi+x\psi').
$$

相减得到：

$$
[\hat x,\hat p]\psi
=i\hbar\psi.
$$

因此：

$$
[\hat x,\hat p]=i\hbar I.
$$

这条正则对易关系是位置—动量不确定性原理的代数来源。

## Hamiltonian 算符

非相对论粒子在势能 $V(x)$ 中的 Hamiltonian 常写为：

$$
H=
\frac{\hat p^2}{2m}+V(\hat x).
$$

在位置表象中：

$$
H=
-\frac{\hbar^2}{2m}
\frac{d^2}{dx^2}
+V(x).
$$

时间依赖 Schrödinger 方程是：

$$
i\hbar\frac{\partial\psi}{\partial t}
=H\psi.
$$

若：

$$
H|E_n\rangle=E_n|E_n\rangle,
$$

则 $E_n$ 是允许能量本征值。边界条件和势能形状共同决定能谱。

## 有界与无界算符

若存在有限常数 $C$，使定义域内所有向量满足：

$$
\|A\psi\|\le C\|\psi\|,
$$

则 $A$ 是有界算符。

有限维算符都自动有界。无限维量子力学中的位置、动量和 Hamiltonian 通常无界，因此不能定义在 Hilbert 空间的每个向量上。

例如，一个平方可积函数的导数未必平方可积，所以动量算符只能作用于满足一定光滑性和边界条件的定义域。

## 对称算符与自伴算符

对称算符在其定义域内满足：

$$
\langle\phi|A\psi\rangle
=
\langle A\phi|\psi\rangle.
$$

严格自伴还要求 $A$ 与 $A^\dagger$ 的定义域一致。无限维情况下，一个形式上通过分部积分看起来“没有边界项”的微分算符，仍可能因边界条件不同拥有不同自伴扩张和不同物理谱。

因此边界条件不是纯计算细节。粒子在直线、圆周或有限区间上运动时，动量和 Hamiltonian 的定义域会不同。

## 算符函数

若 $A$ 有谱分解：

$$
A=\sum_a aP_a,
$$

可以定义：

$$
f(A)=\sum_a f(a)P_a.
$$

例如：

$$
e^{-iAt}=\sum_a e^{-iat}P_a.
$$

这让我们能定义时间演化指数、算符平方根和其他函数。连续谱时需要相应的谱积分。

## 产生与湮灭算符预告

量子谐振子可用两个互为伴随的算符表示：

$$
a,
\qquad
a^\dagger.
$$

它们满足：

$$
[a,a^\dagger]=1.
$$

$a^\dagger$ 把能级提高一级，$a$ 把能级降低一级。弦的每个振动模式都产生类似的振子算符，但还带模式编号和目标时空指标。

## 约束算符与物理态

规范理论中，并非辅助 Hilbert 空间中的每个向量都表示物理态。约束常写成：

$$
G|\psi_{\mathrm{phys}}\rangle=0.
$$

物理态是被约束算符消去的子空间，或在 BRST 量子化中由上同调类表示。

弦理论中的 Virasoro 约束会变成作用在候选弦态上的算符条件。量子化时还会出现正规排序常数和中心项，它们直接影响质量谱和临界维数。

## 具体例子：Pauli 矩阵

二维 Hilbert 空间中的 Pauli $z$ 矩阵是：

$$
\sigma_z=
\begin{pmatrix}
1&0\\
0&-1
\end{pmatrix}.
$$

它是自伴算符，本征态为：

$$
|0\rangle=
\begin{pmatrix}1\\0\end{pmatrix},
\qquad
|1\rangle=
\begin{pmatrix}0\\1\end{pmatrix},
$$

对应本征值 $+1$ 和 $-1$。

对状态：

$$
|\psi\rangle
=
\frac{\sqrt3}{2}|0\rangle
+
\frac12|1\rangle,
$$

测量 $\sigma_z$ 时：

$$
P(+1)=\frac34,
\qquad
P(-1)=\frac14.
$$

期望值为：

$$
\langle\sigma_z\rangle
=(+1)\frac34+(-1)\frac14
=\frac12.
$$

单次结果只能是 $+1$ 或 $-1$，不会是 $1/2$；$1/2$ 是大量重复测量的平均。

## 适用条件与事实边界

1. 可观测量由自伴算符表示，是标准量子力学结构；一般测量结果还可由 POVM 描述。
2. 有限维矩阵直觉不能无条件搬到无限维；定义域、自伴扩张和连续谱是实际物理的一部分。
3. 算符表达依赖表象，抽象算符及物理预测不依赖任意基选择。
4. 不同算符可以共享本征态，也可以没有共同完备本征基；是否对易是关键条件之一。
5. 量子场严格说是算符值分布，而不是每个时空点上的普通有界算符；需要用测试函数涂抹并处理正规化。
6. 弦振子算符所在的候选态空间还必须施加世界面约束，才能得到物理 Hilbert 空间。

## 常见误区

1. **量子算符只是把经典变量上面加一个帽子。** 量子化还要指定 Hilbert 空间、定义域、对易关系和排序，不能只改记号。
2. **所有算符都表示可观测量。** 一般线性算符不一定自伴；酉算符、投影和产生算符承担不同角色。
3. **自伴算符的期望值就是每次测量结果。** 单次结果来自其谱，期望值是统计平均。
4. **$AB$ 表示先做 $A$ 再做 $B$。** 对状态右作用的约定中，$AB|\psi\rangle$ 是先 $B$ 后 $A$。
5. **Hermitian 矩阵的有限维结论可直接覆盖所有微分算符。** 无限维必须检查定义域和自伴性。
6. **两个算符不对易意味着绝对不能测量它们。** 可以分别测量，但一般不存在让二者都具有任意精确确定值的共同状态。
7. **约束算符为零表示算符本身处处为零。** 通常只是它消去物理态，算符在更大候选空间上仍可非零。

## 练习

1. 解释算符线性条件，并验证矩阵乘法满足它。
2. 对矩阵 $A=\begin{pmatrix}1&i\\-i&2\end{pmatrix}$，判断它是否自伴。
3. 若 $A|a\rangle=a|a\rangle$ 且状态正是归一化 $|a\rangle$，测量 $A$ 的结果和不确定度是什么？
4. 证明酉算符保持范数。
5. 用 $\hat p=-i\hbar d/dx$ 验证 $e^{ikx}$ 是动量本征函数，并求本征值。
6. 对 Pauli $z$ 例子计算 $\langle\sigma_z^2\rangle$ 和 $\Delta\sigma_z$。
7. 为什么无限维微分算符必须给出定义域和边界条件？

## 答案与提示

1. $A(c_1\psi+c_2\phi)=c_1A\psi+c_2A\phi$；矩阵乘法对向量加法和标量乘法满足分配律。
2. 是。对矩阵复共轭并转置后仍得到原矩阵。
3. 结果以概率 $1$ 为 $a$，且 $\Delta A=0$。
4. $\|U\psi\|^2=\langle\psi|U^\dagger U|\psi\rangle=\langle\psi|\psi\rangle$。
5. $-i\hbar(d/dx)e^{ikx}=\hbar k e^{ikx}$，本征值是 $p=\hbar k$。
6. $\sigma_z^2=I$，所以 $\langle\sigma_z^2\rangle=1$；均值为 $1/2$，故 $\Delta\sigma_z=\sqrt{1-1/4}=\sqrt3/2$。
7. 因为作用后的函数未必仍平方可积，且不同边界条件会改变伴随、自伴性和能谱。

## 与其他概念的关系

- [[量子态]] 是算符作用的对象；
- [[Born 规则]] 用自伴算符的谱投影计算概率；
- [[对易关系]] 比较算符的两种作用顺序；
- [[不确定性原理]] 由自伴算符的方差和对易子得到；
- [[量子谐振子]] 用产生、湮灭和数算符组织能级；
- [[场的正则量子化]] 把经典场及共轭动量提升为算符值分布；
- 弦的振子和 [[Virasoro 约束]] 都在量子态空间上以算符形式作用。

## 参考资料

- [MIT OCW 8.04, Quantum Physics I](https://ocw.mit.edu/courses/8-04-quantum-physics-i-spring-2013/)
- Barton Zwiebach, *Mastering Quantum Mechanics*, MIT Press, 2022.
- J. J. Sakurai and Jim Napolitano, *Modern Quantum Mechanics*, 3rd ed., Cambridge University Press, 2020.
- Claude Cohen-Tannoudji, Bernard Diu and Franck Laloë, *Quantum Mechanics*, Wiley.
- Brian C. Hall, *Quantum Theory for Mathematicians*, Springer, 2013.
- Michael Reed and Barry Simon, *Methods of Modern Mathematical Physics, Vol. I: Functional Analysis*, Academic Press.
- Joseph Polchinski, *String Theory, Vol. 1*, Cambridge University Press, 1998.
