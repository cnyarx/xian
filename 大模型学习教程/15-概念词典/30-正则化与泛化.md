---
aliases: [正则化, 泛化, Regularization, Generalization, L1, L2, Dropout, Early Stopping]
tags: [概念, 机器学习, 泛化]
level: L10
prerequisites: ["[[16-参数梯度与优化器]]", "[[29-损失函数与评估指标]]"]
---
# 正则化与泛化

## 一句话定义

泛化（Generalization）是模型在没见过的数据上仍能表现良好；正则化（Regularization）是减少模型死记训练样本、改善泛化的一组方法。

## 直觉理解

只背练习册答案，训练分数会很高，换一套题却可能失败。正则化相当于限制死记硬背，促使模型学习更稳定的规律。

## 严格定义

常见做法是在经验损失后加惩罚项：

$$J(\theta)=L(\theta)+\lambda R(\theta)$$

L2 正则常取 $R(\theta)=\|\theta\|_2^2$，L1 常取 $R(\theta)=\|\theta\|_1$。$\lambda$ 控制拟合数据与限制复杂度之间的权衡。

Dropout 在训练时随机屏蔽部分中间值；早停（Early Stopping）在验证集性能不再改善时停止训练；数据增强通过合理变换增加训练样本变化。

## 例子

两个模型训练准确率都是 99%。模型 A 测试准确率 98%，模型 B 只有 70%。A 的泛化更好；B 很可能过拟合。

## 边界

- 正则化过强会导致欠拟合；
- Weight Decay 与 L2 只在普通 SGD 等特定条件下等价，自适应优化器中通常不等价；
- 验证集也可能因反复调参而被间接过拟合；
- 数据增强必须保持标签语义。

## 最小自测

1. 训练误差很低、验证误差很高通常说明什么？
2. 增大 $\lambda$ 总会更好吗？

答案：1. 可能过拟合；2. 不会，过强会欠拟合。

## 详细教程

- [[../03-机器学习基础/05-过拟合与正则化|过拟合与正则化]]

## 来源

- Goodfellow, Bengio & Courville (2016), *Deep Learning*, MIT Press, Chapter 7 `[A]`
- Loshchilov & Hutter (2019), *Decoupled Weight Decay Regularization*：https://arxiv.org/abs/1711.05101 `[A]`
