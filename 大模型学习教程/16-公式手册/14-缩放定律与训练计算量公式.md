---
aliases: [缩放定律公式, 训练计算量公式, Scaling Laws, Chinchilla, 6ND]
tags: [公式, 大模型, 训练, 经验规律]
prerequisites: ["[[../15-概念词典/01-数与实数]]", "[[../15-概念词典/02-比例分数与百分数]]", "[[../15-概念词典/03-变量与函数]]", "[[13-语言模型损失与困惑度公式]]"]
---
# 缩放定律与训练计算量公式

## 1. 经验幂律

一种常见形式是：

$$
L(N)\approx L_\infty+aN^{-\alpha}
$$

- $N$：规模变量，如参数量；
- $L(N)$：验证损失；
- $L_\infty$：不可约损失下限；
- $a,\alpha$：通过实验拟合得到的正数；
- $\approx$：经验近似，不是严格恒等式。

若参数量翻倍，幂律项乘以 $2^{-\alpha}$。例如 $\alpha=0.5$ 时，翻 4 倍参数使该项变为一半。

## 2. 对数坐标线性化

忽略 $L_\infty$ 时：

$$
L\approx aN^{-\alpha}
$$

取对数：

$$
\log L\approx\log a-\alpha\log N
$$

所以在 log-log 坐标上近似是一条斜率为 $-\alpha$ 的直线，便于拟合和外推。

## 3. 训练计算量粗估

Dense Transformer 训练常用量级估计：

$$
C\approx6ND
$$

- $N$：通常取非嵌入参数量；
- $D$：训练 token 数；
- $C$：浮点运算次数（FLOPs）的量级；
- 系数 6：来自简化的前向与反向计算估计。

### 数值例子

$N=10^9$，$D=2\times10^{10}$：

$$
C\approx6\times10^9\times2\times10^{10}=1.2\times10^{20}\text{ FLOPs}
$$

## 4. Chinchilla 的含义

Hoffmann 等人在特定模型族和实验范围内发现：固定计算预算时，参数量与训练 token 数应共同扩展，过大模型配过少数据并非最优。常见“每参数约 20 token”来自该论文的特定拟合，不能当作所有模型、数据和训练方法都适用的自然定律。

## 5. 边界

- 缩放定律是实验归纳，不是数学定理；
- 外推超出实验范围可能失效；
- 它不能单独预测涌现能力、事实可靠性或安全性；
- MoE、长序列、稀疏计算等架构下，$6ND$ 需重新核算活跃参数和实际计算。

## 最小自测

1. $N$ 翻 9 倍、$\alpha=0.5$，幂律项变为原来的多少？
2. $N=7\times10^9$、$D=10^{12}$ 时，粗算 $C$。
3. 为什么缩放定律使用 $\approx$ 而非 $=$？

答案：

1. 若幂律项与 $N^{-\alpha}$ 成正比，则变为 $9^{-0.5}=1/3$；若题目指的是 $N^{\alpha}$，则变为 3 倍。使用公式时必须写清指数符号。
2. 按 $C\approx6ND$，$C\approx6\times7\times10^9\times10^{12}=4.2\times10^{22}$ FLOPs。
3. 系数 6 和幂律参数来自特定架构、训练目标和统计拟合；嵌入、注意力、激活、通信及硬件利用率等都会使实际计算量和损失偏离简式。

## 正文

- [[../07-预训练/04-缩放定律|缩放定律]]

## 来源

- Kaplan et al. (2020), *Scaling Laws for Neural Language Models*：https://arxiv.org/abs/2001.08361 `[A]`
- Hoffmann et al. (2022), *Training Compute-Optimal Large Language Models*：https://arxiv.org/abs/2203.15556 `[A]`