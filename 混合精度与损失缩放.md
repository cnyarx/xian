---
aliases: [混合精度, 损失缩放, Mixed Precision, Loss Scaling, FP16, BF16, FP8]
tags: [概念, 训练系统, 数值计算, 大模型, 学习教程]
level: L9
prerequisites: ["[[数与实数]]", "[[浮点数与数值稳定]]", "[[参数梯度与优化器]]"]
---
# 混合精度与损失缩放

## 一句话定义

混合精度训练（Mixed-Precision Training）让适合的运算使用较低精度，同时让容易积累误差或溢出的状态保留较高精度；损失缩放（Loss Scaling）用于降低 FP16 小梯度下溢为零的风险。

## 为什么不全部使用一种精度

低精度张量占用更少显存和带宽，受支持的硬件也能更快完成矩阵乘法。但训练同时包含数量级差异很大的权重、激活、梯度和优化器状态。全部降为低精度可能让小数消失或让大数溢出。

常见分工包括：

- 矩阵乘法输入使用 FP16、BF16 或 FP8；
- 乘加累积和归一化使用更高精度；
- 优化器状态或主权重副本使用 FP32；
- 在更新前检查梯度是否为 `inf` 或 `nan`。

具体分工由硬件、框架和训练配方决定。

## FP16 与 BF16

FP16 有较多有效数字位，但指数范围窄；BF16 的指数位宽与 FP32 相同，因此动态范围更大，但有效数字更少。BF16 通常较少需要损失缩放，不代表它没有舍入误差。

## 损失缩放怎样工作

将损失乘以比例 $s$，反向传播得到的每个梯度也会乘以 $s$。在优化器更新前再除以 $s$，理论上恢复原梯度。中间放大的梯度更不容易在 FP16 中变成零。

动态损失缩放（Dynamic Loss Scaling）会在一段时间没有溢出时尝试增大 $s$，检测到溢出时减小 $s$ 并跳过有问题的更新。

## 小例子

某个真实梯度为 $2\times10^{-8}$，若当前低精度表示把它舍入为 0，使用 $s=1024$ 后，反向过程中的数值变为约 $2.048\times10^{-5}$。更新前除以 1024，数学上回到原值。实际是否保留下来仍取决于运算在哪种精度中执行。

## FP8 的额外缩放

FP8 格式范围更小，训练系统通常为不同张量维护缩放因子，并统计其数值范围。不能只把数据类型改成 FP8 就假定训练稳定。

## 边界

- 损失缩放主要解决小梯度下溢，不能修复错误数据或过大学习率；
- 放大倍数过大可能造成溢出；
- BF16 范围较大不等于所有运算都可安全使用 BF16；
- 混合精度带来的速度收益依赖硬件内核和通信瓶颈。

## 最小自测

题目：损失乘以 512 后，优化器更新前为什么还要把梯度除以 512？

答案：因为放大只是为了让反向过程中的小梯度更容易表示，真正的优化步仍应使用与原损失对应的梯度尺度。

## 详细教程

- [[混合精度训练|混合精度训练]]

## 来源

- Micikevicius et al. (2018), *Mixed Precision Training*：https://arxiv.org/abs/1710.03740 `[A]`
- Kalamkar et al. (2019), *A Study of BFLOAT16 for Deep Learning Training*：https://arxiv.org/abs/1905.12322 `[A]`
- Micikevicius et al. (2022), *FP8 Formats for Deep Learning*：https://arxiv.org/abs/2209.05433 `[A]`
