---
aliases: [训练数据, Training Data, 数据清洗, Data Cleaning, 去重, Deduplication, 去污染, Decontamination, 数据配比, Data Mixture]
tags: [大模型, 数据, 教程, 学习教程]
prerequisites: ["[[预训练概览]]"]
next: ["[[训练目标]]"]
---
# 训练数据

## 核心事实

模型只能从训练信号中更新参数。数据数量、质量、覆盖范围、重复率和许可条件都会影响最终行为。

## 数据处理链

```text
原始来源 → 解析 → 语言/质量过滤 → 安全与隐私处理 → 去重 → 去污染 → 配比 → 分词 → 打包
```

- **解析（Parsing）**：从 HTML、PDF、代码仓提取正文和结构；
- **质量过滤（Quality Filtering）**：移除乱码、模板垃圾、低信息重复页；
- **去重（Deduplication）**：删除相同或近似相同内容，减少重复权重和记忆风险；
- **去污染（Decontamination）**：移除评测题及近似副本，降低评测虚高；
- **数据配比（Data Mixture）**：决定网页、代码、数学、书籍和各语言的采样权重；
- **打包（Packing）**：把若干短文档组织进固定长度序列，并保留文档边界策略。

每一步都会改变后续数据分布，因此应记录代码版本、阈值、输入输出数量和被过滤样本的抽样结果。

## 一个处理例子

假设原始语料有 1000 万文档：

```text
解析成功                         920 万
语言与格式过滤后                 760 万
近似去重后                       510 万
隐私/安全规则处理后              500 万
与评测集去污染后                 499.8 万
```

这些数字只是教学假设，不代表任何真实模型。重点是不能只报告最终 token 数：还应说明每一步删了什么、误删风险和抽样质量。

## 去重和去污染不是一回事

去重在训练语料内部寻找重复；去污染比较训练语料与评测集。一个文档在训练数据中只出现一次，也可能正好包含评测答案；反过来，大量重复网页未必属于评测污染，但会改变样本权重。

近似去重通常依赖哈希、n-gram 或向量相似度。阈值过松会漏掉改写副本，过严会删除合法的引用、模板代码或不同语言翻译。

## 数据配比

“代码占 20%”必须说明按什么计数：文档数、字符数、原始 token 还是实际训练采样 token。小型高质量集合可通过重复采样获得更高训练权重，但重复过多可能增加过拟合和记忆风险。

配比应围绕目标能力做消融，而不是把某个公开模型的比例当作普遍最优值。

## 打包与文档边界

固定长度训练序列可能拼接多个短文档。若不保存边界或注意力规则，模型会看到一个文档紧接另一个文档。不同训练方案会加入结束 token、重置位置或采用文档级掩码，应以具体实现为准。

## 数据不是越多越好

加入低质量或错误数据可能降低效果；过度过滤也会删除方言、小众文化和困难文本，引入偏差。不存在对所有模型都最优的固定配比。

## 数据课程

课程学习（Curriculum Learning）按难度或类型安排训练顺序。部分训练方案也在末期提高高质量数据权重。这些是具体策略，效果取决于模型、数据和训练预算，不能视为普遍定理。

## 合法与伦理边界

需要考虑版权、隐私、个人数据、敏感内容与数据来源条款。公开可访问不自动等于可不受限制地训练和再发布。

数据治理至少记录：来源、获取依据、许可证或条款、个人数据处理、删除请求机制、访问权限和数据保留期限。法律要求随地区和用途变化，应由合格人员核对，教程不能替代法律意见。

## 怎样验收数据管线

- 随机抽样每个过滤阶段的保留与删除样本；
- 统计语言、领域、来源和时间分布；
- 检查文档重复与模板占比；
- 用已知评测题做污染探针；
- 搜索密钥、邮箱、电话等敏感模式并人工复核；
- 确认训练文件可追溯到处理配置；
- 对过滤规则做群体和语言偏差检查。

“脚本成功运行”不能证明数据质量合格。

## 概念与公式导航

- [[数据预处理流水线|数据预处理流水线]]

## 练习

1. 去重和去污染有何区别？
2. 为什么重复数据可能增强记忆风险？
3. 设计一个中文、英文、代码混合的数据配比，并说明目的。
4. 过滤有害内容可能带来哪些副作用？

## 来源

- Dodge, J. et al. (2021). *Documenting Large Webtext Corpora: A Case Study on the Colossal Clean Crawled Corpus*. EMNLP https://arxiv.org/abs/2104.08758 `[A]`
- Lee, K. et al. (2022). *Deduplicating Training Data Makes Language Models Better*. ACL. https://arxiv.org/abs/2107.06499 `[A]`
- Gao, L. et al. (2020). *The Pile*. https://arxiv.org/abs/2101.00027 `[A]`