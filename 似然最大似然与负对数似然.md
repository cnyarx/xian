---
aliases: [似然, 最大似然, 负对数似然, Likelihood, Maximum Likelihood, NLL]
tags: [概念, 概率, 训练, 大模型, 学习教程]
level: L5
prerequisites: ["[[变量与函数]]", "[[随机事件与随机变量]]", "[[指数与对数|指数与对数]]"]
---
# 似然、最大似然与负对数似然

## 一句话定义

似然（Likelihood）把已经观察到的数据固定下来，询问哪组模型参数更能产生这些数据；最大似然选择让观察数据概率最大的参数。

## 概率与似然的视角差异

- 概率：参数固定，问不同数据出现的可能性。
- 似然：数据已看到，比较不同参数对这份数据的支持程度。

数学表达可以是同一个 $P_\theta(x)$，区别在于我们把谁当作变化对象。

## 为什么取对数

一组独立样本的联合似然常是许多小概率相乘：

$$
\mathcal L(\theta)=\prod_iP_\theta(x_i)
$$

取对数后乘积变成和：

$$
\log\mathcal L(\theta)=\sum_i\log P_\theta(x_i)
$$

最大化对数似然等价于最小化负对数似然：

$$
\operatorname{NLL}=-\sum_i\log P_\theta(x_i)
$$

## 与交叉熵的关系

当目标是 one-hot 类别标签时，每个样本的交叉熵就是正确类别的负对数概率。因此常见分类和语言模型训练中的交叉熵，可理解为平均负对数似然。

## 边界

- 最大似然只是在所选模型族和数据下的准则，不保证学到因果真相。
- 数据有偏、泄漏或重复时，似然目标也会学习这些问题。
- 样本并非独立时，不能不加说明地直接使用独立乘积形式。

## 最小自测

题目：模型给真实 token 的概率从 0.2 提高到 0.8，负对数似然怎样变化？

答案：从 $-\ln0.2\approx1.609$ 降到 $-\ln0.8\approx0.223$。

## 来源

- Goodfellow, Bengio, Courville (2016), *Deep Learning*, Chapter 5：https://www.deeplearningbook.org/ `[A]`
