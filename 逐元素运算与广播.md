---
aliases: [逐元素运算, 广播, Hadamard Product, Broadcasting]
tags: [概念, 数学, 张量, 大模型, 学习教程]
level: L6
prerequisites: ["[[坐标维度与形状]]", "[[标量向量矩阵与张量]]"]
---
# 逐元素运算与广播

## 一句话定义

逐元素运算对相同位置的数字分别计算；广播（Broadcasting）让形状兼容的小数组像被重复一样参与运算，但通常不真的复制数据。

## 逐元素乘法

$$
[1,2,3]\odot[4,5,6]=[4,10,18]
$$

$\odot$ 表示对应位置相乘，结果仍是向量。它不同于点积：点积还会把结果相加，得到 $32$。

## 广播例子

矩阵每一行都加同一个偏置：

$$
\begin{bmatrix}1&2&3\\4&5&6\end{bmatrix}+[10,20,30]
=
\begin{bmatrix}11&22&33\\14&25&36\end{bmatrix}
$$

向量 `[10,20,30]` 沿行轴广播。

## 形状规则的直觉

从最后一个轴向前比较：两个长度必须相同，或其中一个为 1，或其中一方没有该轴。具体规则以 NumPy/PyTorch 文档为准。

## 与大模型的关系

- 线性层对每个 token 加同一个偏置；
- 注意力给分数矩阵加 mask；
- LayerNorm 的缩放参数作用于每个位置；
- SwiGLU 使用逐元素门控乘法。

## 常见误区

广播方便但可能隐藏形状错误。能运行不等于语义正确，必须明确每个轴是什么。

## 最小自测

题目：形状 `[2,3]` 的矩阵能否和形状 `[3]` 的向量相加？

答案：能，向量沿第一轴广播。形状 `[2]` 通常不能直接与 `[2,3]` 相加，因为最后一轴 2 与 3 不兼容。

## 来源

- NumPy, Broadcasting：https://numpy.org/doc/stable/user/basics.broadcasting.html `[A]`
- PyTorch, Broadcasting semantics：https://pytorch.org/docs/stable/notes/broadcasting.html `[A]`
