---
aliases: [上下文学习, 少样本学习, 零样本学习, In-Context Learning, ICL, Few-shot, Zero-shot]
tags: [概念, 大模型, 提示工程, 学习教程]
level: L8
prerequisites: ["[[Token词表与Token-ID]]", "[[Logit概率与采样]]", "[[注意力分数与注意力权重]]"]
---
# 上下文学习

## 一句话定义

上下文学习（In-Context Learning, ICL）在本文采用广义口径：模型在推理时不更新参数，仅依据提示中的任务描述和可选示例来适应任务；其中 zero-shot 不提供示例，few-shot 提供一个或多个示例。

## 直觉理解

老师不讲课，直接在黑板上写：“例 1：苹果→fruit；例 2：桌子→furniture；例 3：汽车→？” 你看了前两个例子就明白了规则。大模型也是这样：示例作为上下文，模型参照它们完成新输入。

## 严格定义

给定提示 $P=[(x_1,y_1),\ldots,(x_k,y_k),x_{\text{new}}]$，模型直接输出 $y_{\text{new}}$，不修改任何参数。它不同于微调（fine-tuning）——后者会更新权重。

- Zero-shot：$k=0$，只给任务描述；
- Few-shot：$k\ge1$，给若干示例；
- 一些模型和任务在扩大模型规模后出现更强的 ICL，但效果不保证随参数量单调增长，还取决于训练数据、架构、提示格式和任务。

## 例子

```text
将英文翻译成中文：
hello → 你好
world → 世界
cat → ？
```

模型看到前两个示例后，应该输出"猫"。

## 边界

- ICL 不是微调，不改变模型参数；
- 示例顺序、格式和标签分布会影响结果；
- 长上下文可能稀释效果；
- 不是所有任务都适合 ICL。

## 最小自测

1. ICL 和微调的根本区别是什么？
2. Zero-shot 和 Few-shot 的 ICL 分别指什么？

答案：1. ICL 不更新参数，微调会；2. Zero-shot 无示例，Few-shot 有若干示例。

## 详细教程

- [[Decoder-Only 架构|Decoder-Only 架构]]
- [[监督微调|监督微调]]

## 来源

- Brown et al. (2020), *Language Models are Few-Shot Learners*：https://arxiv.org/abs/2005.14165 `[A]`