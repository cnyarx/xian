---
aliases: [过拟合, 欠拟合, 正则化, Dropout, Overfitting]
tags: [机器学习, 教程, 大模型, 学习教程]
prerequisites: ["[[梯度下降]]"]
next: ["[[分类与回归]]"]
---
# 过拟合与正则化

## 一句话定义

**过拟合**是模型在训练数据上表现很好，但在同分布的未见数据上表现明显较差。记住噪声或偶然规律是常见成因之一，但也可能因模型选择、分布偏差或训练过度导致泛化变差。

## 过拟合 vs 欠拟合

```
欠拟合：模型太简单，训练集和测试集表现都差。
过拟合：模型太复杂，训练集表现极好但测试集表现差。
```

**生活类比**：学生死记硬背练习题答案，换一道同类型的新题就不会了。

**在大模型中的体现**：模型可能“记住”了训练数据中的特定句子，但推理时不能泛化到新问题。

## 怎样检测过拟合

```
训练损失持续下降，验证损失开始上升 → 过拟合
```

## 正则化

正则化是防止过拟合的技术。

### L2 正则化

在损失函数中加入参数大小的惩罚项：

$$
L_{\text{total}} = L_{\text{original}} + \lambda \sum \|\theta\|^2
$$

- $\lambda$：正则化系数，控制惩罚强度。
- 鼓励参数保持较小值，减少模型复杂度。

### Dropout

标准 Dropout 在训练时以概率 $p$ 随机将部分中间激活值置零，减少网络对少数特征组合的依赖。

```python
dropout = nn.Dropout(p=0.5)
```

- `p=0.5`：每个激活值有 50% 概率在本步被置零；
- PyTorch 使用 inverted dropout：训练时缩放保留下来的激活值，推理时不再随机置零，也无需再次缩放。

### 早停

当验证损失不再下降时停止训练，防止继续训练导致过拟合。

### 数据增强

从现有数据生成更多训练样本，增加数据多样性，减少过拟合。

## 在大模型中的应用

- 大模型训练常使用 Weight Decay（权重衰减）。在普通 SGD 等特定更新条件下，它可与 L2 正则化等价；在 Adam 等自适应优化器中，两者通常不等价，AdamW 会把权重衰减与梯度自适应缩放解耦。
- Dropout 在 Transformer 中有一定使用，但现代大模型有时省略。
- 模型规模很大时是否过拟合取决于数据量、重复度、训练步数和任务，不能只凭参数量判断。

## 常见误区

- 正则化不能完全替代足够多的数据。
- 训练损失低不等于模型好。
- 过拟合不一定意味着模型太复杂，也可能数据太少或数据质量差。

## 概念与公式导航

- [[正则化与泛化|正则化与泛化]]
- [[紫外发散与红外发散|紫外发散与红外发散]] 辨析量子场论 regularization 与机器学习正则化：本篇通过惩罚、随机化或数据策略改善泛化，量子场论正规化则先给发散积分一个受控定义，二者不是同一种数学操作。
- [[正规化与重整化|正规化与重整化]] 展示 QFT 中正规化之后如何用参数、场、局域反项和重整化条件建立有限预测；它不等于本篇的 L1/L2、Dropout、早停或数据增强。
- [[弦理论与大模型跨学科关系图谱]] 汇总这类跨学科同名异义关系，避免把 ML regularization、QFT regularization 和 renormalization 误连为等价概念。

## 练习

1. 解释过拟合和欠拟合的区别，各自举一个生活例子。
2. 训练损失 0.01，验证损失 2.5。可能是什么问题？
3. Dropout 在训练和测试时有什么不同？
4. 为什么大模型有时反而不容易过拟合？

## 来源

- 正则化：Goodfellow, I., Bengio, Y., & Courville, A. (2016). *Deep Learning*. MIT Press. 第7章 `[A]`
- Dropout：Srivastava, N. et al. (2014). *Dropout: A Simple Way to Prevent Neural Networks from Overfitting*. JMLR. https://www.jmlr.org/papers/v15/srivastava14a.html `[A]`