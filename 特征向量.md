---
aliases: [Eigenvector, Eigenvectors]
english_name: Eigenvector
tags: [数学, 线性代数, 弦理论先修, 弦理论]
prerequisites:
  - "[[线性映射]]"
  - "[[矩阵]]"
  - "[[特征值]]"
used_by:
  - "[[正规模]]"
  - "[[量子算符]]"
  - "[[Fourier 级数]]"
evidence_status: 教材事实
---

# 特征向量（Eigenvector）

## 一句话解释

特征向量是经过线性变换后方向不被混合、只被缩放的非零向量。

## 为什么要学它

复杂耦合系统若用普通坐标描述，各分量会互相影响；换到特征向量构成的模式后，每个模式可以独立变化。正规模、量子态和 Fourier 模式都体现这一思想。

## 定义

非零向量 $\mathbf v$ 若满足：

$$
A\mathbf v=\lambda\mathbf v,
$$

就是矩阵 $A$ 对应特征值 $\lambda$ 的特征向量。

“方向不变”允许：

- $\lambda>0$：同方向缩放；
- $\lambda<0$：反向并缩放；
- $\lambda=0$：压到零向量。

## 如何求特征向量

先由：

$$
\det(A-\lambda I)=0
$$

求出特征值。然后对每个 $\lambda$ 解：

$$
(A-\lambda I)\mathbf v=\mathbf0.
$$

所有解构成该特征值的特征空间，但要排除零向量作为单个特征向量。

## 一个例子

$$
A=\begin{pmatrix}2&0\\0&3\end{pmatrix}.
$$

对 $\lambda=2$：

$$
(A-2I)\mathbf v=0
$$

给出所有 $(x,0)$，所以横轴上的任意非零向量都是特征向量。

对 $\lambda=3$，纵轴上的任意非零向量都是特征向量。

## 倍数仍是特征向量

若 $\mathbf v$ 是特征向量，则任何非零标量倍数 $c\mathbf v$ 也是：

$$
A(c\mathbf v)=cA\mathbf v=c\lambda\mathbf v.
$$

因此特征向量通常不是唯一的一支箭头，而是一整个特征方向或特征子空间。

## 对角化与特征基

若 $n$ 维空间中能找到 $n$ 个线性无关特征向量，可把它们选作基。在这组基下：

$$
A= S D S^{-1},
$$

$S$ 的列是特征向量，$D$ 为特征值组成的对角矩阵。变换不再混合各基方向。

## 正规模

两个耦合振子各自坐标可能相互牵连。质量和劲度矩阵的特征向量给出特殊集体运动方式：例如两物体同向运动或反向运动。在每个特征模式中，系统像单独的简谐振子。

弦有无限多个此类函数特征模式，正弦和复指数经常作为微分算符的特征函数。

## 特征函数

当线性映射是微分算符时，特征向量变成函数，通常叫特征函数。例如：

$$
-\frac{d^2}{dx^2}\sin(kx)=k^2\sin(kx).
$$

$\sin(kx)$ 是算符 $-d^2/dx^2$ 的特征函数，特征值是 $k^2$。

## 量子理论中的意义

若量子态是可观测量算符的特征态：

$$
\hat A|a\rangle=a|a\rangle,
$$

测量 $A$ 会确定地得到特征值 $a$。一般状态可展开为多个特征态的线性组合。

## 常见误区

1. **零向量是所有特征值的特征向量。** 定义明确排除零向量。
2. **特征向量长度必须为一。** 不必须，但物理中常归一化方便计算。
3. **每个矩阵都有一组完整特征向量基。** 错，有些矩阵不可对角化。
4. **同一特征值只能有一个特征向量。** 错，非零倍数和更高维特征空间都可能存在。
5. **特征函数与普通向量毫无关系。** 函数空间中，函数就是向量。

## 练习

1. 对 $A=\operatorname{diag}(2,5)$，给出各特征方向。
2. 若 $A\mathbf v=3\mathbf v$，$2\mathbf v$ 是否也是特征向量？
3. 为什么定义排除零向量？
4. 验证 $e^{ikx}$ 是 $d/dx$ 的特征函数。
5. 特征向量怎样帮助解耦振动？

## 答案与提示

1. 横轴对应 $2$，纵轴对应 $5$。
2. 是，对应同一特征值 $3$。
3. 零向量对任意 $\lambda$ 都满足等式，无法确定特殊方向或特征值。
4. $d(e^{ikx})/dx=ik e^{ikx}$，特征值为 $ik$。
5. 以特征向量为模式坐标后，各模式独立缩放或振动。

## 与其他概念的关系

- [[特征值]] 给出特征向量的缩放因子；
- 特征向量构成的基可对角化 [[矩阵]]；
- [[正规模]] 是振动系统的特征模式；
- Fourier 模式和量子特征态都是函数或状态空间中的特征向量。

## 参考资料

- [MIT OCW 18.06, Eigenvalues and Eigenvectors](https://ocw.mit.edu/courses/18-06-linear-algebra-spring-2010/)
- Gilbert Strang, *Introduction to Linear Algebra*, 5th ed.
