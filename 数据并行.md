---
aliases: [数据并行, Data Parallelism, AllReduce, ZeRO]
tags: [大模型, 分布式训练, 教程, 学习教程]
prerequisites: ["[[训练系统概览]]", "[[数据并行梯度同步与状态分片|数据并行、梯度同步与状态分片]]"]
next: ["[[模型并行]]"]
---
# 数据并行

## 学习目标

学完本篇后，你应能：

1. 解释为什么不同设备算出的梯度必须同步；
2. 区分本地批量、全局批量和梯度累积；
3. 说明 DDP 的通信如何与反向计算重叠；
4. 说清普通数据并行、ZeRO 和 FSDP 分别复制或分片哪些状态；
5. 根据显存、通信和模型大小判断数据并行是否适用。

## 一句话定义

数据并行（Data Parallelism）为每张设备复制相同模型，各自处理不同 mini-batch，再同步梯度，使所有模型副本完成相同的参数更新。

## 为什么不能各算各的

假设两张 GPU 从相同参数 $w$ 出发：GPU0 看到猫图片，得到梯度 $g_0$；GPU1 看到狗图片，得到梯度 $g_1$。若两卡分别更新：

$$
w_0'=w-\eta g_0,\qquad w_1'=w-\eta g_1
$$

更新后两个模型就不再相同，后续相当于各自训练不同模型。数据并行要先得到共同梯度：

$$
g=\frac{g_0+g_1}{2},\qquad w'=w-\eta g
$$

再让每张卡使用同一个 $g$ 更新。等大小批量、相同损失归约规则下，这等价于在合并后的全局批量上求平均梯度。

若各卡有效 token 数不同，不能机械平均“每卡平均梯度”；应依据有效 token 数和框架的损失归约语义计算全局平均。

## 基本流程

```text
GPU0：数据批次A → 前向 → 反向 → 梯度g0 ┐
GPU1：数据批次B → 前向 → 反向 → 梯度g1 ├→ All-Reduce → 共同梯度g → 同步更新
GPU2：数据批次C → 前向 → 反向 → 梯度g2 ┤
GPU3：数据批次D → 前向 → 反向 → 梯度g3 ┘
```

训练数据还必须由分布式采样器正确切分。若每张卡都读取相同样本，梯度通信仍能成功，但计算被重复，实际全局批量并没有按预期增加。

## 本地批量、全局批量与梯度累积

规则批次下：

$$
B_{global}=B_{device}\times N_{devices}\times N_{accumulation}
$$

例如单卡批量 8、16 张卡、每 4 个 micro-batch 更新一次：

$$
B_{global}=8\times16\times4=512
$$

这表示一次参数更新使用 512 个样本，不表示一次前向能同时容纳 512 个样本。梯度累积减少每次前后向所需激活显存，但总前后向计算并未减少。变长文本训练还应同时记录每次更新的有效 token 数。

完整公式与梯度裁剪顺序见 [[梯度裁剪与全局批量公式|梯度裁剪与全局批量公式]]。

## All-Reduce 到底做什么

All-Reduce 先归约各设备上的同形状张量，再把结果发回每台设备。归约操作可能是求和；框架可能在通信前后除以设备数。因此应核对框架文档和实际损失缩放，不能仅凭“All-Reduce”这个名字断定结果已经平均。

Ring All-Reduce 可拆成 Reduce-Scatter 和 All-Gather 两阶段，让设备沿环分块传输。它避免所有数据都经过单一中心设备，但通信耗时仍受梯度量、带宽、延迟和最慢设备限制。

## DDP 为什么使用 Bucket

反向传播通常从靠近输出的层开始产生梯度。若等全部梯度算完才通信，计算与通信完全串行。PyTorch DistributedDataParallel（DDP）把参数梯度分桶（Bucket）：一个桶就绪后即可开始异步归约，同时更前面的层继续反向计算。

```text
计算后层梯度 ── 该桶通信开始 ─────────┐
              计算前层梯度 ── 下一桶通信 ─┤→ 更新
```

桶太小会增加通信启动次数，太大又可能推迟首个通信。实际最优值取决于模型结构、互联和框架版本，应以 profiler 的时间线为准。

## 普通 DDP 的显存复制

普通数据并行通常在每张卡保留：

- 一份模型参数；
- 一份参数梯度；
- 一份或多份优化器状态；
- 本卡 micro-batch 的激活。

以 Adam 类优化器为例，动量等状态可能比低精度模型参数占用更多字节。即使激活能通过减小批量控制，完整训练状态仍可能放不进单卡。

## ZeRO 与 FSDP

ZeRO（Zero Redundancy Optimizer）按阶段减少重复状态：

1. **Stage 1**：分片优化器状态；
2. **Stage 2**：再分片梯度；
3. **Stage 3**：再分片参数。

Stage 3 并不是计算时永远不需要完整层参数。某层计算前通常要 All-Gather 当层参数，计算后再释放或重新分片；反向阶段还要 Reduce-Scatter 梯度。它以更多通信、调度和实现复杂度换取更低的每卡状态显存。

PyTorch Fully Sharded Data Parallel（FSDP）提供相近的全分片训练接口。具体分片策略、参数预取、CPU offload 和混合精度行为会随配置及版本变化，应以正在使用的官方文档和 profiler 结果为准。

## 什么时候选哪一种

| 情况 | 优先考虑 | 原因与边界 |
|---|---|---|
| 完整模型和优化器状态能放入单卡，主要想提高吞吐 | DDP | 路径简单，但每卡重复训练状态 |
| 参数能放下，优化器状态或梯度太大 | ZeRO-1/2 类分片 | 节省部分状态，仍需评估通信 |
| 完整参数也放不下单卡 | ZeRO-3 / FSDP，或与模型并行组合 | 需要频繁收集参数，不能保证更快 |
| 单层本身无法放入或计算 | [[模型并行|模型并行]] | 单靠数据并行与状态分片未必解决单层峰值 |

## 用什么证据验收

不要只看“程序跑起来了”。至少检查：

1. 每卡读取的样本 ID 是否按预期不同；
2. 同一步更新后各卡参数校验值是否一致；
3. 日志中的全局样本数或有效 token 数是否符合公式；
4. profiler 是否显示梯度通信与反向计算重叠；
5. 增加设备后，吞吐、通信占比和最慢卡等待时间如何变化；
6. 保存并恢复分片检查点后，模型和优化器状态是否一致。

## 常见失败模式

- 忘记设置分布式采样器，所有设备重复读取同一批数据；
- 梯度累积时每个 micro-batch 都同步，产生不必要通信；
- 损失已经按本地 token 平均，又错误地二次除以设备数；
- 只比较每卡批量，改变卡数时没有同步调整学习率或训练步数；
- 认为 ZeRO-3 一定更快，忽略参数收集带来的通信；
- 只保存某张卡的分片，导致检查点无法完整恢复。

## 概念与公式导航

- [[数据并行梯度同步与状态分片|数据并行与状态分片]]
- [[梯度裁剪与全局批量公式|梯度裁剪与全局批量公式]]

## 练习

1. 单卡批量 8、16 卡、梯度累积 4，全局批量是多少？
2. 两卡从相同参数出发却各自使用本地梯度更新，会发生什么？
3. 变长文本下，为什么简单平均每卡平均梯度可能不等于全局 token 平均？
4. ZeRO-3 比普通 DDP 分片哪些训练状态？计算某层前为什么仍可能通信？
5. 训练吞吐没有随卡数增加而提高时，你会检查哪些证据？

## 练习答案入口

- [[训练系统与规模化答案#02-数据并行|数据并行答案]]

## 来源

- PyTorch, *DistributedDataParallel*：https://pytorch.org/docs/stable/generated/torch.nn.parallel.DistributedDataParallel.html `[A]`
- Rajbhandari, S. et al. (2020). *ZeRO*. SC. https://arxiv.org/abs/1910.02054 `[A]`
- PyTorch, *FSDP*：https://pytorch.org/docs/stable/fsdp.html `[A]`