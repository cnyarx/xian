---
aliases:
  - Set
  - Sets
english_name: Set
tags:
  - 数学
  - 基础概念
  - 高中基础
  - 弦理论先修
  - 弦理论
prerequisites: []
used_by:
  - "[[区间]]"
  - "[[函数]]"
  - "[[向量空间]]"
  - "[[群]]"
  - "[[拓扑空间]]"
evidence_status: 教材事实
---

# 集合（Set）

## 一句话解释

集合是把一些确定对象看成一个整体；这些对象叫集合的元素。

## 为什么要学它

现代数学几乎所有结构都先从“有哪些对象”开始，再规定对象之间能做什么：

- 函数要说明定义域和值域；
- 向量空间是一组可相加、可数乘的对象；
- 群是一组带运算的元素；
- 拓扑空间是一个集合加上一族开集；
- 量子态、场配置和弦的可能形状也会组成相应的状态集合或空间。

集合语言让我们准确区分“对象”“对象组成的整体”以及“整体之间的关系”。

## 元素与属于符号

若对象 $x$ 是集合 $A$ 的元素，写成：

$$
x\in A.
$$

若 $x$ 不是 $A$ 的元素，写成：

$$
x\notin A.
$$

例如：

$$
2\in\{1,2,3\},
\qquad
4\notin\{1,2,3\}.
$$

花括号中的对象是集合元素。

## 列举法

元素较少时，可以直接列出：

$$
A=\{1,2,3\}.
$$

集合不关心列举顺序，也不重复计算同一元素：

$$
\{1,2,3\}=\{3,2,1\}=\{1,1,2,3\}.
$$

这与数列不同；数列通常关心顺序和重复次数。

## 描述法

元素很多时，可写成满足某条件的所有对象：

$$
B=\{x\in\mathbb R\mid x^2<4\}.
$$

其中：

- $x\in\mathbb R$：候选元素是实数；
- 竖线 $\mid$：读作“满足”；
- $x^2<4$：元素必须满足的条件。

因此：

$$
B=(-2,2).
$$

## 空集

没有任何元素的集合叫空集：

$$
\varnothing.
$$

空集不是“什么都没写清楚”，而是一个确定的、元素数量为零的集合。

空集与只含空集的集合不同：

$$
\varnothing\ne\{\varnothing\}.
$$

后者有一个元素，这个元素恰好是空集。

## 集合相等

两个集合相等，当且仅当它们含有完全相同的元素：

$$
A=B
\iff
\forall x\,(x\in A\Longleftrightarrow x\in B).
$$

符号含义：

- $\forall x$：对每个对象 $x$；
- $\Longleftrightarrow$：两边互相推出；
- 顺序和重复列举不影响集合相等。

## 子集

若 $A$ 的每个元素也属于 $B$，就说 $A$ 是 $B$ 的子集：

$$
A\subseteq B.
$$

正式写成：

$$
A\subseteq B
\iff
\forall x\,(x\in A\Rightarrow x\in B).
$$

例如：

$$
\{1,2\}\subseteq\{1,2,3\}.
$$

每个集合都是自己的子集，空集是每个集合的子集。

## 真子集

若：

$$
A\subseteq B
$$

而且：

$$
A\ne B,
$$

则 $A$ 是 $B$ 的真子集。

真子集常写成 $A\subsetneq B$。有些教材对 $\subset$ 是否表示真子集采用不同约定，因此本知识库优先使用无歧义的 $\subseteq$ 和 $\subsetneq$。

## 属于与包含不能混用

设：

$$
A=\{1,2,3\}.
$$

正确的是：

$$
1\in A,
$$

以及：

$$
\{1\}\subseteq A.
$$

数字 $1$ 是元素，而集合 $\{1\}$ 是子集。一般不能把 $1\in A$ 与 $1\subseteq A$ 当作同一件事。

## 并集

两个集合的并集包含所有属于 $A$ 或属于 $B$ 的元素：

$$
A\cup B
=\{x\mid x\in A\text{ 或 }x\in B\}.
$$

例如：

$$
\{1,2\}\cup\{2,3\}
=\{1,2,3\}.
$$

共同元素只出现一次。

## 交集

交集包含同时属于 $A$ 和 $B$ 的元素：

$$
A\cap B
=\{x\mid x\in A\text{ 且 }x\in B\}.
$$

例如：

$$
\{1,2\}\cap\{2,3\}
=\{2\}.
$$

若两个集合没有共同元素，则：

$$
A\cap B=\varnothing,
$$

称它们不相交。

## 差集

$A$ 减去 $B$ 的差集是属于 $A$ 但不属于 $B$ 的元素：

$$
A\setminus B
=\{x\mid x\in A\text{ 且 }x\notin B\}.
$$

例如：

$$
\{1,2,3\}\setminus\{2,4\}
=\{1,3\}.
$$

差集通常不满足交换律：

$$
A\setminus B\ne B\setminus A.
$$

## 补集

若先规定全集 $U$，集合 $A\subseteq U$ 的补集为：

$$
A^c=U\setminus A.
$$

补集依赖所选全集。若不说明全集，同一个 $A$ 的“外面”可能有不同含义。

## De Morgan 律

集合运算满足：

$$
(A\cup B)^c=A^c\cap B^c,
$$

$$
(A\cap B)^c=A^c\cup B^c.
$$

第一条表示“不属于 $A\cup B$”，也就是既不属于 $A$，也不属于 $B$；第二条表示“不同时属于 $A$ 和 $B$”，也就是至少不属于其中一个。

这些规律与逻辑中的“或、且、非”相对应。

## 笛卡尔积

集合 $A$ 与 $B$ 的笛卡尔积是所有有序对：

$$
A\times B
=\{(a,b)\mid a\in A,\ b\in B\}.
$$

例如：

$$
\{1,2\}\times\{x,y\}
=\{(1,x),(1,y),(2,x),(2,y)\}.
$$

有序对关心顺序，所以一般：

$$
A\times B\ne B\times A.
$$

平面 $\mathbb R^2$ 就是：

$$
\mathbb R\times\mathbb R.
$$

## 幂集

集合 $A$ 的所有子集组成的集合叫幂集：

$$
\mathcal P(A)=\{B\mid B\subseteq A\}.
$$

例如：

$$
A=\{1,2\}
$$

时：

$$
\mathcal P(A)
=\{\varnothing,\{1\},\{2\},\{1,2\}\}.
$$

若有限集合 $A$ 有 $n$ 个元素，则幂集有：

$$
2^n
$$

个元素，因为每个元素都有“选入”或“不选入”两种选择。

拓扑正是幂集中的一部分子集所组成的集合。

## 有限集与无限集

能与：

$$
\{1,2,\ldots,n\}
$$

建立一一对应的集合叫有限集，元素个数记作：

$$
|A|=n.
$$

自然数、整数、实数等是无限集。无限集合还可细分为可数无限和不可数无限。

集合无限不表示每个元素本身无限大；它只表示元素数量没有有限上限。

## 可数集

若集合有限，或能与自然数集合建立一一对应，就称可数。

整数集合虽然向正负两个方向无限延伸，仍是可数的。实数集合则不可数。

完整证明需要更深入的集合论；本篇只建立术语。

## 集合之间的函数

函数写成：

$$
f:A\to B.
$$

这里：

- $A$：定义域；
- $B$：陪域；
- 每个 $a\in A$ 都对应唯一的 $f(a)\in B$。

函数的像为：

$$
f(A)=\{f(a)\mid a\in A\}.
$$

它是陪域 $B$ 的子集，但不一定等于整个 $B$。

## 原像

若 $S\subseteq B$，其原像为：

$$
f^{-1}(S)
=\{a\in A\mid f(a)\in S\}.
$$

这里 $f^{-1}(S)$ 即使在函数没有反函数时也有定义。

拓扑中，连续函数可用“每个开集的原像仍为开集”定义，所以必须区分集合原像与反函数。

## 指标集合与集合族

一批集合可用指标 $\alpha$ 编号：

$$
\{A_\alpha\}_{\alpha\in I}.
$$

$I$ 叫指标集合。

这批集合的并可写成：

$$
\bigcup_{\alpha\in I}A_\alpha,
$$

交可写成：

$$
\bigcap_{\alpha\in I}A_\alpha.
$$

拓扑定义允许任意多个开集取并，但只保证有限多个开集取交仍开。

## 划分

集合 $X$ 的划分是一批非空子集，它们两两不相交，并且并集等于 $X$。

例如按奇偶性把整数分成：

$$
2\mathbb Z
$$

和：

$$
2\mathbb Z+1.
$$

等价关系会把集合分成互不重叠的等价类，从而形成划分。

## 等价关系

集合 $X$ 上的关系 $\sim$ 若满足：

1. 自反：$x\sim x$；
2. 对称：$x\sim y\Rightarrow y\sim x$；
3. 传递：$x\sim y$ 且 $y\sim z\Rightarrow x\sim z$；

就叫等价关系。

元素 $x$ 的等价类为：

$$
[x]=\{y\in X\mid y\sim x\}.
$$

## 商集

所有等价类组成的集合叫商集：

$$
X/{\sim}.
$$

商集把被视为等价的对象合并成一个新元素。

物理中规范等价常要求把多个数学描述看成同一物理状态；几何中粘合边界也常通过等价关系构造新空间。

## 集合与空间的区别

集合只说明有哪些元素。

“空间”通常是在集合上再加入额外结构，例如：

- 拓扑空间：加入开集结构；
- 度量空间：加入距离；
- 向量空间：加入加法和数乘；
- Hilbert 空间：加入内积和完备性；
- 群：加入满足群公理的运算。

因此，不能只凭元素相同就断定两个带结构空间完全相同。

## 集合与类的边界

朴素集合论若允许“所有满足任意条件的对象都组成集合”，会产生 Russell 悖论。

现代公理化集合论限制哪些对象集合可以合法构造。入门数学通常在已经给定的数集、函数集和子集内工作，不直接构造“所有集合的集合”。

这不是说日常集合记号不能用，而是提醒集合构造需处在一致的公理框架中。

## 在物理和弦理论中的作用

集合语言用于：

- 列出允许的状态、场配置和可观测量；
- 定义坐标图、开覆盖和流形；
- 把规范等价描述组成商空间；
- 用模空间表示几何结构的等价类；
- 按量子数把粒子态分成不同扇区；
- 描述 orbifold 对群作用的取商。

但物理理论不只是一个集合；还需要概率、内积、作用量、算符、拓扑或其他结构。

## 一个综合例子

设：

$$
A=\{1,2,3\},
\qquad
B=\{2,3,4\}.
$$

则：

$$
A\cup B=\{1,2,3,4\},
$$

$$
A\cap B=\{2,3\},
$$

$$
A\setminus B=\{1\},
$$

$$
B\setminus A=\{4\}.
$$

若定义 $f:A\to\mathbb R$ 为 $f(x)=x^2$，则：

$$
f(A)=\{1,4,9\}.
$$

对集合 $S=\{1,4\}$：

$$
f^{-1}(S)=\{1,2\}.
$$

## 适用条件与事实边界

1. 集合由其元素决定，列举顺序和重复次数不影响集合。
2. $x\in A$ 表示元素属于集合，$A\subseteq B$ 表示集合包含关系，两者不能混用。
3. 补集必须相对于某个全集定义。
4. 笛卡尔积中的有序对关心顺序。
5. 函数原像 $f^{-1}(S)$ 不要求函数存在反函数。
6. 商集需要先给出等价关系，不能任意把元素合并。
7. 集合本身不含距离、运算、拓扑或内积；这些都是额外结构。
8. “所有满足任意条件的对象”未必能无矛盾地组成集合；严格集合论采用公理化限制。
9. 本文使用标准朴素集合语言服务后续数学，不展开公理化集合论和基数理论的完整证明。

## 常见误区

1. **集合中元素的顺序重要。** 顺序不影响集合；它会影响序列或有序对。
2. **重复写一个元素会增加集合大小。** 同一元素只计算一次。
3. **空集与 $\{0\}$ 相同。** 空集没有元素，$\{0\}$ 有一个元素 $0$。
4. **$x\in A$ 与 $\{x\}\subseteq A$ 完全是同一记号。** 两者含义相关但对象层级不同。
5. **每个子集都是元素。** 子集是否也是原集合元素取决于具体集合，不能自动假设。
6. **函数像一定等于陪域。** 只有满射才覆盖整个陪域。
7. **$f^{-1}(S)$ 一定表示应用反函数。** 集合原像对任意函数都有定义。
8. **无限集中的元素都无限大。** 无限描述元素数量，不描述单个元素大小。
9. **集合与带结构空间没有区别。** 同一底层集合可承载不同拓扑、度量或运算。

## 练习

1. 说明 $\varnothing$ 与 $\{\varnothing\}$ 的区别。
2. 设 $A=\{1,2\}$、$B=\{2,3\}$，求并集、交集和两个方向的差集。
3. 写出 $\{a,b\}$ 的幂集。
4. 判断 $1\in\{1,2\}$ 与 $\{1\}\subseteq\{1,2\}$ 是否成立。
5. 为什么补集必须先指定全集？
6. 若 $f(x)=x^2$，求集合 $[-2,1]$ 的像。
7. 若 $f:\mathbb R\to\mathbb R$、$f(x)=x^2$，求 $[1,4]$ 的原像。
8. 集合与拓扑空间有什么区别？

## 答案与提示

1. 前者没有元素，后者有一个元素，该元素是空集。
2. 并集为 $\{1,2,3\}$，交集为 $\{2\}$，$A\setminus B=\{1\}$，$B\setminus A=\{3\}$。
3. $\{\varnothing,\{a\},\{b\},\{a,b\}\}$。
4. 两者都成立；前者是元素关系，后者是子集关系。
5. “不属于 $A$ 的对象”范围取决于讨论的总范围。
6. 像为 $[0,4]$。
7. 原像为 $[-2,-1]\cup[1,2]$。
8. 集合只给元素；拓扑空间还指定哪些子集叫开集。

## 与其他概念的关系

- [[区间]] 是实数集合的特殊子集；
- [[函数]] 是集合之间满足唯一输出条件的对应；
- [[向量空间]] 在集合上增加向量加法与标量乘法；
- [[群]] 在集合上增加满足群公理的运算；
- [[拓扑空间]] 在集合上指定一族开集；
- [[群作用]] 描述群怎样变换集合中的元素；
- [[模空间]] 常由几何结构按等价关系形成商集。

## 参考资料

- Paul R. Halmos, *Naive Set Theory*, Springer, 1974.
- Herbert B. Enderton, *Elements of Set Theory*, Academic Press, 1977.
- [OpenStax, Contemporary Mathematics, Set Theory](https://openstax.org/details/books/contemporary-mathematics)
- James R. Munkres, *Topology*, 2nd ed., Pearson, 2000.
- Sheldon Axler, *Linear Algebra Done Right*, 3rd ed., Springer, 2015.
