---
aliases: [工具调用, Function Calling, API Tool Use]
tags: [大模型, 智能体, 教程, 学习教程]
prerequisites: ["[[RAG 基础]]"]
next: ["[[智能体基础]]"]
---
# 工具调用

## 一句话定义

工具调用让模型输出结构化的工具名称和参数，由外部程序真正执行数据库查询、计算、搜索或控制操作，再把结果交回模型。

## 模型不直接执行

```text
用户：北京天气？
模型输出：{"tool":"weather","city":"北京"}
程序验证参数并调用天气 API
工具结果：{"temperature":28,"condition":"晴"}
模型根据结果回答
```

模型只提出调用请求；宿主程序决定是否允许并负责执行。

## 工具定义

通常包含名称、用途和 JSON Schema 参数：

```json
{"name":"calculator","parameters":{"type":"object","properties":{"expression":{"type":"string"}},"required":["expression"]}}
```

## 安全边界

- 参数必须按 schema 验证。
- 高风险操作需权限检查和人工确认。
- 工具结果视为不可信输入，防止提示注入。
- 使用最小权限、审计日志、超时和速率限制。
- 模型生成的 SQL、命令或代码不可直接无条件执行。

## 错误处理

工具可能失败、超时或返回空值。应把真实错误返回模型或用户，不能伪造成功结果。

## 概念与公式导航

- [[工具调用与执行权限|工具调用与执行权限]]

## 练习

1. 为计算器设计工具定义。
2. 为什么模型不能直接执行工具？
3. 哪些操作必须人工确认？
4. 工具返回网页内容时为何仍不可信？

## 来源

- OpenAI, *Function Calling Guide*：https://platform.openai.com/docs/guides/function-calling `[A]`
- Anthropic, *Tool Use*：https://docs.anthropic.com/en/docs/build-with-claude/tool-use `[A]`
- OWASP, *Top 10 for LLM Applications*：https://owasp.org/www-project-top-10-for-large-language-model-applications/ `[A]`