---
aliases: [数学基础练习答案]
tags: [练习答案, 数学, 大模型, 学习教程]
---
# 01-数学基础答案

## [[向量与矩阵|01-向量与矩阵]]

1. $[2,3,-1]+[4,0,5]=[6,3,4]$。
2. $3[1,-2,4]=[3,-6,12]$。
3. 转置为 $\begin{bmatrix}1&3\\0&-1\\2&4\end{bmatrix}$。原来的行变成列。
4. 向量是一列有顺序的数字，可表示一个对象的多个属性；矩阵是按行列排列的数字表，也可看成多个向量排在一起，或一个线性变换的参数。
5. 有 $3\times4=12$ 个数字；转置大小为 $4\times3$。

## [[点积与矩阵乘法|02-点积与矩阵乘法]]

1. $2\times4+(-1)\times0+3\times(-2)=2$。
2. $\begin{bmatrix}1&2\\3&4\end{bmatrix}\begin{bmatrix}5&6\\7&8\end{bmatrix}=\begin{bmatrix}19&22\\43&50\end{bmatrix}$。
3. 结果为 $[2,5]^T$：第一行得 $2$，第二行得 $6-1=5$。
4. 矩阵乘法的每个输出元素，都是左矩阵一行与右矩阵一列的点积；矩阵乘法可看成系统地同时做许多次点积。
5. 例如 $A=\begin{bmatrix}1&1\\0&1\end{bmatrix}$、$B=\begin{bmatrix}1&0\\1&1\end{bmatrix}$，则 $AB=\begin{bmatrix}2&1\\1&1\end{bmatrix}$，$BA=\begin{bmatrix}1&1\\1&2\end{bmatrix}$，二者不同。

## [[概率基础|03-概率基础]]

1. $3/(3+2)=3/5=0.6$。
2. 用补集：$1-P(\text{全反面})=1-1/4=3/4$。
3. $P(\text{数学})=18/30=0.6$；$P(\text{物理}\mid\text{数学})=8/18=4/9\approx0.444$。
4. 条件概率是在已知信息缩小样本范围后重新计算的概率。例如已知今天是周末，再问商场拥挤的概率，计算范围已经不是所有日期。
5. 模型需要概率来表示多个后续的相对可能性并据此训练、采样和评估。总选概率最高者是贪心解码，可能产生重复、僵硬文本，也不保证整段序列的联合概率最高。

## [[概率分布|04-概率分布]]

1. 合法。所有概率非负，且 $5\times0.1+0.5=1$。
2. 正态分布经验法则：在均值上下一个标准差内约 $68\%$；高于 $90$ 是高于均值两个标准差，单侧约 $2.3\%$（用“95% 在两倍标准差内”的粗略规则可答约 $2.5\%$）。
3. 共有 36 个等可能结果。和 $2$ 到 $12$ 的概率依次为 $1,2,3,4,5,6,5,4,3,2,1$ 除以 36。
4. PMF 直接给离散取值的概率，某一点可以有正概率；PDF 给连续变量单位区间附近的密度，单点概率通常为 0，区间概率等于密度曲线在该区间下的面积。
5. 可用贪心、按完整分布采样、Top-k、Top-p 等。每次选最大概率简单稳定，但可能重复和局部最优，不能保证整段最好。

## [[信息论基础|05-信息论基础]]

1. 以 2 为底：$H(A)=1$ bit；$H(B)=-0.9\log_2 0.9-0.1\log_2 0.1\approx0.469$ bit。A 更大，因为结果更不确定。
2. $H(P,Q)=-\log Q(x_1)=-\log0.7$。若用自然对数约 $0.357$ nat；若用以 2 为底约 $0.515$ bit。
3. 变大：$-\ln0.3\approx1.204>-\ln0.7$。
4. 目标 token 的预测概率越高，负对数损失越小；最小化平均交叉熵会推动模型把更多概率分给训练数据中真实出现的下一个 token。
5. 交叉熵为 0 意味着对所有计分样本都给正确结果概率 1。对有限训练集可能表示记忆或数据泄漏，不保证新数据表现；在真实有噪声语言数据上长期严格为 0 也应优先检查实现和评测口径。

## [[导数与梯度|06-导数与梯度]]

1. $f'(x)=6x+2$，所以 $f'(2)=14$。
2. $\partial f/\partial x=2xy+3y^2$；$\partial f/\partial y=x^2+6xy$。
3. $\nabla f=[2x,2y,2z]$，在 $(1,2,3)$ 处为 $[2,4,6]$。
4. $df/dx=3(x^2+1)^2\cdot2x=6x(x^2+1)^2$。
5. 梯度指向局部上升最快方向；做足够小的一步 $-\eta\nabla f$，一阶近似下函数值会下降约 $\eta\|\nabla f\|^2$。

## [[指数与对数|07-指数与对数]]

1. $e^0=1$，$\ln1=0$，$\ln e=1$。
2. $\ln(e^5)=5$，$e^{\ln3}=3$。
3. 为避免大数可减最大值：$e^{[0,-2,-3]}=[1,0.1353,0.0498]$，总和约 $1.1851$，softmax 约为 $[0.8438,0.1142,0.0420]$。
4. $-\ln0.01\approx4.6052$；$-\ln0.5\approx0.6931$；$-\ln0.99\approx0.01005$。
5. 指数恒为正、光滑、单调，并把“分数相差一个常数”转成概率比的乘法关系；归一化后容易求导。平方会丢失正负顺序，绝对值在 0 处不可导。指数不是唯一可能选择，但与多项分类的对数线性模型及交叉熵组合非常自然。

## [[高维空间直觉|08-高维空间直觉]]

1. $\|a-b\|=\sqrt{27}=3\sqrt3\approx5.196$。点积为 $32$，$\|a\|=\sqrt{14}$，$\|b\|=\sqrt{77}$，余弦相似度 $32/\sqrt{1078}\approx0.9746$。
2. 余弦相似度为 $1$；欧氏距离为 $99$。它们方向完全相同，所以余弦给 1；余弦没有说长度也相同。
3. 维度增加时空间体积、需要覆盖的样本数和距离行为会快速改变；有限数据会变得稀疏，搜索、估计和泛化可能更困难。这不是“高维一定坏”，而是需要更多数据、结构假设或合适度量。
4. 3 维通常承载能力不足，许多信息会被压到相同方向；100000 维增加参数、显存、计算和过拟合风险。4096 只是一些模型的工程折中，不是普适合理值；不同模型会依据规模、头数、训练预算和实验证据选择不同维度。
