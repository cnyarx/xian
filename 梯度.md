---
aliases: [Gradient]
english_name: Gradient
tags: [数学, 多变量微积分, 弦理论先修, 弦理论]
prerequisites:
  - "[[向量]]"
  - "[[偏导数]]"
used_by:
  - "[[散度]]"
  - "[[旋度]]"
  - "[[电势]]"
evidence_status: 教材事实
---

# 梯度（Gradient）

## 一句话解释

梯度把标量场在各坐标方向的变化率组成一个向量，它指向数值上升最快的方向。

## 为什么要学它

温度、电势和势能都是给每个位置分配一个数的标量场。梯度把这些数在空间中的变化转换成方向信息，例如静电场是电势的负梯度。

## 定义

对三维标量函数 $f(x,y,z)$：

$$
\nabla f=
\left(
\frac{\partial f}{\partial x},
\frac{\partial f}{\partial y},
\frac{\partial f}{\partial z}
\right).
$$

$\nabla$ 读作 nabla。每个分量是沿一个坐标轴的偏导数。

二维时：

$$
\nabla f=(f_x,f_y).
$$

## 计算例子

若：

$$
f(x,y)=x^2+3y^2,
$$

则：

$$
\nabla f=(2x,6y).
$$

在点 $(1,1)$：

$$
\nabla f(1,1)=(2,6).
$$

这表示该点附近朝 $(2,6)$ 方向上升最快。

## 方向导数

沿单位向量 $\hat{\mathbf u}$ 的方向导数为：

$$
D_{\hat{\mathbf u}}f=
\nabla f\cdot\hat{\mathbf u}.
$$

根据点积公式，它在 $\hat{\mathbf u}$ 与梯度同向时最大，最大值为 $|\nabla f|$；反向时下降最快。

## 等值面

$f(x,y,z)=c$ 表示一个等值面。沿等值面移动时函数值不变，所以切向方向导数为零。于是梯度与等值面垂直。

地图上的等高线就是二维例子：梯度垂直等高线并指向海拔升高最快处。

## 电势与力

静电场满足：

$$
\mathbf E=-\nabla V.
$$

负号表示电场指向电势下降最快的方向。

势能 $U$ 产生的保守力类似地满足：

$$
\mathbf F=-\nabla U.
$$

## 坐标依赖

上面的简单分量公式适用于 Cartesian 坐标。柱坐标、球坐标等曲线坐标中，基向量和尺度因子会随位置变化，梯度公式会出现额外系数。梯度作为几何对象不依赖坐标，但它的分量公式依赖坐标选择和度量。

## 常见误区

1. **梯度是一个数。** 错，它是向量。
2. **梯度沿等值面方向。** 错，它垂直等值面。
3. **梯度指向函数值最大的点。** 更准确地说，它指向当前位置的最快上升方向，不保证直接指向全局最大点。
4. **$\nabla f=0$ 必定是最大值。** 也可能是最小值或鞍点。
5. **任何坐标系都直接套 Cartesian 分量公式。** 错。

## 练习

1. 求 $f=x^2+y^2$ 的梯度。
2. 求第 1 题在 $(3,4)$ 的梯度和大小。
3. $f=x+y$ 沿单位向量 $(1,0)$ 的方向导数是多少？
4. 为什么梯度垂直等值线？
5. 电势向右增加时，电场大致朝哪个方向？

## 答案与提示

1. $(2x,2y)$。
2. $(6,8)$，大小 $10$。
3. $(1,1)\cdot(1,0)=1$。
4. 沿等值线函数不变，切向方向导数为零，因此切向量与梯度正交。
5. 向左，因为 $\mathbf E=-\nabla V$。

## 与其他概念的关系

- 梯度由 [[偏导数]] 组成；
- [[散度]] 和 [[旋度]] 作用于向量场；
- [[电势]] 的负梯度给出静电场；
- [[梯度下降|梯度下降]] 在模型参数空间中使用负梯度方向更新参数；梯度是局部微分对象，梯度下降则是调用它的迭代优化算法，不能把二者等同；
- 微分几何中梯度还依赖空间的度量；
- [[弦理论与大模型跨学科关系图谱]] 汇总梯度在两条学习路径中的共同数学含义和应用边界。

## 参考资料

- [OpenStax, Calculus Volume 3, Section 4.6](https://openstax.org/books/calculus-volume-3/pages/4-6-directional-derivatives-and-the-gradient)
- [MIT OCW 18.02SC, Multivariable Calculus](https://ocw.mit.edu/courses/18-02sc-multivariable-calculus-fall-2010/)
