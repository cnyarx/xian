---
aliases:
  - Inverse Function
  - Inverse Functions
english_name: Inverse Function
tags:
  - 数学
  - 高中基础
  - 弦理论先修
  - 弦理论
prerequisites:
  - "[[函数]]"
  - "[[复合函数]]"
used_by:
  - "[[对数函数]]"
  - "[[坐标变换]]"
evidence_status: 教材事实
---

# 反函数（Inverse Function）

## 一句话解释

反函数把原函数的输出还原成输入，相当于撤销原函数完成的变换。

## 为什么要学它

若已知距离由时间怎样决定，可能反过来想求达到某个距离所需的时间；若指数函数把指数变成一个正数，对数函数就把这个正数还原成指数。坐标变换也必须能够在不同坐标之间来回转换。

## 正式定义

若函数 $f:A\to B$ 存在函数 $f^{-1}:B\to A$，使得：

$$
f^{-1}(f(x))=x
$$

并且

$$
f(f^{-1}(y))=y,
$$

那么 $f^{-1}$ 叫 $f$ 的反函数。

两条式子表示：

- 先做 $f$ 再做 $f^{-1}$，回到原输入；
- 先做 $f^{-1}$ 再做 $f$，也回到原输入。

用 [[复合函数]] 写成：

$$
f^{-1}\circ f=\operatorname{id}_A,
\qquad
f\circ f^{-1}=\operatorname{id}_B.
$$

## 一个简单例子

设：

$$
f(x)=2x+3.
$$

要从输出 $y$ 找回输入 $x$，先解：

$$
y=2x+3.
$$

得到：

$$
x=\frac{y-3}{2}.
$$

所以：

$$
f^{-1}(x)=\frac{x-3}{2}.
$$

检验：

$$
f^{-1}(f(x))
=\frac{(2x+3)-3}{2}
=x.
$$

## 不是每个函数都有反函数

考虑：

$$
f(x)=x^2,
\qquad x\in\mathbb R.
$$

因为 $f(2)=f(-2)=4$，从输出 $4$ 无法判断原输入是 $2$ 还是 $-2$。所以它在全部实数上没有反函数。

如果把定义域限制为 $[0,\infty)$，每个输出只对应一个非负输入，此时反函数是：

$$
f^{-1}(x)=\sqrt{x}.
$$

## 一一对应

函数要有覆盖整个陪域的反函数，需要满足：

1. 不同输入得到不同输出，这叫单射；
2. 陪域中的每个元素都能由某个输入得到，这叫满射。

既是单射又是满射，叫双射。双射才能在给定定义域与陪域之间建立完整反函数。

## 图像关系

函数 $f$ 与反函数 $f^{-1}$ 的图像关于直线

$$
y=x
$$

镜面对称。原因是原图像上的点 $(a,b)$ 经过输入输出交换后，成为反函数图像上的点 $(b,a)$。

## $f^{-1}$ 不是倒数

$$
f^{-1}(x)
$$

通常表示反函数，而

$$
\frac1{f(x)}
$$

表示函数值的倒数。两者不是一回事。

例如 $f(x)=2x$ 时：

$$
f^{-1}(x)=\frac x2,
$$

但

$$
\frac1{f(x)}=\frac1{2x}.
$$

## 对数是指数的反函数

若 $a>0$ 且 $a\ne1$，指数函数

$$
f(x)=a^x
$$

把所有实数映射到正实数。它的反函数就是 [[对数函数]]：

$$
f^{-1}(x)=\log_a x.
$$

因此：

$$
\log_a(a^x)=x,
\qquad
a^{\log_a x}=x\quad(x>0).
$$

## 常见误区

1. **每个函数都有反函数。** 错；若多个输入得到同一输出，就无法唯一还原。
2. **$f^{-1}(x)=1/f(x)$。** 错；前者是撤销映射，后者是数值倒数。
3. **交换 $x,y$ 就一定得到合法反函数。** 错；还要检查是否是函数以及定义域、值域是否匹配。
4. **$x^2$ 永远没有反函数。** 不准确；限制到非负实数后有反函数 $\sqrt{x}$。
5. **只检验一个方向的复合就总是足够。** 完整定义要求两个方向都得到相应恒等函数。

## 练习

1. 求 $f(x)=3x-6$ 的反函数。
2. 检验第 1 题的两个复合。
3. 解释为什么 $f(x)=|x|$ 在全部实数上没有反函数。
4. 把 $f(x)=x^2$ 的定义域限制在哪个常见区间后，可取 $\sqrt{x}$ 为反函数？
5. 比较 $f^{-1}(x)$ 和 $1/f(x)$ 的意义。

## 答案与提示

1. $f^{-1}(x)=(x+6)/3$。
2. 代入后 $f^{-1}(f(x))=x$ 且 $f(f^{-1}(x))=x$。
3. 正负两个输入可能有相同绝对值，例如 $|-2|=|2|$。
4. $[0,\infty)$；也可以限制到 $(-\infty,0]$，但对应反函数为 $-\sqrt{x}$。
5. 反函数恢复输入；倒数只是用 $1$ 除以函数值。

## 与其他概念的关系

- 反函数通过 [[复合函数]] 定义；
- [[对数函数]] 是指数函数的反函数；
- 可逆 [[坐标变换]] 依靠反函数在坐标系统之间往返；
- 线性代数中的逆矩阵是同一“撤销变换”思想的矩阵版本。

## 参考资料

- [OpenStax, Precalculus 2e, Section 1.5](https://openstax.org/books/precalculus-2e/pages/1-5-transformation-of-functions)
- [OpenStax, Algebra and Trigonometry 2e, Section 3.7](https://openstax.org/books/algebra-and-trigonometry-2e/pages/3-7-inverse-functions)
