---
aliases: [Probability, Probability Theory Basics]
english_name: Probability
tags: [物理学, 数学基础, 量子力学先修, 弦理论先修, 弦理论]
prerequisites:
  - "[[实数]]"
  - "[[区间]]"
  - "[[函数]]"
  - "[[积分]]"
used_by:
  - "[[量子态]]"
  - "[[Born 规则]]"
  - "[[期望值]]"
  - "[[量子测量]]"
evidence_status: 教材事实
---

# 概率（Probability）

## 一句话解释

概率用 $0$ 到 $1$ 之间的数表示某个结果在重复试验中出现的可能程度，并给出组合、条件化和平均结果的统一计算规则。

## 为什么要学它

量子理论不只预测一个确定结果，而是预测各种测量结果的概率。Born 规则把复概率振幅转换成概率，期望值和不确定度则用概率分布描述重复实验统计。

理解普通概率后，才能准确区分经典概率混合与具有相对相位、能够干涉的量子叠加。

## 随机试验与样本空间

一个结果不能在现有信息下被预先唯一确定的过程，可建模为随机试验。例如掷一枚普通六面骰子。

所有可能基本结果组成样本空间：

$$
\Omega=\{1,2,3,4,5,6\}.
$$

事件（event）是样本空间的一个子集合。例如“掷出偶数”对应：

$$
A=\{2,4,6\}.
$$

事件发生，就是实际结果属于该集合。

## 概率的基本范围

事件 $A$ 的概率写成 $P(A)$，满足：

$$
0\le P(A)\le1.
$$

其中：

- $P(A)=0$：事件概率为零；
- $P(A)=1$：事件几乎必然发生；
- 中间数值：表示不同可能程度。

在连续概率中，概率为零不总等于逻辑上绝不可能。例如从区间 $[0,1]$ 均匀取实数，恰好取到某个指定单点的概率为零，但该点仍属于样本空间。

## Kolmogorov 三条公理

现代概率论常从三条公理出发：

1. 非负性：

$$
P(A)\ge0.
$$

2. 整个样本空间概率为一：

$$
P(\Omega)=1.
$$

3. 对两两互斥事件 $A_1,A_2,\ldots$：

$$
P\left(\bigcup_{n=1}^{\infty}A_n\right)
=
\sum_{n=1}^{\infty}P(A_n).
$$

“互斥”表示这些事件不能同时发生。第三条称为可列可加性。

## 补事件

$A$ 不发生的事件记为 $A^c$。因为 $A$ 与 $A^c$ 互斥，且二者合起来覆盖整个样本空间：

$$
P(A^c)=1-P(A).
$$

例如，普通骰子掷出偶数的概率为 $1/2$，掷出非偶数的概率也是：

$$
1-\frac12=\frac12.
$$

## 加法公式

两个事件至少一个发生的概率是：

$$
P(A\cup B)
=
P(A)+P(B)-P(A\cap B).
$$

最后减去交集，是因为同时属于 $A$ 和 $B$ 的结果在前两项中被数了两遍。

若 $A$ 和 $B$ 互斥，则 $P(A\cap B)=0$，公式化为：

$$
P(A\cup B)=P(A)+P(B).
$$

量子振幅的相加规则与这条经典概率规则不同；不可区分量子路径可能先加复振幅，再取绝对值平方，从而产生干涉项。

## 条件概率

已知事件 $B$ 发生后，事件 $A$ 的条件概率定义为：

$$
P(A|B)
=
\frac{P(A\cap B)}{P(B)},
\qquad P(B)>0.
$$

它表示把注意范围缩小到 $B$ 内，再看其中有多少属于 $A$。

公式可改写为乘法规则：

$$
P(A\cap B)=P(A|B)P(B).
$$

## 独立事件

若知道 $B$ 是否发生不改变 $A$ 的概率，则称二者独立：

$$
P(A|B)=P(A).
$$

等价地，在概率非退化的常见情形中：

$$
P(A\cap B)=P(A)P(B).
$$

独立不等于互斥。两个非零概率事件若互斥，知道一个发生就能确定另一个没有发生，因此通常不独立。

例如连续掷两次公平硬币，两次结果可建模为独立；但同一次掷硬币中的“正面”和“反面”互斥而不独立。

## Bayes 公式

由条件概率的两个方向：

$$
P(A|B)P(B)
=
P(B|A)P(A),
$$

得到 Bayes 公式：

$$
P(A|B)
=
\frac{P(B|A)P(A)}{P(B)},
\qquad P(B)>0.
$$

这里：

- $P(A)$：看到新证据前的先验概率；
- $P(B|A)$：假设 $A$ 成立时看到证据 $B$ 的概率；
- $P(A|B)$：看到证据后的后验概率。

Bayes 公式是普通概率恒等式；如何选择先验属于具体建模问题，不能由公式本身自动决定。

## 随机变量

随机变量 $X$ 是把样本空间结果映射成数的函数：

$$
X:\Omega\to\mathbb R.
$$

例如掷骰子时，$X$ 可以直接等于点数；也可定义 $X=1$ 表示偶数、$X=0$ 表示奇数。

随机变量不是“自己随意变化的神秘变量”，而是用数值记录随机试验结果的函数。

## 离散概率分布

离散随机变量可能取 $x_1,x_2,\ldots$，其概率质量函数为：

$$
p_i=P(X=x_i).
$$

满足：

$$
p_i\ge0,
\qquad
\sum_i p_i=1.
$$

公平六面骰子中：

$$
P(X=k)=\frac16,
\qquad k=1,\ldots,6.
$$

## 连续概率密度

连续随机变量常用概率密度 $p(x)$ 描述：

$$
P(a\le X\le b)
=
\int_a^b p(x)\,dx.
$$

概率密度满足：

$$
p(x)\ge0,
\qquad
\int_{-\infty}^{\infty}p(x)\,dx=1.
$$

$p(x)$ 本身通常不是“恰好取到 $x$ 的概率”。单点概率在普通连续分布中为零；真正的概率来自区间积分。

密度可以大于 $1$，只要在整个范围的积分为 $1$。密度具有“每单位 $x$”的量纲，使 $p(x)dx$ 无量纲。

## 期望值

离散随机变量的期望值是：

$$
\mathbb E[X]
=
\sum_i x_i p_i.
$$

连续随机变量的期望值是：

$$
\mathbb E[X]
=
\int_{-\infty}^{\infty}x p(x)\,dx,
$$

前提是相应和或积分收敛。

期望值是大量重复试验结果平均值趋近的理论值，不一定是随机变量一次可能取到的值。例如公平骰子的期望值是：

$$
\frac{1+2+3+4+5+6}{6}=3.5,
$$

但一次掷骰子不会得到 $3.5$ 点。

## 方差与标准差

方差衡量结果围绕平均值的分散程度：

$$
\operatorname{Var}(X)
=
\mathbb E\left[(X-\mathbb E[X])^2\right].
$$

也可写成：

$$
\operatorname{Var}(X)
=
\mathbb E[X^2]-\mathbb E[X]^2.
$$

标准差是：

$$
\sigma_X=\sqrt{\operatorname{Var}(X)}.
$$

标准差与 $X$ 具有相同量纲，更容易和原始数值比较。

量子力学中，可观测量 $A$ 的不确定度：

$$
\Delta A
=
\sqrt{\langle A^2\rangle-\langle A\rangle^2}
$$

正是概率分布标准差的量子版本。

## 联合概率与边缘概率

两个随机变量 $X,Y$ 的联合分布描述它们同时取值的概率。离散情况下写成：

$$
p(x,y)=P(X=x,Y=y).
$$

只关心 $X$ 时，对所有 $Y$ 求和得到边缘分布：

$$
p_X(x)=\sum_y p(x,y).
$$

连续情况下把求和换成积分：

$$
p_X(x)=\int p(x,y)dy.
$$

这与量子复合系统中“忽略一个子系统”有相似的数学方向，但量子部分迹还包含振幅、密度算符和纠缠结构，不能简单等同于普通经典边缘化。

## 大数定律的直观意义

若在合适条件下独立重复同一种试验，样本平均值会随着次数增加而趋近理论期望值。

例如公平硬币正面概率为 $1/2$，掷的次数越来越多时，正面所占比例通常越来越接近 $1/2$。

这不表示短序列必须非常接近理论概率，也不表示之后的结果会主动“补偿”之前的偏差。每次独立试验的概率规则不因过去结果而产生赌徒谬误式修正。

## 频率解释与概率模型

重复实验中的相对频率为：

$$
f_N(A)=
\frac{\text{前 }N\text{ 次中 }A\text{ 发生次数}}{N}.
$$

在适当独立同分布条件下，大数定律说明 $f_N(A)$ 会以概率意义趋近 $P(A)$。

但数学概率不只是有限次数观察频率。它是一套模型规则；模型是否适合现实，需要由实验设计、数据和假设检验支持。

## 量子概率

若归一化量子态在正交测量基中展开：

$$
|\psi\rangle=
\sum_n c_n|n\rangle,
$$

Born 规则给出：

$$
P(n)=|c_n|^2.
$$

因为：

$$
\sum_n|c_n|^2=1,
$$

这些数满足普通概率的非负性和归一化要求。

量子理论特殊之处不在于最后得到的概率违反概率公理，而在于概率来自复振幅及 Hilbert 空间投影；不可区分过程的振幅能发生干涉。

## 量子叠加与经典混合

考虑两个基状态 $|0\rangle,|1\rangle$。

相干叠加：

$$
|+\rangle
=
\frac{|0\rangle+|1\rangle}{\sqrt2}
$$

与经典混合：以 $1/2$ 概率制备 $|0\rangle$、以 $1/2$ 概率制备 $|1\rangle$，在 $\{|0\rangle,|1\rangle\}$ 基测量时都给出各 $1/2$。

但换到包含 $|+\rangle$ 的基测量，相干叠加会确定得到 $|+\rangle$，经典混合仍表现为随机。这说明只比较一个测量基的概率不足以判断状态是否相同。

## 具体例子：有误报的检测

假设某种罕见事件 $A$ 的先验概率为：

$$
P(A)=0.01.
$$

检测在事件发生时有 $90\%$ 概率呈阳性：

$$
P(+|A)=0.9,
$$

事件未发生时有 $5\%$ 误报率：

$$
P(+|A^c)=0.05.
$$

总阳性概率是：

$$
P(+)
=0.9\times0.01
+0.05\times0.99
=0.0585.
$$

Bayes 公式给出：

$$
P(A|+)
=
\frac{0.9\times0.01}{0.0585}
\approx0.154.
$$

所以即使检测阳性，事件发生的后验概率约为 $15.4\%$，而不是 $90\%$。$90\%$ 是 $P(+|A)$，不能与反方向的 $P(A|+)$ 混淆。

## 适用条件与事实边界

1. 概率数值依赖样本空间和模型假设；更换问题定义可能改变概率。
2. “随机”可能来自不可控信息，也可能在标准量子理论中属于测量结果的基本概率结构；概率公式本身不裁决全部哲学解释。
3. 独立性必须由模型或实验依据支持，不能因为两个事件名字不同就假设独立。
4. 期望值存在需要相应求和或积分收敛；某些重尾分布没有有限期望或方差。
5. 量子概率遵守普通概率公理，但振幅组合方式不能用经典互斥概率规则替代。

## 常见误区

1. **概率为零就等于逻辑上绝不可能。** 在连续分布中，单点可有零概率却仍属于样本空间。
2. **概率密度必须小于等于一。** 错，密度可以大于一；区间积分才必须在 $0$ 到 $1$ 之间。
3. **互斥等于独立。** 非零概率互斥事件通常不独立。
4. **$P(A|B)=P(B|A)$。** 一般不相等，二者通过 Bayes 公式联系。
5. **期望值是一次试验最可能出现的结果。** 它是加权平均，甚至可能不是任何一次可取值。
6. **连续做出多个相同结果后，下次会更倾向相反结果来补平。** 对独立试验这是赌徒谬误。
7. **量子概率只是普通未知经典状态的概率。** 相干相位和干涉说明一般量子态不能等同于经典混合。

## 练习

1. 公平骰子掷出大于 $4$ 的概率是多少？其补事件概率是多少？
2. 若 $P(A)=0.4$、$P(B)=0.5$、$P(A\cap B)=0.2$，求 $P(A\cup B)$。
3. 若两个事件独立，$P(A)=0.3$、$P(B)=0.2$，求 $P(A\cap B)$。
4. 公平骰子点数的期望值是多少？为什么一次不会掷出该值？
5. 匀速分布在 $[0,2]$ 的概率密度应是多少？检查归一化。
6. 解释为什么相干态 $(|0\rangle+|1\rangle)/\sqrt2$ 与各占一半的经典混合不相同。
7. 在检测例子中，为什么阳性后的真实事件概率远小于检测灵敏度 $90\%$？

## 答案与提示

1. 结果为 $5,6$，所以概率 $2/6=1/3$；补事件概率为 $2/3$。
2. $0.4+0.5-0.2=0.7$。
3. $0.3\times0.2=0.06$。
4. $(1+2+3+4+5+6)/6=3.5$；期望是长期加权平均，不必属于单次可能结果集合。
5. 常数密度 $p(x)=1/2$；$\int_0^2(1/2)dx=1$。
6. 相干态保留相对相位，在其他测量基中产生确定干涉；经典混合没有该相位信息。
7. 因为事件先验概率只有 $1\%$，大量未发生事件中的少量误报仍会构成多数阳性结果。

## 与其他概念的关系

- [[量子态]] 用复振幅编码测量概率结构；
- [[Born 规则]] 把内积振幅的绝对值平方解释为概率；
- [[期望值]] 是结果按概率加权的平均；
- [[不确定性原理]] 中的 $\Delta A$ 是可观测量概率分布的标准差；
- [[量子叠加]] 与经典概率混合的区别来自相对相位和干涉；
- [[概率基础|概率基础]] 使用经典条件概率、期望和分布描述数据与模型输出；本篇还讨论 Born 规则把复振幅映射为测量概率。两者共享概率公理与统计语言，但复振幅、相位和干涉是量子理论特有结构。

## 参考资料

- Andrey Kolmogorov, *Foundations of the Theory of Probability*.
- Sheldon Ross, *A First Course in Probability*, Pearson.
- [OpenStax, Introductory Statistics](https://openstax.org/details/books/introductory-statistics-2e)
- [MIT OpenCourseWare 18.05, Introduction to Probability and Statistics](https://ocw.mit.edu/courses/18-05-introduction-to-probability-and-statistics-spring-2022/)
- Barton Zwiebach, *Mastering Quantum Mechanics*, MIT Press, 2022.
- J. J. Sakurai and Jim Napolitano, *Modern Quantum Mechanics*, 3rd ed., Cambridge University Press, 2020.
