---
aliases:
  - Curvature
  - Riemann Curvature
  - Riemann Curvature Tensor
english_name: Curvature
tags:
  - 数学
  - 微分几何
  - 广义相对论
  - 弦理论先修
  - 弦理论
prerequisites:
  - "[[联络]]"
  - "[[协变导数]]"
  - "[[联络如何定义协变导数]]"
  - "[[向量场]]"
  - "[[度量]]"
  - "[[张量积]]"
used_by:
  - "[[微分几何]]"
  - "[[黎曼曲面]]"
  - "[[截面曲率]]"
  - "[[Ricci 曲率]]"
  - "[[标量曲率]]"
  - "[[Bianchi 恒等式]]"
  - "[[Einstein 方程]]"
  - "[[世界面曲率]]"
  - "[[背景场方程]]"
  - "[[beta 函数]]"
  - "[[Yang-Mills 理论]]"
  - "[[Wilson 线与 Wilson 圈]]"
evidence_status: 教材事实
---

# 曲率（Curvature）

## 一句话解释

曲率衡量联络沿不同方向比较向量时有多么不一致；沿一个很小的闭合回路平行移动向量后产生的净变化，在最低阶由曲率控制。

## 为什么要学它

“空间弯了”只是直觉，真正可计算的几何量必须满足坐标无关性。

曲率把弯曲写成张量，使我们能够回答：

- 一个空间是否局部平直；
- 沿两种顺序做协变导数为什么会不同；
- 平行移动为什么依赖路径；
- 邻近测地线会聚还是发散；
- 球面、平面和双曲面的弯曲有何区别；
- 广义相对论怎样把时空几何与物质联系起来；
- 弦的世界面和目标时空背景如何进入作用量、量子反常与背景场方程。

## 直观图景

在平面上画一个很小的矩形。把一支箭头沿矩形四边平行移动一圈，它回到起点时方向不变。

在球面上做类似操作，箭头回到起点时通常已经旋转。旋转量不仅与回路面积有关，也与回路所在位置和箭头方向有关。

当回路缩得很小时，“净旋转除以有向面积”的最低阶极限由曲率张量描述。

这个图景适合有度量的 Levi-Civita 联络，但曲率的定义本身只需要联络，不必先有度量。

## 先区分几种曲率

“曲率”不是永远只指一个数字。

1. **曲线曲率**：描述一条曲线在环境空间里转弯多快；
2. **联络曲率**：描述协变导数或平行移动的不对易；
3. **Riemann 曲率张量**：切丛联络的曲率张量；
4. **截面曲率**：给每个二维切平面一个数；
5. **Ricci 曲率**：对 Riemann 曲率做一次缩并；
6. **标量曲率**：再做一次缩并得到标量；
7. **规范场强**：主丛或关联向量丛联络的曲率。

本篇主线是切丛联络的曲率，并说明它怎样产生后几种几何量。

## 曲率算符的正式定义

给定流形 $M$ 上的联络 $\nabla$，对向量场 $X,Y,Z$ 定义：

$$
\boxed{
R(X,Y)Z
=\nabla_X\nabla_YZ
-\nabla_Y\nabla_XZ
-\nabla_{[X,Y]}Z
}.
$$

其中 $[X,Y]$ 是 Lie 括号。

固定 $X,Y$ 后，$R(X,Y)$ 是作用在向量场 $Z$ 上的线性算符。

## 每个符号是什么意思

- $M$：光滑流形；
- $X,Y$：指定两个求导方向的向量场；
- $Z$：被曲率算符作用的向量场；
- $\nabla_X$：沿 $X$ 的协变导数；
- $[X,Y]$：Lie 括号，测量两个方向流动的不对易；
- $R(X,Y)Z$：先后协变求导顺序之差，扣除方向本身不对易后的剩余；
- $R^\rho{}_{\sigma\mu\nu}$：曲率在坐标基中的分量。

本文采用爱因斯坦求和约定：同一项中重复出现的一上一下指标表示求和。

## 为什么必须减去 Lie 括号项

若只写：

$$
\nabla_X\nabla_YZ-\nabla_Y\nabla_XZ,
$$

结果不仅包含联络的弯曲，还包含方向场 $X,Y$ 自身不对易造成的影响。

Lie 括号：

$$
[X,Y]
$$

正好记录这种方向不对易。减去：

$$
\nabla_{[X,Y]}Z
$$

后，剩余量才对 $X,Y,Z$ 在每一点的取值都关于光滑函数线性，从而成为张量。

在坐标基中：

$$
[\partial_\mu,\partial_\nu]=0,
$$

所以该项暂时看不见，但在一般非坐标标架中不能省略。

## 曲率为什么是张量

协变导数本身不是对被求导向量场的点值线性运算。例如：

$$
\nabla_X(fZ)=X[f]Z+f\nabla_XZ.
$$

把曲率定义中的三项全部展开时，所有含 $X[f]$、$Y[f]$ 和 Lie 括号乘积法则的导数项会互相抵消，最后得到：

$$
R(fX,Y)Z=fR(X,Y)Z,
$$

$$
R(X,fY)Z=fR(X,Y)Z,
$$

$$
R(X,Y)(fZ)=fR(X,Y)Z.
$$

因此 $R(X,Y)Z$ 在一点只依赖 $X,Y,Z$ 在该点的值，不依赖它们在远处怎样延伸。

把输出也放入张量槽位后，曲率可看成一个 $(1,3)$ 型张量。

## 最简单的反对称性

由定义直接交换 $X,Y$：

$$
R(Y,X)Z=-R(X,Y)Z.
$$

所以曲率对前两个方向反对称。在分量约定：

$$
R(\partial_\mu,\partial_\nu)\partial_\sigma
=R^\rho{}_{\sigma\mu\nu}\partial_\rho
$$

下，有：

$$
R^\rho{}_{\sigma\mu\nu}
=-R^\rho{}_{\sigma\nu\mu}.
$$

## 坐标分量的定义

在坐标基 $\partial_\mu$ 中定义：

$$
R(\partial_\mu,\partial_\nu)\partial_\sigma
=R^\rho{}_{\sigma\mu\nu}\partial_\rho.
$$

联络系数满足：

$$
\nabla_{\partial_\mu}\partial_\nu
=\Gamma^\rho{}_{\mu\nu}\partial_\rho.
$$

这里的指标顺序必须与所用教材约定一起核对。本文始终使用上述两条定义。

## 从联络系数推导曲率分量

坐标基的 Lie 括号为零，所以：

$$
R(\partial_\mu,\partial_\nu)\partial_\sigma
=\nabla_\mu\nabla_\nu\partial_\sigma
-\nabla_\nu\nabla_\mu\partial_\sigma.
$$

先算第一项：

$$
\nabla_\nu\partial_\sigma
=\Gamma^\lambda{}_{\nu\sigma}\partial_\lambda.
$$

再沿 $\partial_\mu$ 求导：

$$
\begin{aligned}
\nabla_\mu\nabla_\nu\partial_\sigma
&=\nabla_\mu
\left(\Gamma^\lambda{}_{\nu\sigma}\partial_\lambda\right)\\
&=\partial_\mu\Gamma^\lambda{}_{\nu\sigma}\,\partial_\lambda
+\Gamma^\lambda{}_{\nu\sigma}
\Gamma^\rho{}_{\mu\lambda}\partial_\rho.
\end{aligned}
$$

将第一项的哑指标 $\lambda$ 改名为 $\rho$：

$$
\nabla_\mu\nabla_\nu\partial_\sigma
=\left(
\partial_\mu\Gamma^\rho{}_{\nu\sigma}
+\Gamma^\lambda{}_{\nu\sigma}
\Gamma^\rho{}_{\mu\lambda}
\right)\partial_\rho.
$$

交换 $\mu,\nu$ 后相减，得到：

$$
\boxed{
R^\rho{}_{\sigma\mu\nu}
=\partial_\mu\Gamma^\rho{}_{\nu\sigma}
-\partial_\nu\Gamma^\rho{}_{\mu\sigma}
+\Gamma^\rho{}_{\mu\lambda}
\Gamma^\lambda{}_{\nu\sigma}
-\Gamma^\rho{}_{\nu\lambda}
\Gamma^\lambda{}_{\mu\sigma}
}.
$$

这个公式包含：

- 联络系数的一阶导数；
- 联络系数的二次项。

## 为什么不同教材的符号可能相反

有些教材把曲率定义为：

$$
R_{\mathrm{alt}}(X,Y)Z
=\nabla_Y\nabla_XZ
-\nabla_X\nabla_YZ
+\nabla_{[X,Y]}Z.
$$

于是：

$$
R_{\mathrm{alt}}=-R.
$$

还有资料对分量指标的排列采用不同次序。因此只记分量公式而不核对抽象定义，容易把整套符号反过来。

物理结论不靠某个孤立符号决定；同一计算必须从头到尾使用一致约定。

## 对一般向量场的作用

若：

$$
Z=Z^\sigma\partial_\sigma,
$$

由张量性可得：

$$
(R(\partial_\mu,\partial_\nu)Z)^\rho
=R^\rho{}_{\sigma\mu\nu}Z^\sigma.
$$

无挠坐标基下，常写成：

$$
(\nabla_\mu\nabla_\nu-\nabla_\nu\nabla_\mu)Z^\rho
=R^\rho{}_{\sigma\mu\nu}Z^\sigma,
$$

但这里二阶协变导数的指标约定必须与曲率定义一致。

若联络有挠率，直接交换分量协变导数时通常还会出现挠率乘一阶协变导数的项。使用前必须明确所定义的对易算符。

## 平行移动怎样看见曲率

取点 $p$ 附近两个很小方向位移：

$$
\varepsilon X,
\qquad
\delta Y.
$$

把向量 $Z_p$ 沿四条边平行移动一圈。若回路由 $X,Y$ 生成，并忽略更高阶小量，则净变化具有结构：

$$
\Delta Z
\sim \varepsilon\delta\,R(X,Y)Z.
$$

比例的整体正负号取决于回路方向和曲率约定。

关键事实是：

- 回路边长的一阶效应互相抵消；
- 最先留下的是面积阶 $\varepsilon\delta$；
- 面积阶系数由曲率给出。

这就是“曲率是平行移动的无穷小路径依赖”的含义。

## 曲率为零是什么意思

若一个联络在某开区域满足：

$$
R=0,
$$

则称该联络在该区域平直。

在单连通且满足适当正则条件的局部区域，平直联络可找到平行标架，使联络系数在该标架中消失。

但需要区分：

- **某一点 $\Gamma=0$**：可由选择适当坐标或标架实现，不足以推出曲率为零；
- **某开区域 $R=0$**：坐标无关的局部平直条件；
- **全局平行移动无绕行效应**：还受空间拓扑与 holonomy 影响。

因此局部曲率为零不自动消除所有全局效应。

## 平面极坐标为何是重要反例

Euclidean 平面的极坐标度量为：

$$
ds^2=dr^2+r^2d\theta^2.
$$

非零联络系数包括：

$$
\Gamma^r{}_{\theta\theta}=-r,
\qquad
\Gamma^\theta{}_{r\theta}
=\Gamma^\theta{}_{\theta r}
=\frac1r.
$$

尽管 $\Gamma\ne0$，代入曲率公式后所有分量仍为零。

这说明联络系数中的位置变化可以只是坐标基变化，而不是真正的几何弯曲。

## 极坐标中直接算一个曲率分量

计算：

$$
R^r{}_{\theta r\theta}.
$$

按本文约定：

$$
\begin{aligned}
R^r{}_{\theta r\theta}
&=\partial_r\Gamma^r{}_{\theta\theta}
-\partial_\theta\Gamma^r{}_{r\theta}\\
&\quad+\Gamma^r{}_{r\lambda}
\Gamma^\lambda{}_{\theta\theta}
-\Gamma^r{}_{\theta\lambda}
\Gamma^\lambda{}_{r\theta}.
\end{aligned}
$$

各项为：

$$
\partial_r(-r)=-1,
$$

$$
\partial_\theta\Gamma^r{}_{r\theta}=0,
$$

$$
\Gamma^r{}_{r\lambda}
\Gamma^\lambda{}_{\theta\theta}=0,
$$

而最后只有 $\lambda=\theta$ 有贡献：

$$
-\Gamma^r{}_{\theta\theta}
\Gamma^\theta{}_{r\theta}
=-(-r)\frac1r=+1.
$$

因此：

$$
R^r{}_{\theta r\theta}
=-1+1=0.
$$

## 二维球面的正曲率

半径为 $a$ 的二维球面度量为：

$$
ds^2=a^2d\theta^2
+a^2\sin^2\theta\,d\phi^2.
$$

它的 Gaussian 曲率为常数：

$$
K=\frac1{a^2}.
$$

半径越大，球面局部越接近平面，曲率越小。

球面三角形的内角和大于 $\pi$。对足够规则的球面区域，超出的角度与区域面积成正比，这与正曲率一致。

## 双曲面的负曲率

标准双曲平面具有常负 Gaussian 曲率。若归一化为：

$$
K=-\frac1{a^2},
$$

则测地线比 Euclidean 平面中的直线更快分开，三角形内角和小于 $\pi$。

“球面向内收拢、双曲面向外张开”是帮助理解的类比，不是曲率定义。

## 截面曲率

在 Riemannian 流形的一点 $p$，选二维切平面：

$$
\Pi=\operatorname{span}\{u,v\}.
$$

截面曲率定义为：

$$
K(\Pi)
=\frac{g(R(u,v)v,u)}
{g(u,u)g(v,v)-g(u,v)^2}.
$$

分母是由 $u,v$ 张成的平行四边形面积平方。若 $u,v$ 正交且都是单位向量，则：

$$
K(\Pi)=g(R(u,v)v,u).
$$

截面曲率依赖联络和度量；这里默认使用 Levi-Civita 联络。

## Riemann 曲率的全下标形式

给定度量后，可降低输出指标：

$$
R_{\rho\sigma\mu\nu}
=g_{\rho\lambda}R^\lambda{}_{\sigma\mu\nu}.
$$

对 Levi-Civita 联络，全下标 Riemann 张量具有额外对称性：

$$
R_{\rho\sigma\mu\nu}
=-R_{\sigma\rho\mu\nu},
$$

$$
R_{\rho\sigma\mu\nu}
=-R_{\rho\sigma\nu\mu},
$$

$$
R_{\rho\sigma\mu\nu}
=R_{\mu\nu\rho\sigma}.
$$

并满足第一 Bianchi 恒等式：

$$
R_{\rho[\sigma\mu\nu]}=0.
$$

这些额外性质依赖无挠和度量相容等条件，不能无条件套用于任意联络。

## Ricci 曲率

对 Riemann 曲率张量做缩并，定义 Ricci 张量：

$$
R_{\sigma\nu}
=R^\rho{}_{\sigma\rho\nu}
$$

在本文指标约定下成立。

它是 $(0,2)$ 型张量。对 Levi-Civita 联络，Ricci 张量是对称的。

直观上，Ricci 曲率控制小体积相对于平直空间的偏离，也出现在测地线会聚方程中。但完整解释需要体积展开或 Raychaudhuri 方程。

## 标量曲率

用逆度量继续缩并 Ricci 张量：

$$
R=g^{\sigma\nu}R_{\sigma\nu}.
$$

得到标量曲率。

标量曲率把许多方向上的曲率信息压缩成一个数，因此不能完整恢复 Riemann 曲率张量。

在二维 Riemannian 流形上：

$$
R=2K,
$$

其中 $K$ 是 Gaussian 曲率。

## Einstein 张量

在 pseudo-Riemannian 几何和广义相对论中定义：

$$
G_{\mu\nu}
=R_{\mu\nu}
-\frac12g_{\mu\nu}R.
$$

由缩并 Bianchi 恒等式：

$$
\nabla^\mu G_{\mu\nu}=0.
$$

这使 Einstein 张量适合与满足协变守恒的能量动量张量相联系。

本篇只说明几何结构；Einstein 方程及物理解释应在独立概念页展开。

## 测地线偏离怎样测量曲率

考虑一族邻近测地线，令 $u$ 是沿测地线的切向量，$\xi$ 是连接邻近测地线的偏离向量。

在常见约定和适当参数化下，测地线偏离方程写成：

$$
\frac{D^2\xi^\mu}{d\tau^2}
=-R^\mu{}_{\nu\rho\sigma}
 u^\nu\xi^\rho u^\sigma
$$

或整体相反号，取决于曲率及指标排列约定。

它说明即使每条测地线上的观察者都没有普通加速度，邻近自由轨迹之间仍可发生相对加速度。这个效应由曲率控制。

## 曲率与挠率的区别

挠率定义为：

$$
T(X,Y)
=\nabla_XY-\nabla_YX-[X,Y].
$$

曲率定义为：

$$
R(X,Y)Z
=\nabla_X\nabla_YZ
-\nabla_Y\nabla_XZ
-\nabla_{[X,Y]}Z.
$$

二者回答不同问题：

- 挠率比较一阶协变导数的反对称部分；
- 曲率比较二阶协变导数的顺序。

可能出现：

- 无挠但有曲率，如标准球面；
- 有挠但曲率为零的某些联络；
- 二者都为零的平直无挠联络。

## 曲率与拓扑的区别

曲率是局部微分几何量，拓扑描述整体连续结构。

同一个拓扑流形可以承载不同度量和不同曲率。例如二维球面可以装上许多不均匀度量。

反过来，局部曲率为零也不保证全局与 Euclidean 空间完全相同。例如平坦环面局部平直，却有非平凡整体拓扑。

Gauss–Bonnet 定理把二维曲率积分与 Euler 示性数联系起来，但这是一个需要紧致性、定向、边界项等条件的全局定理。

## 规范联络的曲率

一般向量丛或主丛联络的曲率可写成：

$$
F=D^2.
$$

局部矩阵值联络一形式 $A$ 的曲率常写成：

$$
F=dA+A\wedge A
$$

在一种常见约定下成立。

Abelian 情形中矩阵对易子消失，通常化为：

$$
F=dA.
$$

因此规范场强是联络曲率，而不仅是与几何曲率相似的类比。

## 世界面曲率

弦世界面带度量 $h_{ab}$，其 Levi-Civita 联络产生世界面 Riemann 曲率、Ricci 曲率和标量曲率：

$$
R^{(2)}(h).
$$

二维中 Riemann 曲率由一个局部标量函数完全决定。世界面标量曲率会出现在 Gauss–Bonnet 项、非线性 sigma model 的量子计算以及 dilaton 耦合中。

例如 dilaton 耦合常含：

$$
\int d^2\sigma\,\sqrt{h}\,\Phi(X)R^{(2)}(h),
$$

其归一化、Euclidean 或 Lorentzian 符号依作用量约定而定。

## 目标时空曲率与弦传播

弦坐标场：

$$
X^\mu(\sigma)
$$

把世界面映入目标时空。若目标度量 $G_{\mu\nu}(X)$ 弯曲，其 Levi-Civita 曲率会进入背景场展开和量子修正。

在非线性 sigma model 中，度量耦合的 beta 函数在最低阶包含目标时空 Ricci 张量。要求世界面 Weyl 对称在量子层面保持，会导出背景场必须满足的方程。

“beta 函数为零等于完整时空方程”只在给定微扰阶数、场重定义和背景场内容的条件下成立；更高阶还有 $\alpha'$ 修正。

## 一个二维常曲率空间的统一公式

二维常曲率空间满足：

$$
R_{abcd}
=K(g_{ac}g_{bd}-g_{ad}g_{bc}).
$$

进一步：

$$
R_{ab}=Kg_{ab},
$$

$$
R=2K.
$$

因此：

- 平面：$K=0$；
- 半径 $a$ 的球面：$K=1/a^2$；
- 曲率尺度为 $a$ 的双曲平面：$K=-1/a^2$。

这些公式默认使用与本文一致的曲率符号约定；若资料整体反转 Riemann 张量，相关符号也会反转。

## 适用条件与事实边界

1. 联络曲率只需要联络，不必先给度量。
2. 本篇定义采用 $R(X,Y)Z=\nabla_X\nabla_YZ-\nabla_Y\nabla_XZ-\nabla_{[X,Y]}Z$；其他资料可能整体取反。
3. Lie 括号项在一般标架中不可省略，它使曲率成为张量。
4. 曲率对前两个方向反对称；Levi-Civita 联络下还有更多对称性。
5. 联络系数在某坐标中非零不等于曲率非零；平面极坐标是反例。
6. 某一点可令某些联络系数消失，不等于该点曲率必为零。
7. $R=0$ 是局部平直联络条件，但全局平行移动仍可能受拓扑和 holonomy 影响。
8. 截面曲率、Ricci 曲率和标量曲率需要度量，并通常使用 Levi-Civita 联络。
9. Ricci 张量和标量曲率是 Riemann 曲率的缩并，会丢失部分方向信息。
10. 挠率与曲率是不同张量；无挠不意味着无曲率。
11. 曲率是局部几何量，拓扑是全局结构；二者可由 Gauss–Bonnet 等定理在附加条件下联系。
12. 规范场强是规范联络的曲率，具体局部公式依矩阵、耦合和符号约定。
13. 世界面曲率和目标时空曲率属于不同流形，指标及作用对象不能混用。
14. 弦 sigma model 的 beta 函数与背景方程的关系是微扰性结果，存在 $\alpha'$、场重定义和其他背景场修正。

## 常见误区

1. **曲率就是画出来有多弯。** 曲率是坐标无关的局部张量；嵌入图只提供部分直觉。
2. **联络系数非零就有曲率。** 曲线坐标中的平直空间也可有非零联络系数。
3. **某点把 $\Gamma$ 变成零，就证明该点没有曲率。** 曲率还包含 $\Gamma$ 的导数，不能由单点系数判断。
4. **两个协变导数之差永远直接等于 $RZ$。** 需核对 Lie 括号、挠率、二阶导数指标和曲率符号约定。
5. **曲率只需要度量才能定义。** 一般联络本身就有曲率；截面曲率等进一步量才依赖度量。
6. **Ricci 曲率或标量曲率包含全部弯曲信息。** 它们是缩并结果，通常不能恢复完整 Riemann 张量。
7. **无挠就无曲率。** 球面的 Levi-Civita 联络无挠但有正曲率。
8. **局部曲率为零就代表全局一定是 $\mathbb R^n$。** 全局拓扑和 holonomy 仍可能非平凡。
9. **曲率符号在所有教材中都相同。** 常见定义之间可整体相反。
10. **规范场强只是对时空曲率的比喻。** 它严格就是相应规范联络的曲率。
11. **世界面曲率和目标时空曲率是一回事。** 它们属于不同空间，分别由 $h_{ab}$ 与 $G_{\mu\nu}$ 决定。
12. **最低阶 beta 函数条件就是没有任何修正的完整弦方程。** 更高阶、其他背景场和场重定义会改变具体表达。

## 练习

1. 写出曲率算符 $R(X,Y)Z$ 的定义。
2. 为什么定义中要减去 $\nabla_{[X,Y]}Z$？
3. 证明交换 $X,Y$ 后曲率变号。
4. 写出本文采用的 $R^\rho{}_{\sigma\mu\nu}$ 分量定义。
5. 从联络系数推导曲率分量公式。
6. 为什么不同教材中的 Riemann 张量可能整体相反？
7. 解释小闭合回路平行移动与曲率的关系。
8. 为什么 $\Gamma(p)=0$ 不能推出 $R(p)=0$？
9. 计算平面极坐标中的 $R^r{}_{\theta r\theta}$。
10. 半径为 $a$ 的二维球面 Gaussian 曲率是多少？
11. 写出截面曲率公式。
12. 怎样从 Riemann 张量得到 Ricci 张量和标量曲率？
13. 二维流形上标量曲率与 Gaussian 曲率有什么关系？
14. 曲率和挠率分别测量什么？
15. 为什么局部平直不保证全局拓扑平凡？
16. 规范理论中的 $F=dA+A\wedge A$ 表示什么？
17. 世界面曲率与目标时空曲率有何区别？
18. 为什么不能把 sigma model 最低阶 beta 函数条件当作没有修正的完整背景方程？

## 答案与提示

1. $R(X,Y)Z=\nabla_X\nabla_YZ-\nabla_Y\nabla_XZ-\nabla_{[X,Y]}Z$。
2. 它扣除方向场本身不对易造成的贡献，使结果对输入关于光滑函数线性。
3. 交换前两项次序并用 $[Y,X]=-[X,Y]$，得到 $R(Y,X)Z=-R(X,Y)Z$。
4. $R(\partial_\mu,\partial_\nu)\partial_\sigma=R^\rho{}_{\sigma\mu\nu}\partial_\rho$。
5. 依次计算 $\nabla_\mu(\Gamma^\lambda{}_{\nu\sigma}\partial_\lambda)$ 和交换后的表达式，再相减。
6. 有些资料把抽象定义的前三项整体反序，因此得到 $R_{\mathrm{alt}}=-R$；指标排列也可能不同。
7. 净变化的最低非零阶与回路有向面积成正比，系数由 $R(X,Y)Z$ 给出。
8. $R$ 还包含 $\partial\Gamma$；单点令 $\Gamma=0$ 不能令其导数自动为零。
9. $\partial_r(-r)=-1$，二次项贡献 $+1$，其余为零，所以结果为 $0$。
10. $K=1/a^2$。
11. $K(\Pi)=g(R(u,v)v,u)/(g(u,u)g(v,v)-g(u,v)^2)$。
12. $R_{\sigma\nu}=R^\rho{}_{\sigma\rho\nu}$，再用 $R=g^{\sigma\nu}R_{\sigma\nu}$。
13. 按本文约定，$R=2K$。
14. 挠率比较一阶协变导数的反对称部分与 Lie 括号；曲率比较二阶协变导数顺序。
15. 局部曲率不记录全部整体识别方式；平坦环面局部平直但拓扑不是平面。
16. 它是局部联络一形式 $A$ 的曲率，也就是规范场强。
17. 前者由世界面度量 $h_{ab}$ 决定，后者由目标时空度量 $G_{\mu\nu}$ 决定，作用于不同指标。
18. 该关系依微扰阶数、背景场内容和场重定义；更高阶还有 $\alpha'$ 修正。

## 与其他概念的关系

- [[联络]] 定义比较邻近向量的规则，曲率测量该规则的路径依赖；
- [[协变导数]] 的二阶不对易产生曲率；
- [[联络如何定义协变导数]] 推导曲率分量公式所需的一阶协变导数；
- [[向量场]] 提供曲率算符的三个输入及 Lie 括号；
- [[度量]] 用于降指标，并定义截面、Ricci 和标量曲率；
- [[张量积]] 解释 Riemann 曲率为何是 $(1,3)$ 型张量；
- [[挠率]] 与曲率共同描述一般联络，但二者互不等同；
- [[平行移动]] 绕小回路的净变化由曲率控制；
- [[测地线]] 的邻近轨迹偏离由曲率控制；
- [[黎曼曲面]] 在二维使用 Gaussian 曲率和标量曲率；
- [[Gauss-Bonnet 定理]] 把二维曲率积分与拓扑联系起来；
- [[规范联络]] 的曲率就是规范场强；
- [[世界面曲率]] 进入世界面拓扑项和 dilaton 耦合；
- [[广义相对论]] 使用 Ricci、标量和 Einstein 张量描述时空；
- [[非线性 sigma model]] 把目标时空曲率与量子 beta 函数联系起来；
- [[beta 函数]] 给出 $\beta^G_{\mu\nu}=\alpha'R_{\mu\nu}+O(\alpha'^2)$ 的条件、尺度方向和 Weyl 反常系数边界；
- [[Yang-Mills 理论]] 中的 $F_{\mu\nu}$ 是规范联络的曲率，其 Bianchi 恒等式来自协变导数的 Jacobi 恒等式；
- [[Wilson 线与 Wilson 圈]] 用闭合回路上的 holonomy 探测局部曲率所不能单独决定的全局拓扑和群结构。

## 参考资料

- John M. Lee, *Introduction to Riemannian Manifolds*, 2nd ed., Springer, 2018, Chapters 7–8.
- Barrett O'Neill, *Semi-Riemannian Geometry*, Academic Press, 1983, Chapters 3–4.
- Manfredo P. do Carmo, *Riemannian Geometry*, Birkhäuser, 1992, Chapters 3–4.
- Shoshichi Kobayashi and Katsumi Nomizu, *Foundations of Differential Geometry, Vol. 1*, Wiley, 1963, Chapters II–III.
- Michael Spivak, *A Comprehensive Introduction to Differential Geometry, Vol. 2*, 3rd ed., Publish or Perish, 1999.
- Mikio Nakahara, *Geometry, Topology and Physics*, 2nd ed., CRC Press, 2003, Chapters 7 and 10.
- Theodore Frankel, *The Geometry of Physics*, 3rd ed., Cambridge University Press, 2012, Chapters 9–11.
- Robert M. Wald, *General Relativity*, University of Chicago Press, 1984, Chapter 3 and Appendix C.
- Sean M. Carroll, *Spacetime and Geometry*, Cambridge University Press, 2019, Chapters 3–4.
- Joseph Polchinski, *String Theory, Vol. 1*, Cambridge University Press, 1998, Chapters 3–4.
- Barton Zwiebach, *A First Course in String Theory*, 2nd ed., Cambridge University Press, 2009, Chapters 11–12.
- David Tong, *String Theory*, arXiv:0908.0333, [arXiv](https://arxiv.org/abs/0908.0333)
