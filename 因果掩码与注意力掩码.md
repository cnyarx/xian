---
aliases: [因果掩码, 注意力掩码, 填充掩码, Causal Mask, Attention Mask, Padding Mask]
tags: [概念, Transformer, 注意力, 大模型, 学习教程]
level: L8
prerequisites: ["[[注意力分数与注意力权重]]", "[[标量向量矩阵与张量]]"]
---
# 因果掩码与注意力掩码

## 一句话定义

因果掩码（Causal Mask）确保自回归模型在计算位置 $i$ 的隐藏状态时只能使用位置 $0$ 到 $i$ 的 token；这个隐藏状态通常用于预测位置 $i+1$ 的 token，因此不会看到待预测的未来 token。填充掩码（Padding Mask）把不同长度的序列对齐到同一批次，同时让填充位置不参与注意力计算。

## 直觉理解

因果掩码像考试：答第 5 题时只能看到前 4 题，不能偷看后面的题。填充掩码像用等宽表格填不同长度的名单：空位标记为"无"，统计时自动跳过。

## 严格定义

注意力分数矩阵 $S=QK^T/\sqrt{d_k}$。因果掩码添加 $M^c$：

$$M^c_{ij}=\begin{cases}0&\text{若 }i\ge j\\-\infty&\text{若 }i<j\end{cases}$$

填充掩码 $M^p$：若 $j$ 是填充位置，则 $M^p_{ij}=-\infty$。最终掩码 $M=M^c+M^p$（或取并集），再计算：

$$\text{Attention}=\operatorname{softmax}(S+M)V$$

## 例子

序列 `["我", "爱", "学", "习", [PAD], [PAD]]`，位置 3 的因果掩码只能看到 0–3，同时填充掩码把 4–5 强制为零权重。

## 边界

- 因果掩码在 decoder-only 模型的自注意力层中使用；在 encoder-decoder 架构中，decoder 的自注意力通常也使用因果掩码，而 encoder 自注意力和 decoder 到 encoder 的交叉注意力通常不使用这种未来位置掩码；
- 不同框架的掩码约定（True/False 含义）可能相反；
- 掩码错误会导致训练泄漏或推理时输出混乱。

## 最小自测

1. 普通双向 encoder 自注意力通常需要因果掩码吗？encoder-decoder 模型的 decoder 自注意力呢？
2. 如果忘记加填充掩码，会有什么后果？

答案：1. 普通双向 encoder 通常不需要；自回归 decoder 的自注意力通常需要，以免看到未来目标；2. 填充位置会参与注意力计算，干扰模型学习。

## 详细教程

- [[缩放点积注意力|缩放点积注意力]]
- [[Decoder-Only 架构|Decoder-Only 架构]]

## 来源

- Vaswani et al. (2017), *Attention Is All You Need*：https://arxiv.org/abs/1706.03762 `[A]`