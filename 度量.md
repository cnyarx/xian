---
aliases:
  - Metric
  - Metric Tensor
  - 度量张量
english_name: Metric Tensor
tags:
  - 数学
  - 微分几何
  - 广义相对论
  - 弦理论先修
  - 弦理论
prerequisites:
  - "[[流形]]"
  - "[[切空间与余切空间的对偶]]"
  - "[[内积空间]]"
  - "[[矩阵]]"
used_by:
  - "[[联络]]"
  - "[[协变导数]]"
  - "[[曲率]]"
  - "[[黎曼曲面]]"
  - "[[Hodge 星算符]]"
  - "[[Minkowski 时空]]"
  - "[[世界面度量]]"
  - "[[世界面诱导度量]]"
  - "[[Polyakov 作用量]]"
  - "[[Weyl 反常]]"
  - "[[二维 Euclidean 场论]]"
evidence_status: 教材事实
---

# 度量（Metric Tensor）

## 一句话解释

度量张量是在流形每一点上为两支切向量赋予内积型数值的光滑规则，它使我们能够定义长度、夹角、正交、体积，以及 Euclidean 或时空中的因果结构。

## 为什么要学它

光滑流形只告诉我们空间局部像 $\mathbb R^n$，却没有自动告诉我们两点相距多远、向量有多长、两条方向是否垂直。

度量加入这些几何信息。它还是以下概念的基础：

- 指标升降与梯度；
- 曲线长度和测地线；
- Levi-Civita 联络与曲率；
- 体积形式与 Hodge 星算符；
- 广义相对论中的时空间隔；
- 弦世界面的内禀几何；
- 目标时空度量与世界面诱导度量；
- Polyakov 作用量和弦在背景时空中的传播。

## 直观图景

坐标网格只负责给点编号。度量则像贴在每一点上的“测量说明书”，告诉你当地的坐标刻度有多长、坐标轴是否垂直，以及时间方向和空间方向如何贡献间隔。

在普通直角坐标中，这份说明书非常简单，单位基向量长度都是 $1$，不同基方向正交。但在极坐标中，同样增加一个角度 $d\theta$，实际移动距离会随半径 $r$ 改变。度量把这种差异写进公式。

## 先区分两种“度量”

数学中 metric 一词常指两种相关但不同的结构。

度量空间中的距离函数是：

$$
d:X\times X\to\mathbb R_{\ge0},
$$

满足非负、分离、对称和三角不等式。

微分几何中的度量张量则逐点作用在切向量上：

$$
g_p:T_pM\times T_pM\to\mathbb R.
$$

本篇重点是第二种。正定 Riemannian 度量可以通过曲线长度诱导点之间的距离，但 Lorentzian 度量通常不直接给出满足普通距离公理的距离函数。

## 正式定义

流形 $M$ 上的 Riemannian 度量，是对每个点 $p\in M$ 指定一个正定内积：

$$
g_p:T_pM\times T_pM\to\mathbb R,
$$

并要求 $g_p$ 随 $p$ 光滑变化。

具体要求是：

1. 双线性；
2. 对称；
3. 正定；
4. 分量在每张光滑坐标图中都是光滑函数。

若只要求对称、非退化，并允许固定数量的正负方向，则得到 pseudo-Riemannian 度量。Lorentzian 度量是其中恰有一个时间型符号或恰有一个相反符号方向的情形，具体顺序取决于号差约定。

## 每个符号是什么意思

- $M$：光滑流形；
- $p\in M$：流形上的一点；
- $T_pM$：点 $p$ 处的切空间；
- $g_p$：点 $p$ 处的度量；
- $g_p(u,v)$：度量作用在两支切向量上的实数；
- $g_{ij}$：度量在坐标基中的分量；
- $g^{ij}$：逆度量的分量；
- $ds^2$：无穷小线元的传统记号；
- signature：度量矩阵正、负特征值的数量，即号差。

## 双线性

对切向量 $u,v,w$ 和实数 $a,b$，度量对两个输入分别线性：

$$
g_p(au+bv,w)
=a\,g_p(u,w)+b\,g_p(v,w),
$$

以及：

$$
g_p(w,au+bv)
=a\,g_p(w,u)+b\,g_p(w,v).
$$

双线性使度量可以从基向量上的取值扩展到任意切向量。

## 对称性

度量满足：

$$
g_p(u,v)=g_p(v,u).
$$

因此坐标分量满足：

$$
g_{ij}=g_{ji}.
$$

度量是对称的 $(0,2)$ 型张量，不是反对称二形式。

## 正定与非退化

Riemannian 度量要求任意非零切向量 $v$ 满足：

$$
g_p(v,v)>0.
$$

这叫正定性，它允许定义实的非负长度。

pseudo-Riemannian 度量不要求正定，但要求非退化：若对所有 $w$ 都有：

$$
g_p(v,w)=0,
$$

则必有：

$$
v=0.
$$

矩阵语言中，非退化等价于：

$$
\det(g_{ij})\ne0.
$$

## 坐标分量

在局部坐标 $x^1,\ldots,x^n$ 中，坐标基为：

$$
\partial_i=\frac{\partial}{\partial x^i}.
$$

度量分量定义为：

$$
g_{ij}=g(\partial_i,\partial_j).
$$

于是度量写成：

$$
g=g_{ij}\,dx^i\otimes dx^j.
$$

因为 $g_{ij}=g_{ji}$，有些资料用对称张量积符号；本篇使用普通张量积并让对称性体现在分量中。

## 线元记号

物理和几何中常把度量写成：

$$
ds^2=g_{ij}dx^i dx^j.
$$

这里 $dx^i dx^j$ 是线元表达中的传统简写，不应机械理解为两个普通小数相乘。更严格的张量表达是：

$$
g=g_{ij}dx^i\otimes dx^j.
$$

当把度量作用在曲线速度上时，$ds^2$ 才得到沿曲线的无穷小长度平方。

## 向量长度

对 Riemannian 度量和切向量 $v$，定义：

$$
\|v\|=\sqrt{g(v,v)}.
$$

若：

$$
v=v^i\partial_i,
$$

则：

$$
g(v,v)=g_{ij}v^iv^j.
$$

Riemannian 正定性保证非零向量的平方长度为正。

Lorentzian 情形中 $g(v,v)$ 可正、可负或为零，因此通常不把所有向量都用同一个 Euclidean 长度概念处理。

## 夹角与正交

在 Riemannian 情形，非零向量 $u,v$ 的夹角可由：

$$
\cos\theta
=\frac{g(u,v)}{\|u\|\,\|v\|}
$$

定义。

若：

$$
g(u,v)=0,
$$

则称 $u$ 与 $v$ 正交。

在 Lorentzian 几何中，角度和正交仍可讨论，但涉及时间型、空间型和零向量时不能直接照搬所有 Euclidean 公式。

## Euclidean 空间

在 $\mathbb R^n$ 的标准直角坐标中：

$$
g_{ij}=\delta_{ij}.
$$

因此：

$$
ds^2=(dx^1)^2+\cdots+(dx^n)^2.
$$

对向量 $v=(v^1,\ldots,v^n)$：

$$
g(v,v)=\sum_i(v^i)^2.
$$

这是熟悉的勾股定理。

## 平面极坐标

在平面上取：

$$
x=r\cos\theta,
\qquad
y=r\sin\theta.
$$

微分为：

$$
dx=\cos\theta\,dr-r\sin\theta\,d\theta,
$$

$$
dy=\sin\theta\,dr+r\cos\theta\,d\theta.
$$

代入：

$$
ds^2=dx^2+dy^2,
$$

得到：

$$
ds^2=dr^2+r^2d\theta^2.
$$

所以极坐标中的度量矩阵为：

$$
(g_{ij})=
\begin{pmatrix}
1&0\\
0&r^2
\end{pmatrix}.
$$

角方向坐标基 $\partial_\theta$ 的长度为 $r$，不是恒定的 $1$。

## 二维球面

半径为 $R$ 的二维球面采用球坐标 $(\theta,\phi)$ 时：

$$
ds^2
=R^2d\theta^2
+R^2\sin^2\theta\,d\phi^2.
$$

这里：

$$
(g_{ij})=
\begin{pmatrix}
R^2&0\\
0&R^2\sin^2\theta
\end{pmatrix}.
$$

在极点 $\sin\theta=0$ 时，矩阵分量看似退化，是因为球坐标图在极点失效，不是球面真正失去 Riemannian 度量。

## 坐标变换规律

若从坐标 $x^i$ 变到 $y^a$，度量分量变换为：

$$
g'_{ab}(y)
=\frac{\partial x^i}{\partial y^a}
\frac{\partial x^j}{\partial y^b}
g_{ij}(x(y)).
$$

两个下指标各带一个逆 Jacobi 因子。

虽然分量矩阵改变，但几何数值：

$$
g(v,w)
$$

不依赖坐标选择。

## 逆度量

非退化保证度量矩阵存在逆矩阵，记作：

$$
g^{ij}.
$$

它满足：

$$
g^{ik}g_{kj}=\delta^i_j.
$$

$g^{ij}$ 是一个 $(2,0)$ 型张量的分量，不是把 $g_{ij}$ 的每个数字分别取倒数。

例如对角矩阵可以逐个取倒数，但非对角矩阵必须求完整矩阵逆。

## 指标升降

度量把切向量变成余切向量：

$$
v_i=g_{ij}v^j.
$$

逆度量把余切向量变成切向量：

$$
\omega^i=g^{ij}\omega_j.
$$

用 musical maps 表示：

$$
v^\flat=g(v,\cdot),
\qquad
\omega^\sharp=g^{-1}(\omega,\cdot).
$$

指标位置改变不是排版操作，而是应用度量或逆度量。

## 梯度

函数微分：

$$
df=\partial_i f\,dx^i
$$

自然是余切向量。选定度量后，梯度向量定义为：

$$
\operatorname{grad}f=(df)^\sharp.
$$

分量为：

$$
(\operatorname{grad}f)^i
=g^{ij}\partial_jf.
$$

它满足：

$$
g(\operatorname{grad}f,v)=df(v)
$$

对任意切向量 $v$ 成立。

## 曲线长度

设分段光滑曲线：

$$
\gamma:[a,b]\to M.
$$

其速度为：

$$
\dot\gamma(t)\in T_{\gamma(t)}M.
$$

Riemannian 长度定义为：

$$
L(\gamma)
=\int_a^b
\sqrt{g_{\gamma(t)}(\dot\gamma(t),\dot\gamma(t))}\,dt.
$$

坐标中：

$$
L(\gamma)
=\int_a^b
\sqrt{g_{ij}(x(t))
\frac{dx^i}{dt}
\frac{dx^j}{dt}}\,dt.
$$

## 由曲线长度得到距离

在同一连通分支内，可定义两点间的 Riemannian 距离：

$$
d_g(p,q)
=\inf_{\gamma:p\to q}L(\gamma).
$$

这里对所有连接 $p$ 与 $q$ 的分段光滑曲线取长度下确界。

该距离满足普通度量空间公理，并诱导与原流形相同的拓扑。

这一步依赖正定 Riemannian 度量。Lorentzian 时空间隔不能以同样方式直接给出普通距离函数。

## 体积密度与体积形式

Riemannian 度量在局部坐标中给出体积因子：

$$
\sqrt{\det g}\,dx^1\wedge\cdots\wedge dx^n
$$

在选定定向下形成 Riemannian 体积形式。

pseudo-Riemannian 情形常写：

$$
\sqrt{|\det g|}\,d^nx.
$$

绝对值用于保证体积密度为实的非负量；具体符号和定向约定需结合语境。

## Hodge 星的入口

在定向的 pseudo-Riemannian 流形上，度量与体积形式共同定义 Hodge 星算符：

$$
*:\Omega^p(M)\to\Omega^{n-p}(M).
$$

它把 $p$ 形式转换成补维数形式。

Hodge 星的平方符号依维数、形式次数和度量号差；不能把 Euclidean 公式无条件搬到 Lorentzian 情形。

## Riemannian 与 Lorentzian 度量

Riemannian 度量为正定，局部可把矩阵化为：

$$
\operatorname{diag}(+1,\ldots,+1).
$$

Lorentzian 度量有一个符号与其余方向不同，常见约定为：

$$
\operatorname{diag}(-1,+1,+1,+1)
$$

或：

$$
\operatorname{diag}(+1,-1,-1,-1).
$$

两者分别称 mostly plus 和 mostly minus。只要整套公式保持一致，物理内容等价。

## Minkowski 时空

四维平直 Minkowski 时空在 mostly plus 约定下：

$$
ds^2=-dt^2+dx^2+dy^2+dz^2
$$

若取光速 $c=1$。

切向量可按 $g(v,v)$ 的符号分为时间型、空间型和零型。哪一个符号对应哪一类取决于号差约定，但光锥结构不变。

零向量满足：

$$
g(v,v)=0
$$

却可能有 $v\ne0$。这不违反非退化性：非退化要求的是不存在与所有向量都正交的非零向量，而不是要求 $g(v,v)$ 对非零 $v$ 永不为零。

## 度量与拓扑不是同一层结构

流形拓扑决定哪些集合开、哪些点彼此邻近，以及连续性如何定义。

Riemannian 度量进一步定义具体长度与角度。同一个光滑流形可以承载许多不同度量。

例如二维球面可以有标准圆球度量，也可以有被拉伸后的非均匀度量；底层拓扑仍然是 $S^2$。

## 度量与坐标不是同一回事

改变坐标会改变 $g_{ij}$ 的表达式，但不会自动改变几何度量本身。

真正改变度量，是把张量场 $g$ 换成另一个张量场 $\tilde g$。

因此：

- 同一度量可在不同坐标中有不同矩阵；
- 同一坐标系中也可写许多不同度量。

不能仅凭矩阵分量长得不同就断言几何已经改变。

## 共形缩放

给定正函数：

$$
e^{2\omega(x)}>0,
$$

可定义新度量：

$$
\tilde g=e^{2\omega}g.
$$

它改变长度尺度，但保持非零向量之间的角度，因此称为共形相关。

在 Lorentzian 情形，正的共形因子还保持光锥。二维世界面理论中的 Weyl 变换正是这类局部缩放。

## 诱导度量

若流形 $\Sigma$ 通过光滑浸入：

$$
X:\Sigma\to M
$$

嵌入带度量 $G$ 的目标空间，则 $\Sigma$ 上的诱导度量为：

$$
g=X^*G.
$$

局部坐标中：

$$
g_{ab}
=G_{\mu\nu}(X)
\partial_aX^\mu\partial_bX^\nu.
$$

这表示先把世界面切向量推到目标空间，再用目标度量测量。

若 $X$ 不是浸入，拉回张量可能退化，因此要得到真正的非退化诱导度量还需相应正则性条件。

## 弦世界面的两种度量

弦理论中常同时出现：

1. 由嵌入 $X$ 从目标时空拉回的诱导度量 $g_{ab}$；
2. Polyakov 表述中作为独立辅助场引入的世界面度量 $h_{ab}$。

经典方程会把 $h_{ab}$ 与 $g_{ab}$ 联系起来，在 Weyl 缩放意义下等价，但它们在变分前不是同一个定义。

把二者从一开始直接写成相等，会掩盖 Polyakov 作用量的独立变分结构。

## Polyakov 作用量

玻色弦在背景度量 $G_{\mu\nu}(X)$ 中的 Polyakov 作用量常写为：

$$
S_P
=-\frac{T}{2}
\int d^2\sigma\,
\sqrt{-h}\,
h^{ab}G_{\mu\nu}(X)
\partial_aX^\mu\partial_bX^\nu
$$

在一种 Lorentzian 世界面约定下成立。

其中：

- $T$：弦张力；
- $h_{ab}$：独立世界面度量；
- $h=\det(h_{ab})$；
- $h^{ab}$：逆世界面度量；
- $G_{\mu\nu}$：目标时空度量；
- $\partial_aX^\mu$：嵌入映射微分的分量。

整体符号、归一化与 Euclidean 延拓后的因子依约定变化。

## 世界面 Weyl 对称

二维 Polyakov 作用量在经典层面具有局部 Weyl 变换：

$$
h_{ab}(\sigma)
\longmapsto e^{2\omega(\sigma)}h_{ab}(\sigma).
$$

在二维：

$$
\sqrt{-h}\,h^{ab}
$$

在该变换下保持不变，因此作用量不变。

量子理论中 Weyl 对称是否保持取决于反常消除，这与弦的临界维数直接相关。

## 度量不唯一决定联络

仅仅给定一个度量，还可以人为选择许多不同联络。

但若同时要求联络：

1. 与度量相容；
2. 无挠率；

则存在唯一的 Levi-Civita 联络。

因此常说“度量决定 Levi-Civita 联络”，隐含了这两个条件，不能省略。

## 度量与曲率

度量的一阶导数进入 Levi-Civita 联络系数，联络的一阶导数和二次项组成曲率。

粗略地说：

- 度量告诉当地怎样测量；
- 联络告诉邻近点的向量怎样比较；
- 曲率告诉沿不同路径比较时是否产生不一致。

坐标中度量分量变化不一定表示曲率非零。例如平面极坐标的 $g_{\theta\theta}=r^2$ 随位置变化，但平面曲率仍为零。

## 一个长度计算例子

在平面极坐标度量：

$$
ds^2=dr^2+r^2d\theta^2
$$

中，考虑半径固定为 $R$ 的圆：

$$
r=R,
\qquad
0\le\theta\le2\pi.
$$

沿曲线 $dr=0$，所以：

$$
ds=R\,d\theta.
$$

圆周长度为：

$$
L=\int_0^{2\pi}R\,d\theta=2\pi R.
$$

这展示了度量分量 $r^2$ 怎样把角坐标变化转换成真实长度。

## 适用条件与事实边界

1. 本篇主要讨论光滑流形上的 Riemannian 与 pseudo-Riemannian 度量张量，不把它与一般度量空间的距离函数混同。
2. Riemannian 度量是光滑、对称、正定的 $(0,2)$ 张量场；pseudo-Riemannian 度量把正定放宽为非退化且号差固定。
3. 度量分量依赖坐标，但张量 $g$ 及 $g(v,w)$ 的几何意义不依赖坐标。
4. 非退化等价于度量矩阵可逆；Lorentzian 非零零向量的存在不违反非退化性。
5. 逆度量是矩阵逆和 $(2,0)$ 张量，不是逐分量取倒数。
6. 指标升降、梯度、Hodge 星和体积结构依赖度量。
7. Riemannian 度量通过曲线长度诱导普通距离；Lorentzian 度量不能直接用同一公式产生普通距离度量。
8. 坐标奇点可使某套坐标中的度量矩阵看似退化，但不必代表几何度量真正退化。
9. 诱导度量是目标度量的拉回；要保持非退化通常要求嵌入或浸入满足相应正则条件。
10. Polyakov 世界面度量与诱导度量在变分前是不同对象，经典方程才把它们联系起来。
11. “度量决定联络”特指唯一的无挠且度量相容的 Levi-Civita 联络。
12. 世界面 Weyl 对称在经典层面成立；量子层面需要检查反常，不能直接假定永远保持。

## 常见误区

1. **度量张量就是两点之间的距离函数。** 前者逐点作用在切向量上；正定情形下可进一步诱导距离函数。
2. **流形一旦给定，度量也自动唯一。** 同一流形可承载许多不同度量。
3. **度量分量随坐标变化，说明几何对象不客观。** 分量变化正是张量保持几何意义的方式。
4. **$g^{ij}$ 是 $1/g_{ij}$。** 它是完整矩阵逆的分量。
5. **上标和下标可随意互换。** 必须应用度量或逆度量。
6. **任何非零向量都有非零平方长度。** Lorentzian 度量允许非零零向量。
7. **极坐标在原点的矩阵问题表示平面度量退化。** 原因是极坐标在原点失效。
8. **度量分量不是常数就一定有曲率。** 平直空间的曲线坐标也会产生位置依赖分量。
9. **诱导度量与 Polyakov 辅助度量从定义上相同。** 它们在变分前独立，经典方程才建立关系。
10. **经典 Weyl 对称自动保证量子 Weyl 对称。** 量子反常可能破坏它。

## 练习

1. 写出 Riemannian 度量在每一点需要满足的四项条件。
2. 非退化在向量语言和矩阵语言中分别怎样表达？
3. 度量在坐标基中的分量 $g_{ij}$ 如何定义？
4. 为什么度量不是二形式？
5. 标准 $\mathbb R^n$ 的度量矩阵是什么？
6. 推导平面极坐标度量 $ds^2=dr^2+r^2d\theta^2$。
7. 为什么球坐标在极点的分量矩阵看似退化不代表球面度量退化？
8. 写出逆度量和指标升降公式。
9. 函数 $f$ 的梯度分量怎样由 $df$ 和逆度量得到？
10. Riemannian 曲线长度如何定义？
11. 写出世界面诱导度量 $g_{ab}$。
12. Polyakov 表述中的 $h_{ab}$ 与诱导度量有什么区别？
13. 为什么 Lorentzian 度量允许非零向量满足 $g(v,v)=0$？
14. 为什么说度量只有在附加两个条件后才唯一决定 Levi-Civita 联络？

## 答案与提示

1. 双线性、对称、正定，并随点光滑变化。
2. 若 $g(v,w)=0$ 对所有 $w$ 成立，则 $v=0$；矩阵上为 $\det g\ne0$。
3. $g_{ij}=g(\partial_i,\partial_j)$。
4. 度量对称，二形式反对称。
5. $(g_{ij})=I_n$，即 $g_{ij}=\delta_{ij}$。
6. 把 $x=r\cos\theta,y=r\sin\theta$ 微分后代入 $dx^2+dy^2$，交叉项抵消。
7. 球坐标图在极点失效；换用有效局部坐标后度量仍非退化。
8. $g^{ik}g_{kj}=\delta^i_j$；$v_i=g_{ij}v^j$，$\omega^i=g^{ij}\omega_j$。
9. $(\operatorname{grad}f)^i=g^{ij}\partial_jf$。
10. $L(\gamma)=\int\sqrt{g(\dot\gamma,\dot\gamma)}dt$。
11. $g_{ab}=G_{\mu\nu}(X)\partial_aX^\mu\partial_bX^\nu$。
12. 前者是 Polyakov 作用量中独立变分的世界面度量，后者由嵌入和目标度量拉回得到。
13. 非退化禁止的是与所有向量都正交的非零向量，并不禁止某个非零向量与自身正交。
14. 还需要求联络无挠且与度量相容；满足二者的联络才唯一。

## 与其他概念的关系

- [[流形]] 提供度量所在的光滑空间；
- [[切空间与余切空间的对偶]] 解释度量为何是 $(0,2)$ 张量以及怎样升降指标；
- [[内积空间]] 是每个切空间上正定度量的线性代数模型；
- [[矩阵]] 提供度量矩阵、行列式、特征值与逆矩阵；
- [[微分形式]] 与度量共同定义体积形式和 Hodge 星；
- [[向量场]] 可由度量测量长度、夹角并与一形式互换；
- [[联络]] 在加入无挠和度量相容条件后得到唯一 Levi-Civita 联络；
- [[曲率]] 测量由度量相关联络产生的非平直性；
- [[Minkowski 时空]] 使用平直 Lorentzian 度量；
- [[世界面度量]] 控制弦世界面的内禀几何；
- [[世界面诱导度量]] 是目标时空度量沿弦嵌入的拉回；
- [[Polyakov 作用量]] 同时使用世界面逆度量和目标时空度量；
- [[Weyl 对称性]] 描述世界面度量的局部尺度冗余；
- [[Weyl 反常|共形反常]] 决定经典 Weyl 对称能否在量子层面保持；
- [[二维 Euclidean 场论]] 固定 $ds^2=dz\,d\bar z$ 时 $g_{z\bar z}=1/2$ 的复坐标度量约定，并说明二维度量的局部共形平坦性。

## 参考资料

- John M. Lee, *Introduction to Riemannian Manifolds*, 2nd ed., Springer, 2018.
- Barrett O'Neill, *Semi-Riemannian Geometry*, Academic Press, 1983.
- Manfredo P. do Carmo, *Riemannian Geometry*, Birkhäuser, 1992.
- Robert M. Wald, *General Relativity*, University of Chicago Press, 1984.
- Sean M. Carroll, *Spacetime and Geometry*, Cambridge University Press, 2019.
- Mikio Nakahara, *Geometry, Topology and Physics*, 2nd ed., CRC Press, 2003.
- Theodore Frankel, *The Geometry of Physics*, 3rd ed., Cambridge University Press, 2012.
- Joseph Polchinski, *String Theory, Vol. 1*, Cambridge University Press, 1998.
- Barton Zwiebach, *A First Course in String Theory*, 2nd ed., Cambridge University Press, 2009.
- David Tong, *Lectures on String Theory*, [arXiv:0908.0333](https://arxiv.org/abs/0908.0333)
