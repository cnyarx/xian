---
aliases: [集合, 映射, Set, Mapping]
tags: [概念, 数学, 大模型, 学习教程]
level: L3
prerequisites: ["[[基础原语]]", "[[变量与函数]]"]
---
# 集合与映射

## 一句话定义

集合（Set）是一组明确对象的整体；映射（Mapping）是一条把一个集合中的对象对应到另一个集合对象的规则。

## 例子

$$
A=\{\text{猫},\text{狗},\text{鸟}\}
$$

这是包含三种动物名称的集合。若规则把每个名称映射到 token ID：

```text
猫 → 0
狗 → 1
鸟 → 2
```

这就是从名称集合到整数集合的映射。

## 符号

- $x\in A$：$x$ 属于集合 $A$。
- $x\notin A$：$x$ 不属于 $A$。
- $A\cap B$：同时属于 $A$ 和 $B$ 的对象。
- $A\cup B$：属于 $A$ 或 $B$ 的对象。

## 与函数的关系

函数是一类映射：每个允许输入必须有唯一输出。tokenizer 的编码过程把文本片段映射到 ID，嵌入查表再把 ID 映射到向量。

## 边界

集合不关心排列顺序和重复次数；列表则关心。例如集合 `{1,2}` 与 `{2,1,1}` 表示相同成员集合，但两个列表不同。

## 最小自测

题目：若 $A=\{1,2,3\}$、$B=\{3,4\}$，$A\cap B$ 是什么？

答案：$\{3\}$。

## 来源

- OpenStax, *Contemporary Mathematics*, Set Theory：https://openstax.org/details/books/contemporary-mathematics `[A]`
