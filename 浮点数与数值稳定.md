---
aliases: [浮点数, 数值稳定, 上溢, 下溢, Floating Point, Numerical Stability]
tags: [概念, 数值计算, 大模型, 学习教程]
level: L2
prerequisites: ["[[数与实数]]", "[[指数与对数|指数与对数]]"]
---
# 浮点数与数值稳定

## 一句话定义

浮点数（Floating-Point Number）用有限位二进制近似实数；数值稳定（Numerical Stability）是让计算在舍入误差和有限范围下仍得到可靠结果。

## 为什么计算机会有误差

有限位数无法精确表示所有实数。例如许多系统中：

```python
0.1 + 0.2 == 0.3
```

可能得到 `False`，因为三者都由附近的二进制数近似。

## 上溢与下溢

- **上溢**（Overflow）：数太大，超过格式能表示的范围，可能变成 `inf`。
- **下溢**（Underflow）：非零数太小，可能被舍入为 0。

直接计算 $e^{1000}$ 会溢出；softmax 通常先减最大 logit：

$$
\frac{e^{z_i-m}}{\sum_je^{z_j-m}},\qquad m=\max_jz_j
$$

这不改变数学结果，却显著减小溢出风险。

## 精度与范围不是同一件事

FP16、BF16、FP32 使用不同数量的指数位和尾数位。指数位影响范围，尾数位影响相邻可表示数字的精细程度。因此 BF16 范围较大，但有效精度比 FP16 更粗。

## 与训练的关系

训练常混用低精度矩阵计算和高精度累加/状态；损失缩放、稳定 softmax、融合交叉熵与梯度裁剪都是针对不同数值风险的方法。

## 最小自测

题目：为什么 softmax 的所有 logits 同减 1000，不改变概率？

答案：分子分母都乘同一个 $e^{-1000}$，公共因子约掉。

## 来源

- IEEE 754-2019, *IEEE Standard for Floating-Point Arithmetic* `[A]`
- Higham, N. J. (2002), *Accuracy and Stability of Numerical Algorithms* `[A]`
