---
aliases: [矩阵乘法公式, Matrix Multiplication Formula]
tags: [公式, 线性代数, 大模型, 学习教程]
prerequisites: ["[[求和与下标]]", "[[坐标维度与形状]]", "[[标量向量矩阵与张量]]"]
---
# 矩阵乘法与仿射层公式

## 矩阵乘法

若 $A$ 形状为 $m\times k$，$B$ 形状为 $k\times n$：

$$
C=AB,\qquad c_{ij}=\sum_{p=1}^{k}a_{ip}b_{pj}
$$

输出 $C$ 的形状为 $m\times n$。

## 数值展开

$$
\begin{bmatrix}1&2\\3&4\end{bmatrix}
\begin{bmatrix}5\\6\end{bmatrix}
=
\begin{bmatrix}1\times5+2\times6\\3\times5+4\times6\end{bmatrix}
=
\begin{bmatrix}17\\39\end{bmatrix}
$$

## 仿射层

$$
y=Wx+b
$$

- $x$：输入向量，形状 $d_{in}$；
- $W$：权重矩阵，形状 $d_{out}\times d_{in}$；
- $b$：偏置向量，形状 $d_{out}$；
- $y$：输出向量，形状 $d_{out}$。

批量行向量实现常写成 $Y=XW^T+b$。两种写法只是向量排列约定不同，必须通过 shape 判断，不能机械照抄。

## 边界

矩阵乘法通常不可交换。`AB` 有定义，不保证 `BA` 有定义；即使都有定义，也通常不相等。

## 相关概念

[[线性变换与仿射变换|线性变换与仿射变换]]

## 最小自测

题目：形状为 $2\times3$ 的矩阵能否右乘形状为 $3\times4$ 的矩阵？结果形状是什么？

答案：能，中间维度 3 相同；结果形状为 $2\times4$。

## 正文

[[点积与矩阵乘法|点积与矩阵乘法]]

## 来源

- Strang, G., *Introduction to Linear Algebra* `[A]`
- PyTorch, `nn.Linear`：https://pytorch.org/docs/stable/generated/torch.nn.Linear.html `[A]`
