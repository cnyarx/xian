---
aliases: [采样, Temperature, Top-k, Top-p, Beam Search]
tags: [大模型, 推理, 教程, 学习教程]
prerequisites: ["[[推理概览]]", "[[概率分布]]"]
next: ["[[KV Cache]]"]
---
# 采样策略

## logits 到概率

模型输出 logits。温度 $T$ 调整后：

$$p_i=\frac{\exp(z_i/T)}{\sum_j\exp(z_j/T)}$$

- $T<1$：分布更尖，更确定。
- $T>1$：分布更平，更随机。
- $T=1$：原分布。

严格的 $T=0$ 不能代入除法，软件通常把它实现为贪心选择。

## 贪心

每步选概率最高 token。可重复、速度简单，但可能陷入重复或局部最优。

## Top-k

只保留概率最高的 $k$ 个 token，重新归一化后采样。$k$ 固定，无法适应分布有时尖、有时平。

## Top-p

按概率从高到低累加，保留总概率首次达到阈值 $p$ 的最小集合，再采样。候选数量会自动变化。

## 束搜索

同时保留总分最高的 $B$ 条序列。适合某些有明确单一答案的任务；开放式对话中可能产生保守、重复文本，现代聊天 LLM 通常更多使用采样。

## 重复惩罚

对已出现 token 降低分数，可缓解重复，但过强会破坏语法和事实。具体实现因库而异。

## 概念与公式导航

- [[Logit概率与采样|Logit、概率与采样]]
- [[Softmax稳定计算与温度公式|Softmax、稳定计算与温度公式]]

## 练习

1. 温度增大如何改变概率分布？
2. Top-k 与 Top-p 的核心区别是什么？
3. 为什么贪心不保证整句概率最高？
4. 创作和代码生成分别会偏好怎样的采样设置？说明理由。

## 来源

- Holtzman, A. et al. (2020). *The Curious Case of Neural Text Degeneration*. ICLR. https://arxiv.org/abs/1904.09751 `[A]`
- Hugging Face, *Generation Strategies*：https://huggingface.co/docs/transformers/generation_strategies `[A]`