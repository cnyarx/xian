---
aliases: [特征, Feature, 标签, Label, 训练集, Training Set, 验证集, Validation Set, 测试集, Test Set]
tags: [机器学习, 教程, 大模型, 学习教程]
prerequisites: ["[[什么是机器学习]]"]
next: ["[[损失函数]]"]
---
# 数据与特征

## 一句话定义

**特征**是模型用来做判断的输入信息。**标签**是想要模型学会的正确答案。

## 特征

特征是和问题有关的信息。比如预测房价：

```
特征：面积、卧室数量、位置、房龄
```

每个样本是一个**特征向量**：

```
[120, 3, 市中心, 10]
```

在大模型中，特征是 token 序列，每个 token 被映射成高维向量，然后输入模型。

## 标签

标签是希望模型预测的目标。监督学习中每个样本都有标签。

```
房价预测：标签 = 实际成交价
猫狗分类：标签 = "猫" 或 "狗"
大模型预训练：标签 = 下个 token
```

## 数据划分

```
全部数据 100%
├── 训练集 60~80%    → 学习参数
├── 验证集 10~20%    → 调超参数、检测过拟合
└── 测试集 10~20%    → 最终评估
```

核心原则：测试集在训练完成前不能被模型“看到”。用测试集调参会导致测试集变成验证集，评估结果不再有效。

## 数据类型

- **数值型**：面积、温度、价格。可直接输入模型。
- **类别型**：颜色、城市、物种。需要先编码成数字。
- **文本型**：句子、文档。需要先分词、转成 token。
- **图像型**：像素值。通常是三维张量（高×宽×通道）。

## 数据预处理

模型通常需要预处理后的数据：

- **归一化**：把不同特征缩放到相近范围，避免某些特征因数值范围大而主导训练。
- **缺失值处理**：填充缺失值或删除不完整样本。
- **数据增强**：对图像旋转、裁剪，对文本替换同义词，增加训练数据多样性。

## 大模型的数据

大模型训练数据通常以万亿 token 计，来源包括：

- 网页文本（Common Crawl）
- 书籍
- 学术论文
- 代码仓库
- 维基百科
- 对话数据

数据处理包括过滤低质量内容、去重、去除个人信息、去除有害内容、数据配比。

## 常见误区

- 特征多不一定是好事：无关特征反而增加噪声。
- 标签不准确比没有标签更糟糕：模型会学到错误模式。
- 验证集任意划分可能导致评估偏差：类别不均衡时需分层抽样。

## 概念与公式导航

- [[数据划分与预处理|数据划分与预处理]]
- [[数据预处理流水线|数据预处理流水线]]

## 练习

1. 预测学生成绩，列出 5 个可能相关的特征。
2. 一个数据集有 1000 条记录，建议怎样划分训练集、验证集和测试集？
3. 为什么测试集不能用于调参？
4. 思考大模型预训练中特征和标签分别是什么。

## 来源

- Bishop, C. M. (2006). *Pattern Recognition and Machine Learning*. Springer. 第1章 `[A]`
- Goodfellow, I., Bengio, Y., & Courville, A. (2016). *Deep Learning*. MIT Press. 第5章 `[A]`