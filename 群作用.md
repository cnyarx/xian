---
aliases:
  - Group Action
  - Group Actions
english_name: Group Action
tags:
  - 数学
  - 群论
  - 几何学
  - 弦理论先修
  - 弦理论
prerequisites:
  - "[[集合]]"
  - "[[函数]]"
  - "[[群]]"
  - "[[Lie 群]]"
  - "[[拓扑空间]]"
  - "[[流形]]"
used_by:
  - "[[轨道]]"
  - "[[稳定子群]]"
  - "[[商空间]]"
  - "[[模空间]]"
  - "[[群表示]]"
  - "[[规范对称性]]"
  - "[[世界面微分同胚]]"
  - "[[模空间积分]]"
evidence_status: 教材事实
---

# 群作用（Group Action）

## 一句话解释

群作用是让群中的每个元素都变成对象上的一个可逆变换，并要求群乘法与连续做变换完全一致。

## 为什么要学它

群本身只描述抽象的组合规则。群作用告诉我们这个抽象群具体怎样移动点、旋转图形、变换向量或重新描述物理场。

通过群作用可以定义：

- 一个点能被变到哪些位置，也就是轨道；
- 哪些群元素保持某点不动，也就是稳定子群；
- 把对称相关的对象视为同一个，也就是商空间；
- 对象有多少真正不同的自由度；
- 群表示、齐性空间和模空间；
- 物理中的全局对称、规范冗余和微分同胚。

## 直观图景

考虑正方形。把它旋转 $90^\circ$、$180^\circ$、$270^\circ$ 或保持不动，都会让正方形与自身重合。

这些旋转构成一个群。每个群元素都对正方形的顶点集合执行一个变换，这就是群作用。

连续做两次旋转，应等于先把对应群元素相乘，再做一次旋转。群作用的公理正是在保证这件事。

## 左群作用的正式定义

设 $G$ 是群，$X$ 是集合。一个左群作用是映射：

$$
\Phi:G\times X\to X,
$$

常简写为：

$$
(g,x)\longmapsto g\cdot x,
$$

满足：

$$
e\cdot x=x,
$$

$$
(g_1g_2)\cdot x
=g_1\cdot(g_2\cdot x)
$$

对所有 $g_1,g_2\in G$ 和 $x\in X$ 成立。

第一条说单位元不改变对象；第二条说群乘法与变换复合一致。

## 每个符号是什么意思

- $G$：执行变换的群；
- $X$：被变换的集合或空间；
- $e$：群的单位元；
- $g\cdot x$：群元素 $g$ 作用在点 $x$ 后的结果；
- $g_1g_2$：群乘法；
- $G\times X$：群元素与空间点组成的有序对集合；
- $\Phi$：作用映射；
- $\operatorname{Orb}(x)$ 或 $G\cdot x$：点 $x$ 的轨道；
- $G_x$：点 $x$ 的稳定子群；
- $X/G$：按群轨道取商得到的轨道空间。

## 为什么每个群元素给出可逆变换

固定 $g\in G$，定义：

$$
\Phi_g:X\to X,
\qquad
\Phi_g(x)=g\cdot x.
$$

群元素 $g$ 有逆元 $g^{-1}$。由作用公理：

$$
g^{-1}\cdot(g\cdot x)
=(g^{-1}g)\cdot x
=e\cdot x=x.
$$

同理：

$$
g\cdot(g^{-1}\cdot x)=x.
$$

所以 $\Phi_g$ 的逆映射是 $\Phi_{g^{-1}}$。群作用不会把两个可区分点不可逆地压成同一点。

## 用同态重新表述

所有 $X$ 上的双射构成置换群：

$$
\operatorname{Sym}(X).
$$

左群作用等价于群同态：

$$
\rho:G\to\operatorname{Sym}(X),
$$

满足：

$$
\rho(g_1g_2)
=\rho(g_1)\circ\rho(g_2).
$$

这里：

$$
\rho(g)(x)=g\cdot x.
$$

这个观点说明，群作用就是把抽象群实现为一组具体可逆变换。

## 平凡作用

任何群都能在任何集合上作平凡作用：

$$
g\cdot x=x
$$

对所有 $g,x$ 成立。

此时每个点都不动，每条轨道只含一个点。

平凡作用合法，但没有体现群的非平凡结构。

## 正整数上的符号作用

群：

$$
\mathbb Z_2=\{+1,-1\}
$$

可作用在实数集合 $\mathbb R$ 上：

$$
\epsilon\cdot x=\epsilon x.
$$

轨道通常是：

$$
\{x,-x\}.
$$

但 $x=0$ 的轨道只有：

$$
\{0\},
$$

因为反射仍把零送到零。

这个例子展示：不同点的稳定子群可以不同，商空间附近的结构也会不同。

## 旋转对圆周的作用

圆周群：

$$
U(1)=\{e^{i\theta}\mid\theta\in\mathbb R\}
$$

作用在单位圆 $S^1$ 上：

$$
e^{i\theta}\cdot z
=e^{i\theta}z.
$$

任意一点都能被旋转到圆周上的任意另一点，所以作用是传递的。

每个点只有单位旋转保持不动，因此稳定子群是平凡的。

## 对平面的旋转作用

旋转群 $SO(2)$ 作用在 $\mathbb R^2$：

$$
R\cdot v=Rv.
$$

原点轨道为单点：

$$
SO(2)\cdot0=\{0\}.
$$

非零向量 $v$ 的轨道是以原点为圆心、半径 $\|v\|$ 的圆。

所以轨道把平面按半径分层。

## 轨道

点 $x\in X$ 的轨道定义为：

$$
G\cdot x
=\{g\cdot x\mid g\in G\}.
$$

它收集从 $x$ 出发，通过群作用能到达的全部点。

定义关系：

$$
x\sim y
\quad\Longleftrightarrow\quad
\text{存在 }g\in G\text{ 使 }y=g\cdot x.
$$

这是等价关系：

- 单位元给出自反性；
- 逆元给出对称性；
- 群乘法给出传递性。

所以轨道正是等价类。

## 稳定子群

点 $x$ 的稳定子群定义为：

$$
G_x
=\{g\in G\mid g\cdot x=x\}.
$$

它由所有保持 $x$ 不动的群元素组成。

$G_x$ 确实是子群：

- $e\in G_x$；
- 若 $g,h\in G_x$，则 $gh\in G_x$；
- 若 $g\in G_x$，则 $g^{-1}\in G_x$。

稳定子群也称 isotropy subgroup 或 little group，具体术语依领域而定。

## 轨道—稳定子定理

若 $G$ 是有限群，则：

$$
|G\cdot x|
=\frac{|G|}{|G_x|}.
$$

等价地：

$$
|G|=|G\cdot x|\,|G_x|.
$$

原因是轨道与左陪集集合 $G/G_x$ 一一对应。

这说明对称性越多，也就是 $G_x$ 越大，轨道通常越小。

## 一个有限群计算例子

正方形的旋转群：

$$
C_4=\{e,r,r^2,r^3\}
$$

作用在四个顶点上。

固定一个顶点 $v$。只有单位元保持它不动，因此：

$$
|G_v|=1.
$$

由轨道—稳定子定理：

$$
|G\cdot v|
=\frac4{1}=4.
$$

所以一个顶点的轨道包含全部四个顶点。

## 传递作用

以下关于传递、自由与忠实作用的讨论默认 $X$ 非空。

若任意 $x,y\in X$ 都存在 $g\in G$ 使：

$$
g\cdot x=y,
$$

则称作用是传递的。

等价地，$X$ 恰好只有一条轨道。

固定 $x_0\in X$ 后，传递的 $G$-集合可写成：

$$
X\cong G/G_{x_0}.
$$

这就是齐性空间的基本形式。

## 自由作用

若：

$$
g\cdot x=x
$$

只可能在 $g=e$ 时成立，则称作用自由。

等价地，每个稳定子群都平凡：

$$
G_x=\{e\}.
$$

自由不等于传递：

- 自由关心有没有非单位元素固定点；
- 传递关心能否从一个点到达所有点。

## 忠实作用

若只有单位元对所有点都不起作用，则称作用忠实。

作用的核为：

$$
\ker\rho
=\{g\in G\mid g\cdot x=x
\text{ 对所有 }x\in X\}.
$$

忠实等价于：

$$
\ker\rho=\{e\}.
$$

在 $X$ 非空的前提下，自由作用一定忠实，但忠实作用不一定自由。一个非单位元素可能固定某些点，只要它不固定全部点即可。

## 有效作用

在几何文献中，effective action 通常就是 faithful action，即作用核平凡。

若核不平凡，可让商群：

$$
G/\ker\rho
$$

在 $X$ 上忠实作用，而不改变实际发生的点变换。

## 右群作用

右作用写成：

$$
x\cdot g,
$$

并满足：

$$
(x\cdot g_1)\cdot g_2
=x\cdot(g_1g_2).
$$

固定 $g$ 并定义 $R_g(x)=x\cdot g$，则：

$$
R_{g_1g_2}=R_{g_2}\circ R_{g_1}.
$$

所以 $g\mapsto R_g$ 对通常的复合顺序是反同态；等价地，它可看成从反群 $G^{\mathrm{op}}$ 到 $\operatorname{Sym}(X)$ 的同态。

左作用和右作用的复合顺序不同，不能在推导中随意混用。

给定右作用，可定义左作用：

$$
g\star x=x\cdot g^{-1}.
$$

逆元保证左作用公理成立。

## 连续群作用

若 $G$ 是拓扑群，$X$ 是拓扑空间，并且：

$$
\Phi:G\times X\to X
$$

连续，则称为连续群作用。

此时轨道空间 $X/G$ 带商拓扑。

即使 $G$ 和 $X$ 都很好，商空间也可能不满足 Hausdorff 性或流形条件，所以构造商时必须检查额外性质。

## 光滑群作用

若 $G$ 是 Lie 群，$M$ 是光滑流形，且：

$$
\Phi:G\times M\to M
$$

光滑，则称 $G$ 光滑作用在 $M$ 上。

固定 $g$ 后：

$$
\Phi_g:M\to M
$$

是微分同胚，逆映射是 $\Phi_{g^{-1}}$。

连续一参数子群会产生向量场，也就是无穷小生成元。

## 无穷小生成元

取 Lie 代数元素：

$$
\xi\in\mathfrak g.
$$

它生成一参数子群：

$$
\exp(t\xi)\in G.
$$

对应的基本向量场可定义为：

$$
\xi_M(x)
=\left.\frac{d}{dt}\right|_{t=0}
\exp(t\xi)\cdot x.
$$

按这里采用的左作用与正号定义，Lie 括号满足：

$$
[\xi_M,\eta_M]
=-[\xi,\eta]_M.
$$

因此 $\xi\mapsto\xi_M$ 是 Lie 代数反同态。若在基本向量场的定义前加入负号，则得到同态。不同教材可能使用不同符号约定，引用括号公式前必须核对定义。

## 等距作用

若流形带度量 $g$，群作用满足：

$$
\Phi_h^*g=g
$$

对每个 $h\in G$ 成立，则称群作等距作用。

此时群元素保持长度、角度和测地距离。

连续等距作用的无穷小生成元是 Killing 向量场，满足：

$$
\mathcal L_{\xi_M}g=0.
$$

## 群表示是线性群作用

若 $V$ 是向量空间，群作用还保持向量加法和数乘，则每个群元素给出可逆线性映射：

$$
\rho(g)\in GL(V).
$$

于是：

$$
\rho:G\to GL(V)
$$

是群表示。

所以群表示不是与群作用无关的另一件事，而是群在线性空间上的作用。

## 共轭作用

群 $G$ 可作用在自身上：

$$
g\cdot x=gxg^{-1}.
$$

这称为共轭作用。

点 $x$ 的轨道是共轭类，稳定子群是中心化子：

$$
C_G(x)
=\{g\in G\mid gx=xg\}.
$$

共轭类和中心化子在群结构、Lie 群和规范理论中都很重要。

## 群作用在函数上

若 $G$ 作用在 $X$ 上，可诱导其在函数 $f:X\to\mathbb R$ 上的作用。常见左作用约定为：

$$
(g\cdot f)(x)
=f(g^{-1}\cdot x).
$$

逆元使：

$$
(g_1g_2)\cdot f
=g_1\cdot(g_2\cdot f).
$$

函数不变意味着：

$$
g\cdot f=f
$$

对所有 $g$ 成立，等价于 $f$ 在每条轨道上取相同值。

## 商集合与轨道空间

把同一轨道中的点视为等价，得到：

$$
X/G
=\{G\cdot x\mid x\in X\}.
$$

它的元素不是原来的点，而是整条轨道。

例如 $SO(2)$ 作用在 $\mathbb R^2$ 上时，轨道由半径分类，因此轨道空间可与：

$$
[0,\infty)
$$

对应。

原点的稳定子群比非零点大，所以商空间的原点是一个特殊轨道类型。

## 商空间何时仍是流形

若 Lie 群 $G$ 光滑、自由且 proper 地作用在标准光滑流形 $M$ 上，则商空间：

$$
M/G
$$

存在唯一的自然光滑流形结构，使投影：

$$
\pi:M\to M/G
$$

成为光滑浸没，并且：

$$
\dim(M/G)=\dim M-\dim G.
$$

将左作用改写为右作用 $x\cdot g=g^{-1}\cdot x$ 后，$\pi$ 构成主 $G$-丛。

“proper”是控制轨道聚集行为的拓扑条件。常用定义要求映射：

$$
G\times M\to M\times M,
\qquad
(g,x)\mapsto(g\cdot x,x)
$$

是 proper 映射。

自由但不 proper 时，商可能非 Hausdorff，且投影未必构成主丛；proper 但不自由时，非平凡稳定子通常导致 orbifold 或更一般的分层商，不过底层拓扑空间在特殊例子中仍可能是流形。

## 非自由作用与 orbifold

若 Lie 群作用 proper 且局部自由，也就是每个稳定子群都有限，则商通常具有 orbifold 结构。

由 slice 定理，轨道像附近的商局部模型为：

$$
V/G_x,
$$

其中 $V$ 是横截于轨道的 slice，$G_x$ 是有限稳定子群。离散群适当不连续作用的情形也给出熟悉的局部有限群商模型。

若作用存在对所有点都不起作用的有限核，得到的可称为非有效 orbifold；若要求有效作用，可先除去这个核。

若稳定子群具有正维数或轨道类型发生变化，商空间还可能具有更复杂的分层奇点。

所以“按对称性取商”不自动得到光滑流形。

## 轨道维数

对光滑 Lie 群作用，稳定子 $G_x$ 是闭 Lie 子群，轨道 $G\cdot x$ 是浸入子流形，并可识别为：

$$
G\cdot x\cong G/G_x.
$$

因此：

$$
\dim(G\cdot x)
=\dim G-\dim G_x.
$$

若作用 proper，轨道还是嵌入的闭子流形。

稳定子维数改变会导致轨道维数改变；更细的轨道类型由稳定子群的共轭类决定。即使两个稳定子维数相同，只要它们不共轭，仍可能属于不同轨道类型。

## 规范对称中的群作用

在选定场的函数空间和边界条件后，规范群 $\mathcal G$ 作用在场配置空间 $\mathcal A$ 上。若两个配置由允许的规范变换联系，它们通常代表同一物理配置的不同描述。

相应的规范轨道空间形式上写作：

$$
\mathcal A/\mathcal G.
$$

若研究经典解的模空间，则通常先取满足场方程或约束的解集，再除以允许的规范变换。

哪些变换属于规范冗余取决于边界条件；在边界或无穷远处非平凡的变换有时是携带守恒荷的物理对称，不能机械地全部商去。

这些商往往是无限维的，并可能具有固定点、零模、分层奇点和 Gribov 问题，不能简单当作普通有限维光滑流形。严格处理可能需要分层空间、商 stack 或其他更精细结构。

[[规范固定]]、[[Faddeev-Popov 行列式]] 和 BRST 方法都与如何处理这个群作用有关。

## 微分同胚作用

微分同胚群：

$$
\operatorname{Diff}(M)
$$

作用在流形上的张量场。例如对度量：

$$
g\longmapsto\phi^*g.
$$

在广义相对论或世界面理论中，微分同胚常作为描述冗余的一部分。

但“主动变换”和“被动换坐标”的符号与解释必须保持一致；不能把所有坐标变化都机械地当作新的物理状态变化。

## Weyl 变换不是普通点作用

世界面 Weyl 变换：

$$
h_{ab}(\sigma)
\longmapsto e^{2\omega(\sigma)}h_{ab}(\sigma)
$$

作用在度量场的配置空间，而不是把世界面点 $\sigma$ 移到另一个点。

它仍可用群作用语言描述，但作用对象是场，而不是底层点集合。

这说明同一个群作用框架可作用在点、向量、函数、张量或整个场配置空间上。

## 黎曼曲面上的自同构群

黎曼曲面的双全纯自同构组成群：

$$
\operatorname{Aut}(\Sigma).
$$

它作用在曲面点和标记点配置上。

Riemann 球面的自同构群是 Möbius 群：

$$
PSL(2,\mathbb C).
$$

对任意三个互异的有序标记点，存在唯一的 Möbius 变换将它们依次映到 $0,1,\infty$。因此球面相关函数中，可利用全局共形对称选定三个互异插入点的位置。

## 环面上的模群作用

复环面模参数：

$$
\tau\in\mathbb H
$$

位于上半平面。常把模群写作 $SL(2,\mathbb Z)$，其作用为：

$$
\tau\longmapsto
\frac{a\tau+b}{c\tau+d},
$$

其中：

$$
\begin{pmatrix}a&b\\c&d\end{pmatrix}
\in SL(2,\mathbb Z).
$$

矩阵 $A$ 与 $-A$ 对 $\tau$ 产生相同变换，因此该作用的核为 $\{\pm I\}$，有效作用群是：

$$
PSL(2,\mathbb Z)
=SL(2,\mathbb Z)/\{\pm I\}.
$$

模群相关的 $\tau$ 描述同一个格的不同整数基，因此物理振幅应按该作用正确变换或保持不变，并避免对等价区域重复积分。

## 模空间是“解空间再取商”

许多模空间具有形式：

$$
\mathcal M
=\frac{\{\text{满足方程或结构条件的对象}\}}
{\{\text{等价变换}\}}.
$$

分母中的等价变换由群或群胚作用描述。

例如固定亏格黎曼曲面的模空间，把复结构按适当微分同胚等价取商；规范场模空间把方程解按规范变换取商。

固定点会让商空间出现 orbifold 或 stack 结构，所以模空间不总是普通流形。

## Burnside 引理

有限群 $G$ 作用在有限集合 $X$ 上时，轨道数量为：

$$
|X/G|
=\frac1{|G|}
\sum_{g\in G}|X^g|,
$$

其中固定点集合：

$$
X^g
=\{x\in X\mid g\cdot x=x\}.
$$

它说轨道数等于各群元素固定点数的平均值。

这是用对称性计数不同配置的基本工具。

## Burnside 引理的简单例子

用两种颜色给正方形四个顶点染色，只把旋转相关的染色视为相同。

旋转群 $C_4$ 的四个元素固定染色数为：

- 恒等旋转：$2^4=16$；
- $90^\circ$ 旋转：$2$；
- $180^\circ$ 旋转：$2^2=4$；
- $270^\circ$ 旋转：$2$。

所以不同旋转等价类数量为：

$$
\frac{16+2+4+2}{4}=6.
$$

## 适用条件与事实边界

1. 群作用要求单位元作用恒等，并与群乘法复合一致。
2. 每个群元素自动给出可逆变换，逆变换由群逆元给出。
3. 左作用和右作用的复合顺序不同，必须明确约定。
4. 轨道是群作用产生的等价类；稳定子群记录保持一点不动的对称。
5. 有限群的轨道—稳定子公式使用集合基数；光滑 Lie 群作用的轨道维数公式来自 $G\cdot x\cong G/G_x$。
6. 自由、传递和忠实是不同性质；“自由蕴含忠实”等表述默认被作用集合非空。
7. 群表示是向量空间上的线性群作用。
8. 连续或光滑群作用要求作用映射本身连续或光滑。
9. 自由且 proper 的光滑 Lie 群作用给出自然光滑商流形；自由但不 proper 时商可能非 Hausdorff。
10. proper 且局部自由的作用通常给出 orbifold；更一般的非自由商可需要分层空间或 stack。
11. 规范群和微分同胚群通常作用在无限维场配置空间上；边界条件决定哪些变换是规范冗余，形式商可能需要更精细数学结构。
12. 按本文左作用和正号基本向量场定义，$\xi\mapsto\xi_M$ 是 Lie 代数反同态；其他符号约定可改变这个负号。
13. 模空间的等价关系常由群作用产生，但存在自同构时应保留稳定子信息。
14. $SL(2,\mathbb Z)$ 对环面参数的作用核为 $\{\pm I\}$，所以有效作用群是 $PSL(2,\mathbb Z)$。
15. Burnside 引理只直接适用于有限群作用在有限集合上的计数。

## 常见误区

1. **群作用就是群乘法。** 群乘法发生在 $G$ 内部；群作用把 $G$ 的元素变成 $X$ 上的变换。
2. **任何群元素都可能把两个点合并。** 群元素的作用一定可逆，因此固定元素对应双射。
3. **轨道是群的子群。** 轨道是 $X$ 的子集；稳定子才是 $G$ 的子群。
4. **自由作用就是只有一条轨道。** 只有一条轨道叫传递；自由指没有非单位固定元。
5. **忠实作用意味着每个点的稳定子都平凡。** 忠实只要求没有非单位元素固定所有点。
6. **取商一定得到更低维光滑流形。** 非适当作用可非 Hausdorff，非自由作用可产生奇点。
7. **群表示与群作用是两套无关概念。** 群表示就是线性空间上的群作用。
8. **规范变换把一个物理状态变成另一个可观测状态。** 在通常规范理论中，规范相关配置代表同一物理配置的不同描述。
9. **Weyl 变换就是移动世界面上的点。** 它变换度量场，不移动底层点。
10. **模群相关的环面参数应重复积分。** 它们代表等价复结构，应选择基本域或正确除以冗余。
11. **模空间总是普通流形。** 自同构和退化可要求 orbifold、stack 或紧化空间。
12. **所有无限维规范商都可直接使用有限维轨道—稳定子公式。** 无限维分析还需函数空间、边界条件和算符性质。

## 练习

1. 写出左群作用的两条公理。
2. 为什么固定群元素的作用一定是双射？
3. 群作用怎样等价地写成到 $\operatorname{Sym}(X)$ 的同态？
4. 写出点 $x$ 的轨道和稳定子群。
5. 证明“在同一轨道中”是等价关系。
6. 有限群的轨道—稳定子公式是什么？
7. 自由、传递和忠实分别表示什么？
8. 在 $X$ 非空时，为什么自由作用一定忠实，反过来不一定？
9. 右作用怎样转成左作用？
10. 连续群作用和光滑群作用额外要求什么？
11. 按本文约定，Lie 代数元素怎样产生基本向量场，其 Lie 括号满足什么关系？
12. 群表示为什么是群作用的特例？
13. 共轭作用的轨道和稳定子分别是什么？
14. 为什么函数上的诱导左作用要写 $f(g^{-1}\cdot x)$？
15. 什么条件下 $M/G$ 是自然的光滑流形？
16. proper 但非自由的作用为何可能产生 orbifold 或分层商？
17. 规范轨道空间和经典解模空间在形式上分别怎样表示？
18. Weyl 变换作用在什么对象上？
19. 模群怎样作用在环面参数 $\tau$ 上？为什么有效作用群是 $PSL(2,\mathbb Z)$？
20. 用 Burnside 引理计算两色正方形顶点染色在旋转下的等价类数。

## 答案与提示

1. $e\cdot x=x$；$(g_1g_2)\cdot x=g_1\cdot(g_2\cdot x)$。
2. $g^{-1}$ 的作用是 $g$ 作用的逆映射。
3. $\rho:G\to\operatorname{Sym}(X)$ 且 $\rho(g_1g_2)=\rho(g_1)\circ\rho(g_2)$。
4. $G\cdot x=\{g\cdot x\mid g\in G\}$；$G_x=\{g\in G\mid g\cdot x=x\}$。
5. 分别用单位元、逆元和群乘法证明自反、对称、传递。
6. $|G\cdot x|=|G|/|G_x|$。
7. 自由：各稳定子平凡；传递：恰好一条轨道；忠实：作用核平凡。
8. 自由禁止非单位元素固定任何点，所以更不可能固定所有点；忠实仍允许固定部分点。非空条件排除了空集合上的真空例外。
9. 定义 $g\star x=x\cdot g^{-1}$；固定 $g$ 的右作用映射满足 $R_{g_1g_2}=R_{g_2}\circ R_{g_1}$。
10. 要求 $G\times X\to X$ 连续，或 $G\times M\to M$ 光滑。
11. $\xi_M(x)=\left.\frac{d}{dt}\right|_0\exp(t\xi)\cdot x$；按本文左作用与正号定义，$[\xi_M,\eta_M]=-[\xi,\eta]_M$。
12. 它是保持向量空间线性结构的作用，即同态 $G\to GL(V)$。
13. 轨道是共轭类，稳定子是中心化子。
14. 逆元保证群乘法顺序与函数变换复合满足左作用公理。
15. Lie 群光滑、自由且 proper 地作用在标准光滑流形上时；商投影是光滑浸没。
16. 非平凡稳定子改变横截方向的局部识别，slice 局部模型为 $V/G_x$；稳定子类型变化还会产生分层。
17. 场配置的规范轨道空间形式上是 $\mathcal A/\mathcal G$；经典解模空间通常是满足方程或约束的解集再除以允许的规范变换。
18. 作用在世界面度量场的配置空间：$h_{ab}\mapsto e^{2\omega}h_{ab}$。
19. $\tau\mapsto(a\tau+b)/(c\tau+d)$，矩阵属于 $SL(2,\mathbb Z)$；$A$ 与 $-A$ 作用相同，故有效群为 $PSL(2,\mathbb Z)$。
20. $(16+2+4+2)/4=6$。

## 与其他概念的关系

- [[集合]] 与 [[函数]] 给出作用映射的基础语言；
- [[群]] 提供单位元、逆元和群乘法；
- [[Lie 群]] 的光滑作用产生无穷小生成向量场；
- [[拓扑空间]] 与 [[流形]] 用于定义连续、光滑和适当作用；
- [[轨道]] 收集一个对象在群作用下的全部等价形态；
- [[稳定子群]] 记录对象保留的对称；
- [[群表示]] 是在线性空间上的群作用；
- [[商空间]] 把同一轨道视为一个点；
- [[模空间]] 常把满足条件的结构按等价群作用取商；
- [[旋转矩阵与位置编码数学|旋转矩阵与位置编码数学]] 中，整数位置通过 $m\mapsto R(m\theta)$ 作用在线性表示上，是群作用与群表示的具体有限维例子；无位置输入的自注意力对 token 排列具有置换等变结构。RoPE 的旋转规则和注意力的置换性质只是群作用的应用，不等同于规范对称性或世界面模群；
- [[黎曼曲面]] 的自同构群与模群都通过群作用出现；
- [[规范对称性]] 把规范相关场配置识别为同一物理配置；
- [[世界面微分同胚]] 作用在世界面点与场上；
- [[Weyl 对称性]] 作用在世界面度量场上；
- [[BRST 对称性]] 用代数方法处理规范群作用及其冗余；
- [[模空间积分]] 要避免对群作用相关的等价结构重复计数。

## 参考资料

- Joseph J. Rotman, *An Introduction to the Theory of Groups*, 4th ed., Springer, 1995.
- John B. Fraleigh, *A First Course in Abstract Algebra*, 7th ed., Pearson, 2003.
- Dummit and Foote, *Abstract Algebra*, 3rd ed., Wiley, 2004.
- John M. Lee, *Introduction to Smooth Manifolds*, 2nd ed., Springer, 2013, Chapters 7 and 21.
- Glen E. Bredon, *Introduction to Compact Transformation Groups*, Academic Press, 1972.
- Richard S. Palais, “On the Existence of Slices for Actions of Non-Compact Lie Groups,” *Annals of Mathematics* 73 (1961), 295–323, [DOI](https://doi.org/10.2307/1970335)
- Mikio Nakahara, *Geometry, Topology and Physics*, 2nd ed., CRC Press, 2003.
- Theodore Frankel, *The Geometry of Physics*, 3rd ed., Cambridge University Press, 2012.
- Michael Henneaux and Claudio Teitelboim, *Quantization of Gauge Systems*, Princeton University Press, 1992.
- Joseph Polchinski, *String Theory, Vol. 1*, Cambridge University Press, 1998.
- David Tong, *String Theory*, arXiv:0908.0333, [arXiv](https://arxiv.org/abs/0908.0333)
