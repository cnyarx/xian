---
aliases: [计算图, 自动微分, 梯度累加, Computational Graph, Automatic Differentiation]
tags: [概念, 微积分, 神经网络, 大模型, 学习教程]
level: L7
prerequisites: ["[[变量与函数]]", "[[极限与趋近]]", "[[逐元素运算与广播]]"]
---
# 计算图、自动微分与梯度累加

## 一句话定义

计算图（Computational Graph）把复杂计算拆成小步骤和依赖箭头；自动微分按这些步骤应用链式法则；当一个变量通过多条路径影响损失时，各路径梯度需要相加。

## 小计算图

$$
a=wx,\qquad b=a+1,\qquad L=b^2
$$

图可写成：

```text
w ─┐
   × → a → +1 → b → 平方 → L
x ─┘
```

反向计算：

$$
\frac{\partial L}{\partial b}=2b,\quad
\frac{\partial b}{\partial a}=1,\quad
\frac{\partial a}{\partial w}=x
$$

所以 $\partial L/\partial w=2bx$。

## 多路径为何相加

若 $L=x^2+3x$，$x$ 通过两条支路影响 $L$：

$$
\frac{dL}{dx}=2x+3
$$

反向传播不仅是“把导数一路乘回去”，还要在支路汇合处相加。

## 自动微分不是什么

- 不是用很小差分近似所有导数；
- 不是符号代数把整个公式化简；
- 它使用程序中已知基本操作的精确局部导数，按图组合。

## 与 PyTorch 的关系

PyTorch 默认把多次 `backward()` 得到的梯度累加到 `.grad`，所以普通训练步前需要 `optimizer.zero_grad()`；有意做梯度累积时则按计划延后清零。

## 跨学科边界

[[Feynman 图|Feynman 图]] 与计算图都能用节点和边压缩复杂表达式的记账过程，但二者不是同一种图：计算图描述一次确定计算的依赖并沿边应用链式法则；Feynman 图枚举量子场论微扰项、传播子、顶点和圈积分。传播子不等于反向传播，Feynman 图也不负责自动微分。

## 最小自测

题目：$L=(x+1)^2+(2x)^2$，求 $dL/dx$。

答案：第一条路径给 $2(x+1)$，第二条给 $8x$，相加得 $10x+2$。

## 来源

- Baydin et al. (2018), *Automatic Differentiation in Machine Learning: a Survey*：https://jmlr.org/papers/v18/17-468.html `[A]`
- PyTorch, Autograd mechanics：https://pytorch.org/docs/stable/notes/autograd.html `[A]`
