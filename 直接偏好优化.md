---
aliases: [DPO, ORPO, 直接偏好优化]
tags: [大模型, 对齐, 教程, 学习教程]
prerequisites: ["[[监督微调]]", "[[人类反馈强化学习]]", "[[似然最大似然与负对数似然|似然、最大似然与负对数似然]]", "[[激活函数|激活函数]]"]
next: ["[[组相对策略优化]]"]
---
# 直接偏好优化

## 一句话定义

DPO 不训练奖励模型，直接从偏好比较中优化模型，使偏好回答的概率相对非偏好回答更高。

## DPO 损失

$$\mathcal{L}_{\text{DPO}}=-\log\sigma\left(\beta\log\frac{\pi_\theta(y_w\mid x)}{\pi_{\text{ref}}(y_w\mid x)}-\beta\log\frac{\pi_\theta(y_l\mid x)}{\pi_{\text{ref}}(y_l\mid x)}\right)$$

- $y_w,y_l$：偏好和非偏好回答。
- $\pi_\theta$：当前模型，$\pi_{\text{ref}}$：参考模型。
- $\sigma$：sigmoid 函数。
- $\beta$：控制偏离参考模型的程度。

**这条公式在说**：希望当前模型增加偏好回答相对于参考模型的概率，或减少非偏好回答的相对概率，或两者同时发生。

完整推导、ORPO odds 项和实现边界见[[DPO与ORPO损失公式|DPO 与 ORPO 损失公式]]。

## 数值例子

假设当前模型相对参考模型：

- 把偏好回答概率从 0.20 提高到 0.30；
- 把非偏好回答概率从 0.20 降到 0.10。

相对对数优势为：

$$
\Delta=\ln(0.30/0.20)-\ln(0.10/0.20)=\ln3\approx1.099
$$

取 $\beta=1$：

$$
\mathcal L_{DPO}=-\ln\sigma(1.099)\approx0.288
$$

若模型反而更偏向非偏好回答，$\Delta$ 会减小，损失会增大。

## 与 RLHF 的区别

- DPO 不需要单独训练奖励模型。
- 损失直接在偏好对上定义，训练更简单。
- 离线训练：数据来自参考模型或外部模型，不实时生成新回答。

## ORPO

ORPO 在 SFT 损失上直接加入偏好惩罚，不需要参考模型：

$$\mathcal{L}_{\text{ORPO}}=\mathcal{L}_{\text{SFT}}+\lambda\mathcal{L}_{\text{ODDS}}$$

其中 odds 项惩罚生成非偏好回答。训练更简单，但效果取决于数据质量和任务。

## 与 SFT 的关系

DPO 通常接在监督微调之后：SFT 先教会模型基本的指令格式和目标行为，DPO 再利用偏好对调整优选回答与非偏好回答的相对概率。若基础模型尚未学会任务格式，单独使用 DPO 往往不能补足这类示范信号。

ORPO 的目标本身包含 $\mathcal L_{\text{SFT}}$，可在同一个训练阶段同时学习指令回答和偏好排序，因此不要求预先单独完成一个 SFT 阶段。它仍需要带有期望回答和偏好关系的训练数据，不能理解成“不需要监督示范”。

## 常见误区

- DPO 不是“不需要数据”，只是不需要同时训练奖励模型。
- 偏好数据也需要质量控制。
- 不同算法的最佳 $\beta$ 和设定应通过实验确定，不是固定值。

## 概念与公式导航

- [[DPO与GRPO|DPO、ORPO 与 GRPO]]
- [[DPO与ORPO损失公式|DPO 与 ORPO 损失公式]]

## 练习

1. DPO 为什么不需要奖励模型？
2. 解释 DPO 损失中 $\beta$ 的作用。
3. 比较 DPO 和 ORPO 的异同。
4. DPO 为什么通常建立在 SFT 后？ORPO 为什么可以把 SFT 信号合并到同一阶段？

## 来源

- Rafailov, R. et al. (2023). *Direct Preference Optimization*. NeurIPS. https://arxiv.org/abs/2305.18290 `[A]`
- Hong, J. et al. (2024). *ORPO: Monolithic Preference Optimization without Reference Model*. https://arxiv.org/abs/2403.07691 `[A]`