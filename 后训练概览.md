---
aliases: [后训练, Post-training, Alignment]
tags: [大模型, 训练, 对齐, 教程, 学习教程]
prerequisites: ["[[预训练概览]]"]
next: ["[[监督微调]]"]
---
# 后训练概览

## 什么是后训练

预训练基础模型会继续生成，但不一定按指令回答、遵循偏好或避免不安全输出。后训练用额外数据和目标调整行为。

## 主要阶段

- **SFT**：用指令—回答示范训练，教模型以目标格式响应。
- **偏好对齐**：让模型输出更符合人类或规则偏好，常见方法包括 RLHF、DPO、ORPO 等。
- **推理训练**：用过程监督或可验证奖励训练特定问题求解行为。

这些不是每个模型都必须按同一顺序经历的统一流水线。发布者可能组合、重复或省略某些阶段，具体事实必须以模型报告为准。

## 一个行为分布例子

预训练模型看到：

```text
法国的首都是
```

可能自然续写“巴黎”。但看到：

```text
请只返回 JSON：法国的首都是哪里？
```

基础模型不一定稳定遵守 JSON 格式。SFT 可以用大量“指令 → 合规格式回答”示范提高这种行为概率。

偏好训练还可比较两个都能回答的候选：一个简洁、有依据，另一个啰嗦或包含无法核实内容。训练目标调整的是这些候选的相对概率，不是向模型数据库中插入一条可精确读取的规则。

## 后训练改变什么

后训练会更新参数，因此并非完全不影响知识和语言能力。更准确地说，它通常以较少、目标更集中的数据调整预训练模型的行为分布：格式、语气、拒答、工具调用和特定任务表现。

若基础模型缺乏某领域表征，仅靠少量后训练数据难以可靠补全；若数据过窄或学习率不当，也可能遗忘原能力、过拟合模板或产生新的偏差。

## 训练信号来源

- 人工标注示例；
- 人类偏好比较；
- AI 模型生成并经筛选的合成数据；
- 可验证奖励，如代码测试、数学答案或规则检查；
- 安全原则、红队样本和拒答示范。

每种信号都可能出错。人工标注有分歧，模型生成数据会复制教师错误，自动验证器可能覆盖不全，规则奖励也可能被钻空子。

## 数据与目标的区别

同一条偏好数据可以支持不同训练目标：

- 先训练奖励模型，再用 PPO 优化策略；
- 直接使用 DPO 类损失调整优选与拒选回答的相对概率；
- 转成带权监督样本。

因此看到“使用了偏好数据”不能直接推出使用了 RLHF 或某个具体优化器。

## 评测

后训练前后至少比较：

- 指令遵循和格式正确率；
- 目标任务能力；
- 事实性与幻觉；
- 安全拒答的正确和误拒；
- 原有通用能力是否退化；
- 不同语言、群体和长输入下的表现。

只展示少数聊天样例不能证明整体改进。

## 证据边界

权重和配置只能说明当前模型结构，不能直接推出训练数据、偏好数据或奖励函数细节。没有官方说明时，应标注为未知。模型自己回答“我是用 RLHF 训练的”也不是训练 provenance（数据来源与出处追踪）的可靠证据。

## 概念与公式导航

- [[后训练与指令微调|后训练与指令微调]]

## 练习

1. 为什么基础模型不定向执行指令？
2. 列出四种后训练训练信号。
3. 后训练能补全基础模型缺失的知识吗？为什么？
4. 用一条具体例子，说明合成数据可能引入哪些风险。

## 来源

- Ouyang, L. et al. (2022). *Training Language Models to Follow Instructions with Human Feedback*. NeurIPS. https://arxiv.org/abs/2203.02155 `[A]`
- Bai, Y. et al. (2022). *Training a Helpful and Harmless Assistant with Reinforcement Learning from Human Feedback*. https://arxiv.org/abs/2204.05862 `[A]`