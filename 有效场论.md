---
aliases:
  - Effective Field Theory
  - EFT
  - 有效理论
english_name: Effective Field Theory
tags:
  - 物理学
  - 量子场论
  - 重整化群
  - 弦理论先修
  - 弦理论
prerequisites:
  - "[[经典场论]]"
  - "[[路径积分]]"
  - "[[微扰论]]"
  - "[[S 矩阵]]"
  - "[[正规化与重整化]]"
  - "[[重整化群]]"
  - "[[beta 函数]]"
used_by:
  - "[[Yang-Mills 理论]]"
  - "[[弦理论完整学习路线图]]"
evidence_status: 教材事实
---

# 有效场论（Effective Field Theory）

## 一句话解释

有效场论（effective field theory，EFT）是在指定低能范围和精度内，只保留可传播的低能自由度，并按对称性与幂次计数写出所有允许局域算符的理论；高能物理被编码进 Wilson 系数。

## 为什么要学它

基本理论可能含有实验无法直接产生的重粒子、无限高的激发塔或未知的短距离结构。低能实验通常不需要逐个解析这些高能细节，只需知道它们怎样改变低能相互作用。

有效场论回答六个实际问题：

1. 给定能标下哪些自由度应显式保留；
2. 对称性允许哪些局域算符；
3. 哪些项在目标精度下必须计算；
4. 怎样从高能理论匹配出 Wilson 系数；
5. 怎样用 RG 把系数从高尺度运行到低尺度；
6. 怎样估计截断后遗漏项的大小。

[[正规化与重整化]] 解释怎样用反项得到有限预测，[[重整化群]] 和 [[beta 函数]] 解释参数怎样随尺度运行。本页把这些工具组织成完整的低能预测流程，不重复具体圈积分和 beta 系数推导。

## 直观图景

观察远处城市时，不必描述每块砖和每根钢筋；建筑轮廓、道路和平均材料性质已经足够。只有当观察分辨率提高，先前忽略的细节才逐渐可见。

EFT 中：

- 低能自由度像远处仍能分辨的建筑；
- 被排除的重自由度像看不清的内部结构；
- 局域高维算符记录高能结构对低能过程留下的痕迹；
- $E/\Lambda$ 决定需要保留多少细节。

这不是说高能自由度“不存在”，而是说在 $E\ll\Lambda$ 时，它们的传播不能被低能过程直接解析，影响可按局域算符展开。

## EFT 由什么定义

一个 EFT 不只是“一份删掉重粒子的 Lagrangian”。它至少由四部分共同定义：

1. **低能自由度**：在研究能标内可传播或必须保留的场；
2. **对称性**：包括精确对称性、近似对称性和对称性破缺方式；
3. **幂次计数**（power counting）：规定每个算符、圈和小参数属于哪一阶；
4. **截断阶数**：说明计算保留到何种精度。

若缺少幂次计数和截断规则，只写无限多个算符不能形成可执行的预测方案。

## 一般形式

在 $d$ 维时空中，可把 EFT Lagrangian 写成

$$
\boxed{
\mathcal L_{\mathrm{EFT}}
=
\mathcal L_{\mathrm{light}}^{(\Delta\le d)}
+
\sum_i
\frac{C_i(\mu)}{\Lambda^{\Delta_i-d}}
\mathcal O_i(\mu)
}.
$$

其中：

- $\mathcal L_{\mathrm{light}}^{(\Delta\le d)}$：由低能自由度构成的低维部分；
- $\mathcal O_i$：满足低能对称性的局域算符；
- $\Delta_i$：算符的 canonical dimension；
- $C_i(\mu)$：无量纲 Wilson 系数；
- $\Lambda$：EFT 中最近的被排除尺度或新物理尺度；
- $\mu$：重整化尺度，不等于 EFT 的有效范围上限。

$\Lambda$ 不一定是单个粒子的质量。它也可能由多粒子阈值、强耦合尺度、Kaluza–Klein 质量、弦激发尺度或其他最先失效的尺度决定。

## 质量量纲与算符分类

自然单位下作用量无量纲，因此

$$
[S]=0,
\qquad
[d^d x]=-d,
\qquad
[\mathcal L]=d.
$$

由标准动能项得到

$$
[\partial_\mu]=1,
\qquad
[\phi]=\frac{d-2}{2},
\qquad
[\psi]=\frac{d-1}{2},
\qquad
[A_\mu]=\frac{d-2}{2}.
$$

若 $[\mathcal O_i]=\Delta_i$，其有量纲系数满足

$$
[c_i]=d-\Delta_i.
$$

在四维弱耦合 Gaussian 幂次计数中：

- $\Delta_i<4$：relevant；
- $\Delta_i=4$：marginal；
- $\Delta_i>4$：irrelevant。

这是 canonical dimension 分类。固定点附近的严格 RG 分类应使用包含反常维数的完整 scaling dimension，不能把两者无条件混用。

## 幂次计数

对单次 $\Delta_i>d$ 算符插入，基础量纲估计为

$$
\boxed{
\mathcal A_i
\sim
C_i
\left(\frac{E}{\Lambda}\right)^{\Delta_i-d}
}.
$$

它说明高维算符通常在 $E\ll\Lambda$ 时受幂次压低，但还可能乘上：

- 普通耦合常数；
- 圈因子 $1/(16\pi^2)$；
- $\ln(E/\mu)$ 或 $\ln(\Lambda/E)$；
- 质量比；
- 对称性选择规则；
- 异常大的或异常小的 Wilson 系数。

因此算符维数只给基础排序，不自动决定数值大小。若低维算符被对称性禁止，高维算符也可能给出领先效应。强耦合理论还需另行指定手征、large-$N$ 或 naive dimensional analysis 等幂次计数。

EFT 的幂次计数与 Feynman 圈积分的表面发散度有关，但不是同一问题：前者决定低能振幅应保留哪些贡献，后者判断某张图的 UV 发散程度。

## 算符基与冗余

“写出所有允许算符”还要避免重复。常用等价关系包括：

- 分部积分；
- 低阶运动方程；
- 局域可逆场重定义；
- 指标恒等式、Fierz 恒等式和群论恒等式；
- 全导数和拓扑关系。

利用这些关系可选出独立算符基。被消去的冗余算符不是“从物理中神秘消失”，而是其效应可转移到其他系数或更高阶项；在固定截断阶数内，使用不同完备基应得到相同的 on-shell 可观测量。

## 积分掉重自由度

设 $\ell$ 表示轻场，$H$ 表示重场。有效作用量由路径积分定义：

$$
\boxed{
e^{iS_{\mathrm{eff}}[\ell]}
=
\int \mathcal D H\,
e^{iS[\ell,H]}
}.
$$

“积分掉”不等于把 $H$ 直接设为零。它会留下：

- 对低能参数的有限修正；
- 无限多个局域高维算符；
- 重场圈产生的行列式和量子修正；
- 阈值对数和方案依赖的 Wilson 系数。

只有当外部能量远低于重阈值、低能展开解析且可保持局域性时，重场影响才能系统地写成局域导数展开。

## 树级例子：积分掉一个重标量

考虑 mostly-minus 度规下的模型

$$
\mathcal L
=
\mathcal L_{\mathrm{light}}[\phi]
+
\frac12\partial_\mu H\partial^\mu H
-
\frac12 M^2H^2
-
gHJ(\phi),
$$

其中 $H$ 是质量为 $M$ 的重标量，$J(\phi)$ 是只含轻场的局域流。分部积分后，含 $H$ 的部分为

$$
\mathcal L_H
=
-\frac12H(\Box+M^2)H-gHJ.
$$

经典运动方程给出

$$
(\Box+M^2)H=-gJ,
\qquad
H_{\mathrm{cl}}
=-\frac{g}{\Box+M^2}J.
$$

代回作用量得到树级非局域有效项

$$
\boxed{
\Delta\mathcal L_{\mathrm{eff}}
=
\frac{g^2}{2}
J\frac{1}{\Box+M^2}J
}.
$$

当轻场典型动量满足 $|p^2|\ll M^2$ 时，

$$
\frac{1}{\Box+M^2}
=
\frac{1}{M^2}
\left(
1-
\frac{\Box}{M^2}
+
\frac{\Box^2}{M^4}
-\cdots
\right),
$$

所以

$$
\boxed{
\Delta\mathcal L_{\mathrm{eff}}
=
\frac{g^2}{2M^2}J^2
-
\frac{g^2}{2M^4}J\Box J
+
O(M^{-6})
}.
$$

若 $J=\phi^2/2$，领先项为 $g^2\phi^4/(8M^2)$。这里符号来自本节明确给定的 Lagrangian；若相互作用号或传播子约定改变，不能直接照抄。

在 $p^2\sim M^2$ 附近，传播子可解析出重粒子极点，$1/M$ 展开失效，必须恢复 $H$ 作为显式自由度或改用包含该阈值的理论。

## Matching：确定 Wilson 系数

matching（匹配）要求高能理论和 EFT 对同一组低能量在给定精度下给出相同预测。对低能振幅，可写成

$$
\boxed{
\mathcal A_{\mathrm{UV}}
=
\mathcal A_{\mathrm{EFT}}
+
O\!\left[
\left(\frac{E}{M}\right)^{N+1}
\right]
}.
$$

这条等式必须在以下条件一致时比较：

- 相同的外部轻态；
- 相同的低能运动学；
- 相同的微扰和幂次阶数；
- 相同的重整化方案；
- 指定 matching scale，通常取 $\mu\sim M$ 以避免大对数。

matching 可以在树级或圈级完成，也可比较 on-shell 振幅或适当的 off-shell Green 函数。on-shell matching 自动避开许多冗余算符；off-shell matching 往往更方便组织局域算符，但需处理规范依赖、场重定义和运动方程冗余。

轻场圈会在 UV 理论和 EFT 两边同时产生 IR 非解析结构，例如 $p^2\ln(-p^2)$。matching 比较两边之差时，共同 IR 部分应相消；Wilson 系数只编码不能由低能自由度自身产生的短距离信息。

## Matching 与 running 不同

matching 给出高尺度边界条件：

$$
C_i(M).
$$

RG running 则在固定 EFT 的自由度和算符基内把它演化到低尺度：

$$
C_i(M)
\overset{\mathrm{RG}}{\longrightarrow}
C_i(E).
$$

若算符发生混合，常写成

$$
\boxed{
\mu\frac{dC_i}{d\mu}
=
\gamma_{ij}C_j
}.
$$

$\gamma_{ij}$ 是算符反常维数矩阵，其转置位置会随“系数列向量”与“算符列向量”的约定改变，使用时必须核对定义。

matching 负责短距离边界条件，running 负责重求和 $\ln(M/E)$。只做 running 不能推导未知的高尺度 Wilson 系数，只做 matching 又可能在尺度相差很大时留下大对数。

## 完整 EFT 工作流

一个标准计算流程是：

1. 指定目标过程、能标范围和精度；
2. 选择该范围内的低能自由度；
3. 写出低能对称性允许的独立算符基；
4. 建立一致的幂次计数并选择截断阶数；
5. 在重尺度附近 matching，求 $C_i(M)$；
6. 用 RG 把系数运行到实验尺度；
7. 若跨过新阈值，构造新的 EFT 并再次 matching；
8. 计算可观测量并估计第一项被省略贡献。

## 退耦与阈值

在低外部动量、局域性和适当参数条件下，重粒子效应可吸收到低能参数和 $1/M$ 压低算符中。这是退耦思想的核心。

但在 MS/$\overline{\mathrm{MS}}$ 等质量无关方案中，重场不会仅因 $\mu<M$ 自动从 beta 函数消失。正确流程是：

$$
\text{高能理论}
\xrightarrow[\mu\sim M]{\text{matching}}
\text{低能 EFT}
\xrightarrow{\text{running}}
\text{低能尺度}.
$$

例如 QCD 跨过重夸克阈值时，应构造少一个活跃味的 EFT，并匹配强耦合和相关 Wilson 系数，而不是只在原公式中手工修改 $n_f$。

退耦也不是无条件定理。若耦合随重质量增长、重粒子参与反常或对称性破缺、或低能量中存在非解析效应，可能出现 non-decoupling。此时必须根据具体理论 matching，不能先假定所有重物理都按 $1/M$ 消失。

## EFT cutoff、regulator 与重整化尺度

三个尺度不能混淆：

- **EFT cutoff $\Lambda$**：理论开始遗漏新自由度或新动力学的物理有效范围；
- **regulator**：让中间发散积分可定义的计算工具，如维数正规化或硬动量截止；
- **重整化尺度 $\mu$**：定义重整化参数和 Wilson 系数的任意尺度。

EFT cutoff 为有限值并不妨碍使用维数正规化把圈动量形式上积分到无穷大。高动量区域的局域效应由反项和 Wilson 系数吸收；最终预测仍只能用于 EFT 的适用能区。

## “不可重整化”不等于不可计算

含 $\Delta>d$ 算符的理论若被要求在任意高能、任意精度下成立，通常需要无限多个反项，因此按传统意义不可重整化。

作为 EFT，结论不同：在固定的 $E/\Lambda$ 阶数下，只会有有限个算符和有限个反项参与，因此理论可逐阶重整化并给出有限精度预测。

所以 nonrenormalizable operator 不等于非法、不可计算或应被删除。它只说明该相互作用的系数带负质量量纲，并需要与 EFT 的幂次计数和有效范围一起解释。

## 截断误差

设统一小参数为

$$
Q\sim\frac{E}{\Lambda}<1.
$$

若计算保留到 $Q^N$，可写成

$$
\boxed{
\mathcal A
=
\mathcal A^{(0)}
+
\mathcal A^{(1)}
+
\cdots
+
\mathcal A^{(N)}
+
O(Q^{N+1})
}.
$$

第一项被省略贡献给出误差估计的起点，但还需检查：

- 是否同时存在圈展开或小耦合展开；
- 是否有未重求和的大对数；
- 多个能标中哪个比值最大；
- 低阶项是否因对称性或偶然抵消而消失；
- Wilson 系数是否具有异常大小；
- 是否接近阈值、共振或 EFT 失效边界。

若振幅为

$$
\mathcal A=\mathcal A_0+\delta\mathcal A,
$$

则

$$
|\mathcal A|^2
=
|\mathcal A_0|^2
+2\operatorname{Re}(\mathcal A_0^*\delta\mathcal A)
+|\delta\mathcal A|^2.
$$

因此振幅中 $O(Q^n)$ 的修正通常通过干涉在截面中也产生 $O(Q^n)$ 修正，而不是必然先平方成 $O(Q^{2n})$。若干涉因量子数或对称性消失，阶数才可能改变。

## 一个经典例子：Fermi 理论

低能弱相互作用可用四费米算符表示：

$$
\mathcal L_{\mathrm F}
\supset
-\frac{4G_F}{\sqrt2}
(\bar\psi_1\gamma^\mu P_L\psi_2)
(\bar\psi_3\gamma_\mu P_L\psi_4).
$$

四费米算符在四维的 canonical dimension 为 $6$，所以 $[G_F]=-2$。在 $E\ll m_W$ 时，它可看成积分掉 $W$ 玻色子后的领先局域相互作用，量纲上 $G_F\sim g^2/m_W^2$。

当 $E$ 接近 $m_W$ 时，局域接触近似无法解析传播子极点，必须恢复电弱规范玻色子。这里仅用 Fermi 理论说明 EFT 结构；路线图下一节点 [[Yang-Mills 理论]] 系统介绍非 Abelian 规范场及其自相互作用。

## 与弦理论低能极限的关系

弦理论含有无限重弦激发。若过程能标远低于最先被排除的弦态和其他阈值，重弦塔的影响可组织为时空 EFT 的高导数展开。

弦尺度通常写成

$$
M_s\sim\frac{1}{\sqrt{\alpha'}},
$$

低能或弱曲率展开参数为

$$
\alpha'E^2\ll1,
\qquad
\alpha'|R|\ll1.
$$

时空有效作用量具有示意结构

$$
\boxed{
S_{\mathrm{eff}}
=
S_{2\partial}
+
\alpha'S_{4\partial}
+
\alpha'^2S_{6\partial}
+
\cdots
}.
$$

具体首个非零修正阶数依弦类型、超对称性、背景场、场重定义和方案而异，因此这条式子只表示导数展开结构，不表示每一阶在所有弦理论中都非零。

弦振幅的低能结构可概括为

$$
\mathcal A_{\mathrm{string}}
=
\mathcal A_{\mathrm{massless\ poles}}
+
\mathcal A_{\mathrm{local}}(\alpha's,\alpha't,\alpha'u).
$$

质量为零的低能态产生不能用有限个局域算符替代的极点；重弦态在

$$
|\alpha's|,
|\alpha't|,
|\alpha'u|
\ll1
$$

且远离重弦共振时，可展开为局域高导数接触项。[[S 矩阵]] 已介绍弦散射的低能因子化背景，本页只说明 EFT 组织方式。

还必须区分三个独立展开：

- $\alpha'E^2$ 或 $\alpha'R$：控制导数与曲率修正；
- 弦耦合 $g_s$：控制弦圈和世界面拓扑；
- $E/M_{\mathrm{KK}}$、$E/M_{\mathrm{winding}}$ 等：控制紧致化激发能否被积分掉。

因此只满足 $E<M_s$ 不足以保证某个四维 EFT 有效；能标还必须低于最近的 Kaluza–Klein、绕弦、模场或其他阈值，并控制耦合和背景曲率。

世界面 RG 与时空 EFT 的 RG 也不是同一条流：前者描述二维世界面理论的尺度依赖，后者描述时空低能算符与 Wilson 系数的尺度依赖。

## 与大模型压缩的受限交叉关系

[[模型量化|模型量化]] 与 EFT 都采用“受控近似”方法：先指定使用范围和保真目标，再压缩当前任务不能分辨的细节，并用误差验证近似是否可接受。EFT 用 $E/\Lambda$ 幂次、对称性和第一项被省略贡献估计误差；模型量化用位宽、校准数据和实际任务指标评估舍入与截断误差。

这只是方法结构类比：模型量化不是积分掉量子场，也没有 Wilson 系数、matching、算符基或 RG running；EFT 的截断误差不等于量化误差。[[弦理论与大模型跨学科关系图谱]] 将该关系记录为“共享结构但不等价”，并明确禁止把 EFT、知识蒸馏、LoRA 和量化互相替代。

## 适用条件与事实边界

1. EFT 只承诺在指定自由度、能标范围、对称性、幂次计数和截断精度内给出预测。
2. canonical dimension 给出弱耦合低能展开的基础排序；固定点附近的严格分类需包含反常维数。
3. 高维算符通常受 $E/\Lambda$ 压低，但若低维项被对称性禁止，它可以是领先贡献。
4. 积分掉重场不等于把重场设为零；它会留下局域算符、参数修正和圈效应。
5. 局域导数展开要求外部不变量远离被排除粒子的极点和阈值。
6. matching 决定 Wilson 系数的边界条件，running 演化这些条件；二者不能互相替代。
7. Wilson 系数与算符基各自依赖方案、尺度和场重定义，完整物理可观测量在一致计算阶数下不应依赖任意 $\mu$。
8. MS/$\overline{\mathrm{MS}}$ 中的重场不会因 $\mu$ 降低自动退耦，跨阈值需重新 matching。
9. EFT cutoff、计算 regulator 和重整化尺度 $\mu$ 是不同概念。
10. “不可重整化算符”不等于不可计算；固定 EFT 阶数只需有限个反项。
11. 截断误差从第一项被省略贡献估计，但接近阈值、共振、强耦合或异常系数时需重新评估。
12. 弦理论的 $\alpha'$ 展开、$g_s$ 展开和紧致化尺度展开彼此独立。
13. 具体弦低能有效作用量依弦类型和背景而定；路线图后续“低能有效作用量”节点将单独展开，本页不提前建立链接。

## 常见误区

1. **EFT 是不知道基本理论时才用的临时猜测。** 即使 UV 理论完全已知，低能计算也常通过 EFT 系统简化并重求和大对数。
2. **把重场积分掉就是令它等于零。** 正确操作是对重场做路径积分或求解后代回，会留下无限算符塔。
3. **所有高维算符都可以忽略。** 它们按精度排序；若低维贡献被禁止，高维项可能领先。
4. **不可重整化等于不可预测。** EFT 在固定幂次下只需有限输入，仍可逐阶预测。
5. **matching 就是 RG running。** matching 定边界条件，running 在固定 EFT 内改变尺度。
6. **把 $\mu$ 降到重质量以下就自动退耦。** 质量无关方案中必须显式做阈值 matching。
7. **EFT cutoff 就是硬动量截止。** cutoff 描述物理有效范围，regulator 是计算工具。
8. **Wilson 系数只由算符维数决定。** 维数决定基础幂次，系数还依赖 UV 动力学和对称性。
9. **振幅的 $Q^n$ 修正对截面一定从 $Q^{2n}$ 开始。** 通常先与低阶振幅发生 $Q^n$ 干涉。
10. **轻场圈的所有非解析项都能塞进 Wilson 系数。** Wilson 系数编码短距离局域信息，低能非解析结构应由 EFT 轻场动力学重现。
11. **$E<M_s$ 就足以保证四维弦 EFT 有效。** 还需低于 KK、绕弦和其他阈值，并控制曲率与耦合。
12. **$\alpha'$ 展开与 $g_s$ 展开是一回事。** 前者控制导数修正，后者控制弦圈修正。

## 练习

1. 在 $d$ 维由标准动能项推导 $[\phi]$、$[\psi]$ 和 $[A_\mu]$。
2. 在四维分类 $\phi^2$、$(\partial\phi)^2$、$\phi^4$ 和 $\phi^6$ 的 canonical dimension。若目标精度为 $O((E/\Lambda)^2)$，应保留哪些算符？
3. 对本页重标量模型，推导 $H_{\mathrm{cl}}$ 并展开到 $O(M^{-4})$。
4. 若 $J=\phi^2/2$，求积分掉 $H$ 后的领先 $\phi^4$ 项系数。
5. 为什么 EFT cutoff $\Lambda$、regulator 和重整化尺度 $\mu$ 不是同一个概念？
6. matching 与 running 分别解决什么问题？
7. 为什么 MS-bar 中把 $\mu$ 降到重夸克质量以下不会自动完成退耦？什么情况下重粒子可能出现 non-decoupling？
8. 给定 $\phi^4$ 理论和 $\mathbb Z_2$ 对称性 $\phi\to-\phi$，列出四维中 $\Delta\le6$ 的独立局域算符基，并说明可被分部积分、运动方程或场重定义消去的冗余算符。
9. 若 $\mu\,dC/d\mu=\gamma C$ 且 $\gamma$ 为常数，求 $C(\mu)$ 与 $C(M)$ 的关系。
10. 为什么 $\phi^6/\Lambda^2$ 被称为不可重整化相互作用，却仍可在 EFT 中计算？
11. 若 $\delta\mathcal A/\mathcal A_0=c(E/\Lambda)^2$，写出截面相对修正的领先阶。
12. 解释弦低能展开中质量零极点与局域高导数项为什么必须分开。
13. 比较 $\alpha'$、$g_s$ 和 $E/M_{\mathrm{KK}}$ 三种展开参数。

## 答案与提示

1. 标量动能 $(\partial\phi)^2$ 给 $2+2[\phi]=d$，所以 $[\phi]=(d-2)/2$；费米子动能 $\bar\psi i\slashed\partial\psi$ 给 $1+2[\psi]=d$，所以 $[\psi]=(d-1)/2$；规范场由 $F_{\mu\nu}F^{\mu\nu}$ 得 $[A_\mu]=(d-2)/2$。
2. 四维中 $[\phi]=1$，故 $\Delta_{\phi^2}=2$、$\Delta_{(\partial\phi)^2}=4$、$\Delta_{\phi^4}=4$、$\Delta_{\phi^6}=6$。在 Wilson 系数自然且采用 canonical counting 的前提下，保留到相对 $O((E/\Lambda)^2)$ 通常意味着包括所有允许的 $\Delta\le6$ 算符，以及幂次计数指定为同阶的圈修正；第一个被省略的贡献决定下一阶误差。若低阶项被对称性禁止或系数异常，需重新排序。
3. 运动方程为 $(\Box+M^2)H=-gJ$，所以 $H_{\mathrm{cl}}=-g(\Box+M^2)^{-1}J$。展开后 $\Delta\mathcal L_{\mathrm{eff}}=g^2J^2/(2M^2)-g^2J\Box J/(2M^4)+O(M^{-6})$。
4. $J^2=\phi^4/4$，所以领先项为 $g^2\phi^4/(8M^2)$。
5. $\Lambda$ 是 EFT 开始遗漏物理的范围上限；regulator 只定义中间发散积分；$\mu$ 是定义重整化参数的任意尺度。三者可数值接近，但概念和作用不同。
6. matching 在高尺度比较 UV 理论与 EFT，确定 $C_i(M)$；running 用反常维数把系数演化到其他尺度并重求和尺度对数。
7. MS-bar 是质量无关方案，beta 函数不会仅因 $\mu<m_Q$ 自动删除重夸克。必须在阈值附近匹配到少一个活跃味的 EFT。若重粒子的耦合随其质量增长、参与反常或对称性破缺，重质量极限可能留下不按简单 $1/M$ 消失的 non-decoupling 效应。
8. 若只考虑 Lorentz 不变的单实标量，并模去分部积分、低阶运动方程和局域场重定义，可选的 $\mathbb Z_2$ 偶 on-shell 算符基包含真空项、$\phi^2$、$(\partial\phi)^2$、$\phi^4$ 和 $\phi^6$。枚举 $\Delta=6$ 候选时还会遇到 $\phi^3\Box\phi$、$\phi^2(\partial\phi)^2$ 与 $(\Box\phi)^2$：前两者由分部积分满足 $\phi^3\Box\phi\simeq-3\phi^2(\partial\phi)^2$，再结合低阶运动方程或局域场重定义可把其效应转移到低维参数和 $\phi^6$ 系数；$(\Box\phi)^2$ 也可通过低阶运动方程或 $\phi\to\phi+a\Box\phi/\Lambda^2$ 一类场重定义消去。具体独立基取决于低阶 Lagrangian 和所模去的等价关系；若研究 off-shell Green 函数或不模去运动方程，导数算符集合会不同，不能直接使用这份精简基。
9. 积分 $d\ln C=\gamma\,d\ln\mu$，得到 $C(\mu)=C(M)(\mu/M)^\gamma$。若 $\gamma$ 随耦合运行，则需使用有序指数或联立 RG 方程，不能套此常数结果。
10. 若要求任意高精度会需要无限多个反项；但固定到某个 $E/\Lambda$ 阶数时，只需有限个算符和反项，所以仍可逐阶重整化和预测。
11. $|\mathcal A|^2/|\mathcal A_0|^2=1+2\operatorname{Re}(c)(E/\Lambda)^2+O((E/\Lambda)^4)$，前提是干涉不因量子数或对称性消失。
12. 质量零低能态可真实传播，产生 $1/s$ 等非局域极点；被积分掉的重弦态在远离阈值时产生对 Mandelstam 不变量解析的局域接触项。有限个局域算符不能替代质量零传播极点。
13. $\alpha'E^2$ 控制高导数或曲率修正，$g_s$ 控制弦圈拓扑，$E/M_{\mathrm{KK}}$ 控制紧致化激发是否可被积分掉；三者需分别小，不能相互替代。

## 与其他概念的关系

- [[经典场论]] 提供作用量、Lagrangian 和场的质量量纲基础；
- [[路径积分]] 给出积分掉重自由度的正式定义；
- [[微扰论]] 说明低能比值、导数和小耦合都可作为受控展开参数；
- [[S 矩阵]] 提供 on-shell matching 所比较的散射振幅，并解释弦振幅的低能极点结构；
- [[Feynman 图]] 组织 UV 理论和 EFT 两边的树级与圈级 matching；
- [[正规化与重整化]] 说明反项、方案和“不可重整化”的传统含义；
- [[重整化群]] 负责 Wilson 系数的尺度运行和大对数重求和；
- [[beta 函数]] 给出运行系数的定量来源，并说明质量无关方案中的阈值边界；
- [[Yang-Mills 理论]] 是路线图的下一节点，给出非 Abelian 规范场的定义、作用量、自相互作用及其开弦低能来源；
- [[弦理论完整学习路线图]] 将本节点放在 beta 函数之后、Yang-Mills 理论之前。

## 参考资料

- Steven Weinberg, *The Quantum Theory of Fields, Vol. 2*, Cambridge University Press, 1996, Chapters 19–21.
- Howard Georgi, *Weak Interactions and Modern Particle Theory*, Benjamin/Cummings, 1984.
- A. V. Manohar, "Effective Field Theories," arXiv:hep-ph/9606222. https://arxiv.org/abs/hep-ph/9606222
- C. P. Burgess, "Introduction to Effective Field Theory," *Annual Review of Nuclear and Particle Science* 57 (2007), 329–362. https://doi.org/10.1146/annurev.nucl.56.080805.140508
- Iain W. Stewart, "Effective Field Theory," MIT OpenCourseWare 8.851. https://ocw.mit.edu/courses/8-851-effective-field-theory-spring-2013/
- Thomas Becher, Alessandro Broggio and Andrea Ferroglia, *Introduction to Soft-Collinear Effective Theory*, Springer, 2015.
- Joseph Polchinski, "Effective Field Theory and the Fermi Surface," arXiv:hep-th/9210046. https://arxiv.org/abs/hep-th/9210046
- Joseph Polchinski, *String Theory*, Vol. 1–2, Cambridge University Press, 1998.
- Barton Zwiebach, *A First Course in String Theory*, 2nd ed., Cambridge University Press, 2009.
