---
aliases: [评测安全与局限练习答案]
tags: [练习答案, 评测, 安全, 大模型, 学习教程]
---
# 13-评测安全与局限答案

## [[评测方法|01-评测方法]]

1. MMLU 只覆盖特定多项选择学科、语言和评分方式；不能代表生成质量、工具使用、安全、延迟、成本或真实领域任务。
2. 可能有位置偏好、长度偏好、自家模型偏好、措辞敏感、知识错误和评分标准漂移；应随机顺序、隐藏身份、校准人类样本并报告一致性。
3. 对训练语料与评测题做精确和近似匹配、时间切分和可控新题；检查异常逐字复现；使用私有或动态评测，并公开污染检测口径。
4. 检索层测 recall@k/排名和权限；生成层在固定证据下测忠实度、拒答和引用；端到端层用真实问题测任务成功、延迟、成本和安全。

## [[幻觉|02-幻觉]]

1. 内在幻觉：摘要说原文“收入增长 20%”，原文写 2%；外在幻觉：原文没提 CEO，摘要凭空添加 CEO 发言。
2. 低温只让模型更偏向高概率文本；训练数据中的错误或高概率错误仍会被稳定输出。
3. 检查标题、作者、年份、期刊/会议和 DOI/arXiv 是否存在；打开原文确认论文确实支持所引结论，而不只确认题目相似。
4. 错误可能造成健康伤害；信息还依赖个体病史、剂量和当地规范。应引用权威来源、表达不确定性并由合格专业人员复核。

## [[偏见与公平性|03-偏见与公平性]]

1. 数据中的代表性不均、历史歧视、标签偏差和刻板关联会被预测目标学习；过滤和人工反馈也可能引入新偏差。
2. 大群体高准确率可掩盖小群体严重错误；应分群体报告错误率、假阳性/假阴性、校准和样本量。
3. 保持资历、经历和问题完全相同，只替换与任务无关的姓名、性别代词或群体线索，多次采样比较评分或决策差异。
4. 测试空间巨大且攻击会变化；没找到漏洞只说明本轮样本未触发，不是不存在漏洞。需持续监控、权限隔离和事件响应。

## [[安全与对齐|04-安全与对齐]]

1. 越狱由用户直接诱导模型绕过行为规则；提示注入常藏在网页、邮件、RAG 文档等不可信数据中，试图让系统把数据当更高优先级指令。两者可重叠。
2. 文档作者或外部页面可写恶意指令；检索相关只表示内容相似，不表示内容可信或有权改变系统行为。
3. 默认只读指定邮箱和草稿；发送、批量操作、删除和外部转发需明确确认；限制收件人/附件/频率，隔离凭证并保留审计日志。
4. 拒绝策略是概率性语言行为，可被绕过或误判；真正权限控制必须由确定性的执行器、身份认证和授权系统实施。

## [[当前局限与未来方向|05-当前局限与未来方向]]

1. 示例：幻觉→用检索和引用缓解→在有答案/无答案集测忠实度和拒答；长上下文漏信息→分层检索/摘要→按证据位置分桶测召回；工具误操作→最小权限和确认→用恶意输入与越权用例验证执行器确实拒绝。
2. 基准任务、用户分布和目标可能不同；分数提升也可能伴随更高成本、延迟、安全问题或特定群体退化。
3. 例如某一年一定实现通用人工智能、继续放大必然产生某种意识、某架构必然统一解决所有推理问题。它们目前缺少可重复、可证伪且已完成的证据。
4. 研究方向提出问题、假设和待验证方法；已解决问题需要明确适用条件、可重复证据、强基线比较和已知失败边界。论文取得进展不等于问题被彻底解决。
