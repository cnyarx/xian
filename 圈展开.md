---
aliases:
  - Loop Expansion
  - Loopwise Expansion
  - 圈级展开
english_name: Loop Expansion
tags:
  - 物理学
  - 量子场论
  - 微扰论
  - 弦理论先修
  - 弦理论
prerequisites:
  - "[[Gaussian 积分]]"
  - "[[亏格]]"
  - "[[黎曼曲面]]"
  - "[[路径积分]]"
  - "[[传播子]]"
  - "[[Wick 定理]]"
  - "[[生成泛函]]"
  - "[[Feynman 图]]"
  - "[[微扰论]]"
used_by:
  - "[[紫外发散与红外发散]]"
  - "[[弦理论完整学习路线图]]"
evidence_status: 教材事实
---

# 圈展开（Loop Expansion）

## 一句话解释

圈展开是按 Feynman 图的独立闭环数 $L$ 组织量子修正的微扰展开：树图是 $L=0$，一圈图是 $L=1$，更高圈图逐阶加入更复杂的量子涨落。

## 为什么要学它

[[Feynman 图]] 告诉我们怎样把每个微扰项画成图，但同一个关联函数往往有许多树图、一圈图和多圈图。圈展开给这些图建立第二种秩序：不是只看耦合常数的幂，而是看有多少个独立内部动量积分。

它回答以下问题：

- 哪些图属于经典或半经典最低阶；
- 第一批量子修正来自哪里；
- 为什么一圈计算会出现 Gaussian 行列式和迹对数（trace log，记为 $\operatorname{Tr}\ln$），即先对二次涨落算符取对数再对其全部模态取迹；
- 耦合常数阶数和圈数为什么不能混为一谈；
- 紫外发散、反项和重整化怎样按圈阶递归出现；
- 闭弦微扰中的球面、环面和更高亏格（genus，即闭曲面把手数）怎样对应树级、一圈与高圈。

圈展开通常是从“会画图”走向“会系统计算量子修正”的关键一步。

## 本篇约定

平时采用自然单位 $\hbar=c=1$。其中 $\hbar$ 是约化 Planck 常数（reduced Planck constant），控制量子相位和半经典展开。为了追踪圈阶，本篇在计数时显式恢复 $\hbar$：

$$
Z[J]
=\int\mathcal D\phi\,
\exp\left\{
\frac{i}{\hbar}
\left[S[\phi]+\int d^Dx\,J(x)\phi(x)\right]
\right\}.
$$

仍采用 mostly-minus 度规：

$$
\eta_{\mu\nu}=\operatorname{diag}(+1,-1,-1,-1).
$$

主要例子是四维实标量 $\phi^4$ 理论：

$$
\mathcal L
=\frac12\partial_\mu\phi\partial^\mu\phi
-\frac12m^2\phi^2
-\frac{\lambda}{4!}\phi^4.
$$

在恢复 $\hbar$ 的记账 convention 下，自由传播子携带一个 $\hbar$，相互作用顶点携带一个 $1/\hbar$；计算结束后可再令 $\hbar=1$。

## 直观图景

把量子涨落想成网络中可以独立循环流动的内部动量：

- 树图没有闭合回路，所有内部动量都由外动量和顶点守恒确定；
- 一圈图有一个独立内部动量，需要做一次 $\int d^D\ell$；
- 两圈图有两个独立圈动量；
- 圈越多，积分维数、发散结构和图的组合复杂度通常越高。

这个图景只用于微扰计数。圈不是粒子在时空中绕圆运动，也不表示量子效应只有在图上形成几何圆环时才出现；关键是独立内部动量的数目。

## 正式定义

设一张 Feynman 图有：

- $I$ 条内部线；
- $V$ 个顶点；
- $C$ 个连通分量。

其独立圈数定义为图的第一 Betti 数：

$$
\boxed{L=I-V+C}.
$$

这条公式表示图中可以独立选择的闭合回路数。$I$、$V$、$C$、$L$ 都是无量纲非负整数；它适用于有限 Feynman 图，并把每个连通分量单独计入。比如两块互不连接的图合计有 $I=7,V=6,C=2$，则 $L=3$。推导依赖图论中“生成树含 $V-C$ 条边”：超过生成树的每条内部边各增加一个独立回路。

对连通图 $C=1$：

$$
\boxed{L=I-V+1}.
$$

这是上一公式在单个连通分量上的特例。例如 $I=4,V=3$ 得 $L=2$。它的符号、量纲和成立条件与一般式相同，推导只需代入 $C=1$。

圈展开就是把某个量按 $L$ 分类：

$$
\mathcal Q
=\mathcal Q_{L=0}
+\mathcal Q_{L=1}
+\mathcal Q_{L=2}
+\cdots.
$$

这里 $\mathcal Q$ 可以是连通 Green 函数、1PI 顶点、有效作用量（effective action，即把所有 1PI 量子修正组织成背景场泛函的对象）或散射振幅；$\mathcal Q_L$ 是其 $L$ 圈部分，必须与 $\mathcal Q$ 具有相同物理量纲。该分解要求存在可控的微扰或半经典展开。例如保留 $\mathcal Q_{0}+\mathcal Q_{1}$ 就是截断到一圈。它依赖先用 Feynman 图枚举贡献，再用 $L=I-V+C$ 分类。不同对象的整体 $\hbar$ 幂次 convention 不完全相同，但“每增加一圈多一个相对 $\hbar$”的关系保持不变。

## 核心公式

### 图的 $\hbar$ 计数

恢复 $\hbar$ 后，每条内部传播子贡献一个 $\hbar$，每个相互作用顶点贡献一个 $\hbar^{-1}$。因此一张图的内部结构贡献：

$$
\boxed{\hbar^{I-V}=\hbar^{L-C}}.
$$

对连通图：

$$
\boxed{\hbar^{I-V}=\hbar^{L-1}}.
$$

这表示连通真空图或未把外部源导数的 $\hbar$ 因子计入时，树级从 $\hbar^{-1}$ 开始，每增加一圈相对多一个 $\hbar$。$I$、$V$、$L$、$C$ 都是无量纲整数，所以 $\hbar$ 幂次只是量子阶数记账。该公式要求作用量整体出现在 $e^{iS/\hbar}$ 中，并采用“传播子含 $\hbar$、顶点含 $1/\hbar$”的 convention。例如连通图 $I=4,V=3$ 有 $L=2$，内部结构为 $\hbar^{1}$。推导依赖拓扑恒等式 $L=I-V+1$。

### 有效作用量的圈展开

对 1PI 有效作用量，通常把整体 convention 整理为：

$$
\boxed{
\Gamma[\varphi]
=S[\varphi]
+\hbar\,\Gamma_1[\varphi]
+\hbar^2\Gamma_2[\varphi]
+\cdots
}.
$$

这条公式表示经典作用量是树级项，$\Gamma_1$ 汇总一圈 1PI 图，$\Gamma_2$ 汇总两圈 1PI 图。$\Gamma$、$S$ 和每个 $\hbar^L\Gamma_L$ 都具有作用量量纲；显式保留 $\hbar$ 时，$\Gamma_L$ 的量纲按该 convention 补偿相应的 $\hbar^{-L}$。它适用于围绕可控鞍点的半经典或微扰展开。例如只计算到一圈时，丢弃 $O(\hbar^2)$。它由 [[生成泛函]] 的 Legendre 变换和背景场附近的 Gaussian 涨落积分得到。

### 一圈行列式

令背景场为 $\varphi$，涨落为 $\eta$：

$$
\phi=\varphi+\eta.
$$

这表示把完整场拆成选定背景与围绕它的小涨落；三者具有相同场量纲。该分解适用于背景场方法或鞍点展开，并要求积分变量的平移与边界条件可被一致处理。最简单例子是普通函数 $x=x_0+\xi$，其中 $x_0$ 是驻点、$\xi$ 是偏离量。它依赖 [[路径积分]] 和 Taylor 展开。

把作用量展开到二次阶：

$$
S[\varphi+\eta]
=S[\varphi]
+\int\frac{\delta S}{\delta\varphi}\eta
+\frac12\int\eta\,S^{(2)}[\varphi]\eta
+O(\eta^3),
$$

其中：

$$
S^{(2)}[\varphi](x,y)
=\frac{\delta^2S}{\delta\varphi(x)\delta\varphi(y)}.
$$

这两式是泛函 Taylor 展开：线性项检验背景是否为驻点，二次项控制 Gaussian 涨落，三次及更高项生成更高圈相互作用。各项都与 $S$ 具有相同作用量量纲；积分核 $S^{(2)}(x,y)$ 的量纲由两个场变分和两个时空积分共同补偿。公式要求作用量在所选背景附近至少可作所需阶数的泛函展开。例如有限维函数 $S(x)=\frac12ax^2+\frac{\lambda}{4!}x^4$ 在 $x=\varphi+\eta$ 下有 $S''(\varphi)=a+\lambda\varphi^2/2$。推导依赖泛函导数、Taylor 展开和驻点法。

在背景满足相应驻点条件并正确处理边界、零模与 $i\varepsilon$ 后，玻色 Gaussian 积分给出形式上的一圈项：

$$
\boxed{
\Gamma_1[\varphi]
=\frac{i}{2}\operatorname{Tr}\ln S^{(2)}[\varphi]
}
$$

从而：

$$
\Gamma[\varphi]
=S[\varphi]
+\frac{i\hbar}{2}\operatorname{Tr}\ln S^{(2)}[\varphi]
+O(\hbar^2).
$$

$S^{(2)}$ 是背景上的二次涨落算符；$\operatorname{Tr}$ 同时对时空和内部指标取迹；$\ln$ 是算符对数。$\operatorname{Tr}\ln S^{(2)}$ 形式上无量纲，但严格定义通常需要引入参考尺度并进行正规化。该公式适用于玻色场的 Minkowski 路径积分；Euclidean convention 下通常写成 $+\frac12\operatorname{Tr}\ln S_E^{(2)}$。若 $S^{(2)}$ 有零模、负模或规范冗余，必须先单独处理。有限维原型是 Gaussian 积分 $(\det A)^{-1/2}$，并使用 $\ln\det A=\operatorname{Tr}\ln A$。例如 $A=\operatorname{diag}(4,9)$ 时，$\frac12\operatorname{Tr}\ln A=\frac12(\ln4+\ln9)=\ln6$，与 $\frac12\ln\det A=\frac12\ln36$ 一致；这直接展示了迹对数怎样汇总各个 Gaussian 模态。

## 公式中每个符号的意思

- $L$：独立圈数；
- $I$：内部线数；
- $V$：相互作用顶点数；
- $C$：连通分量数；
- $\hbar$：约化 Planck 常数，在这里也是半经典展开参数；
- $\mathcal Q_L$：量 $\mathcal Q$ 的 $L$ 圈贡献；
- $\Gamma[\varphi]$：1PI 有效作用量；
- $\Gamma_L[\varphi]$：$L$ 圈 1PI 贡献去掉显式 $\hbar^L$ 后的系数；
- $\varphi$：背景场或经典场；
- $\eta$：围绕背景场的量子涨落；
- $S^{(2)}[\varphi]$：作用量关于场的二阶泛函导数；
- $\operatorname{Tr}$：对连续坐标、动量及内部指标取迹；
- $\det$：行列式；
- $O(\hbar^2)$：两圈及更高圈贡献。

## 推导一：为什么一圈多一个 $\hbar$

从：

$$
e^{iS/\hbar}
$$

出发。自由二次项的逆给传播子。因为 Gaussian 指数中二次算符前有 $1/\hbar$，其逆传播子带一个 $\hbar$。每展开一次相互作用：

$$
\exp\left(\frac{i}{\hbar}S_{\mathrm{int}}\right)
$$

就带一个 $1/\hbar$。因此有 $I$ 条内部线、$V$ 个顶点的图得到：

$$
\hbar^I\hbar^{-V}=\hbar^{I-V}.
$$

对连通图代入：

$$
I-V=L-1,
$$

得到 $\hbar^{L-1}$。从 $L$ 增到 $L+1$，相对因子多一个 $\hbar$。

外部场插入、源导数、归一化与是否计算 $W$ 或 $\Gamma$ 会改变整张图共同的整体 $\hbar$ 幂次。因此最稳健的说法是：**固定同一类对象和外部插入 convention 后，每增加一圈相对多一个 $\hbar$。**

## 推导二：为什么一圈产生 $\operatorname{Tr}\ln$

在驻点背景附近只保留涨落二次项：

$$
Z
\approx e^{iS[\varphi]/\hbar}
\int\mathcal D\eta\,
\exp\left[
\frac{i}{2\hbar}
\int\eta S^{(2)}[\varphi]\eta
\right].
$$

Gaussian 积分形式上给：

$$
\left(\det S^{(2)}[\varphi]\right)^{-1/2}.
$$

把乘积写进指数：

$$
(\det A)^{-1/2}
=\exp\left[-\frac12\operatorname{Tr}\ln A\right].
$$

再与 Minkowski 权重 $e^{i\Gamma/\hbar}$ 对比，就得到：

$$
\Gamma^{(1)}[\varphi]
=\frac{i\hbar}{2}\operatorname{Tr}\ln S^{(2)}[\varphi].
$$

这里的行列式通常是无限维且发散的。公式说明一圈结构从哪里来，不等于已经完成正规化、重整化或相位选择。

## 具体例子一：同为 $O(\lambda)$，圈数却不同

在 $\phi^4$ 理论中，四点函数的一阶树图有：

$$
V=1,\qquad I=0,\qquad E=4.
$$

所以：

$$
L=I-V+1=0.
$$

其截肢顶点因子为：

$$
-i\lambda.
$$

同一理论的二点函数一阶蝌蚪图（tadpole diagram，即同一顶点上的两个场自收缩成闭合内部线的图）也只有一个顶点，因此同样是 $O(\lambda)$，但它有一条自收缩内部线：

$$
V=1,\qquad I=1,\qquad E=2,
$$

所以：

$$
L=1-1+1=1.
$$

其 1PI 核含：

$$
-i\Sigma_{\mathrm{tad}}
=\frac{-i\lambda}{2}
\int\frac{d^4\ell}{(2\pi)^4}
\frac{i}{\ell^2-m^2+i\varepsilon}.
$$

因此：

- 四点树图：$O(\lambda)$、$L=0$；
- 二点蝌蚪图：$O(\lambda)$、$L=1$。

这个对比直接证明“耦合常数阶数”等于“圈数”是错误的。

## 具体例子二：$\phi^4$ 四点函数的一圈计数

考虑连通四点图，取两个 $\phi^4$ 顶点：

$$
V=2,\qquad E=4.
$$

半边计数：

$$
4V=2I+E
$$

给出：

$$
8=2I+4
\quad\Rightarrow\quad
I=2.
$$

于是：

$$
L=I-V+1=2-2+1=1.
$$

这是一圈四点图，耦合阶数为 $O(\lambda^2)$。在 $s$ 道的一种动量路由中，圈积分具有结构：

$$
\mathcal M_s^{(1)}
\propto
\frac{(-i\lambda)^2}{2}
\int\frac{d^4\ell}{(2\pi)^4}
\frac{i}{\ell^2-m^2+i\varepsilon}
\frac{i}{(\ell+p_1+p_2)^2-m^2+i\varepsilon}.
$$

因子 $1/2$ 是该泡图在固定外部标记与通道下的对称因子。完整四点振幅还要加 $t$、$u$ 道，并按统一 convention 处理整体动量守恒 delta、外线截肢和重整化。

## 具体例子三：一圈有效势的背景依赖质量

对常数背景 $\varphi$，四维 $\phi^4$ 理论的经典势为：

$$
V(\varphi)
=\frac12m^2\varphi^2
+\frac{\lambda}{4!}\varphi^4.
$$

二阶涨落的背景依赖质量为：

$$
M^2(\varphi)
=\frac{d^2V}{d\varphi^2}
=m^2+\frac{\lambda}{2}\varphi^2.
$$

因此一圈有效势在正规化前具有形式：

$$
V_{\mathrm{eff}}^{(1)}(\varphi)
=\frac{\hbar}{2}
\int\frac{d^4k_E}{(2\pi)^4}
\ln\left[k_E^2+M^2(\varphi)\right],
$$

这里已 Wick 旋转到 Euclidean 动量 $k_E$。$M^2$ 的质量量纲为二，$d^4k_E$ 为质量四次方，因而有效势具有质量四次方量纲；对数严格来说应写成无量纲比值，例如 $\ln[(k_E^2+M^2)/\mu^2]$。该积分紫外发散，本篇只展示圈展开结构，不把未经正规化的表达式当作有限答案。

## $Z$、$W$ 与 $\Gamma$ 中的圈展开

三种生成泛函组织不同图：

- $Z[J]$ 包含连通与不连通图；
- $W[J]=-i\hbar\ln Z[J]$ 生成连通图；
- $\Gamma[\varphi]$ 生成 1PI 图。

显式恢复 $\hbar$ 时，本篇使用：

$$
W[J]=-i\hbar\ln Z[J],
$$

而不是自然单位下的 $W=-i\ln Z$。二者在令 $\hbar=1$ 后一致。

真空图还需通过 $Z[J]/Z[0]$ 或 $W[J]-W[0]$ 消去。取对数只选出连通分量，不会自动令 $W[0]=0$。

## 发散、反项与圈阶

圈积分经常在大动量或小动量区域出现发散：

- 紫外发散来自大圈动量区域；
- 红外发散来自小动量、无质量传播或简并运动学区域。

正规化先给发散表达式一个受控定义；重整化再用允许的参数和场重定义、反项以及重整化条件得到有限预测。反项本身也按 $\hbar$ 或圈阶展开：一圈发散由一圈阶反项抵消，两圈计算还需包含低圈反项插入产生的图。

圈展开只负责组织阶次，不能代替发散分析和重整化证明。后续 [[紫外发散与红外发散]] 区分高动量、软、共线和特殊运动学区域；[[正规化与重整化]] 则进一步用正规化、反项和重整化条件把 UV 发散转化为有限预测。

## 玻色场、费米场与鬼场的一圈符号

玻色 Gaussian 涨落通常给：

$$
(\det A_B)^{-1/2},
$$

从而产生：

$$
+\frac{i\hbar}{2}\operatorname{Tr}\ln A_B.
$$

复 Grassmann 费米场通常给：

$$
\det A_F,
$$

从而对有效作用量产生相反统计符号的 $\operatorname{Tr}\ln A_F$。规范理论中还要加入 [[规范固定]] 与 [[Faddeev-Popov 行列式]]；后者由 [[Faddeev-Popov 鬼场]] 的 Grassmann 路径积分局域化，闭合鬼圈因此带额外负号。

这些符号依赖场是实还是复、算符定义、Minkowski 或 Euclidean convention 以及测度归一化。不能只背“玻色加、费米减”而忽略场的自由度和行列式幂次。

## 与弦亏格展开的关系

对定向闭弦世界面，亏格为 $g$ 的连通曲面的 Euler 示性数（Euler characteristic，用一个整数概括顶点、边、面拓扑组合的量）为：

$$
\chi=2-2g.
$$

常见拓扑权重为：

$$
\boxed{g_s^{-\chi}=g_s^{2g-2}}.
$$

$g_s$ 是无量纲弦耦合常数，$g$ 是非负整数，因此权重无量纲。第一式适用于闭合、连通、定向曲面；可由 [[亏格]] 中曲面分类和 Euler 示性数公式得到。第二式来自世界面常数 dilaton 与 Euler 密度的耦合，使路径积分权重含 $e^{-\Phi_0\chi}=g_s^{-\chi}$，并代入 $\chi=2-2g$。该拓扑权重适用于定向闭弦连通真空世界面；加入顶点算符并选择外态归一化后，整体 $g_s$ 幂次会相应重分配。例如：

- $g=0$：球面，权重 $g_s^{-2}$，对应闭弦树级拓扑；
- $g=1$：环面，权重 $g_s^0$，对应闭弦一圈拓扑；
- 每增加一个把手，权重相对多 $g_s^2$。

粒子场论的 $L$ 是图的独立动量回路数；闭弦的 $g$ 是光滑二维世界面的把手数。它们在微扰层级上对应树级、一圈和高圈，但不是同一个几何对象。开弦和非定向弦还必须记录边界数与交叉帽数（crosscap number），不能只用亏格分类。

## 适用条件与事实边界

1. 圈展开要求存在可控的微扰或半经典参数；强耦合下截断到少数圈未必可靠。
2. 每增加一圈相对多一个 $\hbar$ 的说法要求固定所计算对象、外部插入和 $\hbar$ convention。
3. 耦合常数阶数与圈数是不同分类；它们可相关，但不普遍相等。
4. 一圈 $\operatorname{Tr}\ln$ 公式要求正确处理驻点、边界条件、零模、负模和规范冗余。
5. 无限维行列式和圈积分通常需要正规化；形式表达式不是自动有限的数。
6. 单张图可能依赖规范和重整化方案，物理量来自满足一致性要求的图与反项之和。
7. $Z$、$W$、$\Gamma$ 分别组织全部图、连通图和 1PI 图，不能混用。
8. 真空图需单独归一化消去，$\ln Z$ 不会自动把 $W[0]$ 设为零。
9. 弦的亏格展开与粒子场论圈展开在拓扑阶次上对应，但世界面不是粒子图的加粗版本。
10. 微扰级数在许多理论中是渐近级数（asymptotic series）：适当前几阶可能改善近似，但无穷求和不保证收敛，因此增加圈数并不保证无限逼近精确答案。

## 常见误区

1. **圈数就是顶点数。** 正确关系是 $L=I-V+C$。
2. **$O(\lambda^n)$ 就是 $n$ 圈。** 同一耦合阶可因外线数和内部连接不同而具有不同圈数。
3. **树图完全没有量子力学。** 树图来自量子场论生成泛函的最低圈阶；把它称为经典项要结合有效作用量或半经典展开语境。
4. **一圈一定只有一个顶点。** 一圈四点泡图可有两个 $\phi^4$ 顶点。
5. **有一个画出来的圆就是一圈。** 圈数由独立内部动量或 $L=I-V+C$ 决定，不由绘图形状决定。
6. **$\operatorname{Tr}\ln$ 已经是有限答案。** 无限维迹通常发散，需正规化和重整化。
7. **取 $\ln Z$ 会消去所有真空图。** 它选出连通图，但 $W[0]$ 仍可含连通真空图。
8. **一圈算完后，两圈只需画全新的两圈图。** 还必须包括低圈反项插入并保持同一重整化方案。
9. **闭弦亏格 $g$ 就是粒子图的圈数 $L$。** 它们对应微扰阶次，但定义在不同几何对象上。
10. **圈数越高结果必然越准确。** 渐近展开存在最佳截断阶，强耦合时低圈近似也可能失效。

## 练习

1. 用自己的话解释圈展开按什么量组织微扰项。
2. 对连通图 $I=7,V=5$，计算圈数。
3. 为什么每增加一圈相对多一个 $\hbar$？
4. 比较 $\phi^4$ 四点树图和二点蝌蚪图的 $\lambda$ 阶数与圈数。
5. 对 $\phi^4$ 连通四点图 $V=2,E=4$，计算 $I$ 和 $L$。
6. 写出有效作用量到一圈的形式，并解释 $\operatorname{Tr}\ln$ 从哪里来。
7. 若常数背景下 $V(\varphi)=\frac12m^2\varphi^2+\frac{\lambda}{4!}\varphi^4$，求背景依赖质量 $M^2(\varphi)$。
8. 为什么 $W[J]=-i\hbar\ln Z[J]$ 不能自动消去 $W[0]$？
9. 两圈重整化为什么还要包含一圈反项插入？
10. 闭弦球面、环面和亏格二曲面分别对应什么圈阶？
11. “弦亏格就是粒子 Feynman 图圈数”为什么不准确？
12. 判断：“只要多算几圈，答案一定单调接近精确值。”

## 答案与提示

1. 它按 Feynman 图中独立内部动量回路数 $L$ 分类：$L=0$ 为树级，$L=1$ 为一圈，依次类推。
2. $L=I-V+1=7-5+1=3$，所以是三圈图。
3. 恢复 $\hbar$ 后，每条内部线给 $\hbar$，每个顶点给 $1/\hbar$；连通图为 $\hbar^{I-V}=\hbar^{L-1}$，故 $L$ 增加一时相对多一个 $\hbar$。
4. 两者都只有一个 $\phi^4$ 顶点，所以都是 $O(\lambda)$；四点树图有 $I=0,L=0$，二点蝌蚪图有 $I=1,L=1$。
5. $4V=2I+E$ 给 $8=2I+4$，所以 $I=2$；再由 $L=I-V+1$ 得 $L=1$。
6. $\Gamma=S+\frac{i\hbar}{2}\operatorname{Tr}\ln S^{(2)}+O(\hbar^2)$。在背景场附近把作用量展开到二次阶，Gaussian 涨落积分给 $(\det S^{(2)})^{-1/2}$，再用 $\ln\det=\operatorname{Tr}\ln$。
7. $M^2(\varphi)=d^2V/d\varphi^2=m^2+\lambda\varphi^2/2$。
8. 对数把图分解为连通分量，但与源无关的连通真空图仍留在 $W[0]$；需使用 $W[J]-W[0]$ 或 $Z[J]/Z[0]$。
9. 低圈发散已由低圈反项定义；把这些反项插入更高圈图，是同一裸理论按 $\hbar$ 展开的一部分，也是抵消两圈子发散所必需的贡献。
10. 对定向闭弦：球面 $g=0$ 对应树级，环面 $g=1$ 对应一圈，亏格二 $g=2$ 对应两圈。
11. 粒子圈数是图论中的独立动量回路数；弦亏格是光滑世界面的把手数。二者组织相应微扰阶次，但几何对象、积分变量和中间态结构不同。
12. 错。许多微扰级数是渐近级数，超过最佳截断阶后误差可能增大；强耦合时低圈近似也未必可靠。

## 与其他概念的关系

- [[Gaussian 积分]] 给出一圈行列式的有限维原型；
- [[亏格]] 和 [[黎曼曲面]] 描述弦世界面圈阶的拓扑数据；
- [[路径积分]] 提供鞍点与 $\hbar$ 展开的起点；
- [[传播子]] 为每条内部线提供一个二次算符的逆；
- [[Wick 定理]] 枚举形成各圈图的收缩；
- [[生成泛函]] 区分全部图、连通图和 1PI 图；
- [[Feynman 图]] 定义圈数、顶点、内部线和对称因子；
- [[微扰论]] 说明圈展开与耦合展开、半经典展开的关系；
- [[紫外发散与红外发散]] 系统区分圈积分的高动量、软、共线和特殊运动学发散区域；
- [[正规化与重整化]] 用正规化、局域反项和参数重定义处理圈积分中的 UV 发散；
- [[弦理论完整学习路线图]] 将本节点放在 Feynman 图之后、发散与重整化之前。

## 参考资料

- Michael E. Peskin and Daniel V. Schroeder, *An Introduction to Quantum Field Theory*, Westview Press, 1995, Chapters 9–11.
- Mark Srednicki, *Quantum Field Theory*, Cambridge University Press, 2007, Chapters 9–14.
- Michael D. Schwartz, *Quantum Field Theory and the Standard Model*, Cambridge University Press, 2014, Chapters 13–16.
- Steven Weinberg, *The Quantum Theory of Fields, Vol. 2*, Cambridge University Press, 1996, Chapters 16–18.
- Claude Itzykson and Jean-Bernard Zuber, *Quantum Field Theory*, McGraw-Hill, 1980.
- [David Tong, Quantum Field Theory](https://www.damtp.cam.ac.uk/user/tong/qft.html)
- Joseph Polchinski, *String Theory, Vol. 1*, Cambridge University Press, 1998, Chapters 6–7.
- David Tong, *String Theory*, arXiv:0908.0333, [arXiv](https://arxiv.org/abs/0908.0333)
