---
aliases: [语言与Token练习答案]
tags: [练习答案, Token, 大模型, 学习教程]
---
# 05-语言与Token答案

## [[什么是 Token|01-什么是 Token]]

1. 字符：`人/工/智/能/改/变/世/界`；词：`人工智能/改变/世界`；一种可能的子词切分：`人工/智能/改变/世界`。切分不唯一，实际结果由 tokenizer 词表与算法决定。
2. 完整词级词表不可能列尽新词、人名、拼写变化和组合词；未知词会丢失细节，词表也会过大。
3. 中文词之间通常没有空格，空格更多用于句子或排版，不能直接提供稳定词边界。
4. 不会。词表、归一化规则和分词算法不同都会改变 token 边界与数量。

## [[词表与编码|02-词表与编码]]

1. 示例词表：`<pad>,<unk>,我,爱,学,习,大,模,型,。` 映射到 `0..9`；“我爱学习。”可编码为 `[2,3,4,5,9]`。实际 tokenizer 可能按子词合并。
2. 同一批张量通常需要相同长度；`<pad>` 补齐较短序列，mask 告诉注意力或损失忽略补位。
3. 嵌入参数量为 $Vd$。嵌入维度不变时，词表翻倍使嵌入参数量翻倍；若输出层不共享权重，输出层也会相应增加。
4. 模型权重与特定 token ID 含义一同训练。换 tokenizer 后同一个 ID 可能代表另一个文本，输入嵌入和输出概率都会错位。

## [[语言模型的基本思想|03-语言模型的基本思想]]

1. 对 token `[x1,x2,x3,x4,x5]`，输入可为 `[x1,x2,x3,x4]`，目标为 `[x2,x3,x4,x5]`；训练时每个位置预测右移一个 token。
2. $P(x_1,x_2,x_3)=P(x_1)P(x_2\mid x_1)P(x_3\mid x_1,x_2)$。
3. 训练已有整段真实 token，可用因果 mask 同时计算各位置预测；生成的下一个 token 还不存在，必须先产生它才能继续，形成串行依赖。
4. 不等价。下一个 token 预测可迫使模型学习大量语法、语义和世界规律，但“理解”有多种操作定义；仅凭训练目标不能证明模型拥有某种人类式理解，应通过具体能力和失败模式评测。
