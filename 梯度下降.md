---
aliases: [梯度下降, SGD, 学习率, Optimizer]
tags: [机器学习, 优化, 教程, 大模型, 学习教程]
prerequisites: ["[[损失函数]]", "[[导数与梯度]]"]
next: ["[[过拟合与正则化]]"]
---
# 梯度下降

## 一句话定义

梯度下降是寻找损失函数最小值的迭代方法。每一步朝梯度反方向移动一小步，逐步降低损失。

## 直觉

```
你蒙着眼睛站在山上，要找最低点。

每次你用脚感受周围坡度：
- 朝最陡的下坡方向迈一小步
- 重复，直到地面变平

梯度下降就是这样做的
```

## 数学定义

$$
\theta_{t+1} = \theta_t - \eta \cdot \nabla L(\theta_t)
$$

- $\theta_t$：当前参数。
- $\eta$：学习率，控制每步多大。
- $\nabla L(\theta_t)$：损失函数在当前位置的梯度。
- 负号：朝梯度反方向走，让损失下降。

## 学习率

学习率是梯度下降中的关键超参数。

- 太大：参数可能来回跳动，甚至发散。
- 太小：进展可能非常慢。
- 合适数值取决于优化器、批量大小、模型、数据和学习率计划，没有对所有任务通用的固定值。

## 三种梯度下降

### 批量梯度下降

每步用全部数据计算梯度。精确但数据量大时每一步都太慢。

### 随机梯度下降

每步只用一条数据。快但波动大，可能不收敛。

### 小批量梯度下降

每步用一小批（mini-batch）数据。它适合硬件并行，也能在计算成本和梯度噪声之间折中，是深度学习工程中的常见训练方式 `[C]`。

```python
for batch in data_loader:
    predictions = model(batch.x)
    loss = loss_function(predictions, batch.y)
    optimizer.zero_grad()
    loss.backward()
    optimizer.step()
```

## 动量

动量像物理学中的惯性，累积过去梯度方向，帮助穿过狭长或平坦区域。

Momentum、Adam 和 AdamW 是深度学习中的常见选择 `[C]`，但没有一种优化器对所有模型和任务都最好。

## 在大模型训练中的应用

- 大模型训练中的“学习率”和“优化器”就是梯度更新规则的具体配置。
- AdamW 是公开 Transformer 训练方案中的常见优化器选择 `[C]`，具体设置应以目标模型的论文或训练配置为准。
- 学习率常按计划变化，例如先 warmup 再衰减。

## 常见误区

- 梯度为零的点不一定是全局最优，可能是局部最优或鞍点。
- 学习率不是越小越好：过小学习率意味着训练无法在合理时间内完成。
- 损失不下降不等于梯度下降有问题，可能学习率不合适或数据有问题。

## 概念与公式导航

- [[参数梯度与优化器|参数、梯度与优化器]]
- [[梯度下降与Adam公式|梯度下降与 Adam 公式]]
- [[梯度|梯度]] 给出更新式中 $\nabla L$ 的共同数学定义；梯度下降是利用负梯度反复更新参数的算法，而梯度本身只是当前位置的局部变化率向量。
- [[路径积分|路径积分]] 的半经典分析也研究作用量驻点，但完整路径积分对全部历史按复相位加权求和；梯度下降只沿离散更新轨迹优化有限维损失。因此路径积分不是 SGD，量子振幅求和也不是训练过程。
- [[弦理论与大模型跨学科关系图谱]] 汇总优化与物理、数学路径之间可复用的概念及类比边界。

## 练习

1. 函数 $f(x) = x^2$，从 $x=3$ 开始，学习率 $0.5$，做 3 步梯度下降，每一步后 $x$ 是多少？
2. 学习率设为 $0.1$ 和 $2.0$ 分别可能出现什么问题？
3. 解释小批量梯度下降和批量梯度下降的区别。
4. 为什么 Adam 比标准 SGD 更适合大模型训练？

## 来源

- 优化算法：Goodfellow, Bengio & Courville (2016), *Deep Learning*, Chapter 8：https://www.deeplearningbook.org/ `[A]`
- Adam：Kingma & Ba (2015), *Adam: A Method for Stochastic Optimization*：https://arxiv.org/abs/1412.6980 `[A]`
- AdamW：Loshchilov & Hutter (2019), *Decoupled Weight Decay Regularization*：https://arxiv.org/abs/1711.05101 `[A]`
- 小批量与优化器采用情况属于工程实践概括，应以具体训练配方为准 `[C]`