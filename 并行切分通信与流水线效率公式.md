---
aliases: [并行切分公式, 通信量公式, 流水线气泡公式, Parallelism and Pipeline Efficiency]
tags: [公式, 分布式训练, 大模型, 学习教程]
prerequisites: ["[[求和与下标]]", "[[标量向量矩阵与张量]]", "[[模型并行与通信]]", "[[矩阵乘法与仿射层公式]]"]
---
# 并行切分、通信与流水线效率公式

## 1. 列并行

对线性变换 $Y=XW$，将权重按输出列切分：

$$
W=[W_1\;W_2\;\cdots\;W_p]
$$

则第 $i$ 台设备计算：

$$
Y_i=XW_i
$$

完整输出为：

$$
Y=[Y_1\;Y_2\;\cdots\;Y_p]
$$

- $p$：张量并行设备数；
- $W_i$：第 $i$ 个输出列分片；
- $Y_i$：对应输出分片。

若下一层需要完整 $Y$，就要收集这些分片。

## 2. 行并行

按输入维切分：

$$
X=[X_1\;X_2\;\cdots\;X_p],\qquad
W=\begin{bmatrix}W_1\\W_2\\\vdots\\W_p\end{bmatrix}
$$

每台设备计算部分结果 $Z_i=X_iW_i$，完整输出为：

$$
Y=\sum_{i=1}^{p}Z_i
$$

这里需要求和型通信，而不是按列拼接。

## 3. 简化的通信时间模型

传输 $n$ 字节的一次通信可粗略写成：

$$
T_{comm}\approx \alpha+\frac{n}{B}
$$

- $\alpha$：启动延迟；
- $B$：有效带宽；
- $n$：通信数据量。

这是理解用的简化模型。真实集合通信还受拓扑、设备数、算法和链路竞争影响。

## 4. 流水线气泡

若有 $p$ 个流水线阶段、$m$ 个等耗时微批次，最简单的只前向流水线完成时间为：

$$
T=(m+p-1)t
$$

其中 $t$ 是一个阶段处理一个微批次的时间。理想有效计算占比为：

$$
\eta=\frac{m}{m+p-1}
$$

气泡占比为：

$$
1-\eta=\frac{p-1}{m+p-1}
$$

训练还包含反向传播和不同调度，上式不是所有训练流水线的精确性能公式，只用于说明：微批次越多，填充和排空成本所占比例通常越小。

## 5. 数值例子

4 个阶段处理 8 个等耗时微批次：

$$
\eta=\frac{8}{8+4-1}=\frac{8}{11}\approx72.7\%
$$

若增加到 32 个微批次：

$$
\eta=\frac{32}{32+4-1}=\frac{32}{35}\approx91.4\%
$$

但微批次更多会增加调度复杂度，并可能改变激活内存和优化器更新语义。

## 6. 1F1B

1F1B（One Forward One Backward）表示稳定阶段交替执行一个微批次的前向和一个微批次的反向。它可降低同时保存的激活数量，但仍有预热、排空和阶段不均衡，不能把气泡视为完全消失。

## 边界

- $1/p$ 的参数分片不保证获得 $p$ 倍加速；
- 通信字节数少也可能受启动延迟影响；
- 阶段耗时不等时，应由最慢阶段决定流水线节拍；
- 专家并行的 All-to-All 通信量取决于实际路由和容量策略。

## 最小自测

1. 2 个阶段、6 个等耗时微批次，简化前向流水线有效占比是多少？
2. 行并行的部分输出为什么需要求和？

答案：

1. $6/(6+2-1)=6/7\approx85.7\%$；
2. 每个设备只计算了输入维的一部分对同一输出的贡献，完整输出是这些贡献之和。

## 正文

- [[模型并行|模型并行]]

## 来源

- Shoeybi et al. (2019), *Megatron-LM*：https://arxiv.org/abs/1909.08053 `[A]`
- Huang et al. (2019), *GPipe*：https://arxiv.org/abs/1811.06965 `[A]`
- Narayanan et al. (2021), *Efficient Large-Scale Language Model Training on GPU Clusters Using Megatron-LM*：https://arxiv.org/abs/2104.04473 `[A]`
