---
aliases: [缩放定律, Scaling Laws, Chinchilla]
tags: [大模型, 训练, 教程, 学习教程]
prerequisites: ["[[训练目标]]"]
next: ["[[训练稳定性]]"]
---
# 缩放定律

## 一句话定义

缩放定律是在特定实验范围内，模型损失与参数量、数据量、计算量之间呈现的经验幂律关系。

## 基本形式

例如损失随参数量 $N$ 的不可约部分加幂律下降：

$$L(N)\approx L_\infty+aN^{-\alpha}$$

- $L_\infty$：给定数据分布下不可继续消除的部分。
- $a,\alpha$：从实验拟合得到，不是普适常数。

在对数坐标下，幂律接近直线，便于外推。

## Kaplan 与 Chinchilla

Kaplan 等 2020 研究发现语言模型损失随模型、数据和计算规模呈经验幂律，并提出当时实验下的计算分配建议。

Hoffmann 等 2022（Chinchilla）在不同设置和更多训练组合上发现：固定计算预算时，应给模型使用比先前常见做法更多的数据；其论文中的计算最优结果约表现为参数量与训练 token 数同比例增长。

这些结论依赖模型族、优化方法、数据和实验范围。不能把某个固定“每参数 20 token”当作适用于所有现代模型的自然定律。

## 计算预算

Dense Transformer 训练计算常粗略估计为：

$$C\approx6ND$$

$N$ 是非嵌入参数量、$D$ 是训练 token 数。系数 6 是特定简化下前向和反向计算估计，不包含所有系统开销。

## 能告诉我们什么

- 在训练超大模型前，用小模型实验估算趋势。
- 判断增加参数还是增加数据更划算。
- 估计计算预算和预期损失。

不能单独预测涌现能力、事实可靠性或安全性。

## 概念与公式导航

- [[缩放定律与训练计算量公式|缩放定律与训练计算量公式]]

## 练习

1. 参数翻四倍、$\alpha=0.5$，幂律项变成多少？
2. $N=10^9,D=2\times10^{10}$，粗算 $6ND$。
3. 为什么缩放定律叫经验规律而不是数学定理？
4. 固定计算下为什么模型过大、数据过少可能不理想？

## 来源

- Kaplan, J. et al. (2020). *Scaling Laws for Neural Language Models*. https://arxiv.org/abs/2001.08361 `[A]`
- Hoffmann, J. et al. (2022). *Training Compute-Optimal Large Language Models*. https://arxiv.org/abs/2203.15556 `[A]`