---
aliases: [Calculus of Variations, Variational Calculus]
english_name: Calculus of Variations
tags: [数学, 变分法, 弦理论先修, 弦理论]
prerequisites:
  - "[[泛函]]"
  - "[[积分]]"
  - "[[导数]]"
used_by:
  - "[[Euler-Lagrange 方程]]"
  - "[[Hamilton 原理]]"
  - "[[Polyakov 作用量]]"
evidence_status: 教材事实
---

# 变分法（Calculus of Variations）

## 一句话解释

变分法不是在许多数中找极值，而是在许多可能函数或路径中寻找使某个泛函的一阶变化为零的函数。

## 为什么要学它

经典力学、场论和弦理论都可由作用量变分得到运动方程。一次统一的变分计算，可以同时给出内部方程、边界条件和守恒结构。

## 从普通极值到函数极值

普通函数 $f(x)$ 在极值候选点满足：

$$
f'(x)=0.
$$

变分法把变量 $x$ 换成整条函数 $y(x)$，把普通函数换成泛函 $J[y]$。驻定条件写成：

$$
\delta J=0.
$$

“驻定”比“最小”更准确，因为作用量可能是最小值、最大值或鞍点。

## 路径的微小变化

取一条候选函数 $y(x)$，考虑附近函数：

$$
y_\varepsilon(x)=y(x)+\varepsilon\eta(x).
$$

$\eta(x)$ 是任意允许变化，$\varepsilon$ 控制变化大小。定义一阶变分：

$$
\delta J
=
\left.\frac d{d\varepsilon}
J[y+\varepsilon\eta]
\right|_{\varepsilon=0}.
$$

若对所有允许 $\eta$ 都有 $\delta J=0$，候选函数才驻定。

## 固定端点

若路径端点固定：

$$
y(a)=y_a,
\qquad y(b)=y_b,
$$

那么允许变化必须满足：

$$
\eta(a)=\eta(b)=0.
$$

如果端点不固定，边界项不会自动消失，会产生自然边界条件。

## 积分型泛函

考虑：

$$
J[y]=\int_a^bL(x,y,y')dx.
$$

代入变化后，一阶项为：

$$
\delta J=
\int_a^b
\left(
\frac{\partial L}{\partial y}\eta
+
\frac{\partial L}{\partial y'}\eta'
\right)dx.
$$

对第二项分部积分：

$$
\int_a^b
\frac{\partial L}{\partial y'}\eta'dx
=
\left[
\frac{\partial L}{\partial y'}\eta
\right]_a^b
-
\int_a^b
\frac d{dx}
\left(\frac{\partial L}{\partial y'}\right)
\eta dx.
$$

固定端点时边界项为零。

## 基本引理

若连续函数 $F(x)$ 对所有在端点消失的任意光滑 $\eta(x)$ 都满足：

$$
\int_a^bF(x)\eta(x)dx=0,
$$

则：

$$
F(x)=0
$$

在区间内部处处成立。这把整体积分条件变成局部微分方程。

## 最短路径例子

平面曲线长度泛函：

$$
J[y]=\int_a^b\sqrt{1+y'^2}\,dx.
$$

对它应用变分法可得到直线是两点间的驻定路径，并在 Euclidean 平面上给出最短路径。

## 作用量原理

粒子作用量：

$$
S[q]=\int L(q,\dot q,t)dt.
$$

要求：

$$
\delta S=0
$$

会导出 Euler–Lagrange 方程。弦作用量对整个二维函数 $X^\mu(\tau,\sigma)$ 变分，原理相同，但需要多变量分部积分。

## 边界项的重要性

边界项不能未经检查就丢弃。它可能因为：

- 变化在边界为零；
- 周期边界使贡献抵消；
- 选择 Neumann 条件；
- 加入额外边界作用量；

而消失或被处理。开弦的端点条件正来自这种分析。

## 常见误区

1. **$\delta S=0$ 表示作用量一定最小。** 错，只表示一阶驻定。
2. **只检查某一种变化 $\eta$ 就够。** 必须对所有允许变化成立。
3. **边界项总可以直接删除。** 错，必须说明边界条件。
4. **变分 $\delta y$ 等于时间导数 $dy/dt$。** 错，它描述在函数空间中换一条邻近路径。
5. **整体积分为零就自动说明被积函数为零。** 需要对任意测试变化成立并满足相应条件。

## 练习

1. 写出路径变化 $y\to y+\varepsilon\eta$。
2. 固定端点时 $\eta$ 满足什么条件？
3. 为什么要对含 $\eta'$ 的项分部积分？
4. $\delta S=0$ 是否保证全局最小？
5. 开弦变分中为什么必须保留边界项检查？

## 答案与提示

1. $y_\varepsilon=y+\varepsilon\eta$。
2. $\eta(a)=\eta(b)=0$。
3. 把导数从任意变化上移开，才能用基本引理得到局部方程。
4. 不保证，可能是最大或鞍点。
5. 世界面有边界，边界项决定允许的端点条件。

## 与其他概念的关系

- 变分法研究 [[泛函]] 的驻定点；
- [[Euler-Lagrange 方程]] 是其局部方程；
- [[Hamilton 原理]] 把物理轨迹定义为作用量驻定路径；
- 弦作用量变分同时给出运动方程和边界条件。

## 参考资料

- I. M. Gelfand and S. V. Fomin, *Calculus of Variations*
- [MIT OCW 8.09, Classical Mechanics III](https://ocw.mit.edu/courses/8-09-classical-mechanics-iii-fall-2014/)
