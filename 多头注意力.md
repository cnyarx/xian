---
aliases: [多头注意力, Multi-Head Attention, MHA]
tags: [Transformer, 注意力, 教程, 大模型, 学习教程]
prerequisites: ["[[缩放点积注意力]]"]
next: ["[[位置编码]]"]
---
# 多头注意力

## 一句话定义

多头注意力把隐藏向量投影到多个较小子空间，每个头独立做注意力，再拼接结果。它允许同一层同时学习不同类型的关系。

## 公式

$$\operatorname{head}_i=\operatorname{Attention}(QW_i^Q,KW_i^K,VW_i^V)$$

$$\operatorname{MHA}(Q,K,V)=\operatorname{Concat}(\operatorname{head}_1,\ldots,\operatorname{head}_h)W^O$$

- $h$：头数。
- $W_i^Q,W_i^K,W_i^V$：第 $i$ 个头的投影矩阵。
- `Concat`：沿特征方向拼接。
- $W^O$：把拼接结果混合回模型隐藏维度。

## 形状例子

模型维度 $d_{model}=4096$，头数 $h=32$，常见头维度：

$$d_{head}=4096/32=128$$

输入形状 `[batch, sequence, 4096]`，拆成 `[batch, 32, sequence, 128]`，每个头并行计算。

## 为什么多头

单头只产生一套注意力分布。多头可在不同投影空间中关注不同关系，例如局部搭配、指代和长距离依赖。但头的功能通常是分布式形成的，不能保证每个头有固定可读职责。

## 参数量

标准 MHA 的 Q、K、V、O 投影各约有 $d_{model}^2$ 个权重，总计约：

$$4d_{model}^2$$

拆成更多头通常不会改变总投影参数量，因为每头维度相应变小。

## 头数不是越多越好

头多意味着每个头可能更窄。过窄会限制每头表达；过多还会增加实现与通信开销。头数、头维度和模型宽度需共同设计。

## MHA、GQA、MQA

标准 MHA 中每个 Query 头有自己的 K、V 头。GQA 让一组 Query 头共享 K、V；MQA 让所有 Query 头共享一组 K、V。它们主要减少生成时 KV cache，详见 [[注意力变体：GQA 与 MQA]]。

## 概念与公式导航

- [[注意力分数与注意力权重|注意力分数与注意力权重]]
- [[缩放点积与多头注意力公式|缩放点积与多头注意力公式]]

## 练习

1. 模型维度 768、12 个头，每头维度是多少？
2. 解释为什么多头不是简单复制同一个注意力。
3. 拆成更多头为何不一定增加投影参数量？
4. 为什么不能轻易把某个头解释成“语法头”？

## 来源

- Vaswani et al. (2017). *Attention Is All You Need*, §3.2.2 https://arxiv.org/abs/1706.03762 `[A]`
- Michel, P. et al. (2019). *Are Sixteen Heads Really Better than One?* NeurIPS https://arxiv.org/abs/1905.10650 `[B]`