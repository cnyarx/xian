---
aliases:
  - Moduli Space
  - Moduli Spaces
english_name: Moduli Space
tags:
  - 数学
  - 几何学
  - 黎曼曲面
  - 弦理论先修
  - 弦理论
prerequisites:
  - "[[集合]]"
  - "[[拓扑空间]]"
  - "[[流形]]"
  - "[[群作用]]"
  - "[[微分几何]]"
  - "[[黎曼曲面]]"
  - "[[亏格]]"
used_by:
  - "[[Teichmüller 空间]]"
  - "[[Deligne-Mumford 紧化]]"
  - "[[模空间积分]]"
  - "[[环面配分函数]]"
  - "[[顶点算符]]"
  - "[[弦散射振幅]]"
  - "[[亏格展开]]"
evidence_status: 教材事实
---

# 模空间（Moduli Space）

## 一句话解释

模空间把“同一种对象所有真正不同的形状或结构”整理成一个空间，其中只差坐标重命名或允许对称变换的对象被视为同一点。

## 为什么要学它

一个几何对象即使拓扑类型固定，也可能仍有连续变化的形状参数。

例如所有复环面拓扑上都是甜甜圈，但它们的复结构可随参数 $\tau$ 改变。与此同时，许多不同的 $\tau$ 只是同一个格选了不同基，不应重复计数。

模空间同时解决两个问题：

1. 哪些变化是真正不同的结构；
2. 哪些变化只是等价描述，应当识别。

弦理论中，局部的世界面微分同胚与 Weyl 对称可以简化度量，却不能消去全局形状。剩余的有限维全局参数就是模。弦振幅必须对这些模积分，因此模空间不是可选的抽象装饰，而是微扰弦论积分域的一部分。

## 直观图景

想象一批纸做的平行四边形。可以改变倾斜角和高宽比，这些变化看起来给出不同形状。

但若只是在同一个格中重新选择两条整数基向量，平行四边形会变，所生成的复环面却不变。

所以正确的参数空间不是“所有平行四边形”，而是：

$$
\frac{\{\text{允许的参数}\}}
{\{\text{不改变对象的等价变换}\}}.
$$

分子负责列出候选对象，分母负责删掉重复描述。

## 正式定义的基本思想

“模空间”没有一个脱离具体分类问题的单一公式。一般先指定：

- 一类对象 $\mathcal C$；
- 对象之间的等价关系 $\sim$；
- 对象怎样在参数变化下连续或光滑变化。

最粗略的集合级模空间是等价类集合：

$$
\mathcal M=\mathcal C/\!\sim.
$$

其中一个点 $[X]\in\mathcal M$ 代表对象 $X$ 的整个等价类，而不是某个特定坐标表达。

若等价关系由群 $G$ 的作用产生，则常写成：

$$
\mathcal M=\mathcal C/G.
$$

但真正的模空间通常还需要拓扑、光滑、复解析、代数或 stack 结构；只知道商集往往不够。

## 每个符号是什么意思

- $\mathcal C$：待分类的对象集合或参数空间；
- $\sim$：规定何时两个对象等价；
- $[X]$：对象 $X$ 的等价类；
- $G$：产生等价变换的群；
- $\mathcal C/G$：按群轨道取商；
- $\mathcal M_g$：亏格为 $g$ 的紧致黎曼曲面的模空间；
- $\mathcal M_{g,n}$：带 $n$ 个有序标记点的亏格 $g$ 曲面模空间；
- $\overline{\mathcal M}_{g,n}$：加入稳定退化曲线后的 Deligne–Mumford 紧化；
- $\mathcal T_g$：亏格 $g$ 的 Teichmüller 空间；
- $\Gamma_g$：映射类群；
- $\tau$：复环面的复结构参数；
- $[X]$ 中的方括号：表示“只记等价类”。

## 参数空间不等于模空间

参数空间可能多次描述同一个对象。

设参数 $p\in P$ 构造对象 $X_p$。若存在不同参数：

$$
p\ne q
$$

却有：

$$
X_p\cong X_q,
$$

那么 $P$ 只是带冗余的参数空间，不是最终模空间。

需要把等价参数识别：

$$
\mathcal M=P/\!\sim.
$$

这里的 $\cong$ 必须由问题指定。例如可表示同胚、微分同胚、等距、双全纯同构、规范等价或其他结构保持同构。改变等价关系会改变模空间。

## 一个有限例子：三角形的相似形状

考虑边长为正数的三角形：

$$
(a,b,c),
$$

满足三角不等式。

若只关心相似形状，整体缩放：

$$
(a,b,c)\sim(\lambda a,\lambda b,\lambda c),
\qquad \lambda>0
$$

不产生新形状。

可用条件：

$$
a+b+c=1
$$

固定缩放冗余。剩余满足三角不等式的区域可参数化相似三角形。

若还不区分三个顶点标签，则需进一步除以边的置换。这个简单例子展示：连续冗余和离散冗余可能同时存在。

## 环面的模参数

复环面可写成：

$$
E_\tau
=\mathbb C/(\mathbb Z+\tau\mathbb Z),
\qquad
\operatorname{Im}\tau>0.
$$

因此参数 $\tau$ 位于上半平面：

$$
\mathbb H
=\{\tau\in\mathbb C\mid\operatorname{Im}\tau>0\}.
$$

$\operatorname{Re}\tau$ 控制格的倾斜，$\operatorname{Im}\tau$ 控制相对高度。不同的 $\tau$ 可以给出双全纯等价的环面。

## 为什么要除以模群

改变格的整数基不会改变格本身。基变换由：

$$
\begin{pmatrix}a&b\\c&d\end{pmatrix}
\in SL(2,\mathbb Z)
$$

给出，并使：

$$
\tau\longmapsto
\frac{a\tau+b}{c\tau+d}.
$$

所以环面复结构的复解析 orbifold 参数空间可写成：

$$
\mathcal M_1^{\mathrm{orb}}
\cong SL(2,\mathbb Z)\backslash\mathbb H.
$$

这里采用左商记号；也常见 $\mathbb H/SL(2,\mathbb Z)$ 的非严格简写，必须结合作者的作用约定理解。

因为 $A$ 与 $-A$ 对 $\tau$ 作用相同，有效作用群是 $PSL(2,\mathbb Z)$。若只保留同构类而忘掉稳定子，$j$-不变量给出其粗解析空间与复平面 $\mathbb C$ 的对应；在代数几何语言中相应粗模空间是 $j$-线 $\mathbb A^1$。

## 环面基本域

为了避免对模群相关参数重复积分，可选一个基本域：

$$
\mathcal F
=\left\{
\tau\in\mathbb H
\ \middle|\
|\operatorname{Re}\tau|\le\frac12,
\quad |\tau|\ge1
\right\},
$$

并对部分边界作适当识别。

基本域不是说模空间真的等于一个完全不粘边的普通平面区域。边界识别和具有额外自同构的特殊点仍保留重要信息。

## 特殊环面与稳定子

一般 $\tau$ 的模群稳定子只有由 $\pm I$ 导致的无效部分。某些特殊参数有额外自同构，例如：

$$
\tau=i,
\qquad
\tau=e^{2\pi i/3}.
$$

它们在粗商空间中对应 orbifold 点。

这说明仅把每个等价类压成一个点会忘掉“该对象有多少自同构”。stack 或 orbifold 语言能保留这些稳定子信息。

## 黎曼曲面模空间

固定亏格 $g$，把紧致、连通黎曼曲面按双全纯同构分类，得到：

$$
\mathcal M_g.
$$

一个点：

$$
[\Sigma]\in\mathcal M_g
$$

表示黎曼曲面 $\Sigma$ 的双全纯同构类。

固定亏格只固定底层拓扑类型，不固定复结构。因此除低亏格特殊情形外，同一拓扑曲面上存在连续多族不等价复结构。

## 低亏格情形

对于无标记紧致连通黎曼曲面：

- $g=0$：只有 Riemann 球面一种复结构，所以粗模空间只有一个点；
- $g=1$：模空间复维数为 $1$，可由环面参数 $\tau$ 描述并除以模群；
- $g\ge2$：模空间复维数为：

$$
\boxed{\dim_{\mathbb C}\mathcal M_g=3g-3}.
$$

公式 $3g-3$ 不能直接代入 $g=0,1$；低亏格的连续自同构群使它们必须单独处理。

## 带标记点的模空间

给曲面加入 $n$ 个两两不同的有序标记点：

$$
(\Sigma;p_1,\ldots,p_n),
$$

得到模空间：

$$
\mathcal M_{g,n}.
$$

在稳定条件：

$$
2g-2+n>0
$$

下，其复维数为：

$$
\boxed{
\dim_{\mathbb C}\mathcal M_{g,n}
=3g-3+n
}.
$$

每个自由标记点通常贡献一个复参数，但全局自同构会消去一部分位置自由度。

## 球面标记点为什么得到 $n-3$

球面的 Möbius 群 $PSL(2,\mathbb C)$ 可将任意三个互异有序点依次映到：

$$
0,1,\infty.
$$

因此 $n$ 个有序标记点的位置原有 $n$ 个复参数，除去三个复维的 Möbius 冗余后：

$$
\dim_{\mathbb C}\mathcal M_{0,n}
=n-3,
\qquad n\ge3.
$$

当 $n=3$ 时，模空间是一个点；当 $n=4$ 时，只剩一个复模，可用交比描述。

## 四点交比

四个互异点 $z_1,z_2,z_3,z_4$ 的交比可写成：

$$
x
=\frac{(z_1-z_2)(z_3-z_4)}
{(z_1-z_3)(z_2-z_4)}.
$$

它在 Möbius 变换下不变。

利用 Möbius 对称把三个点送到 $0,1,\infty$ 后，第四点的位置就是剩余模参数的一种选择。

若改变点的排列，交比会变成 $x$ 的六种相关表达之一；有序与无序标记点的模空间因此不同。

## 稳定条件是什么意思

稳定标记曲线要求自同构群是有限群。对光滑亏格 $g$、带 $n$ 个标记点的曲面，这等价于：

$$
2g-2+n>0.
$$

例子：

- 无标记球面不稳定，因为 $PSL(2,\mathbb C)$ 是正维群；
- 带三个互异标记点的球面稳定，因为保持三个点的 Möbius 变换只有恒等变换；
- 无标记光滑环面仍有平移自同构，因此不稳定；
- 带一个标记点的环面进入稳定范围；
- 无标记 $g\ge2$ 曲面的自同构群有限。

稳定性不是说曲面“不会运动”，而是控制自同构并使紧化理论具有良好有限型性质。

## Teichmüller 空间为何出现

直接研究 $\mathcal M_g$ 的商可能困难。一个常见做法是先给曲面增加标记。

固定参考拓扑曲面 $S_g$。Teichmüller 空间 $\mathcal T_g$ 的点可理解为带标记的复结构：

$$
(\Sigma,f),
$$

其中 $f:S_g\to\Sigma$ 是定向保持微分同胚，并按与恒等映射同痕的重新标记取等价。这里“同痕”表示存在一族连续变化的微分同胚把一个重新标记变到另一个；对闭曲面的相关微分同胚，标准定理把它与适当的同伦表述联系起来，但 Teichmüller 理论通常使用同痕作为定义。

映射类群：

$$
\Gamma_g
=\operatorname{Diff}^+(S_g)/\operatorname{Diff}_0(S_g)
$$

改变标记，模空间形式上是：

$$
\mathcal M_g
\cong\mathcal T_g/\Gamma_g.
$$

对 $g\ge2$，$\mathcal T_g$ 是复维 $3g-3$ 的复流形，并且可缩；商中的有限稳定子使 $\mathcal M_g$ 自然带 orbifold 或 stack 特征。

## 为什么模空间不总是流形

若对象 $X$ 有非平凡自同构群：

$$
\operatorname{Aut}(X)\ne\{e\},
$$

那么表示 $[X]$ 的商点有稳定子。

商在该点附近可能像：

$$
V/\operatorname{Aut}(X),
$$

而不是普通开球 $V$。

因此模问题可能自然产生：

- orbifold：局部为有限群商；
- 分层空间：不同区域维数或稳定子类型不同；
- stack：不仅记录等价类，还记录对象间同构与自同构；
- 导出模空间：进一步记录方程、对称与高阶形变障碍。

本篇主线只要求理解前两层，并知道粗模空间会丢失自同构信息。

## 粗模空间与精细模空间

粗模空间把每个同构类对应到一个点，适合回答“有多少种等价类”。

精细模空间还应携带一个普遍族，使任意参数空间上的对象族都能由它唯一拉回得到。

自同构常妨碍精细模空间作为普通空间存在。此时 stack 能比粗模空间更忠实地表达模问题。

所以“存在一个等价类集合”不等于“存在一个具有普遍性质的良好精细模空间”。

## 切空间与无穷小形变

模空间一点附近的切向量描述对象的一阶无穷小形变。

对 $g\ge2$ 的紧致黎曼曲面 $\Sigma$，模空间在 $[\Sigma]$ 附近的复切空间可表示为：

$$
T_{[\Sigma]}\mathcal M_g
\cong H^1(\Sigma,T_\Sigma)
$$

在忽略 orbifold 稳定子并采用标准形变理论解释时成立。

由 Serre 对偶，其对偶空间与全纯二次微分相关：

$$
H^1(\Sigma,T_\Sigma)^*
\cong H^0(\Sigma,K_\Sigma^{\otimes2}).
$$

Riemann–Roch 给出：

$$
\dim_{\mathbb C}H^0(\Sigma,K_\Sigma^{\otimes2})
=3g-3,
$$

从而解释 $\mathcal M_g$ 的局部复维数。完整证明需要层上同调、Serre 对偶与 Riemann–Roch，本篇只说明公式来源。

## 模空间为什么通常非紧

一族光滑曲面可以逐渐发展出越来越细长的颈部。例如某条闭合周期长度趋近于零，极限形成节点。

在光滑曲面模空间 $\mathcal M_{g,n}$ 内，这个极限通常没有对应点，因此序列会“跑到无穷远”，使模空间非紧。

非紧不是说模空间没有边界坐标图，而是说某些对象族的极限离开了原先只含光滑曲面的分类范围。

## Deligne–Mumford 紧化

加入适当的稳定节点曲线，可得到：

$$
\overline{\mathcal M}_{g,n}.
$$

它称为 Deligne–Mumford 紧化。

边界：

$$
\overline{\mathcal M}_{g,n}
\setminus\mathcal M_{g,n}
$$

由退化但稳定的节点曲线组成。

节点局部可直观理解为两支曲面在一点相交。把节点展开成光滑细颈的局部平滑族常写成：

$$
zw=q.
$$

当 $q\ne0$ 时得到细颈；当：

$$
q\to0
$$

时，颈部夹缩成节点。

## 分离退化与非分离退化

夹缩闭合曲线时有两类基本情况：

1. **分离退化**：夹缩后曲面分成两个连通分支，它们在节点处连接；
2. **非分离退化**：夹缩后曲面仍连通，但亏格降低一，并出现一对待粘合点。

这两类退化在弦振幅中分别对应不同的因子化通道。

稳定化要求每个亏格零分支至少有三个特殊点，每个亏格一分支至少有一个特殊点；特殊点包括标记点和节点支点。

## 模空间与弦世界面

Polyakov 路径积分要对世界面度量求和。形式上：

$$
\int\frac{\mathcal Dh}{\operatorname{Diff}\times\operatorname{Weyl}}\cdots
$$

表示把度量按世界面微分同胚与 Weyl 变换取商。

局部规范固定后，无限维度量自由度大部分被消去，但固定拓扑类型上仍留下有限维模参数。

对定向闭弦、亏格 $g$、带 $n$ 个插入点的振幅，这些参数由：

$$
\mathcal M_{g,n}
$$

组织。

严格的路径积分测度还包含 ghost、行列式、零模和顶点算符因素，不能把它简化成普通 Euclidean 体积积分。

## 树级球面振幅

球面上 $n$ 个闭弦顶点算符的位置属于 $\mathcal M_{0,n}$。

Möbius 对称可固定三个插入点，所以剩余复维数为：

$$
n-3.
$$

例如四点振幅只需对一个复模积分。这个模可选为第四个点的位置或四点交比。

“球面没有复结构模”只适用于无标记球面；有足够多插入点后，标记点相对位置仍产生模。

## 一圈环面振幅

无标记环面的复结构由：

$$
\tau=\tau_1+i\tau_2,
\qquad \tau_2>0
$$

描述。

一圈闭弦振幅要在模群基本域上积分，而不是整个上半平面重复积分：

$$
\int_{\mathcal F}
\frac{d^2\tau}{\tau_2^2}
\,\mathcal Z(\tau,\bar\tau)
$$

是常见结构示意。

这里 $d^2\tau=d\tau_1d\tau_2$；具体测度幂次与被积函数取决于理论、规范固定和约定。模不变性保证等价环面描述不会改变物理结果。

## 高亏格振幅

亏格 $g\ge2$ 的闭弦真空世界面具有：

$$
3g-3
$$

个复模。

带 $n$ 个标记点时，在稳定范围内增加为：

$$
3g-3+n.
$$

弦微扰论每一阶都要在相应模空间上积分，并处理其退化区域。退化极限与长细管中的中间弦态传播相联系，从而产生振幅因子化。

## 模空间边界与因子化

在：

$$
zw=q,
\qquad q\to0
$$

的退化区域，世界面出现长细管。

从目标时空散射角度看，这对应一个中间弦态传播很长的固有时间。对完整弦态求和后，振幅应在该极限分解成低阶振幅与传播子的组合。

因此模空间边界不仅是数学上的补点，也是检验幺正性、因子化与发散结构的重要区域。

## 紫外与红外解释的边界

世界面上的短距离区域经过模变换后，可能与目标时空中的长距离传播相关。一圈振幅的模不变性会把基本域组织得与普通点粒子 Schwinger 参数积分不同。

但不能笼统地说“弦理论绝无紫外发散”。特定背景和振幅可因快子、质量为零态、非紧方向体积、退化区域或不稳定背景而出现发散。判断发散来源必须分析具体模空间边界和谱。

## 开弦与非定向世界面

开弦世界面有边界，非定向弦还可有 crosscap。此时拓扑类型不只由亏格 $g$ 决定，还需边界数、crosscap 数和标记点数据。

相应模空间可通过带边界曲面、双覆盖、Klein 曲面或实代数曲线等语言描述。

所以 $\mathcal M_{g,n}$ 的标准复曲线记号最直接对应定向闭弦世界面；不能无条件覆盖所有开弦和非定向情形。

## 超弦中的超模空间

RNS 超弦世界面除普通复结构外，还带超几何结构。相应参数空间是超黎曼曲面的超模空间。

在某些亏格和振幅中，可通过 picture-changing operator 等方法把计算表达成普通模空间上的形式；但这并不意味着超模空间在所有情形都能全局无问题地投影为普通模空间。

高亏格超弦测度与超模空间积分存在额外细节。本篇不把玻色曲面模空间结论无条件推广到超弦。

## 规范理论中的模空间

模空间不只属于黎曼曲面。规范理论中也常见：

$$
\mathcal M
=\frac{\{A\mid F_A=0\}}
{\mathcal G},
$$

即平坦联络按规范群取商。

还可分类 instanton、monopole、Yang–Mills 方程解或真空解。

若某个解有非平凡规范稳定子，商点可能是奇点。边界条件决定哪些规范变换应被商掉，也决定模空间的物理意义。

## 模与动力学场的区别

“模”有时指模空间的坐标，有时指低能理论中的无质量标量场。

紧致化中，内部空间形状可随四维时空位置缓慢变化：

$$
m^I=m^I(x).
$$

这些参数在低能有效理论中表现为模场。量子修正、通量或非微扰效应可能产生势能并固定部分模。

因此“几何上存在模参数”不保证对应标量在完整量子理论中始终无质量。

## 适用条件与事实边界

1. 模空间依赖对象类别和等价关系；改变等价关系会改变分类结果。
2. 参数空间可能重复描述同一对象，必须除以允许的等价变换。
3. 商集只记录等价类；拓扑、复结构、orbifold 或 stack 信息需要额外构造。
4. 自同构会产生稳定子，使粗模空间可能有 orbifold 点或奇点，并妨碍普通精细模空间存在。
5. 无标记紧致黎曼曲面中，$g=0$ 粗模空间为一点，$g=1$ 复维数为一，$g\ge2$ 时 $\dim_\mathbb C\mathcal M_g=3g-3$。
6. $\dim_\mathbb C\mathcal M_{g,n}=3g-3+n$ 需要稳定条件 $2g-2+n>0$。
7. 标记点默认两两不同且有序；无序标记点还需除以置换群。
8. Teichmüller 空间保留标记，模空间再按映射类群取商。
9. $\mathcal M_{g,n}$ 只含光滑曲线，通常非紧；节点退化位于 $\overline{\mathcal M}_{g,n}$ 的边界。
10. Deligne–Mumford 紧化加入稳定节点曲线，不是把任意奇异曲线全部加入。
11. 弦路径积分中的模空间测度包含规范固定、ghost、零模和行列式信息，不是随意选择的普通体积元。
12. 球面无标记时没有复结构模，但四点及更多插入产生标记点模。
13. 环面参数须按模群识别；在整个上半平面积分会重复计数等价复结构。
14. 模空间退化区域控制弦振幅因子化，也可能产生红外或其他发散；不能脱离具体谱判断。
15. 开弦、非定向弦和超弦需要带边界、Klein 或超模空间等扩展结构。
16. 低能模场可能被势能稳定，不一定在完整理论中保持无质量。

## 常见误区

1. **模空间就是所有参数的集合。** 参数可能有冗余，模空间要先识别等价对象。
2. **两个对象坐标公式不同就一定是不同模点。** 若存在允许的结构保持同构，它们属于同一点。
3. **取商后一定得到普通光滑流形。** 自同构会产生 orbifold 点、奇点或 stack 结构。
4. **粗模空间已保存全部信息。** 它通常忘掉每个对象的自同构群和族的普遍性质。
5. **$3g-3$ 对所有亏格都成立。** 无标记 $g=0,1$ 必须单独处理。
6. **加入一个标记点总是简单增加一个参数。** 还要除去自同构，并满足稳定条件。
7. **所有 $\tau\in\mathbb H$ 都代表不同环面。** 模群相关的参数表示同一复结构。
8. **基本域边界不需要识别。** 边界仍有模群粘合，特殊点还带稳定子。
9. **$\mathcal M_{g,n}$ 已包含退化曲面。** 退化稳定曲线属于紧化 $\overline{\mathcal M}_{g,n}$ 的边界。
10. **局部共形规范消除了世界面全部度量自由度。** 全局模仍要积分。
11. **球面振幅永远没有模积分。** 四个及更多标记点有相对位置模。
12. **模空间积分只是几何体积。** 实际测度由世界面场论、ghost 与规范固定共同决定。
13. **模不变性自动保证任何弦背景都有限。** 快子、质量为零态和不稳定背景仍可能造成发散。
14. **玻色模空间可无条件替代超弦超模空间。** 高亏格超弦存在额外超几何和全局积分问题。

## 练习

1. 参数空间与模空间有什么区别？
2. 为什么模空间必须先指定等价关系？
3. 写出由群作用产生模空间的形式商。
4. 复环面 $E_\tau$ 怎样由 $\tau$ 构造？
5. 模群怎样作用于 $\tau$？
6. 为什么有效作用群是 $PSL(2,\mathbb Z)$？
7. 写出一个环面模群基本域的常见条件。
8. 为什么特殊环面参数会产生 orbifold 点？
9. $\mathcal M_g$ 的一个点代表什么？
10. 无标记 $g=0,1,\ge2$ 模空间维数分别怎样处理？
11. 写出稳定范围内 $\mathcal M_{g,n}$ 的复维数。
12. 稳定条件是什么，它控制什么？
13. 为什么 $\dim_\mathbb C\mathcal M_{0,n}=n-3$？
14. 四点交比为什么能作为球面四点模参数？
15. Teichmüller 空间与模空间有什么关系？
16. 粗模空间为什么可能丢失信息？
17. 为什么 $\mathcal M_{g,n}$ 通常非紧？
18. $zw=q$ 中 $q\to0$ 表示什么几何退化？
19. Deligne–Mumford 紧化加入什么对象？
20. 弦路径积分为什么要对模空间积分？
21. 球面四点振幅有多少个复模？
22. 一圈环面振幅为什么不能在整个 $\mathbb H$ 上重复积分？
23. 模空间边界为什么与振幅因子化相关？
24. 几何模参数为什么不一定对应完整理论中的无质量场？

## 答案与提示

1. 参数空间可重复描述同一对象；模空间把允许等价的参数识别。
2. 同胚、等距、双全纯或规范等价会给出不同分类问题。
3. $\mathcal M=\mathcal C/G$，但严格结构可能不只是商集。
4. $E_\tau=\mathbb C/(\mathbb Z+\tau\mathbb Z)$，且 $\operatorname{Im}\tau>0$。
5. $\tau\mapsto(a\tau+b)/(c\tau+d)$，矩阵属于 $SL(2,\mathbb Z)$。
6. $A$ 与 $-A$ 诱导同一分式线性变换，作用核为 $\{\pm I\}$。
7. $|\operatorname{Re}\tau|\le1/2$ 且 $|\tau|\ge1$，同时要作边界识别。
8. 特殊对象有额外自同构，即模群稳定子非平凡。
9. 一个亏格 $g$ 紧致黎曼曲面的双全纯同构类。
10. $g=0$ 为一点；$g=1$ 复维一；$g\ge2$ 为 $3g-3$。
11. 在 $2g-2+n>0$ 时为 $3g-3+n$。
12. $2g-2+n>0$；它等价于光滑标记曲线的自同构群有限。
13. $n$ 个点给出 $n$ 个复坐标，球面 Möbius 群可固定三个互异点。
14. 它在 Möbius 变换下不变，固定三个点后记录第四点的相对位置。
15. $\mathcal M_g\cong\mathcal T_g/\Gamma_g$；前者再除去了改变标记的映射类群。
16. 把等价类压成一点会忘掉对象的自同构群和普遍族信息。
17. 光滑曲面族可夹缩周期形成节点，而节点不属于 $\mathcal M_{g,n}$。
18. 非零 $q$ 是细颈，$q\to0$ 时颈部夹缩成节点。
19. 满足稳定条件的节点曲线。
20. 局部 Diff 与 Weyl 规范固定后仍留下不能消去的全局复结构和标记点参数。
21. 一个复模，可由第四点位置或交比表示。
22. 模群相关的 $\tau$ 描述同一环面，在全上半平面积分会重复计数。
23. 退化长细管对应中间弦态长时间传播，使振幅分解成低阶振幅和传播子。
24. 通量、量子修正或非微扰效应可产生模势并赋予模场质量。

## 与其他概念的关系

- [[集合]] 提供等价类和商集的基础语言；
- [[拓扑空间]] 说明模空间为何还需拓扑而不只是点集；
- [[流形]] 与 [[微分几何]] 描述模空间的局部坐标、切空间和几何；
- [[群作用]] 产生参数冗余、轨道、稳定子与商空间；
- [[黎曼曲面]] 是弦世界面模问题的基本对象；
- [[亏格]] 固定世界面的拓扑类型并决定模空间维数的主公式；
- [[Teichmüller 空间]] 保留拓扑标记，再由映射类群取商得到模空间；
- [[上同调]] 描述无穷小形变与模空间切空间；
- [[Riemann-Roch 定理]] 帮助推出 $3g-3$ 维数；
- [[Deligne-Mumford 紧化]] 加入稳定节点退化；
- [[世界面微分同胚]] 与 [[Weyl 对称性]] 消除局部世界面度量冗余；
- [[规范固定]]、[[Faddeev-Popov 鬼场]] 和 [[BRST 对称性]] 构造正确的模空间积分测度；
- [[顶点算符]] 在世界面上产生标记点；
- [[环面配分函数]] 要求在环面模群下正确变换；
- [[模不变性]] 避免重复计算等价环面；
- [[模空间积分]] 是弦散射振幅每个亏格阶的核心步骤；
- [[亏格展开]] 按不同 $\mathcal M_{g,n}$ 组织微扰阶数。

## 参考资料

- Joe Harris and Ian Morrison, *Moduli of Curves*, Springer, 1998.
- Benson Farb and Dan Margalit, *A Primer on Mapping Class Groups*, Princeton University Press, 2012.
- John H. Hubbard, *Teichmüller Theory and Applications to Geometry, Topology, and Dynamics, Vol. 1*, Matrix Editions, 2006.
- Jürgen Jost, *Compact Riemann Surfaces*, 3rd ed., Springer, 2006.
- Otto Forster, *Lectures on Riemann Surfaces*, Springer, 1981.
- Pierre Deligne and David Mumford, “The Irreducibility of the Space of Curves of Given Genus,” *Publications Mathématiques de l'IHÉS* 36 (1969), 75–109, [DOI](https://doi.org/10.1007/BF02684599)
- David Mumford, “Stability of Projective Varieties,” *L'Enseignement Mathématique* 23 (1977), 39–110.
- Edward Witten, “Notes On Super Riemann Surfaces And Their Moduli,” *Pure and Applied Mathematics Quarterly* 15 (2019), [arXiv:1209.2459](https://arxiv.org/abs/1209.2459)
- Eric D'Hoker and D. H. Phong, “The Geometry of String Perturbation Theory,” *Reviews of Modern Physics* 60 (1988), 917–1065, [DOI](https://doi.org/10.1103/RevModPhys.60.917)
- Joseph Polchinski, *String Theory, Vol. 1*, Cambridge University Press, 1998, Chapters 6–7.
- Michael B. Green, John H. Schwarz, and Edward Witten, *Superstring Theory, Vol. 1*, Cambridge University Press, 1987.
- David Tong, *String Theory*, arXiv:0908.0333, [arXiv](https://arxiv.org/abs/0908.0333)
