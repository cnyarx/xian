---
aliases: [损失函数, MSE, 交叉熵, 损失]
tags: [机器学习, 教程, 大模型, 学习教程]
prerequisites: ["[[数据与特征]]", "[[信息论基础]]"]
next: ["[[梯度下降]]"]
---
# 损失函数

## 一句话定义

**损失函数**衡量模型预测和真实答案之间的差距。损失值越小，模型越好。

损失函数是训练的目标函数，模型通过学习让损失不断下降。

## 均值平方误差

**MSE** 用于回归问题。计算预测值和真实值之间差的平方，再取平均。

$$
\text{MSE} = \frac{1}{n}\sum_{i=1}^{n}(y_i - \hat{y}_i)^2
$$

- $y_i$：真实值。
- $\hat{y}_i$：模型预测值。
- 平方确保正负误差不会相互抵消。

**数值例子**：

```
真实值：  [100, 200, 300]
预测值：  [110, 190, 310]
误差：    [10, -10, 10]
平方误差：[100, 100, 100]
MSE：     (100+100+100)/3 = 100
```

## 交叉熵

**交叉熵**用于分类问题，是大模型训练的核心损失函数。

$$
\text{CrossEntropy} = -\sum_{i} y_i \log(\hat{y}_i)
$$

- $y_i$：真实分布（通常是 one-hot，也就是正确答案位置为 1，其余为 0）。
- $\hat{y}_i$：模型预测的概率分布。

**在大模型中的应用**：每个位置，模型预测下一个 token 的概率分布，正确答案是实际出现的 token。取对数加负号，得到该位置的损失。所有位置的平均损失就是模型训练时的 loss。

**数值例子**：

```
词表：["好", "不错", "热"]

真实下一个 token 是 "好" → 真实分布：[1, 0, 0]

模型预测：[0.7, 0.2, 0.1]

CrossEntropy = -log(0.7) ≈ 0.357
```

如果模型预测更准确，概率更高，损失更小：

```
模型预测：[0.9, 0.05, 0.05]

CrossEntropy = -log(0.9) ≈ 0.105
```

## 为什么交叉熵有效

正确类别的预测概率越低，负对数损失越大；概率越高，损失越接近 0：

| 预测概率 | 损失 |
|---|---:|
| 0.99 | 0.01 |
| 0.90 | 0.11 |
| 0.50 | 0.69 |
| 0.10 | 2.30 |
| 0.01 | 4.61 |

要注意“损失很大”和“训练梯度无界增大”不是一回事。若直接把损失 $L=-\log p_y$ 对正确类别概率 $p_y$ 求导，则：

$$
\frac{\partial L}{\partial p_y}=-\frac{1}{p_y}
$$

$p_y$ 越小，这个导数的绝对值越大。但神经网络通常通过 softmax 前的 logit $z_i$ 反向传播；softmax 与交叉熵合并后：

$$
\frac{\partial L}{\partial z_i}=p_i-y_i
$$

单个 logit 的梯度分量在 $[-1,1]$ 内。低正确类别概率会产生方向明确、接近最大幅度的 logit 梯度，但最终参数梯度还取决于前面网络的雅可比、批量平均和优化器，不能仅凭损失值断言它一定“很大”。

## 常见误区

- 损失为 0 不一定是好事：可能过拟合。
- 训练损失下降，验证损失不降：可能过拟合。
- 不同损失函数之间不能直接比较。

## 概念与公式导航

- [[损失函数与评估指标|损失函数与评估指标]]
- [[熵交叉熵与KL散度公式|熵、交叉熵与 KL 散度公式]]

## 练习

1. 计算以下预测的 MSE：真实值 `[5, 10, 15]`，预测值 `[4, 11, 14]`。
2. 真实分布 `[1, 0, 0]`，模型预测 `[0.3, 0.4, 0.3]`，计算交叉熵。
3. 如果模型预测变成 `[0.8, 0.1, 0.1]`，交叉熵变大还是变小？
4. 解释为什么 MSE 不适合用于分类问题。

## 来源

- 信息论基础：Cover, T. M. & Thomas, J. A. (2006). *Elements of Information Theory* (2nd ed.). Wiley. `[A]`
- 损失函数：Goodfellow, I., Bengio, Y., & Courville, A. (2016). *Deep Learning*. MIT Press. 第6章 `[A]`