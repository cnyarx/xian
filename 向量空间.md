---
aliases: [Vector Space, Linear Space]
english_name: Vector Space
tags: [数学, 线性代数, 弦理论先修, 弦理论]
prerequisites:
  - "[[向量]]"
  - "[[实数]]"
used_by:
  - "[[线性映射]]"
  - "[[矩阵]]"
  - "[[内积空间]]"
  - "[[Hilbert 空间]]"
  - "[[量子态]]"
evidence_status: 教材事实
---

# 向量空间（Vector Space）

## 一句话解释

向量空间是一组可以相加、也可以乘标量，并始终遵守同一套线性规则的对象。

## 为什么要学它

向量不只可以是空间箭头，也可以是函数、矩阵、场和量子态。弦的振动模式组成函数空间，量子弦态位于 Hilbert 空间，因此必须掌握抽象向量空间。

## 正式定义

集合 $V$ 配有向量加法与标量乘法。若标量来自实数或复数，并满足封闭性、结合律、交换律、分配律、零向量和加法逆元等公理，$V$ 就是向量空间。

核心操作写成：

$$
\mathbf u+\mathbf v\in V,
\qquad
\lambda\mathbf v\in V.
$$

线性组合为：

$$
\lambda_1\mathbf v_1+
\lambda_2\mathbf v_2+
\cdots+
\lambda_n\mathbf v_n.
$$

## 不只是箭头

以下都可以构成向量空间：

- $\mathbb R^n$ 中的坐标向量；
- 固定次数以内的多项式；
- 满足给定边界条件的函数；
- 相同尺寸的矩阵；
- 量子态。

这些对象外形不同，但加法与数乘遵守相同规则。

## 子空间

$W\subseteq V$ 若包含零向量，并对加法和数乘封闭，就是 $V$ 的子空间。

例如 $\mathbb R^3$ 中所有满足 $z=0$ 的向量组成一个平面子空间。一个不经过原点的普通平面通常不是向量子空间，因为它不包含零向量。

## 张成

向量组 $\{\mathbf v_1,\ldots,\mathbf v_n\}$ 的张成是它们所有线性组合的集合：

$$
\operatorname{span}\{\mathbf v_1,\ldots,\mathbf v_n\}.
$$

如果一组向量的张成等于整个 $V$，它们就能生成空间中的每个向量。

## 线性无关

若：

$$
c_1\mathbf v_1+
\cdots+c_n\mathbf v_n=\mathbf0
$$

只在所有 $c_i=0$ 时成立，这组向量线性无关。否则至少有一个向量能由其他向量线性组合得到，信息有重复。

## 基与维数

既线性无关又张成整个空间的向量组叫基（basis）。选定基后，每个向量都有唯一坐标：

$$
\mathbf v=v^1\mathbf e_1+
\cdots+v^n\mathbf e_n.
$$

有限维空间任意一组基所含向量数相同，这个数叫维数。

$\mathbb R^3$ 的维数是 $3$；函数空间常常是无限维的。

## 零向量

零向量 $\mathbf0$ 满足：

$$
\mathbf v+\mathbf0=\mathbf v.
$$

它必须唯一。每个向量 $\mathbf v$ 都有逆元 $-\mathbf v$，使两者相加为零。

## 弦振动的函数空间

弦的形状是位置参数 $\sigma$ 的函数。满足特定边界条件的允许形状可以相加，也可以整体乘一个数，因此形成函数向量空间。不同驻波模式可以作为基，把一般弦形状展开为：

$$
X(\sigma)=\sum_n c_n\phi_n(\sigma).
$$

这里 $\phi_n$ 是基函数，$c_n$ 是坐标分量。

## 常见误区

1. **向量空间中的元素必须是几何箭头。** 错，函数和量子态也可以是向量。
2. **任意集合都能叫向量空间。** 错，必须定义操作并满足公理。
3. **任意生成组都是基。** 错，还需线性无关。
4. **坐标分量就是向量本身。** 错，坐标依赖所选基。
5. **所有向量空间都有限维。** 错，函数空间常为无限维。

## 练习

1. $\mathbb R^2$ 中 $(1,0),(0,1)$ 是否构成基？
2. $(1,0),(2,0)$ 是否线性无关？
3. 为什么不经过原点的直线通常不是子空间？
4. 多项式 $a+bx$ 的空间维数是多少？给出一组基。
5. 说明弦形状为什么可以看作函数空间中的向量。

## 答案与提示

1. 是。
2. 否，第二个是第一个的两倍。
3. 它不包含零向量，也通常不对数乘封闭。
4. 维数 $2$，可取 $\{1,x\}$。
5. 允许形状可相加并乘标量，仍是允许函数。

## 与其他概念的关系

- [[线性映射]] 保持向量空间的线性组合；
- [[矩阵]] 是选定基后线性映射的坐标表示；
- [[内积空间]] 在向量空间上增加长度与夹角；
- 量子态和弦的模式都生活在适当向量空间中；
- [[高维空间直觉|高维空间直觉]] 与 [[嵌入|嵌入]] 使用有限维实向量空间表示 token、特征和隐藏状态。它们直接复用线性组合、基、维数和内积等数学结构，但向量坐标的语义由训练数据与模型参数决定。

## 参考资料

- [MIT OCW 18.06, Linear Algebra](https://ocw.mit.edu/courses/18-06-linear-algebra-spring-2010/)
- [OpenStax, Linear Algebra](https://openstax.org/subjects/math)
- Sheldon Axler, *Linear Algebra Done Right*, 3rd ed.
