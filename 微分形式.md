---
aliases:
  - Differential Form
  - Differential Forms
  - 微分形式
english_name: Differential Form
tags:
  - 数学
  - 微分几何
  - 外代数
  - 弦理论先修
  - 弦理论
prerequisites:
  - "[[余切空间]]"
  - "[[切空间与余切空间的对偶]]"
  - "[[向量空间]]"
  - "[[张量积]]"
used_by:
  - "[[外微分]]"
  - "[[Stokes 定理]]"
  - "[[上同调]]"
  - "[[辛结构]]"
  - "[[体积形式]]"
  - "[[规范场强]]"
  - "[[Kalb-Ramond 场]]"
  - "[[Ramond-Ramond 场]]"
evidence_status: 教材事实
---

# 微分形式（Differential Form）

## 一句话解释

微分形式是一种能接收若干个切向量、按方向和顺序给出带符号数值，并且能在流形上自然拉回和积分的反对称张量场。

## 为什么要学它

微积分里看似不同的对象——函数、线积分中的 $Pdx+Qdy$、曲面积分中的面积元、三维体积元——都可以统一写成不同次数的微分形式。

微分形式最重要的优点是：

1. 它们的定义不依赖某套特殊坐标；
2. 可以用楔积组合；
3. 可以用外微分求“边界变化”；
4. 可以沿光滑映射拉回；
5. 可以在同维定向区域上积分；
6. 可用一个统一的 Stokes 定理连接局部变化与边界积分。

在弦理论中，世界面面积形式、Kalb–Ramond 二形式 $B$、Ramond–Ramond $p$ 形式势、规范场强和拓扑项都使用微分形式语言。

## 直观图景

零形式像普通温度函数：每个点给一个数。

一形式像一把“方向测量尺”：给它一个切向量，它返回沿该方向测得的数。

二形式像一个“有向面积测量器”：给它两支切向量，它返回这两支向量张成的有向平行四边形面积经过某种加权后的数。

三形式则测量三个方向张成的有向体积。更高次形式以同样方式测量更高维的有向体积元素。

交换两个输入方向会翻转朝向，所以正次数微分形式具有反对称性。

## 点上的 $p$ 形式

设 $M$ 是光滑流形，$p\in M$。点 $p$ 处的一个 $p$ 形式是交替的 $p$ 重线性映射：

$$
\omega_p:(T_pM)^p\to\mathbb R.
$$

也就是说，它接收 $p$ 个切向量：

$$
v_1,\ldots,v_p\in T_pM,
$$

并返回实数：

$$
\omega_p(v_1,\ldots,v_p)\in\mathbb R.
$$

“$p$ 重线性”表示对每一个输入分别线性；“交替”表示只要两个输入相同，结果就是零。

## 反对称性

交换任意两个输入后，$p$ 形式变号。例如二形式满足：

$$
\omega_p(u,v)=-\omega_p(v,u).
$$

因此：

$$
\omega_p(v,v)=0.
$$

更一般地，对任意排列 $\pi$：

$$
\omega_p(v_{\pi(1)},\ldots,v_{\pi(p)})
=\operatorname{sgn}(\pi)
\omega_p(v_1,\ldots,v_p).
$$

其中 $\operatorname{sgn}(\pi)$ 在偶排列时为 $+1$，奇排列时为 $-1$。

## 微分 $p$ 形式场

如果在流形每一点 $p$ 都选取一个 $p$ 形式 $\omega_p$，并且这些系数随点光滑变化，就得到微分 $p$ 形式：

$$
\omega\in\Omega^p(M).
$$

这里：

$$
\Omega^p(M)=\Gamma(\Lambda^pT^*M)
$$

表示余切丛第 $p$ 次外幂的光滑截面空间。

直观地说，微分形式不是只放在一个点上的测量器，而是给流形每一点都配置一台平滑变化的测量器。

## 每个符号是什么意思

- $M$：光滑流形；
- $p\in M$：流形上的一点；
- $T_pM$：点 $p$ 处的切空间；
- $T_p^*M$：点 $p$ 处的余切空间；
- $\omega_p$：点 $p$ 处的 $p$ 形式；
- $p$：形式的次数，也是它接收的切向量数量；
- $\Omega^p(M)$：$M$ 上所有光滑 $p$ 形式的集合；
- $\Lambda^pT^*M$：余切空间的第 $p$ 次外幂组成的向量丛；
- $\Gamma$：取一个向量丛的光滑截面；
- $\operatorname{sgn}(\pi)$：排列的符号。

## 零形式

零形式不接收切向量，它就是一个光滑函数：

$$
\Omega^0(M)=C^\infty(M).
$$

例如：

$$
f(x,y)=x^2+y^2
$$

是 $\mathbb R^2$ 上的零形式。

零形式可以在点上取值，但不能直接沿曲线或曲面积分；要在哪种维数的对象上积分，与形式次数有关。

## 一形式

一形式在每一点接收一个切向量并返回一个数，因此它就是余切向量场。

在局部坐标 $x^1,\ldots,x^n$ 中，一形式写成：

$$
\alpha=\alpha_i(x)dx^i.
$$

若：

$$
v=v^j\frac{\partial}{\partial x^j},
$$

则：

$$
\alpha(v)=\alpha_i v^i.
$$

函数的微分：

$$
df=\frac{\partial f}{\partial x^i}dx^i
$$

是最基本的一形式。

## 二形式

二形式在每一点接收两个切向量，并对交换输入变号。

局部写成：

$$
\omega=\frac12\omega_{ij}(x)\,dx^i\wedge dx^j.
$$

其中系数满足：

$$
\omega_{ij}=-\omega_{ji}.
$$

因而对角分量自动为零：

$$
\omega_{ii}=0.
$$

前面的 $1/2$ 用来补偿对 $(i,j)$ 与 $(j,i)$ 的重复求和。若只对 $i<j$ 求和，则可以不写 $1/2$。

## 一般 $p$ 形式的坐标表达

在 $n$ 维流形上，一个 $p$ 形式可写成：

$$
\omega
=\frac{1}{p!}
\omega_{i_1\cdots i_p}(x)
\,dx^{i_1}\wedge\cdots\wedge dx^{i_p}.
$$

系数对所有指标完全反对称：

$$
\omega_{i_{\pi(1)}\cdots i_{\pi(p)}}
=\operatorname{sgn}(\pi)
\omega_{i_1\cdots i_p}.
$$

若只对严格递增的指标组：

$$
i_1<i_2<\cdots<i_p
$$

求和，就不需要 $1/p!$。

## 楔积

楔积把一个 $p$ 形式与一个 $q$ 形式组合成一个 $(p+q)$ 形式：

$$
\wedge:\Omega^p(M)\times\Omega^q(M)
\to\Omega^{p+q}(M).
$$

若：

$$
\alpha\in\Omega^p(M),
\qquad
\beta\in\Omega^q(M),
$$

则：

$$
\alpha\wedge\beta\in\Omega^{p+q}(M).
$$

楔积可以理解为先做张量积，再把所有输入完全反对称化。

## 楔积的分次交换律

楔积一般不满足普通交换律，而满足：

$$
\alpha\wedge\beta
=(-1)^{pq}\beta\wedge\alpha.
$$

因此：

- 若 $pq$ 为偶数，交换后不变号；
- 若 $pq$ 为奇数，交换后变号。

特别地，对一形式 $\alpha$ 和 $\beta$：

$$
\alpha\wedge\beta=-\beta\wedge\alpha.
$$

所以：

$$
\alpha\wedge\alpha=0.
$$

这里假设标量域的特征不为 $2$，本教程使用实数或复数，因此满足该条件。

## 坐标一形式的楔积

坐标一形式满足：

$$
dx^i\wedge dx^j=-dx^j\wedge dx^i,
$$

以及：

$$
dx^i\wedge dx^i=0.
$$

例如：

$$
(dx+2dy)\wedge(3dx-dy)
$$

展开为：

$$
3dx\wedge dx-dx\wedge dy
+6dy\wedge dx-2dy\wedge dy.
$$

去掉同方向项并使用 $dy\wedge dx=-dx\wedge dy$，得到：

$$
(dx+2dy)\wedge(3dx-dy)
=-7dx\wedge dy.
$$

## 二形式怎样测量有向面积

在 $\mathbb R^2$ 中，基本二形式：

$$
dx\wedge dy
$$

作用在两个向量：

$$
u=u^x\partial_x+u^y\partial_y,
\qquad
v=v^x\partial_x+v^y\partial_y
$$

上，结果为：

$$
(dx\wedge dy)(u,v)
=u^xv^y-u^yv^x.
$$

这正是矩阵：

$$
\begin{pmatrix}
u^x&v^x\\u^y&v^y
\end{pmatrix}
$$

的行列式，也是 $u,v$ 张成的有向平行四边形面积。

交换 $u,v$ 会翻转朝向，所以结果变号。

## $p$ 形式有多少独立分量

在 $n$ 维向量空间中，$p$ 形式的独立基元素由选择 $p$ 个不同方向决定，因此数量为：

$$
\binom np.
$$

例如在四维中：

- 零形式有 $\binom40=1$ 类分量；
- 一形式有 $\binom41=4$ 个分量；
- 二形式有 $\binom42=6$ 个独立分量；
- 三形式有 $\binom43=4$ 个独立分量；
- 四形式有 $\binom44=1$ 个独立分量。

若 $p>n$，则：

$$
\Omega^p(M)=\{0\},
$$

因为无法从 $n$ 个方向中选出 $p$ 个互不重复的方向。

## 顶次形式

在 $n$ 维流形上，$n$ 形式称为顶次形式。局部可写为：

$$
\omega=f(x)\,dx^1\wedge\cdots\wedge dx^n.
$$

顶次形式接收 $n$ 个切向量并测量它们张成的有向 $n$ 维体积。

非处处为零的顶次形式还与流形的定向有关。能否在整个流形上选择处处非零的顶次形式，取决于流形是否可定向。

## 微分形式与张量的关系

一个 $p$ 形式是 $(0,p)$ 型完全反对称张量场。也就是说，它只有余切指标，并对所有输入交换变号。

并非所有 $(0,p)$ 型张量都是微分形式。例如一般二阶协变张量：

$$
T=T_{ij}dx^i\otimes dx^j
$$

不要求：

$$
T_{ij}=-T_{ji}.
$$

度量 $g_{ij}$ 通常是对称的，所以度量不是二形式。

## 外代数

在固定点 $p$，所有次数的交替余切张量组成外代数：

$$
\Lambda^*T_p^*M
=\bigoplus_{r=0}^{n}\Lambda^rT_p^*M.
$$

乘法是楔积。整个代数的维数为：

$$
\sum_{r=0}^{n}\binom nr=2^n.
$$

这与 $n$ 维 Clifford 代数的向量空间维数相同，但两者乘法不同：外代数中任意一形式满足 $\alpha\wedge\alpha=0$；Clifford 代数中向量平方由度量决定。

## 微分形式不需要度量

定义微分形式、楔积和拉回，不需要先给流形选度量。

这是微分形式特别适合拓扑和坐标无关论证的重要原因。

度量可以增加新的操作，例如内积、Hodge 星算符和余微分，但这些不是微分形式本身定义的一部分。

## 光滑映射的拉回

设：

$$
F:M\to N
$$

是光滑映射，且：

$$
\omega\in\Omega^p(N).
$$

拉回形式：

$$
F^*\omega\in\Omega^p(M)
$$

定义为：

$$
(F^*\omega)_x(v_1,\ldots,v_p)
=\omega_{F(x)}
(F_{*x}v_1,\ldots,F_{*x}v_p).
$$

拉回保持形式次数，并逆着映射方向移动微分形式。

## 拉回的坐标计算

若 $N$ 上的一形式为：

$$
\alpha=\alpha_i(y)dy^i,
$$

映射的坐标表达为：

$$
y^i=F^i(x),
$$

则：

$$
F^*\alpha
=\alpha_i(F(x))\,dF^i.
$$

而：

$$
dF^i
=\frac{\partial F^i}{\partial x^a}dx^a.
$$

所以：

$$
F^*\alpha
=\alpha_i(F(x))
\frac{\partial F^i}{\partial x^a}dx^a.
$$

## 拉回与楔积相容

对任意形式 $\alpha,\beta$，拉回满足：

$$
F^*(\alpha\wedge\beta)
=F^*\alpha\wedge F^*\beta.
$$

这意味着可以先在目标空间组合形式再拉回，也可以分别拉回后在定义域组合，结果相同。

这种自然性使几何结构能沿嵌入、参数化和坐标变换稳定传递。

## 微分形式怎样积分

一个 $p$ 形式自然在 $p$ 维定向流形或 $p$ 维参数化链上积分。

若参数化：

$$
\Phi:U\subseteq\mathbb R^p\to M,
$$

则定义：

$$
\int_{\Phi(U)}\omega
=\int_U\Phi^*\omega.
$$

右边是普通多变量积分。拉回把流形上的 $p$ 形式转换为参数区域上的顶次形式。

严格定义还需处理图册、分割单位、定向、重叠和边界；本篇先给核心机制。

## 一形式的曲线积分

设曲线：

$$
\gamma:[a,b]\to M
$$

以及一形式 $\alpha$。曲线积分定义为：

$$
\int_\gamma\alpha
=\int_a^b\gamma^*\alpha.
$$

若在 $\mathbb R^n$ 中：

$$
\alpha=A_i(x)dx^i,
\qquad
\gamma(t)=(x^1(t),\ldots,x^n(t)),
$$

则：

$$
\int_\gamma\alpha
=\int_a^b A_i(\gamma(t))
\frac{dx^i}{dt}dt.
$$

这就是熟悉的线积分表达式。

## 二形式的曲面积分

设曲面参数化：

$$
X:U\subseteq\mathbb R^2\to M,
$$

局部参数为 $(\sigma^1,\sigma^2)$。对二形式：

$$
B=\frac12B_{\mu\nu}(x)
\,dx^\mu\wedge dx^\nu,
$$

拉回为：

$$
X^*B
=\frac12B_{\mu\nu}(X)
\partial_aX^\mu\partial_bX^\nu
\,d\sigma^a\wedge d\sigma^b.
$$

于是可在二维参数区域上积分 $X^*B$。

## 外微分的预告

外微分把 $p$ 形式变成 $(p+1)$ 形式：

$$
d:\Omega^p(M)\to\Omega^{p+1}(M).
$$

对函数 $f$，它就是已经熟悉的微分：

$$
df=\partial_i f\,dx^i.
$$

对一般 $p$ 形式，外微分直接推广了函数微分和三维向量分析中的旋度型结构，并满足：

$$
d^2=0.
$$

散度也能用微分形式表达，但通常还要借助体积形式或度量诱导的 Hodge 星算符。完整定义和证明放在 [[外微分]] 中。

## Stokes 定理的统一形式

若 $M$ 是具有适当定向的 $n$ 维带边界流形，$\omega$ 是满足相应支撑或可积条件的 $(n-1)$ 形式，则：

$$
\int_M d\omega
=\int_{\partial M}\omega.
$$

它统一了微积分基本定理、Green 定理、经典 Stokes 定理和 Gauss 散度定理。

严格条件与边界定向见 [[Stokes 定理]]。

## 闭形式与恰当形式的预告

若：

$$
d\omega=0,
$$

则称 $\omega$ 为闭形式。

若存在 $\eta$，使：

$$
\omega=d\eta,
$$

则称 $\omega$ 为恰当形式。

因为 $d^2=0$，每个恰当形式都是闭形式；但在具有非平凡拓扑的空间上，闭形式未必全局恰当。两者之差由 de Rham 上同调刻画。

## 电磁场强的例子

规范势可写成一形式：

$$
A=A_\mu dx^\mu.
$$

Abelian 场强是二形式：

$$
F=dA
=\frac12F_{\mu\nu}
\,dx^\mu\wedge dx^\nu,
$$

其中：

$$
F_{\mu\nu}
=\partial_\mu A_\nu-
\partial_\nu A_\mu.
$$

反对称性由楔积自动编码。由 $d^2=0$ 得：

$$
dF=0,
$$

这对应 Abelian 电磁学的 Bianchi 恒等式。

## 世界面面积形式

二维定向世界面上，局部顶次形式可写成：

$$
\omega=f(\sigma)
\,d\sigma^0\wedge d\sigma^1.
$$

若世界面带度量 $h_{ab}$，常见体积形式为：

$$
\operatorname{vol}_h
=\sqrt{|\det h|}
\,d\sigma^0\wedge d\sigma^1
$$

在选定定向和局部坐标下成立。

Lorentzian 世界面常写 $\sqrt{-h}\,d^2\sigma$，其中具体符号依度量号差与 $h=\det(h_{ab})$ 的约定。

## 弦与 Kalb–Ramond 二形式

目标时空中的 Kalb–Ramond 场是二形式：

$$
B=\frac12B_{\mu\nu}(X)
\,dX^\mu\wedge dX^\nu.
$$

弦世界面映射：

$$
X:\Sigma\to M
$$

把它拉回为世界面二形式：

$$
X^*B
=\frac12B_{\mu\nu}(X)
\partial_aX^\mu\partial_bX^\nu
\,d\sigma^a\wedge d\sigma^b.
$$

世界面耦合可概念性地写成：

$$
\int_\Sigma X^*B,
$$

整体归一化和因子 $i$ 依 Euclidean 或 Lorentzian 作用量约定而不同。

## 为什么点粒子耦合一形式而弦耦合二形式

点粒子的世界线是一维对象，所以自然积分一形式：

$$
\int_\gamma A.
$$

弦扫出的世界面是二维对象，所以自然积分二形式：

$$
\int_\Sigma X^*B.
$$

更一般地，一个空间维数为 $p$ 的扩展对象扫出 $(p+1)$ 维世界体，因而可电耦合于 $(p+1)$ 形式规范势。

这是形式次数与几何对象维数之间的直接匹配。

## Ramond–Ramond 形式

超弦理论中的 Ramond–Ramond 势以不同次数的微分形式出现。不同超弦理论允许的形式次数、场强、自对偶约束和 D-brane 耦合并不相同。

微分形式语言可把它们统一写成：

$$
C=\sum_p C_p,
$$

但这个形式和还需结合 Type IIA、Type IIB、民主表述、Bianchi 恒等式及自对偶条件具体解释，不能把所有 $C_p$ 都当作彼此独立的传播场。

## 适用条件与事实边界

1. 微分 $p$ 形式是在每一点上的交替 $p$ 重余切张量，并且随点光滑变化。
2. 零形式是光滑函数，一形式是余切向量场，顶次形式测量有向体积。
3. 微分形式的定义、楔积和拉回不需要度量。
4. $p$ 形式有 $\binom np$ 个独立局部分量；当 $p>n$ 时只能为零。
5. 楔积满足分次交换律 $\alpha\wedge\beta=(-1)^{pq}\beta\wedge\alpha$，而不是普通交换律。
6. 一形式的自楔积在实数或复数标量域上为零；对一般偶次形式，自楔积不必为零。
7. 形式只能自然地在与其次数相同的定向维数上积分；严格积分还需定向、参数化和适当的全局条件。
8. 拉回逆着光滑映射方向作用，并保持形式次数、楔积及外微分结构。
9. 度量通常是对称 $(0,2)$ 张量，不是二形式；两者不能因都有两个下指标就混同。
10. 闭形式局部与全局是否恰当涉及拓扑，不能仅靠点附近计算决定。
11. Kalb–Ramond 场与 Ramond–Ramond 场的形式次数和归一化依具体弦理论与约定。
12. 微分形式与 Clifford 代数作为向量空间可密切相关，但楔积不等于完整 Clifford 乘法。

## 常见误区

1. **$dx^i$ 是一个很小的坐标增量。** 在微分形式中，它首先是余切基一形式，是作用在切向量上的线性函数。
2. **二形式就是任意带两个下标的张量。** 二形式必须完全反对称；度量通常对称，因此不是二形式。
3. **楔积与普通乘法相同。** 楔积具有分次反交换规则，并使重复的一形式方向消失。
4. **任何 $p$ 形式都能在任意维对象上直接积分。** 自然积分要求积分域维数与形式次数匹配，或先通过拉回得到匹配形式。
5. **交换两个输入不会影响面积。** 有向面积会翻转符号，微分形式记录的是有向量。
6. **$p$ 形式总有 $n^p$ 个独立分量。** 完全反对称性把独立分量数降为 $\binom np$。
7. **定义微分形式必须先有度量。** 不需要；度量只为 Hodge 星等附加操作所需。
8. **拉回会把形式次数改变。** 拉回保持次数；改变次数的是外微分或楔积等操作。
9. **每个闭形式都能全局写成另一个形式的外微分。** 只有在拓扑条件允许时才成立。
10. **$B_{\mu\nu}$ 只是一个普通矩阵。** 它是二形式分量，随坐标变化并满足反对称性。

## 练习

1. 什么是点 $p$ 处的 $p$ 形式？
2. 为什么二形式 $\omega$ 满足 $\omega(v,v)=0$？
3. 写出局部坐标中一般 $p$ 形式的表达式。
4. 计算 $(dx+2dy)\wedge(3dx-dy)$。
5. 三维空间中的二形式有多少个独立分量？
6. 四维空间中的三形式有多少个独立分量？
7. 为什么五维空间中不存在非零六形式？
8. 设 $u=2\partial_x+\partial_y$，$v=-\partial_x+3\partial_y$，求 $(dx\wedge dy)(u,v)$。
9. 拉回 $F^*$ 的方向是什么？它会改变形式次数吗？
10. 为什么度量 $g_{ij}$ 通常不是二形式？
11. 点粒子和弦分别自然耦合什么次数的形式势？
12. 写出 Kalb–Ramond 二形式沿世界面映射 $X$ 的拉回。

## 答案与提示

1. 它是从 $(T_pM)^p$ 到 $\mathbb R$ 的交替 $p$ 重线性映射。
2. 交换两个相同输入既应变号又不改变输入，因此 $\omega(v,v)=-\omega(v,v)$，在实数或复数域上只能为零。
3. $\omega=(1/p!)\omega_{i_1\cdots i_p}dx^{i_1}\wedge\cdots\wedge dx^{i_p}$。
4. 结果为 $-7dx\wedge dy$。
5. $\binom32=3$。
6. $\binom43=4$。
7. 反对称形式要求六个不同方向，但五维只有五个独立方向。
8. $2\times3-1\times(-1)=7$。
9. 若 $F:M\to N$，则 $F^*: \Omega^p(N)\to\Omega^p(M)$；方向相反，次数不变。
10. 度量通常满足 $g_{ij}=g_{ji}$，二形式要求 $\omega_{ij}=-\omega_{ji}$。
11. 世界线上的点粒子自然耦合一形式；二维世界面上的弦自然耦合二形式。
12. $X^*B=(1/2)B_{\mu\nu}(X)\partial_aX^\mu\partial_bX^\nu d\sigma^a\wedge d\sigma^b$。

## 与其他概念的关系

- [[余切空间]] 提供一形式所在的余切空间；
- [[切空间与余切空间的对偶]] 解释形式怎样接收切向量；
- [[张量积]] 是构造多重余切张量的基础；
- [[Grassmann 变量]] 与楔积共享分次反交换结构，但两者的几何角色不同；
- [[Clifford 代数]] 同时编码度量与反对称乘积，其乘法不同于纯楔积；
- [[外微分]] 把 $p$ 形式变成 $(p+1)$ 形式；
- [[Stokes 定理]] 把 $d\omega$ 的整体积分化为边界上的 $\omega$ 积分；
- [[上同调]] 用闭形式模去恰当形式刻画全局拓扑信息；
- [[度量]] 可进一步定义 Hodge 星和形式内积；
- [[辛结构]] 是闭且非退化的二形式；
- [[规范场强]] 常自然写成二形式；
- [[Kalb-Ramond 场]] 是与弦世界面自然耦合的目标时空二形式；
- [[Ramond-Ramond 场]] 由不同次数的形式势和场强描述；
- [[D-brane]] 通过世界体积分耦合于相应次数的形式势。

## 参考资料

- John M. Lee, *Introduction to Smooth Manifolds*, 2nd ed., Springer, 2013.
- Loring W. Tu, *An Introduction to Manifolds*, 2nd ed., Springer, 2011.
- Michael Spivak, *Calculus on Manifolds*, Westview Press, 1998.
- Harley Flanders, *Differential Forms with Applications to the Physical Sciences*, Dover, 1989.
- Theodore Frankel, *The Geometry of Physics*, 3rd ed., Cambridge University Press, 2012.
- Mikio Nakahara, *Geometry, Topology and Physics*, 2nd ed., CRC Press, 2003.
- Raoul Bott and Loring W. Tu, *Differential Forms in Algebraic Topology*, Springer, 1982.
- Joseph Polchinski, *String Theory, Vol. 1*, Cambridge University Press, 1998.
- Joseph Polchinski, *String Theory, Vol. 2*, Cambridge University Press, 1998.
- David Tong, *Lectures on String Theory*, [arXiv:0908.0333](https://arxiv.org/abs/0908.0333)
