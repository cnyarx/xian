---
aliases:
  - 信息论
  - 熵
  - 交叉熵
  - KL 散度
  - Entropy
  - Cross Entropy
  - KL Divergence
tags:
  - 数学
  - 信息论
  - 教程
  - 大模型
  - 学习教程
prerequisites:
  - "[[概率基础]]"
  - "[[概率分布]]"
next:
  - "[[损失函数]]"
---

# 信息论基础

## 一句话定义

**信息论**（Information Theory）是研究"信息有多少"和"信息怎样传递"的数学理论。在大模型中，它提供衡量模型预测好坏的数学工具。

---

## 为什么需要它

大模型训练中最核心的概念——**交叉熵损失**（Cross Entropy Loss）——直接来自信息论。

每当你看到"模型的 loss 是 2.3"这样的数字，它在用信息论的语言告诉你：模型的预测和真实答案之间差了多少。

---

## 信息量

### 直觉理解

**信息量**（Information Content / Self-Information）衡量一条消息有多"令人惊讶"。

- 越不可能发生的事，发生后带来的信息量越大
- 一定会发生的事，发生后带来的信息量为 0

**生活例子：**

```
"太阳从东边升起"        → 信息量 ≈ 0（早就知道）
"今天下雨了"            → 信息量中等（有时下有时不下）
"今天下雪了"（在热带）   → 信息量极大（极其罕见）
```

### 严格定义

事件 $x$ 的信息量（单位：比特）定义为：

$$
I(x) = -\log_2 P(x)
$$

**每个符号在说什么：**
- $I(x)$：事件 $x$ 的信息量
- $P(x)$：事件 $x$ 发生的概率
- $\log_2$：以 2 为底的对数（见 `[[指数与对数]]`）
- 负号：因为 $P(x) \leq 1$，所以 $\log_2 P(x) \leq 0$，加负号让信息量变成正数

**这条公式在说：** 概率越小，信息量越大。

### 数值例子

```
P(事件) = 1     → I = -log₂(1) = 0      比特  （确定事件，信息量为0）
P(事件) = 0.5   → I = -log₂(0.5) = 1    比特  （抛硬币的结果）
P(事件) = 0.25  → I = -log₂(0.25) = 2   比特
P(事件) = 0.125 → I = -log₂(0.125) = 3  比特
```

---

## 熵

### 一句话定义

**熵**（Entropy）是信息量的**期望值**——整个概率分布"平均有多令人惊讶"。

### 直觉理解

```
一个公平的骰子：熵很高（6 个面概率相等，难以预测掷出什么）
一个作弊骰子（99%掷出6）：熵很低（几乎可以确定会掷出6）
```

**熵越高 = 越不确定 = 越难预测**

### 严格定义

$$
H(P) = -\sum_{x} P(x) \log_2 P(x)
$$

**每个符号在说什么：**
- $H(P)$：分布 $P$ 的熵
- $P(x)$：事件 $x$ 的概率
- $\sum_{x}$：对所有可能的事件求和
- 这条公式在说：每个事件的信息量乘以它的概率，再全部加起来

### 数值例子

**例子 1：公平硬币**

```
P(正面) = 0.5, P(反面) = 0.5
H = -0.5×log₂(0.5) - 0.5×log₂(0.5)
  = -0.5×(-1) - 0.5×(-1)
  = 0.5 + 0.5
  = 1 比特
```

**例子 2：作弊硬币（永远正面）**

```
P(正面) = 1, P(反面) = 0
H = -1×log₂(1) - 0×log₂(0)
  = -1×0 - 0
  = 0 比特
```

熵为 0 表示完全确定，不需要任何信息。

**例子 3：公平骰子**

```
每个面概率 = 1/6
H = -6 × (1/6 × log₂(1/6))
  = -log₂(1/6)
  = log₂(6)
  ≈ 2.58 比特
```

---

## 交叉熵

### 一句话定义

**交叉熵**（Cross Entropy）衡量：用分布 $Q$ 来编码真实分布为 $P$ 的数据，平均每个事件需要多少比特。

### 直觉理解

```
真实分布 P：明天 70% 概率下雨，30% 概率晴天
你的预测 Q：明天 50% 概率下雨，50% 概率晴天

交叉熵衡量：你的预测有多"不准"？
如果你的预测和真实分布一模一样，交叉熵 = 熵
你的预测越不准，交叉熵比熵大得越多
```

### 严格定义

$$
H(P, Q) = -\sum_{x} P(x) \log_2 Q(x)
$$

**每个符号在说什么：**
- $H(P, Q)$：用 $Q$ 来近似 $P$ 的交叉熵
- $P(x)$：真实概率
- $Q(x)$：预测概率
- 注意：里面是 $P(x)$ 乘以 $\log_2 Q(x)$，不是 $\log_2 P(x)$

### 在大模型中的核心角色

**大模型训练就是最小化交叉熵。**

```
真实分布 P: 下一个词应该是 "好"（概率 1.0，"好" 就是正确答案）
模型预测 Q: "好" 的概率 0.3，"不错" 0.2，"热" 0.1 ...

交叉熵 = -1 × log₂(0.3) = -log₂(0.3) ≈ 1.74 比特

如果模型后来学会了，预测 "好" 的概率变成 0.9：
交叉熵 = -1 × log₂(0.9) ≈ 0.15 比特

交叉熵降低了 → 模型变好了
```

这也就是为什么训练时看到 "loss 下降" 意味着模型在进步。

---

## KL 散度

### 一句话定义

**KL 散度**（Kullback-Leibler Divergence）衡量两个概率分布之间的"距离"。

$$
D_{KL}(P \parallel Q) = H(P, Q) - H(P)
$$

**每个符号在说什么：**
- $D_{KL}(P \parallel Q)$：从 $P$ 到 $Q$ 的 KL 散度
- $H(P, Q)$：交叉熵
- $H(P)$：$P$ 自身的熵
- 这条公式在说：KL 散度 = 交叉熵减去熵 = 因为用 $Q$ 代替 $P$ 而"多浪费"的信息

**性质：**

1. $D_{KL}(P \parallel Q) \geq 0$（永远非负）
2. $D_{KL}(P \parallel Q) = 0$ 当且仅当 $P = Q$（两个分布完全一样）
3. $D_{KL}(P \parallel Q) \neq D_{KL}(Q \parallel P)$（不对称）

### 在大模型中的角色

- 训练时最小化交叉熵等价于最小化 KL 散度（因为 $H(P)$ 是常数）
- RLHF 中，KL 散度用来约束模型不要偏离原始模型太远
- 知识蒸馏中，用 KL 散度让学生模型模仿教师模型

---

## 三者关系

```
交叉熵 = 熵 + KL 散度

H(P, Q) = H(P) + D_{KL}(P ∥ Q)
```

**直觉理解：**

- 熵 $H(P)$：真实分布本身的不确定性（无法消除的）
- KL 散度 $D_{KL}(P \parallel Q)$：你的预测和真实分布之间的差距（可以通过学习消除的）
- 交叉熵 $H(P, Q)$：两者之和

训练模型时，熵 $H(P)$ 是固定的（数据决定了），所以最小化交叉熵就等于最小化 KL 散度。

---

## 常见误区

1. **熵不是"混乱"**
   日常语言中"熵"常被理解为"混乱程度"。在信息论中，熵更准确的理解是"不确定性"或"平均信息量"。

2. **交叉熵不是对称的**
   $H(P, Q) \neq H(Q, P)$。用 A 来近似 B 和用 B 来近似 A，代价不同。

3. **KL 散度不是真正的距离**
   它不对称，不满足距离的三角不等式。但它在很多场景中表现得像距离。

4. **交叉熵不等于准确率**
   交叉熵低不代表模型一定正确率高，它只表示模型给正确答案的概率高。

---

## 概念与公式导航

- [[熵与信息度量|熵与信息度量]]
- [[熵交叉熵与KL散度公式|熵、交叉熵与 KL 散度公式]]

## 练习

1. 计算以下分布的熵：
   - 分布 A：$P(x_1)=0.5, P(x_2)=0.5$
   - 分布 B：$P(x_1)=0.9, P(x_2)=0.1$
   哪个熵更大？为什么？

2. 真实分布 $P = [1, 0, 0]$（即第一个事件一定发生），模型预测 $Q = [0.7, 0.2, 0.1]$。计算交叉熵 $H(P, Q)$。

3. 接上题，如果模型预测变成 $Q' = [0.3, 0.4, 0.3]$，交叉熵变大还是变小？

4. 用你自己的话解释：为什么大模型训练要最小化交叉熵？

5. （思考题）如果训练时模型的交叉熵降到了 0，这意味着什么？这是好事还是坏事？

---

## 本篇引入的新概念

| 中文 | 英文 | 简要说明 |
|---|---:|---|
| 信息量 | Information Content | 事件有多"令人惊讶" |
| 熵 | Entropy | 分布的平均不确定性 |
| 交叉熵 | Cross Entropy | 用一个分布近似另一个的代价 |
| KL 散度 | KL Divergence | 两个分布之间的差距 |
| 比特 | Bit | 信息量的单位 |

---

## 下一步

→ `[[损失函数]]`：看交叉熵如何成为大模型训练的核心损失函数。

---

## 来源

- 信息论基础：Cover, T. M. & Thomas, J. A. (2006). *Elements of Information Theory* (2nd ed.). Wiley. 第2章 `[A]`
- 交叉熵在机器学习中：Goodfellow, I., Bengio, Y., & Courville, A. (2016). *Deep Learning*. MIT Press. 第3章, 第5章 `[A]`

---

> 创建日期：2026-09-04 | 最后修订：2026-09-04