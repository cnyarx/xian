---
aliases: [反向传播, Backpropagation, 计算图]
tags: [神经网络, 教程, 大模型, 学习教程]
prerequisites: ["[[多层感知机]]", "[[导数与梯度]]"]
next: ["[[优化器]]"]
---
# 反向传播

## 一句话定义

反向传播把输出误差从后往前传，利用链式法则高效计算损失对每个参数的梯度。

## 一个最小例子

设：

$$z=wx+b,\quad y=z^2$$

取 $x=3,w=2,b=1$，则 $z=7,y=49$。

要算 $y$ 对 $w$ 的变化率：

$$\frac{\partial y}{\partial w}=\frac{\partial y}{\partial z}\frac{\partial z}{\partial w}=2z\cdot x=2\times7\times3=42$$

这就是链式法则：局部变化率沿计算路线相乘。

## 计算图

```text
x --×w-- +b --> z --平方--> y
```

前向传播保存中间值。反向传播从损失开始：

1. 求最后一步的局部导数。
2. 乘上传来的梯度。
3. 继续向前一节点传播。
4. 分叉路径的梯度相加。

## 神经网络中的公式

若一层为：

$$z=Wx+b,\quad h=\phi(z)$$

上游传来 $\partial L/\partial h$，则：

$$\frac{\partial L}{\partial z}=\frac{\partial L}{\partial h}\odot\phi'(z)$$

$$\frac{\partial L}{\partial W}=\frac{\partial L}{\partial z}x^T$$

$\odot$ 表示对应元素相乘。矩阵形状决定了公式怎样排列。

## 自动求导

PyTorch 会构建计算图并自动反向：

```python
prediction = model(x)
loss = loss_fn(prediction, target)
optimizer.zero_grad()
loss.backward()
optimizer.step()
```

`backward()` 不是猜测梯度，而是严格应用链式法则。

## 梯度消失与爆炸

深层网络要连乘许多导数：

- 多个小于 1 的数连乘，梯度趋近 0：梯度消失。
- 多个大于 1 的数连乘，梯度变得巨大：梯度爆炸。

残差连接、归一化、合适初始化和梯度裁剪有助于缓解。

## 概念与公式导航

- [[计算图自动微分与梯度累加|计算图、自动微分与梯度累加]]
- [[梯度消失与梯度爆炸|梯度消失与梯度爆炸]]

## 练习

1. $y=(2x+1)^2$，求 $x=3$ 时 $dy/dx$。
2. 画出 $L=(wx-y)^2$ 的计算图。
3. 为什么反向传播需要保存前向中间值？
4. 解释梯度消失与激活函数的关系。

## 来源

- Rumelhart, D. E., Hinton, G. E., & Williams, R. J. (1986). *Learning Representations by Back-propagating Errors*. Nature. https://doi.org/10.1038/323533a0 `[A]`
- Goodfellow et al. (2016). *Deep Learning*. 第6章 `[A]`