---
aliases: [推理时扩展, Test-Time Compute, Self-Consistency]
tags: [大模型, 推理, 教程, 学习教程]
prerequisites: ["[[推理训练]]", "[[采样策略]]"]
next: ["[[训练系统概览]]"]
---
# 推理时扩展

## 一句话定义

推理时扩展是在不更新权重的情况下，为一个问题投入更多生成、搜索或验证计算，希望提高答案质量。

## 常见方法

- 生成更长的推理过程；
- 对同一问题采样多条路线，用多数投票或验证器选答案；
- 树搜索或分步规划；
- 调用代码执行器、定理验证器或检索工具；
- 让模型批判并修订候选。

这些方法增加的计算位置不同：更长回答增加单条序列长度，多样采样增加候选数量，搜索增加分支，外部验证增加工具调用。报告“推理计算量”时应说明具体增加了什么。

## Self-Consistency

对同一题采样多条思维链，选择出现最频繁的最终答案。在论文实验中，它提升了若干算术和常识推理基准，但成本随样本数增加，且共同错误不会因投票消失。

### 逐步例子

同一题采样 5 次，最终答案为：

```text
候选1: 42
候选2: 40
候选3: 42
候选4: 42
候选5: 41
```

多数票选择 42。这个结论只利用最终答案频率；如果 5 条路线都受同一个错误公式影响，可能一致投出错误答案。

对于开放回答，还要先定义“答案相同”的判定方法。字符串不同不一定语义不同，交给另一个模型归类又会引入裁判偏差。

## 生成—验证

当结果可以检查时，候选数量不是唯一重点。例如代码题可采用：

1. 生成 4 个候选实现；
2. 在隔离环境运行相同测试；
3. 淘汰语法错误、超时和测试失败者；
4. 对通过者比较隐藏测试、复杂度和安全规则；
5. 若都失败，把真实错误反馈给下一轮生成。

验证器必须覆盖真正目标。测试过弱时，错误程序仍会被认证为正确；验证环境不隔离时，生成代码还可能造成系统风险。

## 预算与收益

更多计算不保证更好：模型可能反复纠结、产生更多错误或被错误验证器误导。应按任务、模型和预算测量准确率—成本曲线。

假设单次回答成本记为 1，采样 8 条再验证的成本至少接近 8 次生成加验证开销。若准确率只从 70% 增至 71%，是否值得取决于错误代价、延迟目标和预算，不能只看分数上涨。

应同时报告：候选数、平均输出 token、验证调用数、端到端延迟、成本和成功率。

## 什么时候停止

可设置：

- 固定 token 或调用预算；
- 找到可形式验证的正确答案；
- 连续若干轮没有新候选；
- 候选置信或投票差距达到预先规定阈值；
- 时间或费用上限。

停止规则应在实验前确定，避免看到结果后不断加预算直到挑出满意答案。

## 验证比自信重要

当答案可程序验证时，应优先使用外部验证。模型自评并不总能可靠区分正确与错误。无法形式验证的任务可用多位人工评审、来源核查和失败案例分析，但这些方法也有成本和偏差。

## 边界

- 推理时扩展不更新模型权重，与继续训练不同；
- 更长输出不等于更有效的推理；
- 多数投票要求候选具有一定多样性，完全相关的错误不会相互抵消；
- 外部工具的结果必须真实执行并记录，不能由模型虚构；
- 在某个基准上的收益不能自动外推到其他任务和模型。

## 概念与公式导航

- [[推理训练与推理时扩展|推理训练与推理时扩展]]

## 练习

1. 推理时扩展与训练扩展有什么区别？
2. 多数投票何时会失效？
3. 给代码题设计一个生成—验证流程。
4. 为什么“想得更久”不必然更正确？

## 来源

- Wang, X. et al. (2023). *Self-Consistency Improves Chain of Thought Reasoning*. ICLR. https://arxiv.org/abs/2203.11171 `[A]`
- Snell, C. et al. (2024). *Scaling LLM Test-Time Compute Optimally can be More Effective than Scaling Model Parameters*. https://arxiv.org/abs/2408.03314 `[B]`