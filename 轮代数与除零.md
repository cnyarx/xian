---
aliases:
  - 轮代数
  - 轮理论
  - Wheel Theory
  - 全域除法
  - 除以零的代数
  - 0/0 代数
english_name: Wheel Theory
tags:
  - 跨学科
  - 数学
  - 计算机学
  - 数值计算
related:
  - "[[复数]]"
  - "[[极限]]"
  - "[[黎曼曲面]]"
  - "[[Laurent 级数]]"
  - "[[留数定理]]"
  - "[[浮点数与数值稳定]]"
  - "[[梯度消失与梯度爆炸]]"
  - "[[弦理论与大模型跨学科关系图谱]]"
status: 持续维护
---

# 轮代数与除零

## 一句话解释

轮代数（wheel theory）是一种代数结构，它把“除以零”纳入封闭运算：在其中 $0/0$ 是一个合法元素（记作 $\perp$），代价是必须放弃实数、复数中一些我们习以为常的恒等式（如 $0\cdot x=0$、完整分配律、$x/x=1$）。它和“引入 $i=\sqrt{-1}$”属于同一种数学操作模式：**为了让某类方程总有解而扩展数系，同时接受原有运算规则的局部失效**。

## 为什么把它和 $i$ 放在一起讲

复数 [[复数|复数]] 的构造逻辑是：实数范围内 $x^2=-1$ 无解，于是新增符号 $i$ 并规定 $i^2=-1$，得到更大的数系 $\mathbb C$。扩展不是免费的——实数上的序关系（大小比较）在 $\mathbb C$ 中不再成立。

轮代数用同样的思路处理除法：

- 在域（如实数 $\mathbb R$）中，$0$ 没有乘法逆元，$1/0$ 无定义；
- 轮代数新增一个“全域除法”结构，使得对任意 $x,y$ 都存在 $x/y$；
- 代价是 $0$ 现在有了“逆” $0/0=\perp$，但 $\perp$ 不满足普通数的运算直觉，一系列恒等式必须放弃。

所以“像构造 $i$ 一样构造 $1/0$”这个说法，在**方法论层面**是准确的：两者都是通过牺牲部分公理来换取运算的封闭性。但要注意：复数是极其成熟、被物理和工程全面验证的数系；轮代数是一个真实存在、但小众且主要停留在纯数学与逻辑层面的结构，**尚未成为物理或机器学习的标准工具**。

## 标准数学已经如何处理“除零”

在介绍轮代数之前，先厘清：主流数学和计算科学其实已经用三种**互不相同**的方式处理除零，它们都不是轮代数，也不应被混为一谈。

### 1. 极限与不定式（分析路径）

[[极限|极限]] 中，$1/x$ 当 $x\to0$ 时没有有限极限；而 $0/0$ 是**不定式**，其值取决于具体趋近方式：

$$
\lim_{x\to0}\frac{x}{x}=1,\qquad
\lim_{x\to0}\frac{2x}{x}=2,\qquad
\lim_{x\to0}\frac{x}{x^2}=\infty.
$$

这里的关键是：$0/0$ 不是一个“数”，而是一个“需要更多信息才能确定”的形式记号。分析学通过极限过程而非新增元素来处理它。

### 2. 黎曼球与射影紧化（几何路径）

[[黎曼曲面|黎曼曲面]] 相关讨论中的**黎曼球** $\hat{\mathbb C}=\mathbb C\cup\{\infty\}$，通过添加单个无穷远点，使 $1/0=\infty$ 成为良定义。这是**射影紧化**：它把 $\infty$ 当作一个点加入，但代价是 $\infty$ 上的算术不完全封闭（例如 $\infty-\infty$、$0\cdot\infty$ 仍无定义）。

它与轮代数的区别很关键：黎曼球只加**一个** $\infty$ 且放弃部分运算；轮代数要加的是让**所有**除法都封闭的元素 $\perp$，且 $\perp$ 与 $\infty$ 语义不同（$\perp=0/0$ 是“完全不确定”，而 $\infty=1/0$ 是“定向发散”）。

### 3. IEEE 754 浮点标准（工程路径）

[[浮点数与数值稳定|浮点数与数值稳定]] 中，计算机硬件按 IEEE 754 规定：

$$
\frac{x}{0}=\pm\infty\ (x\ne0),\qquad
\frac{0}{0}=\text{NaN},\qquad
\frac{\pm\infty}{\pm\infty}=\text{NaN}.
$$

其中 **NaN（Not a Number）** 是工程上对“无意义结果”的标记，它会“传染”——任何含 NaN 的算术通常仍返回 NaN。NaN 的设计目标是**让程序不崩溃并能检测到错误**，而不是构造一个自洽的代数。

## 轮代数的正式构造

轮代数由 Anders Carlström 系统提出，论文为 *Wheels – on division by zero*，发表于 *Mathematical Structures in Computer Science*（2004），DOI: [10.1017/S0960129503004110](https://doi.org/10.1017/S0960129503004110)。

> **勘误**：网络上流传的介绍常把轮代数归于“Anders Kock 等人”。经核实，主要提出者是 **Anders Carlström**。Anders Kock 是另一位丹麦数学家（以合成微分几何、Kock–Lawvere 公理著称），二者不是同一人，不应混淆。

### 核心新元素

轮代数在域的基础上引入元素 $\perp$（读作“bottom”或“nil”），直观上代表

$$
\perp = \frac{0}{0}.
$$

同时定义一个一元运算 $\sim$（对“取逆”的推广），使得 $x/y$ 可写成 $x\cdot(\sim y)$，且对**所有** $x,y$（包括 $y=0$）都有定义。

### 必须放弃的恒等式

轮代数的代价体现在下列实数中成立的等式**不再普遍成立**：

| 在 $\mathbb R/\mathbb C$ 中成立 | 在轮代数中 |
|---|---|
| $0\cdot x = 0$ | 不普遍成立（涉及 $\perp$ 时失效） |
| $x - x = 0$ | 不普遍成立 |
| 分配律 $a(b+c)=ab+ac$ | 只在受限条件下成立，需修正项 |
| $x/x = 1$ | 不普遍成立（$0/0=\perp\ne1$） |
| 乘法交换律、结合律 | 大部分保留，但需重新验证 |

换言之，轮代数保留了“加法交换群 + 大部分乘法结构”，但对分配律和零元吸收律作了实质修改。它**不是一个域，也不是一个环**（严格说是不满足完整分配律的“非分配环状结构”）。

## 轮代数能做什么、不能做什么

**能做的（纯数学层面）**：

- 提供一个所有除法封闭的代数，便于某些逻辑、计算机科学中的**全域函数（total function）**建模——除法不必再作为“偏函数”特殊处理；
- 为“未定义/不确定”给出一个**代数对象**而非仅仅一个语法标记，从而可以对它做等式推理。

**不能做的（当前事实边界）**：

- 它**没有**成为数学分析、代数几何或物理的主流工具；
- 它**不能**替代极限理论——$0/0$ 的具体数值仍须由趋近方式决定，$\perp$ 只是一个“占位的封闭元素”，本身不携带“等于 1 还是等于 2”的信息；
- 它**不解决**数值计算的稳定性问题（见下节）。

## 与大模型训练的关系：区分现实与猜想

用户流传的材料提到轮代数可能用于大模型训练（处理 NaN、硬件、热带几何）。这里必须**严格区分已验证事实与推测**。

### 已验证的现实：训练中确实有除零/NaN 问题

- [[梯度消失与梯度爆炸|梯度消失与梯度爆炸]] 中，梯度更新可能出现 $0/0$ 或极大值；
- [[浮点数与数值稳定|浮点数与数值稳定]] 中，softmax、LayerNorm、损失计算里的除法在分母趋零时产生 Inf/NaN；
- 混合精度训练中损失缩放不当会触发 NaN（见 `大模型学习教程/15-概念词典/26-混合精度与损失缩放`）。

**但当前工程界的处理方式是 IEEE 754 的 NaN/Inf 检测 + 梯度裁剪 + epsilon 平滑（如 $x/(y+\epsilon)$）+ 跳过坏 batch**，不是轮代数。这些方法成熟、经过大规模验证。

### 属于猜想：轮代数替代 NaN 处理

“用轮代数把 NaN 变成合法代数元素从而避免训练中断”是一个**有意思的设想，但没有可靠的已发表工程证据**表明它在真实大模型训练中被采用或验证过。它面临的问题包括：

1. 轮代数放弃 $0\cdot x=0$ 和完整分配律，而自动微分与反向传播的正确性**依赖**这些恒等式，直接替换会破坏梯度计算；
2. $\perp$ 不提供数值稳定性——它让 $0/0$ “有定义”，但不让它“有有意义的有限值”，对训练收敛无直接帮助；
3. 硬件（GPU/TPU）实现的是 IEEE 754 语义，轮代数需要全新的算术硬件，属于长远设想。

因此本页把“轮代数用于大模型训练”标注为**开放猜想**，不作为事实陈述。

### 热带几何：一个真实但独立的方向

材料提到的“热带几何与神经网络”确有其事，但**与轮代数是不同课题**：

- Liwen Zhang, Gregory Naitzat, Lek-Heng Lim, *Tropical Geometry of Deep Neural Networks*, ICML 2018（arXiv: [1805.07091](https://arxiv.org/abs/1805.07091)）研究了热带半环 $(\max,+)$ 下 ReLU 网络的分段线性结构；
- 热带几何使用**热带半环**（把加法换成 $\max$ 或 $\min$、乘法换成 $+$），这与轮代数是**完全不同的代数结构**，不应混为一谈；
- 热带几何目前是数学与理论机器学习的交叉研究方向，也不是生产训练中的除零方案。

## 黑洞与弦理论中的“除零”：区分真实工具与猜想

材料提到轮代数对黑洞、弦理论的意义。同样需要分层：

**真实的数学工具（与轮代数无关）**：

- 黑洞奇点、坐标奇点处度规分量发散，物理学家用**极限、紧化、正则坐标变换**处理，例如 [[黎曼曲面|黎曼曲面]] 与紧化、[[Laurent 级数|Laurent 级数]] 与 [[留数定理|留数定理]] 分析极点结构；
- 弦理论中的发散（紫外发散）通过**正规化与重整化**处理，而不是通过轮代数；
- T-对偶、$B$ 场等结构涉及几何与代数，但标准表述不使用轮代数。

**属于猜想**：

- “轮代数为黑洞奇点或弦理论除零提供新框架”是**推测性的**，没有公认的已发表理论把轮代数作为黑洞或弦论的标准工具；
- 任何此类主张都必须明确：它替代或补充的是哪一步现有计算，以及是否保持物理可观测量不变。

## 为何理论上适用却未落地：三重否定的方向性思考

一个自然的问题是：轮代数让除法全域封闭、给 $0/0$ 一个合法元素，**理论上似乎正对训练里除零/NaN 的痛点，为什么反而没人用？** 下面用“否定之否定之否定”的辩证结构回答，目的是既不神话它、也不武断地永久否定它。

### 正题：它“应该有用”

轮代数的吸引力是真实的：除法封闭、无偏函数、$\perp$ 让“未定义”成为一个可以做等式推理的代数对象。从纯数学看，这是一个干净的目标。

### 第一重否定：工程现实否定了直接落地

截至 2026-09，**未检索到任何真实的大模型训练或推理管线使用轮代数**。原因是它治的病不是 ML 真正得的病：

1. **方向治反了**：ML 要的是让 $0/0$ **响亮报错**以便检测，而 NaN 的“传染性”正是错误检测的特性；$\perp$ 把报警器变成安静的合法值，等于拆掉报警；
2. **破坏自动微分**：反向传播、链式法则依赖 $0\cdot x=0$、分配律、$x/x=1$，而轮代数恰恰放弃这些，用它就得在不满足分配律的代数上重建微积分；
3. **$\perp$ 不携带可用信息**：训练真正要问“这个值本该是多少”，由极限/epsilon 给出**有限、可回传梯度**的数；$\perp$ 只封装“未定义”，不助收敛；
4. **硬件与生态锁定**：GPU/TPU、cuDNN、flash-attention 全按 IEEE 754 实现，换算术等于换新指令集并失去所有优化算子；
5. **在位方案已够好**：epsilon 平滑、log-sum-exp、梯度裁剪、loss scaling 成本低、已验证、与自动微分无缝集成；
6. **哲学气质不合**：ML 处理奇点的惯用手法是**转化语境**（改参数化、归一化、加小项），而非**扩张数系**。

### 第二重否定：但“未被采用”不等于“没有价值”

否定之否定：上面否定的是“轮代数替代 IEEE 754 做训练算术”，**不是**否定它的全部意义。轮代数真正稳的贡献是一份**概念清单**——它精确刻画了“要让除法全域封闭，必然牺牲哪些代数恒等式”。这份“**封闭性的代价表**”本身是有用的元知识：

- 它解释了为什么工程界宁可用 epsilon/NaN 这类“不封闭但保留分配律”的方案——因为**保留自动微分所依赖的恒等式**比“除法封闭”更重要；
- 它给任何未来提出的“total-division”方案提供了一个**先验诊断工具**：凡宣称让除零良定义的代数，都应对照这份代价表说明它放弃了什么、又靠什么补回梯度正确性。

换句话说，轮代数作为**参照系**已经在发挥作用，只是不作为**运行时算术**。

### 第三重否定：为它保留一份真实的可能性方向

否定之否定之否定：连“它只有概念价值”这个安慰性结论也不应被固化，否则就是用今天的工程现状去永久判决一个数学结构。下面列出几个**比“用于大模型训练”更具体、更有希望**的方向，并诚实标注证据等级——它们目前都还是**开放方向**，但性质不同，值得持续观察：

| 可能方向 | 为什么轮代数思想在这里更契合 | 证据等级 |
|---|---|---|
| **验证式/可认证数值**（定理证明器、验证编译器、certified robustness） | 这类系统需要**无未定义分支的全域函数**，$\perp$ 让除法成为 total function，便于等式推理与形式化证明 | 【方向性猜想】最可能的真实生态位，但尚无成熟 ML 落地 |
| **神经符号 / 程序合成 / DSL 推理** | 对“可能除零”的程序做符号推理时，把 $\perp$ 当代数值可避免崩溃、支持方程化推导部分程序 | 【方向性猜想】与全域除法在程序逻辑中的用途同源 |
| **自动微分的奇点边界情形** | 现有 AD 在可去奇点处常直接吐 NaN，而极限其实存在；一个能记录“此处是 $\perp$ 但极限为 $L$”的框架可提升鲁棒性 | 【方向性猜想】真正答案是极限而非轮代数，但“给奇点一个代数把手”的直觉相通 |
| **非 IEEE 算术的研究型硬件探索** | posit、对数域、热带算术等替代数系正在被研究；轮算术可作为“无陷阱/无异常”处理器的一种候选语义 | 【低概率开放方向】属研究探索，无生产证据 |
| **奇点几何的形式化**（黑洞、流形退化） | 若有人在证明助手中形式化奇异几何，全域除法结构可能自然出现 | 【高度推测】仍属猜想，需保持物理可观测量不变 |

这三重否定的净结论是：**不神话、不落地、也不永久否定**。轮代数今天不是、也短期不会是大模型训练的运行时工具；但它作为“封闭性代价”的参照系已有价值，且在**验证式计算、神经符号推理、AD 奇点处理**等前沿方向保留着一份**具体而诚实的可能性**。判断其未来的标准始终是同一句话：**拿出在真实模型上可复现的稳定性或效率证据**——在此之前，一切停留在【方向性猜想】。

## 禁止误连

以下等价关系**不成立**，仅可作术语对照或方法论类比：

- 轮代数的 $\perp=0/0$ ≠ IEEE 754 的 NaN（前者是自洽代数元素，后者是错误标记且会传染）；
- 轮代数 ≠ 黎曼球射影紧化（$\perp$ 是“完全不确定”，$\infty$ 是“定向发散”，语义不同）；
- 轮代数 ≠ 极限不定式（极限通过趋近方式赋值，$\perp$ 不携带具体数值信息）；
- 引入 $1/0$ ≠ 引入 $i=\sqrt{-1}$ 的成熟度（复数已被物理全面验证，轮代数是纯数学小众结构）；
- 轮代数 ≠ 热带半环 / 热带几何（是完全不同的代数结构）；
- 轮代数用于大模型训练 ≠ 已验证工程实践（属开放猜想，现有方案是 NaN 检测 + 梯度裁剪 + epsilon 平滑）；
- 轮代数用于黑洞/弦理论 ≠ 现有标准工具（现有工具是极限、紧化、Laurent/留数、正规化重整化）；
- 放弃 $0\cdot x=0$ 与分配律 ≠ 可直接替换自动微分（反向传播依赖这些恒等式，替换会破坏梯度正确性）。

## 与其他概念的关系

- [[复数]]：同为“扩展数系换取方程可解性”的方法论范例，本页以此类比轮代数的构造动机；
- [[极限]]：分析路径处理 $0/0$ 不定式，与轮代数是不同方案；
- [[黎曼曲面]]：几何路径（射影紧化、黎曼球）处理 $1/0=\infty$，与轮代数语义不同；
- [[Laurent 级数]] 与 [[留数定理]]：分析极点/发散的标准工具，物理中处理奇点用它而非轮代数；
- [[浮点数与数值稳定]]：IEEE 754 NaN/Inf 是工程现实，与轮代数 $\perp$ 需严格区分；
- [[梯度消失与梯度爆炸]]：训练中除零/溢出的真实来源及现行处理方法；
- [[弦理论与大模型跨学科关系图谱]]：本页作为“除零的多种处理”跨学科节点登记于图谱，并标注轮代数应用的猜想边界。

## 参考资料

- Anders Carlström, *Wheels – on division by zero*, Mathematical Structures in Computer Science 14 (2004), 143–184. DOI: [10.1017/S0960129503004110](https://doi.org/10.1017/S0960129503004110).
- Weisstein, Eric W., “Wheel Algebra,” *MathWorld* — A Wolfram Web Resource.
- Liwen Zhang, Gregory Naitzat, Lek-Heng Lim, *Tropical Geometry of Deep Neural Networks*, Proceedings of the 35th International Conference on Machine Learning (ICML), PMLR 80, 2018. arXiv: [1805.07091](https://arxiv.org/abs/1805.07091).
- IEEE Standard for Floating-Point Arithmetic, IEEE 754-2019（NaN/Inf 语义）。
- 库内相关页：[[极限]]、[[黎曼曲面]]、[[浮点数与数值稳定]]。
