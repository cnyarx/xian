---
aliases:
  - String Theory and LLM Interdisciplinary Knowledge Graph
  - 跨学科关系图谱
  - 学科交叉导航
english_name: String Theory and LLM Interdisciplinary Knowledge Graph
tags:
  - 跨学科
  - 知识图谱
  - 弦理论
  - 大模型
status: 持续维护
---

# 弦理论与大模型跨学科关系图谱

## 使用原则

本页连接两条学习路径中真实共享的数学对象、结构和方法。链接分为三类：

1. **数学对象直接相同**：两边使用同一定义或公式，可以双向复用基础知识；
2. **共享结构但不等价**：存在有帮助的结构类比，但对象、目标或运算规则不同；
3. **术语辨析**：中文或英文名称相近，却不能互相替代。

每条跨学科链接都应说明关系和边界，不能只因名称相似就建立等价关系。

## 数学对象直接相同

### Gaussian 积分与正态分布

- [[Gaussian 积分|Gaussian 积分]] 给出正态概率密度归一化常数的数学来源；
- [[概率分布|概率分布]] 说明正态分布在参数初始化、高斯噪声和高斯过程中的使用；
- [[正态分布与常见分布|正态分布与常见分布]] 提供正态分布、PMF 与 PDF 的原子概念入口。

边界：Gaussian 积分是一类积分方法，正态分布是使用 Gaussian 函数并满足归一化条件的概率分布；二者关系紧密但不是同一个概念。

### 梯度与梯度下降

- [[梯度|梯度]] 定义标量函数在当前位置最快上升的方向；
- [[梯度下降|梯度下降]] 使用负梯度方向迭代更新模型参数。

边界：梯度是局部微分对象，梯度下降是使用梯度构造的优化算法；梯度为零也不保证到达全局最小值。

### 矩阵与大模型线性计算

- [[矩阵|矩阵]] 给出选定基后线性映射的坐标表示；
- [[点积与矩阵乘法|点积与矩阵乘法]] 展示矩阵乘法、线性层和注意力点积中的实际计算。

边界：矩阵是通用数学对象，大模型权重矩阵只是其一种应用；矩阵元素的语义由模型架构、基和张量轴约定决定。

### 指数、对数与概率建模

- [[指数函数|指数函数]] 和 [[对数函数|对数函数]] 给出定义域、反函数关系与运算规律；
- [[指数与对数|指数与对数]] 把这些规律用于 softmax、交叉熵、数值稳定性和缩放定律。

边界：softmax 和交叉熵使用指数、对数函数，但它们还包含归一化、概率模型和损失定义，不能只靠指数与对数的基础性质完整解释。

### 向量空间与大模型高维表示

- [[向量空间|向量空间]] 给出线性组合、基、维数和线性映射的共同数学语言；
- [[高维空间直觉|高维空间直觉]] 与 [[嵌入|嵌入]] 将 token、特征和隐藏状态表示为有限维实向量。

边界：两条路径直接共享向量空间结构，但具体空间可以是实或复、有限维或无限维；大模型隐藏维度不等于弦理论的临界时空维数。

### 经典概率与 Born 规则

- [[概率基础|概率基础]] 使用经典条件概率、期望和分布描述数据与模型输出；
- [[概率|概率]] 与 [[Born 规则|Born 规则]] 在共同概率公理之上，引入复振幅、绝对值平方、相位和干涉。

边界：量子测量结果仍形成普通概率分布，但复概率振幅不是经典概率；不能把语言模型 token 概率解释成量子振幅或量子叠加。

## 共享结构但不等价

### Fourier 表示与位置编码

- [[Fourier 级数|Fourier 级数]] 用正弦、余弦或复指数分解周期结构；
- [[旋转矩阵与位置编码数学|旋转矩阵与位置编码数学]] 中的正弦位置编码与 RoPE 复用频率、相位和旋转结构。

边界：位置编码不是对任意序列执行 Fourier 变换；它把位置信息编码进表示，具体频率和旋转规则由模型设计决定。

### 群作用、旋转位置编码与置换等变性

- [[群作用|群作用]] 描述群元素如何一致地作用于集合或向量空间；
- [[Yang-Mills 理论|Yang-Mills 理论]] 使用 Lie 群的局域作用、群表示和协变对象描述规范场与物质场；
- [[规范协变导数|规范协变导数]] 通过规范联络补偿位置依赖内部标架的变化，使 $D_\mu\psi$ 与 $\psi$ 按同一表示变换；
- [[旋转矩阵与位置编码数学|旋转矩阵与位置编码数学]] 中的位置旋转 $m\mapsto R(m\theta)$ 是群表示的有限维例子；无位置输入的自注意力则具有 token 排列的置换等变结构。

共同结构：这些页面都使用群、表示、作用与协变/等变性的数学语言；可以复用群作用和表示论的基础页面。

边界：Yang-Mills 的局域规范变换作用于时空各点并伴随规范联络及非齐次变换项；规范协变导数是该补偿机制的微分形式。RoPE 是预先规定的位置相关旋转，注意力的置换等变性是架构性质。RoPE 的旋转或 token 置换不等于规范对称性、规范轨道、世界面微分同胚或模群，$D_\mu$ 也不等于自动微分算子。

### Wilson 线、路径排序与 holonomy

- [[Wilson 线与 Wilson 圈|Wilson 线与 Wilson 圈]] 描述规范联络沿有限路径的平行输运矩阵，闭合回路取迹后得到规范不变的 Wilson 圈算符。

边界：Wilson 线依赖整条定向路径，不是 token 序列、注意力路径或计算图路径；路径排序 $\mathcal P$ 来自非 Abelian 联络在不同路径参数处的不对易，不等于位置编码或自回归顺序；holonomy 是规范联络的有限群元素，不等于参数更新、隐藏状态累积或循环网络记忆；Wilson 圈面积律描述特定非微扰极限中静态源的线性势，不等于模型规模定律；闭合路径取迹是类函数投影，不等于 attention 权重归一化。

### Feynman 图与计算图

- [[Feynman 图|Feynman 图]] 枚举微扰展开中的传播子、顶点和积分项；
- [[反向传播|反向传播]] 与 [[计算图自动微分与梯度累加|计算图]] 使用计算图记录一次确定计算的依赖关系，并沿图应用链式法则。

边界：两者都能作图形化记账，但 Feynman 图不是计算图，传播子也不是反向传播；两类图的节点、边、求和规则和物理含义均不同。

### 路径积分驻点与优化

- [[路径积分|路径积分]] 对全部历史按 $e^{iS/\hbar}$ 加权，经典驻定路径只在半经典极限中因相位相消而突出；
- [[梯度下降|梯度下降]] 在有限维参数空间中沿负梯度迭代降低经验损失。

边界：驻点或鞍点思想可以帮助比较，但路径积分不是 SGD，量子振幅求和不是训练轨迹；Euclidean 权重 $e^{-S_E/\hbar}$ 与概率模型相似也不意味着训练目标等于物理作用量。

### 重整化群与特定概率网络

- [[重整化群|重整化群]] 描述参数、算符和有效作用怎样随尺度流动，并用粗粒化、固定点和相关方向组织多尺度行为；
- [[弦理论与大模型训练的已知关系]] 记录变分重整化群与特定受限 Boltzmann 机、深度信念网络之间已有的精确映射及证据边界。

边界：该映射针对特定变分 RG 与特定概率网络，不代表任意深度网络都在执行重整化；Transformer 层数不等于 RG 步数。有限步的严格映射需要明确粗粒化核、尺度变量和所保留的分布或可观测量；变分 RG 的精确对应还要求保持配分函数或可见变量边缘分布的 trace condition（迹条件）。固定点不是单次映射的必要条件，但若声称临界行为或普适类，就必须进一步检验固定点和稳定方向。当前大模型教程没有独立讲解该映射的原子页面，因此不伪造教程侧强双链。

### 有效场论与模型量化中的受控近似

- [[有效场论|有效场论]] 在指定能标和精度内保留低能自由度，按 $E/\Lambda$ 幂次截断算符，并用第一项被省略贡献估计理论误差；
- [[模型量化|模型量化]] 用较少比特近似权重或激活，以任务精度、延迟和显存实测判断近似是否可接受。

共同结构：两者都先指定使用范围和保真目标，再删除或压缩当前任务无法分辨的细节，并要求用可测误差验证近似，而不是只凭“规模更小”判断成功。

边界：模型量化不是积分掉量子场，也没有 EFT 的 Wilson 系数、matching、算符基或 RG running；EFT 的 $E/\Lambda$ 截断误差也不等于量化的舍入、截断和校准误差。该关系属于“受控近似与误差验收”的方法结构类比，不是同一数学理论。

## 术语辨析

### 机器学习正则化与量子场论正规化、重整化

- [[过拟合与正则化|过拟合与正则化]] 中的 regularization 主要通过约束、惩罚或数据策略改善泛化；
- [[正则化与泛化|正则化与泛化]] 提供 L1/L2、Dropout、早停、数据增强的原子概念入口；
- [[圈展开|圈展开]] 暴露量子场论圈积分中的紫外与红外发散；
- [[紫外发散与红外发散|紫外发散与红外发散]] 系统区分 UV 与 IR 动量区域，并逐条辨析 ML regularization 与 QFT regularization/renormalization 的边界；
- [[正规化与重整化|正规化与重整化]] 展示维数正规化、局域反项、重整化条件和运行耦合的完整 QFT 操作流程。

边界：机器学习正则化不等于量子场论正规化。量子场论正规化先给发散表达式一个受控定义，重整化再通过参数、场、反项和重整化条件建立有限预测。它们共享英文词根或中文近义译法，只能做术语对照，不能视为相同数学操作。

### 泛函与软件命名

- [[泛函|泛函]] 是“以函数为输入、输出数或其他对象”的数学映射；
- 软件库中的 `functional` 常表示无状态函数式接口，命名相近不代表它是数学泛函。

### 除零的多种处理与轮代数

- [[极限|极限]] 用趋近过程把 $0/0$ 当作不定式，其值由具体路径决定，不新增元素；
- [[黎曼曲面|黎曼曲面]] 相关的黎曼球用射影紧化添加单个 $\infty$，使 $1/0=\infty$，但 $\infty-\infty$、$0\cdot\infty$ 仍无定义；
- [[浮点数与数值稳定|浮点数与数值稳定]] 采用 IEEE 754 语义，$0/0=\text{NaN}$、$x/0=\pm\infty$，NaN 作为错误标记会传染；
- [[轮代数与除零]] 给出第四种方案：轮代数引入 $\perp=0/0$ 使全域除法封闭，代价是放弃 $0\cdot x=0$、完整分配律与 $x/x=1$。

共同结构：四者都试图给“除以零”一个受控的处理，都属于“为换取运算封闭性或可解性而牺牲部分公理”的同一方法论家族（与引入 $i$ 构造复数同型）。

边界：这四套语义互不等价，不能互相替换。$\perp$ 是代数元素、NaN 是工程错误标记、$\infty$ 是紧化点、不定式是需极限赋值的记号。轮代数是纯数学小众结构，未成为分析、物理或机器学习的标准工具；“轮代数替代 NaN 处理”“轮代数形式化黑洞奇点或弦理论 T-对偶”均属开放猜想，无可靠已发表证据。热带几何（ICML 2018, arXiv:1805.07091）使用的是热带半环 $(\max,+)$，与轮代数是完全不同的代数结构。放弃 $0\cdot x=0$ 与分配律也意味着轮代数不能直接替换依赖这些恒等式的自动微分与反向传播。

### 中国古典数学的异常量处理

- [[中国古典数学与异常量处理]] 记录有据可查的史实：《九章算术》正负术（负数）、刘徽割圆术“不可割”（极限动机）、齐同术（分数通分）、出入相补原理（几何等积守恒）、秦九韶正负开方术与大衍求一术；
- [[极限|极限]] 与 [[轮代数与除零]] 提供割圆术极限思想的现代对照，并区分“极限过程”与“新增 $\perp/\infty$ 元素”两条路径；
- [[梯度消失与梯度爆炸|梯度消失与梯度爆炸]] 与 [[正则化与泛化|正则化与泛化]] 是更正原文两处错误的锚点。

共同结构：古典“重算法、重关系、转化语境而非新增实体”的风格，可作为“面对训练异常先改度量/参照系”的**提问启发**，与 [[轮代数与除零]] 的“扩展系统 vs 转化语境”同层。

边界：这些是**方法论类比与再诠释**，不是数学等价。中国古典数学发展了负数但**未发展虚数/复数**，“秦九韶虚积=虚数中间态”无史料支持；出入相补（几何等积）≠ 轮代数 $0\cdot\perp\ne0$；齐同术（通分）≠ 消除除零奇点，≠ 正则化/重整化。原文两处硬错误须更正：正则化 Regularization ≠ 重整化 Renormalization（见 [[正规化与重整化|正规化与重整化]]）；全局范数梯度裁剪 $g\cdot\min(1,T/\lVert g\rVert)$ 保留方向，不是“丢失方向”。四条“训练优化建议”对应既有方法（AGC/NFNet、自然梯度/K-FAC/Shampoo、熵正则、Grokking），非由古典数学推导，“突破 scaling law”属过度宣称。

## 禁止误连

以下名称或表面图景相近，但不得建立等价关系：

- 传播子 ≠ 反向传播；
- Feynman 图 ≠ 计算图；
- 路径积分 ≠ SGD；
- 深度网络层数 ≠ 重整化群步数；
- 机器学习正则化 ≠ 量子场论正规化或重整化；
- 大模型高维参数空间 ≠ 弦理论临界时空维数；
- 数学泛函 ≠ `torch.nn.functional`；
- 量子叠加 ≠ 注意力加权；
- 圈展开的圈数 ≠ 神经网络层数或训练轮数；
- EFT 积分掉重自由度或截断高维算符 ≠ 模型量化、知识蒸馏或 LoRA；
- EFT 的 Wilson 系数与 matching ≠ 量化 scale/zero-point 或校准过程；
- Yang-Mills 局域规范变换 ≠ token 位置旋转或注意力的置换等变性；
- 规范协变导数 $D_\mu$ ≠ 自动微分算子，规范势 $A_\mu$ ≠ 可学习的位置编码；
- Yang-Mills 规范轨道 ≠ 参数优化轨迹，[[规范固定|规范固定]] ≠ 模型正则化；
- [[Faddeev-Popov 行列式|Faddeev-Popov 行列式]] ≠ 神经网络 Jacobian 行列式，规范群体积 ≠ 参数空间体积，Gribov horizon ≠ 损失景观鞍点；
- [[Faddeev-Popov 鬼场|Faddeev-Popov 鬼场]] ≠ 隐变量、辅助 token、负样本权重或辅助损失，鬼圈负号 ≠ 损失惩罚项；
- [[BRST 对称性|BRST 对称性]] 中的微分 $s$ ≠ 反向传播，$s^2=0$ ≠ 梯度消失或梯度裁剪，BRST 荷 $Q$ ≠ 优化器或模型算子，$Q|\mathrm{phys}\rangle=0$ ≠ 分类器输出或停止条件，gauge-fixing fermion ≠ 训练损失，Nakanishi-Lautrup 场 ≠ bias、隐变量或辅助 token；
- [[BRST 上同调|BRST 上同调]] ≠ 聚类、降维或表征学习，$\ker Q/\operatorname{im}Q$ ≠ quotient embedding，closed/exact 分类 ≠ 正负样本分类，ghost number ≠ token 位置、层数或训练步数，BRST-exact 态 ≠ 冗余特征或被剪枝参数，quartet mechanism ≠ 剪枝、蒸馏、MoE 路由或特征消除，BRST 可观测量 ≠ 可解释特征，BRST 反常 ≠ 模型幻觉、训练异常或数据漂移；
- [[量子反常|量子反常]] ≠ 模型幻觉、训练异常、数据漂移或分布外检测，规范反常 ≠ 约束违反或权限错误，轴反常 ≠ 类别不均衡或标签噪声，Weyl/迹反常 ≠ 梯度范数异常或归一化失败，Wess–Zumino 一致性条件 ≠ KKT 条件或一致性正则化，anomaly matching ≠ 训练集与测试集分布匹配，anomaly inflow ≠ 残差连接或跨层信息注入，反常系数 ≠ 损失权重或超参数，中心荷 ≠ 模型容量或隐藏维度，Green–Schwarz 机制 ≠ 梯度裁剪、剪枝或错误补偿；
- [[手征反常|手征反常]] ≠ 异常检测、离群点或分布外样本，轴流 ≠ token 流、激活流或梯度流，左/右手费米子 ≠ 正向/反向传播或左右上下文，$\gamma^5$ ≠ 门控函数或分类头，AVV 三角形图 ≠ 三层网络或计算图回路，$F\widetilde F$ ≠ Hessian、损失曲率或参数空间面积，instanton ≠ 优化跳跃或训练重启，Dirac 零模 ≠ 零梯度、死亡神经元或零奇异值，$\pi^0\to\gamma\gamma$ ≠ 多模态生成或双输出解码，手征规范反常消除 ≠ 正则化、安全对齐或梯度裁剪；
- [[Weyl 反常|Weyl 反常]] ≠ 特征缩放、标准化、LayerNorm 或 RMSNorm，中心荷 ≠ 参数量、隐藏维度、注意力头数或模型容量，临界维数 ≠ embedding dimension 或 context length，$bc$ 鬼 ≠ 辅助 token、负样本或 dropout mask，$\beta\gamma$ superghost ≠ Adam 的 $\beta_1,\beta_2$，Polyakov 非局域作用量 ≠ 全局注意力，sigma-model beta ≠ 优化器 beta，RG fixed point ≠ 优化器收敛点，Liouville 场 ≠ 新增网络层或 scale token，Weyl 反常消除 ≠ 梯度裁剪、ensemble 抵消或安全对齐；
- [[反常消除|反常消除]] ≠ 修复模型幻觉、训练异常或软件异常，规范反常系数相消 ≠ 多个损失项正负抵消，表示求和 ≠ ensemble 投票或模型融合，Green–Schwarz 补偿变换 ≠ 梯度补偿、误差校正或残差连接，anomaly inflow ≠ token/激活/梯度流或跨层信息注入，世界面中心荷抵消 ≠ 参数正负抵消、归一化或容量平衡，Witten anomaly ≠ OOD 检测或程序异常，anomaly polynomial 因子化 ≠ 矩阵分解或低秩分解，RR tadpole cancellation ≠ 梯度零均值或偏置消除，K-theory 电荷 ≠ 模型量化等级或知识图谱标签；
- [[二维 Euclidean 场论|二维 Euclidean 场论]] 的 $S_E$ ≠ 训练损失，$e^{-S_E}$ ≠ softmax 或 token 概率，场构型泛函积分 ≠ SGD 参数轨迹，Wick 旋转 ≠ 复值训练，Euclidean 时间 ≠ 训练步数，圆柱—平面映射 ≠ 位置编码，径向量子化半径 ≠ 网络深度，Green 函数与场二点函数 ≠ attention matrix、kernel matrix 或 embedding 相似度，反射正性 ≠ 损失非负、概率归一化或安全对齐，红外零模 ≠ 零梯度、死亡神经元或零奇异值，质量调节器 ≠ weight decay，共形不变性 ≠ 大模型 scaling law；
- [[复坐标与全纯分解|复坐标与全纯分解]] 中的 $z,\bar z$ ≠ 左右上下文或双向注意力，全纯/反全纯扇区 ≠ encoder/decoder、正向/反向传播、MoE 分支或模型集成，$\partial\bar\partial\phi=0$ ≠ 零梯度或训练收敛，局部分解 $f(z)+g(\bar z)$ ≠ 张量分解、模型拆分或低秩分解，$(h,\bar h)$ ≠ hidden dimension 或 tensor shape，闭弦两组振子 ≠ 双塔网络或两套模型参数，单值化、周期、绕数与 monodromy ≠ token 循环、上下文窗口或位置编码；
- [[二维自由玻色子|二维自由玻色子]] 的对数二点函数 ≠ attention matrix、embedding 相似度或 kernel score，Gaussian 路径积分 ≠ 参数初始化，$\partial X$ 的全纯性 ≠ causal attention，分点正规序 ≠ LayerNorm、BatchNorm 或梯度裁剪，自收缩奇点 ≠ 训练发散或过拟合，能动张量 $T(z)$ ≠ 损失梯度，中心荷 $c$ ≠ 参数量、隐藏维度、注意力头数或容量评分，常数零模 ≠ 零梯度或死亡神经元，紧致识别 $X\sim X+2\pi R$ ≠ tokenization、权重量化或周期位置编码，$D$ 个自由玻色子给 $c=D$ ≠ 增加 $D$ 层网络；
- [[二维自由费米子|二维自由费米子]] 的 Grassmann 反对易 ≠ 负权重、负概率或符号位，费米换序负号 ≠ loss 惩罚或梯度取反，Pauli 不相容 ≠ token 去重、稀疏激活或专家互斥，简单极点 $\psi(z)\psi(w)\sim1/(z-w)$ ≠ attention score，左右手征 ≠ 左右上下文或正反向传播，NS/R 边界条件 ≠ train/eval 模式或缓存策略，Ramond 零模 ≠ 零梯度或零初始化，中心荷 $c=1/2$ ≠ 半个参数或半层网络，费米 Wick 配对负号 ≠ attention mask，自旋结构 ≠ tensor shape、位置编码或数据分片；
- [[算符乘积展开|算符乘积展开]] 的极点与 OPE 系数 ≠ attention score、attention matrix、embedding 相似度或 kernel score，算符基展开 ≠ token vocabulary、隐藏层、字典学习或 MoE 专家路由，短距离极限 $z\to w$ ≠ 训练后期、token 相邻或局部 attention，OPE 收敛域 ≠ context window，Laurent 负幂 ≠ 负权重、负 loss 或梯度爆炸，Wick 收缩 ≠ attention mask 或计算图连边，单重/双重收缩 ≠ 单头/多头注意力，围道提取简单极点 ≠ top-k 或采样，OPE 结合一致性 ≠ Transformer 层组合或知识图谱边传递，OPE 因子化 ≠ 低秩分解、模型切分或张量并行；
- [[正规序|正规序]] ≠ LayerNorm、BatchNorm、RMSNorm 或权重归一化，产生/湮灭算符重排 ≠ tensor transpose、算子调度或计算图拓扑排序，自收缩减法 ≠ 去噪、异常值删除、数据清洗或梯度裁剪，真空期望减法 ≠ 数据中心化、均值归一化或 bias 校正，分点减法 ≠ 数值差分、滑动窗口或邻近 token 比较，交叉收缩 ≠ attention、跨层连接或特征交互，费米换序负号 ≠ 负 loss 或惩罚项，正规序指数 ≠ softmax、指数移动平均或 exponential activation，有限局域反项 ≠ 超参数调优或后处理补丁，复合算符混合矩阵 $Z_i{}^j$ ≠ 神经网络权重矩阵或 MoE 路由，零点常数减法 ≠ 删除模型 bias，正规序后的中心荷 ≠ 模型容量或归一化统计量；
- Yang-Mills 三/四规范玻色子自相互作用 ≠ 多头注意力交互；
- Wilson 线 ≠ token 序列、注意力路径或计算图路径；
- 路径排序 $\mathcal P$ ≠ 位置编码或自回归顺序；
- holonomy ≠ 参数更新、隐藏状态累积或循环网络记忆；
- Wilson 圈面积律 ≠ 模型规模定律，闭合路径取迹 ≠ attention 权重归一化；
- [[轮代数与除零|轮代数]] 的 $\perp=0/0$ ≠ IEEE 754 的 NaN，≠ 黎曼球紧化点 $\infty$，≠ 极限不定式，≠ 热带半环 $(\max,+)$；引入 $1/0$ ≠ 引入 $i=\sqrt{-1}$ 的成熟度；轮代数用于大模型训练或黑洞/弦理论 ≠ 已验证实践（均属开放猜想，现行方案是 NaN 检测、梯度裁剪、epsilon 平滑、正规化与重整化）；
- [[中国古典数学与异常量处理|中国古典数学]] 的秦九韶正负开方术 ≠ 虚数/复数，中国古典数学 ≠ 已发展 $i$，“虚积=虚数中间态” ≠ 史料事实；出入相补（几何等积守恒）≠ 轮代数 $0\cdot\perp\ne0$，齐同术（通分）≠ 消除除零奇点或正则化/重整化；割圆术极限 ≠ $\perp/\infty/\text{NaN}$；正则化 Regularization ≠ 重整化 Renormalization；全局范数梯度裁剪 ≠ “丢失方向”；AGC/自然梯度/K-FAC/Shampoo/Grokking ≠ 由古典数学推导而来，“过程本体论突破 scaling law” ≠ 已验证结论。

## 维护规则

1. 新增关系前先确认两端文件真实存在；
2. 强连接优先写回双方页面，形成可解释的双向导航；
3. 类比关系必须同时写出共同结构和失效边界；
4. 若后续概念文件尚不存在，只保留普通文本，不创建断链；
5. 每轮修改后检查跨目录链接、断链、歧义和术语边界。
