---
aliases: [推理, Inference, 自回归生成, Prefill, Decode]
tags: [大模型, 推理, 教程, 学习教程]
prerequisites: ["[[Decoder-Only 架构]]"]
next: ["[[采样策略]]"]
---
# 推理概览

## 一句话定义

推理（Inference）是使用已经训练好的模型，根据输入计算输出，不更新模型参数。

## 两个阶段

### 预填充

Prefill 一次处理提示中的全部 token，建立每层 KV cache。它可大量并行，通常更偏计算密集。

### 解码

Decode 每次生成一个新 token，再把它的 K/V 加入缓存。每步矩阵较小但要读取大量权重和缓存，常更受内存带宽限制。

## 自回归循环

```text
提示 → 前向计算 → logits → 采样 token
     → 追加 token → 再前向 → 再采样
     → 遇到 EOS 或长度上限后停止
```

## 关键指标

- 首 token 延迟（TTFT）：请求到第一个输出 token 的时间，受 prefill 影响大。
- 每 token 延迟（TPOT）：后续 token 的平均间隔。
- 吞吐量：单位时间处理或生成的 token 数。
- 并发：同时服务的请求数。

优化一个指标可能损害另一个，例如批量增大通常提高吞吐但可能增加单请求等待。

## 停止条件

- 生成 EOS。
- 达到最大新 token 数。
- 命中停止字符串。
- 外部程序主动取消。

## 概念与公式导航

- [[推理阶段与延迟指标|推理阶段与延迟指标]]

## 练习

1. 训练和推理最根本的区别是什么？
2. prefill 与 decode 分别处理什么？
3. 吞吐和延迟为什么可能冲突？
4. 列出三种停止条件。

## 来源

- Hugging Face, *Generation*：https://huggingface.co/docs/transformers/main_classes/text_generation `[A]`
- Kwon, W. et al. (2023). *Efficient Memory Management for Large Language Model Serving with PagedAttention*. SOSP https://arxiv.org/abs/2309.06180 `[A]`