---
aliases:
  - Canonical Quantization of Fields
  - Canonical Field Quantization
english_name: Canonical Quantization of Fields
tags:
  - 物理学
  - 量子场论
  - 正则量子化
  - 弦理论先修
  - 弦理论
prerequisites:
  - "[[经典场论]]"
  - "[[Hamiltonian]]"
  - "[[Poisson 括号]]"
  - "[[量子算符]]"
  - "[[对易关系]]"
  - "[[量子谐振子]]"
  - "[[产生算符]]"
  - "[[湮灭算符]]"
  - "[[Fock 空间]]"
  - "[[Klein-Gordon 场]]"
  - "[[Dirac 场]]"
  - "[[Maxwell 场量子化]]"
used_by:
  - "[[传播子]]"
  - "[[时序乘积]]"
  - "[[Wick 定理]]"
  - "[[Feynman 图]]"
  - "[[二维自由玻色子]]"
  - "[[弦的模展开]]"
  - "[[振子代数]]"
  - "[[弦的量子态]]"
evidence_status: 教材事实
---

# 场的正则量子化（Canonical Quantization of Fields）

## 一句话解释

场的正则量子化把每个时刻的经典场和共轭动量提升为算符，并用等时对易关系或反对易关系代替经典 Poisson 括号；对自由场而言，这等价于把每个正规模量子化成一个谐振子。

## 为什么要学它

经典场是时空上的函数，不能产生量子叠加、真空涨落或粒子数变化。正则量子化给出一条从经典 Lagrangian 到量子场算符和 Fock 空间的具体路线：

1. 选定时间切片并求共轭动量；
2. 构造 Hamiltonian；
3. 把经典场提升为算符值分布；
4. 施加等时正则关系；
5. 用产生与湮灭算符展开自由场；
6. 从真空构造多粒子态；
7. 用 Hamiltonian 生成时间演化。

这套方法把 [[Klein-Gordon 场]]、[[Dirac 场]] 和 [[Maxwell 场量子化]] 放进同一框架。弦量子化也从世界面场的正则括号出发，得到振子代数，再施加 Virasoro 或 BRST 条件筛选物理态。

## 本篇约定

采用自然单位：

$$
\hbar=c=1,
$$

以及 mostly-minus 度规：

$$
\eta_{\mu\nu}=\operatorname{diag}(+1,-1,-1,-1).
$$

先以四维自由实 Klein-Gordon 场为主要例子：

$$
\mathcal L
=\frac12\partial_\mu\phi\,\partial^\mu\phi
-\frac12m^2\phi^2.
$$

Fourier 相位取：

$$
k\cdot x=\omega_{\mathbf k}t-\mathbf k\cdot\mathbf x,
\qquad
\omega_{\mathbf k}=\sqrt{|\mathbf k|^2+m^2}.
$$

若教材改变度规号差、Fourier 指数或模式归一化，场展开和算符对易关系中的系数会一起改变，不能只比较单个公式。

## 每个符号是什么意思

- $\phi(t,\mathbf x)$：经典或量子实标量场；
- $\pi(t,\mathbf x)$：与 $\phi$ 共轭的动量场；
- $\mathcal L$：Lagrangian 密度；
- $\mathcal H$：Hamiltonian 密度；
- $H$：总 Hamiltonian；
- $a(\mathbf k)$、$a^\dagger(\mathbf k)$：动量模式的湮灭与产生算符；
- $|0\rangle$：由所有湮灭算符消去的真空；
- $\delta^{(3)}(\mathbf x-\mathbf y)$：三维 Dirac delta 分布；
- $[A,B]=AB-BA$：对易子；
- $\{A,B\}=AB+BA$：反对易子；
- $\omega_{\mathbf k}$：动量 $\mathbf k$ 模式的正频率；
- $N(\mathbf k)=a^\dagger(\mathbf k)a(\mathbf k)$：形式上的模式占据数密度。

## 从有限自由度到场

有限自由度系统的正则变量满足经典括号：

$$
\{q^i,p_j\}_{\mathrm P}=\delta^i{}_j.
$$

正则量子化把它们提升为算符，并作形式替换：

$$
\{q^i,p_j\}_{\mathrm P}
\longrightarrow
\frac1i[\hat q^i,\hat p_j].
$$

于是：

$$
[\hat q^i,\hat p_j]=i\delta^i{}_j.
$$

场论把离散编号 $i$ 换成连续空间位置 $\mathbf x$。Kronecker delta 因而变成 Dirac delta：

$$
\{\phi(t,\mathbf x),\pi(t,\mathbf y)\}_{\mathrm P}
=\delta^{(3)}(\mathbf x-\mathbf y).
$$

量子化后相应为：

$$
[\hat\phi(t,\mathbf x),\hat\pi(t,\mathbf y)]
=i\delta^{(3)}(\mathbf x-\mathbf y).
$$

“等时”很重要：两边的场位于同一时间 $t$，空间位置可以不同。

## 第一步：求共轭动量

对一般玻色场 $\phi^A$，共轭动量定义为：

$$
\pi_A(t,\mathbf x)
=\frac{\partial\mathcal L}
{\partial(\partial_0\phi^A)}.
$$

对自由实标量场：

$$
\mathcal L
=\frac12(\partial_t\phi)^2
-\frac12|\nabla\phi|^2
-\frac12m^2\phi^2,
$$

所以：

$$
\boxed{\pi=\partial_t\phi}.
$$

这与粒子力学中的 $p=\partial L/\partial\dot q$ 完全平行，只是每个空间点都有一对正则变量。

## 第二步：构造 Hamiltonian

Hamiltonian 密度由 Legendre 变换得到：

$$
\mathcal H
=\pi\partial_t\phi-\mathcal L.
$$

对自由实标量场：

$$
\boxed{
\mathcal H
=\frac12\pi^2
+\frac12|\nabla\phi|^2
+\frac12m^2\phi^2
}.
$$

总 Hamiltonian 是：

$$
H=\int d^3x\,\mathcal H.
$$

在量子理论中，$H$ 变成算符并生成时间演化。Heisenberg 图景中：

$$
i\frac{d\hat O}{dt}
=[\hat O,\hat H]
+i\frac{\partial\hat O}{\partial t}.
$$

对没有显含时间的场算符，第二项为零。

## 第三步：提升为算符并施加等时关系

把 $\phi$ 和 $\pi$ 提升为算符值分布：

$$
\phi(t,\mathbf x)
\longrightarrow
\hat\phi(t,\mathbf x),
$$

$$
\pi(t,\mathbf x)
\longrightarrow
\hat\pi(t,\mathbf x).
$$

自由实玻色场的等时正则对易关系是：

$$
[\hat\phi(t,\mathbf x),\hat\pi(t,\mathbf y)]
=i\delta^{(3)}(\mathbf x-\mathbf y),
$$

$$
[\hat\phi(t,\mathbf x),\hat\phi(t,\mathbf y)]=0,
$$

$$
[\hat\pi(t,\mathbf x),\hat\pi(t,\mathbf y)]=0.
$$

场严格说不是每一点上都良好定义的普通算符，而是算符值分布。实际可对测试函数 $f$ 做涂抹：

$$
\phi(f)=\int d^4x\,f(x)\phi(x),
$$

从而得到定义更稳妥的算符对象。

## 第四步：作自由场模展开

自由 Klein-Gordon 方程为：

$$
(\Box+m^2)\phi=0.
$$

其量子场可展开为：

$$
\boxed{
\hat\phi(x)
=\int\frac{d^3k}{(2\pi)^3\sqrt{2\omega_{\mathbf k}}}
\left[
a(\mathbf k)e^{-ik\cdot x}
+a^\dagger(\mathbf k)e^{+ik\cdot x}
\right]
}.
$$

因为 $\hat\phi$ 是 Hermitian 场，负频部分的系数是正频部分系数的 Hermitian 共轭。

由 $\hat\pi=\partial_t\hat\phi$ 得：

$$
\hat\pi(x)
=-i\int\frac{d^3k}{(2\pi)^3}
\sqrt{\frac{\omega_{\mathbf k}}2}
\left[
a(\mathbf k)e^{-ik\cdot x}
-a^\dagger(\mathbf k)e^{+ik\cdot x}
\right].
$$

这里 $k^0=\omega_{\mathbf k}>0$。

## 第五步：得到模式算符代数

要使场满足等时正则对易关系，模式算符必须满足：

$$
\boxed{
[a(\mathbf k),a^\dagger(\mathbf q)]
=(2\pi)^3\delta^{(3)}(\mathbf k-\mathbf q)
},
$$

$$
[a(\mathbf k),a(\mathbf q)]=0,
$$

$$
[a^\dagger(\mathbf k),a^\dagger(\mathbf q)]=0.
$$

验证第一条场对易关系时，$a a^\dagger$ 与 $a^\dagger a$ 两部分共同给出：

$$
[\hat\phi(t,\mathbf x),\hat\pi(t,\mathbf y)]
=i\int\frac{d^3k}{(2\pi)^3}
e^{i\mathbf k\cdot(\mathbf x-\mathbf y)}
=i\delta^{(3)}(\mathbf x-\mathbf y).
$$

因此场的连续正则代数和每个动量模的振子代数是同一结构的两种写法。

## 为什么自由场是一组量子谐振子

把经典自由场 Fourier 分解后，不同 $\mathbf k$ 模式彼此解耦。每个独立实模式的 Hamiltonian 都具有：

$$
H_{\mathbf k}
=\frac12p_{\mathbf k}^2
+\frac12\omega_{\mathbf k}^2q_{\mathbf k}^2
$$

的谐振子形式。

量子化后，每个模式都有自己的产生与湮灭算符。场不是“一个谐振子”，而是连续无穷多个模式谐振子的集合。自由场的线性运动方程正是不同模式能够独立量子化的原因。

实场的 Fourier 系数还满足实条件，$\mathbf k$ 与 $-\mathbf k$ 不能被误当作两套完全独立的经典自由度。上述标准场展开已经正确处理这项关系。

## 第六步：构造真空与 Fock 空间

真空定义为：

$$
a(\mathbf k)|0\rangle=0
$$

对所有 $\mathbf k$ 成立。

单粒子动量态为：

$$
|\mathbf k\rangle
=a^\dagger(\mathbf k)|0\rangle.
$$

在本文归一化下：

$$
\langle\mathbf q|\mathbf k\rangle
=(2\pi)^3\delta^{(3)}(\mathbf q-\mathbf k).
$$

多粒子态由多个产生算符构造：

$$
|\mathbf k_1,\ldots,\mathbf k_n\rangle
=a^\dagger(\mathbf k_1)\cdots
a^\dagger(\mathbf k_n)|0\rangle.
$$

由于产生算符彼此对易，交换两个玻色子的标签不产生新状态。这些不同粒子数的子空间共同组成 [[Fock 空间]]。

## 第七步：Hamiltonian 与零点能

把模展开代入 Hamiltonian，可得形式表达式：

$$
H
=\int\frac{d^3k}{(2\pi)^3}\,
\omega_{\mathbf k}
\left[
a^\dagger(\mathbf k)a(\mathbf k)
+\frac12(2\pi)^3\delta^{(3)}(0)
\right].
$$

第二项是所有模式的 [[零点能]]。它在无限体积和连续动量下发散。

正规排序把所有产生算符放到湮灭算符左边，并定义：

$$
:H:
=\int\frac{d^3k}{(2\pi)^3}\,
\omega_{\mathbf k}
a^\dagger(\mathbf k)a(\mathbf k).
$$

于是：

$$
:H:|\mathbf k\rangle
=\omega_{\mathbf k}|\mathbf k\rangle.
$$

正规排序能移除这套自由场表示中的真空常数，但不能代替一般曲背景、边界效应或引力耦合下的真空能重整化。

## 动量算符

正规排序后的总空间动量为：

$$
:\mathbf P:
=\int\frac{d^3k}{(2\pi)^3}\,
\mathbf k\,
a^\dagger(\mathbf k)a(\mathbf k).
$$

因此单粒子态满足：

$$
:\mathbf P:|\mathbf k\rangle
=\mathbf k|\mathbf k\rangle.
$$

能量和动量共同满足：

$$
E_{\mathbf k}^2-|\mathbf k|^2=m^2.
$$

这说明 $a^\dagger(\mathbf k)$ 产生的是一个质量为 $m$、动量为 $\mathbf k$ 的场量子。

## Heisenberg 方程恢复经典场方程

由等时对易关系和自由 Hamiltonian 可得：

$$
\partial_t\hat\phi
=i[\hat H,\hat\phi]
=\hat\pi,
$$

$$
\partial_t\hat\pi
=i[\hat H,\hat\pi]
=(\nabla^2-m^2)\hat\phi.
$$

再对第一式求时间导数：

$$
(\partial_t^2-\nabla^2+m^2)\hat\phi=0.
$$

即：

$$
(\Box+m^2)\hat\phi=0.
$$

因此量子场算符满足与经典自由场相同形式的线性运动方程，但它的取值是非对易算符，状态空间也是 Fock 空间。

## 微观因果性

等时对易关系只直接规定同一时间切片上的正则代数。相对论量子场论还要求局域玻色可观测量在类空分离时对易：

$$
[\hat\phi(x),\hat\phi(y)]=0,
\qquad
(x-y)^2<0.
$$

自由标量场的对易子是一个只依赖时空间隔的分布：

$$
[\hat\phi(x),\hat\phi(y)]
=i\Delta(x-y),
$$

其中 Pauli–Jordan 分布 $\Delta$ 在类空间隔处为零。

这不表示真空关联函数在类空分离时必须为零。因果要求的是局域操作不能通过对易子传递可控超光速信号，而不是所有量子关联都消失。

## 复标量场

复标量场 $\phi$ 与 $\phi^\dagger$ 是独立的算符场。其展开需要两套模式算符：

$$
\hat\phi(x)
=\int\frac{d^3k}{(2\pi)^3\sqrt{2\omega_{\mathbf k}}}
\left[
a(\mathbf k)e^{-ik\cdot x}
+b^\dagger(\mathbf k)e^{+ik\cdot x}
\right].
$$

$a^\dagger$ 产生粒子，$b^\dagger$ 产生反粒子。两套算符分别满足玻色对易关系，并彼此对易。

不能把实场公式中的一套 $a,a^\dagger$ 无条件照搬到带守恒 $U(1)$ 电荷的复场，因为复场的粒子和反粒子是独立激发。

## Dirac 场为何使用反对易关系

经典 Dirac 场通常用 Grassmann 奇变量描述。量子化后，等时基本关系取反对易形式：

$$
\{\hat\psi_\alpha(t,\mathbf x),
\hat\psi_\beta^\dagger(t,\mathbf y)\}
=\delta_{\alpha\beta}
\delta^{(3)}(\mathbf x-\mathbf y),
$$

其余同类反对易子为零。

相应的粒子与反粒子模式算符满足：

$$
\{b_s(\mathbf p),b_{s'}^\dagger(\mathbf q)\}
=(2\pi)^3\delta_{ss'}
\delta^{(3)}(\mathbf p-\mathbf q),
$$

$$
\{d_s(\mathbf p),d_{s'}^\dagger(\mathbf q)\}
=(2\pi)^3\delta_{ss'}
\delta^{(3)}(\mathbf p-\mathbf q).
$$

这使每个费米模式的占据数只能是 $0$ 或 $1$，并与相对论局域性、正能量和自旋—统计关系一致。

“把 Poisson 括号直接乘 $i$”只是玻色正则量子化的形式记忆法，不能据此推导费米场的全部符号。费米场需要 Grassmann 经典结构和一致的左、右导数约定。

## 规范场为何更复杂

对 Maxwell 场：

$$
\Pi^0=0.
$$

速度—动量关系因此不可逆，说明理论有约束。若直接对四个 $A_\mu$ 分量施加普通标量场对易关系，会重复量子化规范方向并引入非物理态。

可采用不同路线：

- 在 Coulomb 规范中先约化到两个横向自由度，再量子化；
- 在 Lorenz 协变规范中保留非物理分量，再用 Gupta–Bleuler 或 BRST 条件定义物理态；
- 在路径积分中加入规范固定和 Faddeev–Popov 因子。

所以正则量子化不是“对任何场都机械写一个 delta 对易子”。必须先检查 Legendre 变换是否退化、是否存在约束和规范冗余。

完整的自由电磁场例子见 [[Maxwell 场量子化]]。

## 相互作用场的边界

自由场能精确分解成独立模式谐振子。加入相互作用后，例如：

$$
\mathcal L
=\frac12\partial_\mu\phi\partial^\mu\phi
-\frac12m^2\phi^2
-\frac{\lambda}{4!}\phi^4,
$$

等时正则对易关系仍可作为量子理论的起点，但完整 Hamiltonian 不再是独立谐振子之和。

相互作用真空不是自由 Fock 真空，精确粒子态也不等于简单自由数态。微扰论通常先用自由场建立相互作用图景，再通过 [[时序乘积]]、[[Wick 定理]] 和 [[Feynman 图]] 组织修正。

在严格数学层面，连续相互作用量子场的构造还涉及重整化、算符定义域和表示选择；不能仅凭形式正则关系就证明理论完整存在。

## 正则量子化与路径积分

正则量子化以某个时间切片上的场和动量代数为起点，Hilbert 空间和算符结构较直观。

路径积分则对场配置加权求和：

$$
Z=\int\mathcal D\phi\,e^{iS[\phi]}.
$$

两种方法在适当条件和一致测度下给出相同的物理关联函数与散射振幅，但各有优势：

- 正则量子化清楚展示状态、算符、Hamiltonian 和约束；
- 路径积分更方便保持协变形式、推导 Feynman 规则和处理规范固定；
- 异常、拓扑扇区或非平凡测度可能在路径积分语言中更直接；
- 约束系统必须在两种语言中分别正确处理，不能把形式等价当作自动证明。

## 与弦理论的关系

平直背景、共形规范下，弦坐标 $X^\mu(\tau,\sigma)$ 是二维自由标量场。其共轭动量密度在一种常见约定下为：

$$
P_\mu(\tau,\sigma)
=T\,\partial_\tau X_\mu,
$$

其中 $T$ 是弦张力。

正则量子化施加：

$$
[X^\mu(\tau,\sigma),P_\nu(\tau,\sigma')]
=i\delta^\mu{}_\nu\delta(\sigma-\sigma').
$$

把 $X^\mu$ 作开弦或闭弦模展开后，这条场对易关系转化为零模和振子的对易关系，例如：

$$
[x^\mu,p^\nu]=i\eta^{\mu\nu},
$$

$$
[\alpha_m^\mu,\alpha_n^\nu]
=m\,\delta_{m+n,0}\eta^{\mu\nu}.
$$

闭弦还有独立的右移动振子 $\widetilde\alpha_n^\mu$。

不过，振子 Fock 空间只是候选状态空间。世界面 Diff×Weyl 冗余留下 Virasoro 约束；协变量子化还会出现不定范数候选态。只有满足物理态条件，或属于适当 [[BRST 上同调]] 的等价类，才代表物理弦态。

因此弦量子化沿用了“场 $\to$ 模式 $\to$ 振子代数 $\to$ Fock 空间”的正则路线，但又比无约束标量场多出世界面规范约束、反常消除和临界维数条件。

## 适用条件与事实边界

1. 本篇主要以四维平直时空中的自由实标量场说明正则量子化。
2. 等时正则关系依赖选定的时间切片；最终相对论物理必须满足 Lorentz 协变性和微观因果性。
3. 场是算符值分布，逐点乘积通常需要正规化或重整化。
4. 自由场可精确分解为独立谐振子；相互作用场一般不能。
5. 实标量场只有一套粒子模式；复标量场通常有独立的粒子和反粒子模式。
6. 玻色场使用对易关系，费米场使用反对易关系；后者不能由朴素 Poisson 括号替换法完整推出。
7. 规范理论的 Legendre 变换退化，必须先处理约束或在扩大的状态空间中再施加物理态条件。
8. 正规排序依赖所选真空和模式分解，不是所有背景中的唯一真空能处方。
9. 无穷自由度系统可能存在幺正不等价的表示，因此真空与粒子概念并非在所有背景中绝对唯一。
10. 正则量子化是一种构造原则；形式写下对易关系不自动保证相互作用理论在紫外区数学完备。
11. 曲时空、有限温度、时变背景和非平凡边界会改变自然模式与真空选择。
12. 弦振子 Fock 空间含非物理候选态，还必须施加 Virasoro、BRST 和超弦中的 GSO 等条件。

## 常见误区

1. **正则量子化只是给经典场加一个帽子。** 还必须指定算符代数、状态空间、Hamiltonian、定义域和物理态条件。
2. **场在每个时空点都是普通有界算符。** 场通常是算符值分布，需要涂抹或重整化后才得到良好对象。
3. **所有场都满足同一种对易关系。** 玻色场用对易子，费米场用反对易子，规范场还带约束。
4. **每个动量 $\mathbf k$ 与 $-\mathbf k$ 都是实场的完全独立经典自由度。** 实条件会联系相反动量的 Fourier 系数。
5. **负频部分表示真实负能粒子。** 量子场展开把它组织为正能量激发的产生算符。
6. **真空是没有任何物理结构的零向量。** 真空是归一化态，并有涨落、关联和形式零点能。
7. **正规排序解决了所有真空能问题。** 它只是在给定表示中重新定义算符次序，不能取代一般背景中的重整化。
8. **有了等时对易关系就自动保证相对论因果性。** 还需检查类空分离的局域可观测量对易。
9. **相互作用场仍是互不耦合的谐振子。** 只有自由二次理论具有这种精确分解。
10. **规范场可以像四个独立标量场一样量子化。** 约束和规范冗余必须先被处理。
11. **正则量子化和路径积分彼此矛盾。** 在共同适用范围内，它们是同一量子理论的两种组织方式。
12. **弦的所有振子 Fock 态都是物理态。** 世界面约束会排除或识别大量候选态。

## 练习

1. 写出一般玻色场共轭动量的定义。
2. 写出自由实标量场的共轭动量和 Hamiltonian 密度。
3. 场论中的 Kronecker delta 为什么变成 Dirac delta？
4. 写出自由实标量场的三条等时正则对易关系。
5. 写出本文归一化下的实标量场模展开。
6. 由场模展开写出共轭动量场的展开。
7. 模式算符必须满足什么对易关系？
8. 为什么自由场可看作无穷多个量子谐振子？
9. 真空和单粒子态怎样定义？
10. 写出正规排序后的自由标量场 Hamiltonian。
11. Heisenberg 方程怎样恢复 Klein-Gordon 方程？
12. 微观因果性对自由实标量场提出什么要求？
13. 为什么复标量场需要两套模式算符？
14. Dirac 场的基本等时关系为何是反对易关系？
15. 为什么 Maxwell 场不能机械套用无约束标量场量子化？
16. 相互作用加入后，哪些自由场结论不再精确成立？
17. 正则量子化与路径积分各自强调什么结构？
18. 为什么说场是算符值分布？
19. 弦坐标的场对易关系怎样变成振子对易关系？
20. 为什么弦振子 Fock 空间还不是最终物理 Hilbert 空间？

## 答案与提示

1. $\pi_A=\partial\mathcal L/\partial(\partial_0\phi^A)$。
2. $\pi=\partial_t\phi$；$\mathcal H=\frac12\pi^2+\frac12|\nabla\phi|^2+\frac12m^2\phi^2$。
3. 空间位置是连续标签；离散单位矩阵 $\delta^i{}_j$ 的连续核是 $\delta^{(3)}(\mathbf x-\mathbf y)$。
4. $[\phi(\mathbf x),\pi(\mathbf y)]=i\delta^{(3)}(\mathbf x-\mathbf y)$，且 $[\phi,\phi]=[\pi,\pi]=0$，所有场取同一时刻。
5. $\phi(x)=\int d^3k[(2\pi)^3\sqrt{2\omega_{\mathbf k}}]^{-1}[ae^{-ikx}+a^\dagger e^{ikx}]$。
6. 对场展开求 $\partial_t$：$\pi=-i\int d^3k(2\pi)^{-3}\sqrt{\omega/2}[ae^{-ikx}-a^\dagger e^{ikx}]$。
7. $[a(\mathbf k),a^\dagger(\mathbf q)]=(2\pi)^3\delta^{(3)}(\mathbf k-\mathbf q)$，其余同类对易子为零。
8. 二次自由作用量经 Fourier 变换后，不同动量正规模彼此解耦，每个模的 Hamiltonian 都是谐振子形式。
9. $a(\mathbf k)|0\rangle=0$；$|\mathbf k\rangle=a^\dagger(\mathbf k)|0\rangle$。
10. $:H:=\int\frac{d^3k}{(2\pi)^3}\omega_{\mathbf k}a^\dagger(\mathbf k)a(\mathbf k)$。
11. 先得 $\partial_t\phi=\pi$ 和 $\partial_t\pi=(\nabla^2-m^2)\phi$，合并为 $(\Box+m^2)\phi=0$。
12. 类空分离 $(x-y)^2<0$ 时，$[\phi(x),\phi(y)]=0$。
13. 复场的正负频系数彼此独立，分别对应粒子湮灭与反粒子产生。
14. 费米统计、正能量和相对论局域性要求费米场采用反对易代数；经典起点也使用 Grassmann 奇变量。
15. $\Pi^0=0$ 表明 Legendre 变换退化并存在一级约束；四个势分量含规范冗余。
16. Hamiltonian 不再是独立振子之和，相互作用真空和精确态也不再等于简单自由 Fock 态。
17. 正则方法突出状态、算符和 Hamiltonian；路径积分突出协变场配置、关联函数和微扰规则。
18. 逐点场及其乘积常有紫外奇异性；对测试函数积分后的涂抹场才是更稳妥的算符对象。
19. 把 $X^\mu$ 和 $P_\nu$ 作 Fourier 模展开并比较各模系数，可得 $[x^\mu,p^\nu]=i\eta^{\mu\nu}$ 与 $[\alpha_m^\mu,\alpha_n^\nu]=m\delta_{m+n,0}\eta^{\mu\nu}$。
20. 还要施加 Virasoro 或 BRST 物理态条件，并在超弦中处理 GSO 投影；不满足约束的候选态不是物理态。

## 与其他概念的关系

- [[经典场论]] 提供场、Lagrangian 密度和场方程；
- [[Hamiltonian]] 与 [[Poisson 括号]] 给出正则变量、时间演化和经典括号；
- [[量子算符]] 与 [[对易关系]] 把经典正则代数提升为算符代数；
- [[量子谐振子]]、[[产生算符]] 和 [[湮灭算符]] 描述每个自由场模式；
- [[Fock 空间]] 容纳真空和任意粒子数的场激发；
- [[Klein-Gordon 场]] 是无约束玻色场的基本例子；
- [[Dirac 场]] 展示费米反对易量子化；
- [[Maxwell 场量子化]] 展示规范约束如何改变正则程序；
- [[传播子]] 是量子场两点关联和 Green 函数的核心对象；
- [[时序乘积]] 与 [[Wick 定理]] 把自由场算符积化为可计算的收缩；
- [[二维自由玻色子]] 把二维无质量标量的量子化具体化为对数二点函数、导数字段和中心荷计算；
- [[弦的模展开]] 和 [[振子代数]] 把世界面场的正则关系变成弦振子代数；
- [[弦的量子态]] 从振子 Fock 空间中筛选满足世界面约束的物理态。

## 参考资料

- Michael E. Peskin and Daniel V. Schroeder, *An Introduction to Quantum Field Theory*, Westview Press, 1995, Chapters 2–3.
- Mark Srednicki, *Quantum Field Theory*, Cambridge University Press, 2007, Chapters 3–5.
- Steven Weinberg, *The Quantum Theory of Fields, Vol. 1*, Cambridge University Press, 1995, Chapters 2–5.
- Michael D. Schwartz, *Quantum Field Theory and the Standard Model*, Cambridge University Press, 2014, Chapters 2–8.
- N. N. Bogoliubov and D. V. Shirkov, *Introduction to the Theory of Quantized Fields*, Wiley, 1980.
- Walter Greiner and Joachim Reinhardt, *Field Quantization*, Springer, 1996.
- Marc Henneaux and Claudio Teitelboim, *Quantization of Gauge Systems*, Princeton University Press, 1992.
- Rudolf Haag, *Local Quantum Physics*, 2nd ed., Springer, 1996.
- David Tong, *Lectures on Quantum Field Theory*, [course notes](https://www.damtp.cam.ac.uk/user/tong/qft.html)
- Joseph Polchinski, *String Theory, Vol. 1*, Cambridge University Press, 1998, Chapters 2–4.
- David Tong, *String Theory*, arXiv:0908.0333, [arXiv](https://arxiv.org/abs/0908.0333)
