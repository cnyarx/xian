---
aliases:
  - Tangent-Cotangent Duality
  - Duality Between Tangent and Cotangent Spaces
english_name: Duality Between Tangent and Cotangent Spaces
tags:
  - 数学
  - 微分几何
  - 关系页
  - 弦理论先修
  - 弦理论
prerequisites:
  - "[[切空间]]"
  - "[[余切空间]]"
  - "[[向量空间]]"
  - "[[线性映射]]"
  - "[[矩阵]]"
used_by:
  - "[[微分形式]]"
  - "[[度量]]"
  - "[[张量]]"
  - "[[联络]]"
  - "[[协变导数]]"
  - "[[世界面诱导度量]]"
evidence_status: 教材事实
---

# 切空间与余切空间的对偶（Duality Between Tangent and Cotangent Spaces）

## 一句话解释

切向量表示可沿哪个方向运动，余切向量表示怎样线性测量这些方向；二者自然配对得到一个数，但若没有度量，就不能自然地把任意切向量直接当成余切向量。

## 为什么要学它

微分几何中的上标和下标并非排版习惯，而是在区分两种不同对象：切向量与余切向量。理解它们的对偶关系，才能正确学习微分形式、张量、度量、指标升降、梯度、动量、推前与拉回。

弦理论里，世界面坐标导数 $\partial_aX^\mu$、世界面度量 $h_{ab}$、逆度量 $h^{ab}$ 和作用量中的指标缩并都使用这套结构。若把切向量和余切向量混为一谈，公式可能看起来相似，却会在坐标变换下出错。

## 直观图景

把切向量想成一支箭头，把余切向量想成一张带刻度的测量卡。

箭头本身说明方向与速度。测量卡接收一支箭头，返回一个数。例如，一张卡可以只读取箭头的“横向分量”。另一张卡可以读取“横向分量的两倍减去纵向分量”。

箭头和测量卡不是同一种东西，但可以通过“把卡作用在箭头上”发生自然联系。

## 正式定义

设 $M$ 是光滑流形，$p\in M$。点 $p$ 处的切空间是 $T_pM$，余切空间定义为它的对偶空间：

$$
T_p^*M=(T_pM)^*.
$$

因此，每个余切向量都是线性映射：

$$
\omega:T_pM\to\mathbb R.
$$

切空间与余切空间之间存在自然配对：

$$
\langle\cdot,\cdot\rangle:
T_p^*M\times T_pM\to\mathbb R,
$$

$$
\langle\omega,v\rangle=\omega(v).
$$

这个配对不需要坐标、内积或度量。

## 每个符号是什么意思

- $M$：光滑流形；
- $p$：流形上的一点；
- $T_pM$：点 $p$ 处的切空间；
- $T_p^*M$：点 $p$ 处的余切空间；
- $v\in T_pM$：切向量；
- $\omega\in T_p^*M$：余切向量；
- $\omega(v)$：余切向量对切向量的测量结果；
- $\langle\omega,v\rangle$：同一个测量结果的配对记号；
- $\mathbb R$：这里采用的实数标量域。

## 什么叫“对偶”

对任意有限维实向量空间 $V$，它的对偶空间定义为：

$$
V^*=\operatorname{Hom}(V,\mathbb R).
$$

其中元素不是 $V$ 中的普通向量，而是从 $V$ 到实数的线性函数。若 $\omega\in V^*$，则：

$$
\omega(av+bw)=a\omega(v)+b\omega(w).
$$

余切空间只是把这个一般定义逐点应用到流形的切空间。

## 自然配对为什么是非退化的

有限维情形下，自然配对是非退化的。这包含两个方向：

1. 若 $v\ne0$，总能找到某个 $\omega$，使 $\omega(v)\ne0$；
2. 若 $\omega\ne0$，总能找到某个 $v$，使 $\omega(v)\ne0$。

这说明余切向量足以区分不同切向量，切向量也足以检验不同余切向量。

“非退化”不等于“两个空间已经被自然识别为同一个空间”。它只说明配对不会把某个非零对象对所有对象都测成零。

## 对偶基

在局部坐标 $x^1,\ldots,x^n$ 中，切空间的坐标基为：

$$
\left\{
\frac{\partial}{\partial x^1}\Big|_p,
\ldots,
\frac{\partial}{\partial x^n}\Big|_p
\right\}.
$$

余切空间的对偶基为：

$$
\{dx^1|_p,\ldots,dx^n|_p\}.
$$

定义关系是：

$$
dx^i|_p\!\left(
\frac{\partial}{\partial x^j}\Big|_p
\right)=\delta^i_j.
$$

其中：

$$
\delta^i_j=
\begin{cases}
1,&i=j,\\
0,&i\ne j.
\end{cases}
$$

所以 $dx^1$ 读取第一个坐标方向的分量，$dx^2$ 读取第二个坐标方向的分量，以此类推。

## 分量怎样配对

写出切向量与余切向量：

$$
v=v^i\frac{\partial}{\partial x^i}\Big|_p,
\qquad
\omega=\omega_i dx^i|_p.
$$

使用 Einstein 求和约定后：

$$
\omega(v)
=\omega_i dx^i\!\left(
 v^j\frac{\partial}{\partial x^j}
\right)
=\omega_i v^j\delta^i_j
=\omega_i v^i.
$$

一个下标与一个上标配对并求和，产生不带指标的标量。

## 一个二维例子

在 $\mathbb R^2$ 中取：

$$
v=3\frac{\partial}{\partial x}
-2\frac{\partial}{\partial y},
$$

以及：

$$
\omega=4dx+5dy.
$$

那么：

$$
\omega(v)
=4\,dx(v)+5\,dy(v)
=4\times3+5\times(-2)
=2.
$$

这里 $dx(v)=3$，$dy(v)=-2$。余切向量没有把箭头变成另一支箭头，而是给出了一个数。

## 坐标变换下的切向量基

设同一区域有两套坐标 $x^i$ 与 $y^a$。链式法则给出：

$$
\frac{\partial}{\partial y^a}
=\frac{\partial x^i}{\partial y^a}
\frac{\partial}{\partial x^i}.
$$

相应地，切向量分量满足：

$$
v^a_{(y)}
=\frac{\partial y^a}{\partial x^i}v^i_{(x)}.
$$

这里的矩阵：

$$
J^a{}_i=\frac{\partial y^a}{\partial x^i}
$$

称为坐标变换的 Jacobi 矩阵。

## 坐标变换下的余切向量基

坐标函数的微分满足：

$$
dy^a
=\frac{\partial y^a}{\partial x^i}dx^i.
$$

若同一个余切向量写成：

$$
\omega=\omega^{(x)}_i dx^i
=\omega^{(y)}_a dy^a,
$$

则分量满足：

$$
\omega^{(y)}_a
=\frac{\partial x^i}{\partial y^a}
\omega^{(x)}_i.
$$

切向量分量使用 Jacobi 矩阵，余切向量分量使用逆 Jacobi 矩阵。正因为变化方向相反，配对值保持不变。

## 配对为什么与坐标无关

在 $x$ 坐标中：

$$
\omega(v)=\omega^{(x)}_i v^i_{(x)}.
$$

在 $y$ 坐标中：

$$
\omega^{(y)}_a v^a_{(y)}
=\frac{\partial x^i}{\partial y^a}
\omega^{(x)}_i
\frac{\partial y^a}{\partial x^j}
 v^j_{(x)}.
$$

逆矩阵关系给出：

$$
\frac{\partial x^i}{\partial y^a}
\frac{\partial y^a}{\partial x^j}
=\delta^i_j.
$$

所以：

$$
\omega^{(y)}_a v^a_{(y)}
=\omega^{(x)}_i v^i_{(x)}.
$$

坐标分量各自改变，但最终测量结果不变。这就是几何对象与坐标表示的区别。

## 上标和下标真正表示什么

在常见张量记号中：

- 切向量分量写成上标 $v^i$，称为反变分量；
- 余切向量分量写成下标 $\omega_i$，称为协变分量。

名称来自它们在坐标变换下使用互逆矩阵。上标和下标不是装饰，也不能在没有额外结构时随意移动。

同一个字母写成 $v^i$ 与 $v_i$，通常表示两个属于不同空间、通过某种额外结构联系的分量，而不是把符号上下挪动一下。

## 为什么同维数不等于自然相同

有限维时：

$$
\dim T_pM=\dim T_p^*M.
$$

因此可以选择某个线性同构：

$$
T_pM\to T_p^*M.
$$

但仅凭向量空间本身，没有一个无需选择、对所有坐标和线性变换都一致的首选同构。选择一组基后可以把基向量与对偶基配对，但换基后，这种人为识别一般不会保持不变。

所以正确说法是：切空间与余切空间维数相同，并且彼此对偶；不是说它们天然就是同一个空间。

## 双对偶为何不同

切向量 $v\in T_pM$ 可以自然定义为余切空间上的线性函数：

$$
\iota(v):T_p^*M\to\mathbb R,
\qquad
\iota(v)(\omega)=\omega(v).
$$

因此有自然映射：

$$
\iota:T_pM\to T_p^{**}M.
$$

在有限维情形，$\iota$ 是同构：

$$
T_pM\cong T_p^{**}M.
$$

这里的自然同构不需要基或度量。要注意，它把切空间连接到**双对偶**，而不是直接连接到余切空间。

## 度量怎样建立切—余切同构

若流形上给定非退化度量 $g$，则可把切向量 $v$ 变成余切向量：

$$
v^\flat=g(v,\cdot).
$$

也就是：

$$
v^\flat(w)=g(v,w).
$$

这个映射称为降指标或 flat 映射：

$$
\flat:T_pM\to T_p^*M.
$$

由于 $g$ 非退化，它有逆映射：

$$
\sharp:T_p^*M\to T_pM.
$$

称为升指标或 sharp 映射。

这些名称来自音乐记号 $\flat$ 与 $\sharp$，中文常读作“降号”和“升号”。

## 指标升降公式

若度量分量为：

$$
g_{ij}=g\!\left(
\frac{\partial}{\partial x^i},
\frac{\partial}{\partial x^j}
\right),
$$

则切向量 $v=v^i\partial_i$ 对应的余切向量分量为：

$$
v_i=g_{ij}v^j.
$$

逆度量 $g^{ij}$ 满足：

$$
g^{ik}g_{kj}=\delta^i_j.
$$

因此：

$$
v^i=g^{ij}v_j.
$$

指标能够升降，是因为已经选择了非退化度量。若没有 $g$，这些公式没有定义。

## Euclidean 空间为何容易让人混淆

在标准正交直角坐标中：

$$
g_{ij}=\delta_{ij},
\qquad
g^{ij}=\delta^{ij}.
$$

于是数值上：

$$
v_i=v^i.
$$

这会让人误以为切向量和余切向量本来就是同一个对象。实际上，是标准 Euclidean 度量在背后完成了识别；换成斜坐标、曲线坐标或 Lorentzian 度量后，上下指标的数值通常不同。

## 函数微分与方向导数

对光滑函数 $f:M\to\mathbb R$，微分：

$$
df|_p\in T_p^*M
$$

通过自然配对作用在切向量 $v$ 上：

$$
df|_p(v)=v[f].
$$

坐标表达为：

$$
df=\frac{\partial f}{\partial x^i}dx^i.
$$

所以偏导数 $\partial_i f$ 自然构成余切向量分量。

## 梯度为什么需要度量

函数微分 $df$ 不需要度量。梯度向量 $\operatorname{grad}f$ 则要由度量定义：

$$
g(\operatorname{grad}f,v)=df(v)
$$

对所有 $v$ 成立。

用 musical map 写成：

$$
\operatorname{grad}f=(df)^\sharp.
$$

坐标中：

$$
(\operatorname{grad}f)^i
=g^{ij}\frac{\partial f}{\partial x^j}.
$$

因此 $df$ 与梯度不是同一个对象；度量把前者转换成后者。

## 线性映射的推前

设 $F:M\to N$ 是光滑映射。点 $p$ 处的切映射或推前为：

$$
F_{*p}:T_pM\to T_{F(p)}N.
$$

它把定义域中的切向量送到值域中的切向量。对曲线速度而言，先沿曲线运动再应用 $F$，等价于先把速度经 $F_*$ 推过去。

## 余切向量的拉回

与推前对偶的映射是拉回：

$$
F_p^*:T_{F(p)}^*N\to T_p^*M.
$$

它满足：

$$
(F_p^*\omega)(v)
=\omega(F_{*p}v).
$$

右边先把 $v$ 推到 $N$，再用 $\omega$ 测量；左边则先把测量规则拉回 $M$，再测量原来的 $v$。两种过程得到同一个数。

## 为什么推前和拉回方向相反

若 $F$ 从 $M$ 指向 $N$，切向量可顺着 $F$ 前进：

$$
T_pM\longrightarrow T_{F(p)}N.
$$

余切向量是测量规则。为了测量 $M$ 中的向量，需要把 $N$ 中的测量规则反向搬回：

$$
T_{F(p)}^*N\longrightarrow T_p^*M.
$$

这不是人为规定，而是对偶映射的普遍方向：若 $A:V\to W$，则其对偶映射为：

$$
A^*:W^*\to V^*.
$$

## 矩阵语言中的转置

选择基后，设线性映射 $A:V\to W$ 的矩阵也是 $A$。对偶映射 $A^*:W^*\to V^*$ 在对偶基下由转置矩阵表示：

$$
[A^*]=[A]^T.
$$

若把余切向量写成列分量，则具体公式可能出现 $A^T$；若写成行向量，则乘法位置会改变。几何内容不依赖这种排版约定。

坐标变换中的逆转置现象，来自既要改变基，又要保持配对不变。

## 张量缩并

切向量属于 $T_pM$，余切向量属于 $T_p^*M$。它们的张量积可以形成更一般的张量。

例如一个 $(1,1)$ 型张量可写为：

$$
T=T^i{}_j
\frac{\partial}{\partial x^i}\otimes dx^j.
$$

把一个上指标和一个下指标配对求和称为缩并。例如：

$$
T^i{}_i
$$

是一个标量。缩并的坐标不变性来自切空间与余切空间的自然配对。

## 动量与速度为什么属于不同空间

在位形空间 $Q$ 中，速度属于切空间：

$$
\dot q\in T_qQ.
$$

正则动量属于余切空间：

$$
p\in T_q^*Q.
$$

给定正则 Lagrangian $L(q,\dot q)$ 时，Legendre 映射局部写成：

$$
p_i=\frac{\partial L}{\partial\dot q^i}.
$$

它把速度信息送入动量空间。只有在特定 Lagrangian 或度量给出额外关系时，才能把动量简单看成“质量乘速度”的降指标版本。

因此一般情况下，速度和动量不能仅因分量数相同就视为同一个对象。

## 世界面上的切与余切指标

设弦世界面为 $\Sigma$，局部坐标为 $\sigma^a$，其中常取：

$$
\sigma^0=\tau,
\qquad
\sigma^1=\sigma.
$$

世界面切空间基为：

$$
\partial_a=\frac{\partial}{\partial\sigma^a},
$$

余切空间基为：

$$
d\sigma^a.
$$

它们满足：

$$
d\sigma^a(\partial_b)=\delta^a_b.
$$

这里的指标 $a,b$ 是世界面指标，不是目标时空指标 $\mu,\nu$。

## 世界面映射的微分

弦嵌入是映射：

$$
X:\Sigma\to M,
$$

其中 $M$ 是目标时空。目标坐标函数 $X^\mu$ 的微分为：

$$
dX^\mu
=\partial_aX^\mu\,d\sigma^a.
$$

这里 $dX^\mu$ 是世界面上的一形式，也就是世界面余切向量场；$\partial_aX^\mu$ 是它在基 $d\sigma^a$ 下的分量。

另一方面，世界面切向量 $\partial_a$ 经 $X_*$ 推到目标时空：

$$
X_*(\partial_a)
=\partial_aX^\mu\frac{\partial}{\partial X^\mu}.
$$

同一组分量 $\partial_aX^\mu$ 同时出现在这两个相容表达式中，但完整几何对象和所在空间不同。

## 世界面度量如何升降指标

给定世界面度量 $h_{ab}$ 后，可以把世界面切向量与余切向量互相转换。逆度量满足：

$$
h^{ac}h_{cb}=\delta^a_b.
$$

例如：

$$
V_a=h_{ab}V^b,
\qquad
V^a=h^{ab}V_b.
$$

对嵌入坐标导数，也可定义：

$$
\partial^aX^\mu=h^{ab}\partial_bX^\mu.
$$

这一步依赖 $h_{ab}$，不是单靠坐标记号完成的。

## 诱导度量中的两类指标

若目标时空有度量 $G_{\mu\nu}$，世界面诱导度量为：

$$
g_{ab}
=G_{\mu\nu}(X)
\partial_aX^\mu\partial_bX^\nu.
$$

其中：

- $a,b$：世界面余切指标；
- $\mu,\nu$：目标时空向量坐标指标；
- $\partial_aX^\mu$：把世界面切方向送到目标时空切方向的映射分量；
- $G_{\mu\nu}$：目标时空度量；
- $g_{ab}$：拉回到世界面的二阶协变张量。

更紧凑地写：

$$
g=X^*G.
$$

这说明诱导度量是目标时空度量的拉回。

## Polyakov 作用量中的缩并

玻色弦的 Polyakov 作用量常含：

$$
S_P
=-\frac{T}{2}\int d^2\sigma\,
\sqrt{-h}\,
h^{ab}G_{\mu\nu}(X)
\partial_aX^\mu\partial_bX^\nu.
$$

世界面上指标 $a,b$ 被 $h^{ab}$ 缩并，目标时空指标 $\mu,\nu$ 被 $G_{\mu\nu}$ 缩并。最终被积函数在两类坐标变换下都成为标量密度。

这里只用该公式展示切—余切配对和指标缩并，不在本篇推导完整弦作用量。

## 适用条件与事实边界

1. $T_p^*M$ 按定义是 $T_pM$ 的对偶空间，二者存在无需额外结构的自然配对。
2. 自然配对非退化，但这不等于 $T_pM$ 与 $T_p^*M$ 已自然成为同一个空间。
3. 有限维切空间与余切空间维数相同，因此存在同构，但一般没有无需选择的首选同构。
4. 有限维向量空间与其双对偶存在自然同构 $V\cong V^{**}$；这不同于 $V\cong V^*$。
5. 非退化度量给出 $\flat$ 与 $\sharp$ 映射，允许切向量和余切向量之间转换。
6. 上标与下标反映不同的坐标变换规律，不能在没有度量等结构时随意移动。
7. 函数微分 $df$ 自然是余切向量；梯度向量需要度量才能由 $df$ 定义。
8. 推前作用于切向量并顺着映射方向；拉回作用于余切向量并逆着映射方向。
9. 速度自然属于切空间，正则动量自然属于余切空间；二者的关系依赖 Lagrangian 的 Legendre 映射。
10. 世界面指标与目标时空指标属于不同空间，必须分别缩并和升降。
11. 诱导度量 $X^*G$ 是目标时空度量沿嵌入映射的拉回。
12. 文中的矩阵转置表达依赖采用行分量还是列分量，但抽象对偶映射不依赖记号选择。

## 常见误区

1. **切向量和余切向量维数相同，所以它们就是同一个空间。** 同维只保证存在同构，不给出自然的首选同构。
2. **上标与下标只是排版风格。** 它们通常对应互为对偶的变换规律。
3. **可以不借助任何结构把 $v^i$ 写成 $v_i$。** 指标升降需要非退化度量或其他指定的非退化双线性型。
4. **$df$ 就是梯度向量。** $df$ 是余切向量；梯度是选定度量后得到的 $(df)^\sharp$。
5. **推前和拉回都沿同一方向。** 推前顺着映射，余切拉回逆着映射。
6. **自然配对就是内积。** 配对接收一个余切向量和一个切向量；内积通常接收两个同类向量，并属于额外结构。
7. **在直角坐标中 $v_i=v^i$，所以任何坐标都如此。** 这是标准 Euclidean 度量和正交基造成的特殊简化。
8. **速度与动量只是同一箭头的两种名字。** 它们分别属于切空间与余切空间，关系由动力学结构决定。
9. **$\partial_aX^\mu$ 本身已经完整说明它属于哪个空间。** 它是映射微分的分量，必须结合基和指标语境解释。
10. **世界面指标和目标时空指标可以互相缩并。** 它们属于不同空间，需分别使用相应度量或映射结构。

## 练习

1. 写出 $T_pM$ 与 $T_p^*M$ 的自然配对。
2. 对偶基 $dx^i$ 与 $\partial/\partial x^j$ 的配对结果是什么？
3. 设 $v=2\partial_x+3\partial_y$，$\omega=5dx-dy$，求 $\omega(v)$。
4. 为什么 $T_pM$ 与 $T_p^*M$ 同维仍不能说它们自然相同？
5. 写出切空间与双对偶之间的自然映射。
6. 给定度量 $g_{ij}$，怎样把 $v^j$ 降成 $v_i$？
7. 为什么 $df$ 不需要度量，而 $\operatorname{grad}f$ 需要度量？
8. 若 $F:M\to N$，写出 $F_{*p}$ 与 $F_p^*$ 的定义域和值域。
9. 解释等式 $(F^*\omega)(v)=\omega(F_*v)$ 的两条计算路径。
10. 在弦世界面上，$dX^\mu$ 怎样用 $d\sigma^a$ 展开？
11. 诱导度量 $g_{ab}$ 怎样由目标时空度量 $G_{\mu\nu}$ 得到？
12. 为什么正则动量自然带下标？

## 答案与提示

1. $\langle\omega,v\rangle=\omega(v)$，映射为 $T_p^*M\times T_pM\to\mathbb R$。
2. $dx^i(\partial/\partial x^j)=\delta^i_j$。
3. $\omega(v)=5\times2-1\times3=7$。
4. 存在许多同构，但仅凭向量空间结构没有一个不依赖基或额外选择的首选同构。
5. $\iota(v)(\omega)=\omega(v)$，即 $\iota:T_pM\to T_p^{**}M$。
6. $v_i=g_{ij}v^j$。
7. $df(v)=v[f]$ 只需光滑结构；梯度需满足 $g(\operatorname{grad}f,v)=df(v)$，因此要先给定 $g$。
8. $F_{*p}:T_pM\to T_{F(p)}N$；$F_p^*:T_{F(p)}^*N\to T_p^*M$。
9. 一条先推前 $v$ 再由 $\omega$ 测量；另一条先拉回 $\omega$ 再测量 $v$，结果相同。
10. $dX^\mu=\partial_aX^\mu d\sigma^a$。
11. $g_{ab}=G_{\mu\nu}(X)\partial_aX^\mu\partial_bX^\nu$，即 $g=X^*G$。
12. 动量是位形空间切向量上的线性测量对象，属于余切空间，其分量按协变规律变换。

## 与其他概念的关系

- [[切空间]] 提供被余切向量测量的方向与速度；
- [[余切空间]] 是切空间的对偶空间；
- [[向量空间]] 给出对偶空间、双对偶和维数的线性代数基础；
- [[线性映射]] 解释对偶映射为何反向；
- [[矩阵]] 提供 Jacobi 矩阵、逆矩阵和转置的坐标表达；
- [[微分形式]] 由余切向量的反对称张量积构成；
- [[度量]] 给出 $\flat$、$\sharp$ 与指标升降；
- [[张量]] 把切空间和余切空间按张量积组合，并通过自然配对缩并；
- [[联络]] 和 [[协变导数]] 描述不同点处张量的比较与微分；
- [[辛结构]] 建立在余切丛及其自然微分形式上；
- [[Lagrangian 力学]] 通过 Legendre 映射把速度联系到正则动量；
- [[世界面诱导度量]] 是目标时空度量的拉回；
- [[Polyakov 作用量]] 使用世界面与目标时空两套指标缩并。

## 参考资料

- John M. Lee, *Introduction to Smooth Manifolds*, 2nd ed., Springer, 2013.
- John M. Lee, *Introduction to Riemannian Manifolds*, 2nd ed., Springer, 2018.
- Loring W. Tu, *An Introduction to Manifolds*, 2nd ed., Springer, 2011.
- Michael Spivak, *Calculus on Manifolds*, Westview Press, 1998.
- Theodore Frankel, *The Geometry of Physics*, 3rd ed., Cambridge University Press, 2012.
- Ralph Abraham and Jerrold E. Marsden, *Foundations of Mechanics*, 2nd ed., AMS Chelsea, 2008.
- Mikio Nakahara, *Geometry, Topology and Physics*, 2nd ed., CRC Press, 2003.
- Joseph Polchinski, *String Theory, Vol. 1*, Cambridge University Press, 1998.
- David Tong, *Lectures on String Theory*, [arXiv:0908.0333](https://arxiv.org/abs/0908.0333)
