---
aliases: [Eigenvalue, Eigenvalues]
english_name: Eigenvalue
tags: [数学, 线性代数, 弦理论先修, 弦理论]
prerequisites:
  - "[[线性映射]]"
  - "[[矩阵]]"
used_by:
  - "[[特征向量]]"
  - "[[正规模]]"
  - "[[量子算符]]"
evidence_status: 教材事实
---

# 特征值（Eigenvalue）

## 一句话解释

特征值是线性变换沿某个特殊方向产生的缩放因子。

## 为什么要学它

耦合振动的自然频率、量子算符的可能测量值、稳定性分析中的增长率，都由特征值给出。弦的每个振动模式也对应某个算符的谱。

## 定义

对线性映射 $T:V\to V$，若存在非零向量 $\mathbf v$ 使：

$$
T\mathbf v=\lambda\mathbf v,
$$

则标量 $\lambda$ 是 $T$ 的特征值，$\mathbf v$ 是对应的 [[特征向量]]。

要求 $\mathbf v\ne\mathbf0$，因为零向量对任意 $\lambda$ 都满足等式，不能提供信息。

## 矩阵求法

对方阵 $A$：

$$
A\mathbf v=\lambda\mathbf v
$$

等价于：

$$
(A-\lambda I)\mathbf v=\mathbf0.
$$

要存在非零解，矩阵必须不可逆，因此：

$$
\det(A-\lambda I)=0.
$$

这叫特征方程；左边是特征多项式。

## 一个例子

若：

$$
A=\begin{pmatrix}2&0\\0&3\end{pmatrix},
$$

则：

$$
\det(A-\lambda I)=(2-\lambda)(3-\lambda)=0.
$$

所以特征值是 $2$ 和 $3$。它们表示沿两个坐标轴方向分别放大两倍和三倍。

## 代数重数

某个根在特征多项式中重复出现的次数叫代数重数。例如：

$$
(\lambda-2)^2(\lambda+1)=0
$$

中，$2$ 的代数重数是 $2$。

重复特征值不自动保证有同样多个线性无关特征向量；这关系到矩阵能否对角化。

## 迹与行列式

对有限维方阵，在计入重数时：

$$
\operatorname{tr}A=\sum_i\lambda_i,
$$

$$
\det A=\prod_i\lambda_i.
$$

迹是对角元素之和。特征值给出矩阵许多不依赖基的内在信息。

## 对角化

若矩阵有足够多线性无关特征向量，可选它们作为基，使矩阵变成对角形式：

$$
A=SDS^{-1}.
$$

$D$ 的对角元素就是特征值。复杂耦合变换因此分解为各独立方向上的简单缩放。

## 特征值可以是复数

实矩阵的特征值不一定全为实数。例如平面旋转矩阵除特殊角度外通常有复特征值。若矩阵或算符是实对称或复 Hermitian 的，则特征值为实数。

## 物理中的谱

一个算符全部允许特征值的集合叫谱。在量子力学中，Hermitian 可观测量的特征值对应可能测量结果；在振动问题中，某个微分算符的特征值决定允许频率。

## 常见误区

1. **任何数都是特征值。** 错，必须存在相应非零特征向量。
2. **特征值是向量。** 错，它是标量。
3. **重复特征值必有同样多独立特征向量。** 错。
4. **实矩阵的特征值一定为实数。** 错；需额外条件。
5. **矩阵换基后特征值改变。** 相似变换保持特征值。

## 练习

1. 求 $\operatorname{diag}(4,-1)$ 的特征值。
2. 为什么求特征值要令 $\det(A-\lambda I)=0$？
3. 若一个矩阵有零特征值，它是否可逆？
4. $2\times2$ 矩阵的两个特征值为 $2,5$，其迹和行列式是多少？
5. 特征值在振动问题中常代表什么？

## 答案与提示

1. $4$ 和 $-1$。
2. 只有矩阵不可逆时，齐次方程才可能有非零解。
3. 不可逆，因为特征值乘积即行列式为零。
4. 迹 $7$，行列式 $10$。
5. 与允许的自然频率平方或相应模式参数有关。

## 与其他概念的关系

- [[特征向量]] 给出对应天然方向；
- 特征值方程使用 [[矩阵]] 和行列式；
- [[正规模]] 由振动算符的特征值问题得到；
- [[量子算符]] 的谱给出可能测量值。

## 参考资料

- [MIT OCW 18.06, Eigenvalues and Eigenvectors](https://ocw.mit.edu/courses/18-06-linear-algebra-spring-2010/)
- Gilbert Strang, *Introduction to Linear Algebra*, 5th ed.
