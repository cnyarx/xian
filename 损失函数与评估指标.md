---
aliases: [损失函数, 目标函数, Loss Function, Objective Function, MSE, Cross Entropy]
tags: [概念, 机器学习, 优化, 大模型, 学习教程]
level: L9
prerequisites: ["[[标量向量矩阵与张量]]", "[[似然最大似然与负对数似然]]", "[[参数梯度与优化器]]"]
---
# 损失函数与评估指标

## 一句话定义

损失函数（Loss Function）把一次预测错得多严重变成训练可优化的数字；评估指标（Evaluation Metric）则回答模型在真实目标上表现怎样。

## 直觉理解

损失像训练时使用的方向盘，必须能给优化器提供连续、稳定的方向；指标像最终成绩单，可以是准确率、召回率或业务成本，不一定适合直接求梯度。

## 严格定义

给定样本 $(x_i,y_i)$、模型 $f_\theta$ 和单样本损失 $\ell$：

$$L(\theta)=\frac1n\sum_{i=1}^n\ell(f_\theta(x_i),y_i)$$

- $\theta$：模型参数；
- $n$：样本数；
- $\ell$：单个样本的误差规则。

回归常用均方误差：

$$\operatorname{MSE}=\frac1n\sum_i(y_i-\hat y_i)^2$$

分类常用交叉熵：

$$L=-\sum_c y_c\log \hat p_c$$

## 例子

真实房价为 100，预测为 110，单样本平方误差是 $(110-100)^2=100$。若分类正确类别概率从 0.5 提高到 0.9，负对数损失从约 0.693 降到约 0.105。

## 边界

- 损失低不自动等于业务指标好；
- 训练损失下降不保证测试集泛化；
- 交叉熵要求概率与标签含义匹配；
- 评估指标的选择包含任务价值判断。

## 最小自测

1. 为什么准确率通常不直接作为梯度训练目标？
2. 真实值 3、预测值 5 的平方误差是多少？

答案：1. 它通常是不连续的，难以提供细致梯度；2. $4$。

## 详细教程

- [[损失函数|损失函数]]
- [[评测方法|评测方法]]

## 来源

- Goodfellow, Bengio & Courville (2016), *Deep Learning*, MIT Press, Chapters 5–6 `[A]`
- Murphy (2022), *Probabilistic Machine Learning: An Introduction*, MIT Press, Chapters 4–5 `[A]`
