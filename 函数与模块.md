---
aliases: [Python 函数, Python 模块, 文件读写]
tags: [Python, 编程, 教程, 大模型, 学习教程]
prerequisites: ["[[变量与基本运算]]"]
next: ["[[PyTorch 入门]]"]
---
# 函数与模块

## 函数是什么

**函数（Function）**是给一组可重复执行的步骤起名字。输入叫参数，输出由 `return` 返回。

```python
def square(x):
    return x * x

result = square(5)
print(result)  # 25
```

- `def`：定义函数。
- `square`：函数名。
- `x`：参数。
- `return`：把结果交回调用者。

## 为什么函数重要

大模型代码由许多函数组成：加载数据、切分 token、计算注意力、计算损失、更新参数。函数让代码可复用、可测试、可组合。

## 多个参数与默认值

```python
def linear(x, weight, bias=0):
    return weight * x + bias

print(linear(3, 2))       # 6
print(linear(3, 2, 1))    # 7
```

`bias=0` 是默认参数；调用时不提供就使用 0。

## 返回多个值

```python
def min_and_max(values):
    return min(values), max(values)

smallest, largest = min_and_max([3, 1, 8])
```

Python 实际返回一个元组（Tuple），也就是不可直接修改的有序值组。

## 作用域

函数内部创建的变量通常只在函数内有效：

```python
def calculate():
    local_value = 10
    return local_value
```

这叫**局部变量（Local Variable）**。外部变量称为全局变量，但应少依赖全局变量，以免函数行为难以判断。

## 模块

**模块（Module）**就是一个 `.py` 文件。假设 `math_utils.py` 中有：

```python
def dot(a, b):
    return sum(x * y for x, y in zip(a, b))
```

另一个文件可导入：

```python
from math_utils import dot
print(dot([1, 2], [3, 4]))  # 11
```

**包（Package）**是组织多个模块的目录。NumPy 和 PyTorch 都是大型包。

## Lambda

`lambda` 创建简单匿名函数：

```python
square = lambda x: x * x
```

它适合非常短的一次性函数；复杂逻辑应使用 `def`，可读性更好。

## 文件读写

```python
from pathlib import Path

path = Path("sample.txt")
path.write_text("大模型学习", encoding="utf-8")
text = path.read_text(encoding="utf-8")
print(text)
```

`utf-8` 是文字编码规则，明确写出可减少不同系统上的乱码。

读取大文件时逐行处理：

```python
with open("data.txt", "r", encoding="utf-8") as file:
    for line in file:
        print(line.strip())
```

`with` 会在代码块结束后自动关闭文件。

## 类型提示

```python
def average(values: list[float]) -> float:
    return sum(values) / len(values)
```

类型提示帮助人和工具理解输入输出，但 Python 默认不会仅因类型提示而阻止传入其他类型。

## 概念与公式导航

- [[变量与函数|变量与函数]]

## 练习

1. 写函数 `relu(x)`，当 `x>0` 返回 `x`，否则返回 0。
2. 写函数计算列表平均值。
3. 把函数放入一个模块，再从另一个文件导入。
4. 保存一行文字到文件，再读回来。
5. 解释参数、返回值、局部变量和模块的区别。

## 来源

- Python Software Foundation, *Defining Functions*：https://docs.python.org/3/tutorial/controlflow.html#defining-functions `[A]`
- Python Software Foundation, *Modules*：https://docs.python.org/3/tutorial/modules.html `[A]`
- Python Software Foundation, *Input and Output*：https://docs.python.org/3/tutorial/inputoutput.html `[A]`
