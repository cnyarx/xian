---
aliases:
  - Renormalization Group
  - RG
  - 重整化群
english_name: Renormalization Group
tags:
  - 物理学
  - 量子场论
  - 临界现象
  - 弦理论先修
  - 弦理论
prerequisites:
  - "[[正规化与重整化]]"
  - "[[微扰论]]"
  - "[[生成泛函]]"
  - "[[对数函数]]"
  - "[[常微分方程]]"
used_by:
  - "[[beta 函数]]"
  - "[[有效场论]]"
  - "[[Weyl 反常]]"
  - "[[弦理论完整学习路线图]]"
evidence_status: 教材事实
---

# 重整化群（Renormalization Group）

## 一句话解释

重整化群（renormalization group，RG）研究：当观察尺度改变时，怎样同步改变耦合、质量、场归一化和有效相互作用，使同一个物理系统的预测保持一致。

## 为什么要学它

[[正规化与重整化]] 在任意重整化尺度 $\mu$ 上定义有限参数，但 $\mu$ 不是新的可观测量。同一个实验不能因为计算者选了不同 $\mu$ 就改变结果，因此参数必须随 $\mu$ 跑动（running）。重整化群把这种尺度补偿写成微分方程，并进一步回答：

- 怎样把微扰展开中的大对数 $\ln(Q/\mu)$ 重求和；
- 理论在高能和低能极限流向哪里；
- 哪些耦合在尺度变化下增强、减弱或保持不变；
- 固定点（fixed point）附近为什么会出现尺度不变性和普适行为；
- Wilson 式粗粒化与微扰重整化为什么描述同一尺度流的不同侧面。

本篇沿用四维单实标量 $\lambda\phi^4/4!$ 理论的一圈结果，并把 beta 函数的完整专题留给路线图后继节点。

## 本篇约定

采用自然单位 $\hbar=c=1$ 和 mostly-minus 度规。相互作用写成

$$
\mathcal L_{\mathrm{int}}
=-\frac{\lambda}{4!}\phi^4.
$$

重整化尺度记为 $\mu$，RG 时间定义为

$$
t\equiv\ln\frac{\mu}{\mu_0}.
$$

因此 $d/dt=\mu\,d/d\mu$。提高 $t$ 表示流向更高重整化尺度；Wilsonian 文献也常用流向红外增大的变量，届时相关与无关方向的指数符号会反转。本篇每次讨论方向时都会说明采用哪一种约定。

## 直观图景

想象用不同倍率看同一张地图：

- 放大时能看到短距离细节，需要更多局部参数描述；
- 缩小时许多细节被平均，只保留影响长距离预测的有效参数；
- 参数随倍率变化形成耦合空间中的一条轨迹，称为 RG 流（RG flow）；
- 若改变倍率后参数回到同一点，就到达固定点；
- 固定点附近会被放大的方向需要精细调节，被压小的方向会逐渐失去影响。

地图类比只说明“同一系统的描述随分辨率改变”。微扰 RG 中改变的是重整化尺度和参数定义；Wilsonian RG 中还显式积分掉高动量模。两者相关，但不能把一次改变 $\mu$ 简化成真的删除了一批自由度。

## 正式定义

### beta 函数

对无量纲耦合 $g_i(\mu)$，beta 函数定义为在裸参数固定时

$$
\boxed{
\beta_i(g)
\equiv
\mu\frac{d g_i}{d\mu}
=
\frac{d g_i}{d\ln\mu}
}.
$$

它是耦合空间中的向量场。给定初值 $g_i(\mu_0)$ 后，解这组 [[常微分方程]] 就得到 RG 轨迹。这里的导数不是改变真实实验，而是在保持裸理论和物理预测不变时改变参数化尺度。

### 质量反常维数与场反常维数

质量参数也会跑动。为避免不同教材对符号和对 $m$、$m^2$ 的选择不同，本篇直接定义

$$
\boxed{
\gamma_{m^2}(g)
\equiv
\frac{\mu}{m_R^2}
\frac{d m_R^2}{d\mu}
}.
$$

于是

$$
\mu\frac{d m_R^2}{d\mu}
=
\gamma_{m^2}(g)m_R^2.
$$

场的反常维数定义为

$$
\boxed{
\gamma_\phi(g)
\equiv
\frac12\mu\frac{d\ln Z_\phi}{d\mu}
\bigg|_{\text{裸参数固定}}
}.
$$

有些教材在 $\gamma_\phi$ 定义前加负号，或把质量函数定义为 $-\mu d\ln m/d\mu$。使用公式前必须先核对定义，不能只比较符号。

## RG 不变性

设无量纲物理量写成

$$
\mathcal O
=
\mathcal O\!\left(\frac{Q}{\mu},g(\mu)\right),
$$

其中 $Q$ 是物理动量尺度。物理量不应依赖任意的 $\mu$，所以

$$
0
=
\mu\frac{d\mathcal O}{d\mu}
=
\left(
\mu\frac{\partial}{\partial\mu}
+
\beta(g)\frac{\partial}{\partial g}
\right)\mathcal O.
$$

若还依赖跑动质量，则加入

$$
\gamma_{m^2}(g)m_R^2
\frac{\partial}{\partial m_R^2}.
$$

这不是说截断到有限圈阶的表达式对 $\mu$ 完全不变。有限阶计算通常留下高一阶量级的残余 $\mu$ 依赖，可用于估计未计算高阶效应，但它不是严格误差界。

## Callan–Symanzik 方程

对含 $n$ 个重整化场插入的重整化连通 Green 函数 $G_R^{(n)}$，在本篇的 $\gamma_\phi$ 定义下，可写为

$$
\boxed{
\left[
\mu\frac{\partial}{\partial\mu}
+
\beta(g)\frac{\partial}{\partial g}
+
\gamma_{m^2}(g)m_R^2
\frac{\partial}{\partial m_R^2}
+
n\gamma_\phi(g)
\right]
G_R^{(n)}
=0
}.
$$

这条式子把显式的 $\mu$ 依赖与耦合、质量、外腿场归一化的隐式依赖联系起来。若改用 1PI 顶点函数、不同的 $Z_\phi$ 方向或带负号的反常维数定义，最后一项可能写成 $-n\gamma_\phi$；这属于约定转换，不是物理矛盾。

## 四维 $\lambda\phi^4$ 的一圈运行耦合

对本篇规范

$$
\mathcal L_{\mathrm{int}}=-\frac{\lambda}{4!}\phi^4,
$$

[[正规化与重整化]] 已得到四维一圈 beta 函数

$$
\boxed{
\frac{d\lambda}{d\ln\mu}
=
\beta(\lambda)
=
\frac{3\lambda^2}{16\pi^2}
+O(\lambda^3)
}.
$$

忽略 $O(\lambda^3)$ 后分离变量：

$$
\frac{d\lambda}{\lambda^2}
=
\frac{3}{16\pi^2}d\ln\mu.
$$

从 $(\mu_0,\lambda_0)$ 积分到 $(\mu,\lambda(\mu))$：

$$
-\frac1{\lambda(\mu)}+\frac1{\lambda_0}
=
\frac{3}{16\pi^2}\ln\frac{\mu}{\mu_0}.
$$

所以一圈 RG 方程的解析解为

$$
\boxed{
\lambda(\mu)
=
\frac{\lambda_0}
{1-\dfrac{3\lambda_0}{16\pi^2}
\ln\dfrac{\mu}{\mu_0}}
}.
$$

对正的 $\lambda_0$，分母随 $\mu$ 增大而减小，因此耦合向紫外增强；这与 $\beta(\lambda)>0$ 一致。

### Landau 极点

在上述一圈近似中，分母在

$$
1-
\frac{3\lambda_0}{16\pi^2}
\ln\frac{\mu_L}{\mu_0}
=0
$$

处消失，得到

$$
\boxed{
\mu_L
=
\mu_0
\exp\!\left(\frac{16\pi^2}{3\lambda_0}\right)
}.
$$

这个尺度称为 Landau 极点（Landau pole）。它首先表示一圈微扰解在此之前已经失效，不能把无穷大本身当作可靠的高能预测。结合更完整的理论分析，四维纯标量理论的紫外完备性还涉及平凡性问题；本篇不把一圈极点单独当作严格证明。

例如 $\lambda_0=0.1$ 时

$$
\ln\frac{\mu_L}{\mu_0}
=
\frac{16\pi^2}{0.3}
\approx526.4,
$$

说明极点在指数级远处，但这不改变“正耦合向紫外增长”的趋势。

## RG 改善与大对数重求和

有限阶振幅常包含

$$
\lambda(\mu)\ln\frac{Q}{\mu}.
$$

若 $Q$ 与 $\mu$ 相差很多，即使 $\lambda$ 较小，对数也可能破坏微扰阶次。一个常用策略是选 $\mu\sim Q$，让显式对数变小，再用 RG 方程把已知参数从 $\mu_0$ 演化到 $Q$。

将运行耦合展开：

$$
\lambda(Q)
=
\lambda_0
\sum_{n=0}^{\infty}
\left[
\frac{3\lambda_0}{16\pi^2}
\ln\frac{Q}{\mu_0}
\right]^n,
$$

其收敛条件是方括号绝对值小于 $1$。这个几何级数展示一圈 RG 解怎样组织一串最高次大对数。真实振幅的 leading-log、next-to-leading-log 精度还取决于相应阶数的 beta 函数、反常维数、边界条件和匹配系数，不能只靠替换耦合自动得到全部高阶项。

## 固定点与线性化

固定点 $g_*$ 满足

$$
\boxed{
\beta_i(g_*)=0
}.
$$

在固定点附近令 $\delta g_i=g_i-g_{i*}$，线性化得到

$$
\frac{d\,\delta g_i}{d\ln\mu}
=
M_{ij}\delta g_j,
\qquad
M_{ij}
=
\left.
\frac{\partial\beta_i}{\partial g_j}
\right|_{g_*}.
$$

若 $M$ 的特征值为 $\theta_a$，对应方向满足

$$
\delta g_a(\mu)
\propto
\left(\frac{\mu}{\mu_0}\right)^{\theta_a}.
$$

按本篇“$\mu$ 增大即向紫外”的约定：

- $\theta_a<0$ 的扰动向紫外衰减，是 UV 吸引方向；
- $\theta_a>0$ 的扰动向紫外增强，是 UV 排斥方向。

统计物理常把粗粒化尺度向红外增大的参数作为 RG 时间，并把红外增长方向称为 relevant、衰减方向称为 irrelevant。由于流参数方向相反，仅看特征值正负容易混淆；最稳妥的做法是直接说明扰动朝 UV 或 IR 是增长还是衰减。

## $d=4-2\varepsilon$ 中的固定点预告

沿用前驱页面的维数正规化约定 $d=4-2\varepsilon$，一圈

$$
\beta_d(\lambda)
=
-2\varepsilon\lambda
+
\frac{3\lambda^2}{16\pi^2}
+O(\lambda^3).
$$

除 Gaussian 固定点 $\lambda_*=0$ 外，还有

$$
\boxed{
\lambda_*
=
\frac{32\pi^2}{3}\varepsilon
+O(\varepsilon^2)
}.
$$

若另一本教材使用 $d=4-\epsilon$，则对应写成 $\lambda_*=16\pi^2\epsilon/3+O(\epsilon^2)$。两者描述同一维数偏离，只是小参数相差因子 $2$，不能混用系数。

在非零固定点处

$$
\beta_d'(\lambda_*)
=
2\varepsilon+O(\varepsilon^2)>0.
$$

按本篇 $\mu$ 增大向 UV 的约定，该方向向 UV 排斥、向 IR 吸引。这是 Wilson–Fisher 固定点的一圈耦合方向。完整临界现象还需同时处理质量方向、场反常维数和复合算符混合，本篇只作路线图预告。

## Wilsonian 重整化群

Wilsonian RG 把场按动量分成低模与高模，并显式积分掉壳层

$$
\Lambda/b<|k|<\Lambda,
\qquad b>1.
$$

随后重标度坐标、动量和场，使截止恢复为原来的 $\Lambda$。结果不是只改变一个耦合，而是一般会生成所有与对称性相容的局域算符：

$$
S_\Lambda
=
\sum_i g_i(\Lambda)\int d^d x\,\mathcal O_i(x).
$$

对无量纲化后的耦合，在指定固定点邻域中按线性化缩放指数分类：

- relevant 算符在流向 IR 时增强；
- irrelevant 算符在流向 IR 时受到尺度幂次压低；
- marginal 算符在线性阶不变，需要由量子修正决定最终方向。

算符的完整缩放维数包含反常维数，而不只是工程量纲。

微扰 RG 通常从重整化尺度独立性推导 beta 函数；Wilsonian RG 直接改变有效截止并积分自由度。二者在共同适用的低能、局域和微扰范围内给出一致的尺度流信息，但具体耦合坐标、方案和中间有效作用量可以不同。

## 与有效场论的边界

RG 回答“系数怎样随尺度变化”，有效场论（effective field theory，EFT）还回答“在给定精度下应该保留哪些算符”。在 $d$ 维中，缩放维数 $\Delta_i>d$ 的算符通常贡献

$$
c_i\left(\frac{E}{\Lambda}\right)^{\Delta_i-d},
$$

此外还可能带耦合、圈因子、对称性选择规则和尺度对数。Wilsonian 粗粒化自然生成这些高维算符，但它们在 $E\ll\Lambda$ 时通常受到尺度幂次抑制。

因此：

- RG 运行不能替代阈值匹配；
- 跨过重粒子质量阈值时，通常要在两侧 EFT 之间匹配参数；
- 质量无关方案中的重粒子不会仅因 $\mu$ 降低而自动从公式中消失；
- “无关算符”不等于永远没有可观测效应，只表示在指定固定点附近、指定流向中被尺度幂次压低。

[[有效场论]] 将系统展开算符选择、matching、阈值处理和截断误差；本页保留 RG 运行、固定点和 Wilsonian 粗粒化的基础。

## 与弦理论的关系

弦理论同样包含尺度流，但要区分不同语境：

1. 弦低能有效作用中的耦合和高维算符满足时空 EFT 的 RG 方程；
2. 世界面二维量子场论的 Weyl 反常系数控制背景耦合；量子共形不变性要求模去场重定义、靶空间微分同胚和规范冗余后，相应的 Weyl 反常系数为零；在适当近似下，这些条件给出背景场的时空运动方程；
3. 世界面 RG 流描述二维场论随世界面尺度的变化，不等同于时空散射能标的 RG 流；
4. 弦模空间退化、红外因子化、临界维数和模不变性各有独立结构，不能全部归结为普通四维参数跑动。

本篇只建立概念边界，不推导非线性 sigma 模型的世界面 beta 函数。

## 与机器学习的关系边界

[[弦理论与大模型训练的已知关系]] 介绍了变分重整化群与特定受限 Boltzmann 机结构之间的研究映射。可以比较的共同点是粗粒化、隐变量、信息保留和多尺度有效描述。

但必须保留以下边界：

- 特定变分 RG 与特定概率网络之间的映射，不代表每个深度网络都在执行 RG；
- Transformer 的层数不是 RG 圈数，也不自动对应粗粒化步数；
- 模型压缩或池化只有在给出明确尺度变量、可组合的粗粒化映射和需要保留的可观测量时，才可能形成严格 RG 类比；对变分 RG—概率网络的精确映射，还需满足保持配分函数或可见变量边缘分布的 trace condition（迹条件）；
- 固定点检验不是建立一次有限步映射的必要条件，但若进一步声称临界行为、普适类或渐近尺度流，就必须分析固定点和稳定方向；
- 机器学习中的权重衰减仍不等于量子场论重整化。

由于当前大模型教程没有独立讲解变分 RG 或受限 Boltzmann 机的概念页，本轮不建立虚假的强双链。

## 适用条件与事实边界

1. beta 函数依赖耦合定义和重整化方案；最前面的普适系数需要在明确理论与方案类别后讨论。
2. 运行耦合是一种参数化，不表示常数在同一次固定能标实验中随时间变化。
3. 物理量全阶上与任意 $\mu$ 无关；固定阶残余 $\mu$ 依赖是截断效应。
4. 一圈运行解只在耦合和被重求和之外的剩余高阶修正可控时可信。
5. Landau 极点首先标记微扰解失效，不能单独作为理论数学不一致的严格证明。
6. 固定点的 relevant/irrelevant 标签依赖流向约定，报告时应说明朝 UV 或 IR 的行为。
7. Wilsonian RG 一般生成所有允许算符，不能只追踪原始 Lagrangian 中的一个耦合。
8. 固定点处的尺度不变性在通常条件下可提升为共形不变性，但不是无需条件的逻辑同义词。
9. $d=4-2\varepsilon$ 与 $d=4-\epsilon$ 的固定点系数不能直接混用。
10. 世界面 RG 与时空 RG 有联系但不是同一个流。

## 常见误区

1. **RG 是普通意义上的群，所以每一步都必须可逆。** 粗粒化会丢失短距离信息，现代 RG 更准确地表现为半群；“重整化群”是历史名称。
2. **改变 $\mu$ 会改变物理。** 改变的是参数化；正确合并显式与隐式依赖后，全阶可观测量不变。
3. **$\beta>0$ 表示耦合随时间增长。** beta 函数描述重整化尺度，不是实验时间。
4. **有 Landau 极点就能在一圈严格证明理论不存在。** 一圈解在到达极点前已失去微扰可信度。
5. **固定点就是所有耦合都为零。** Gaussian 固定点如此，但 Wilson–Fisher 等固定点可在非零耦合处。
6. **无关算符可以永远删除。** 它们只是在指定低能精度和固定点邻域中被压低，也可能影响高精度或匹配条件。
7. **选 $\mu=Q$ 就计算了所有高阶修正。** 它只减小一类显式大对数，重求和精度仍受已知 RG 数据限制。
8. **每一层神经网络都是一次 RG。** 没有明确粗粒化、组合律和保留量时，这只是表面类比。
9. **世界面 beta 函数就是四维标准模型耦合的 beta 函数。** 二者作用对象和尺度语境不同。
10. **固定阶结果应完全不依赖 $\mu$。** 未计算高阶会留下残余依赖。

## 练习

1. 解释为什么重整化参数依赖 $\mu$，但物理可观测量不应依赖任意的 $\mu$。
2. 从 $d\lambda/d\ln\mu=b\lambda^2$ 推导运行耦合 $\lambda(\mu)$。
3. 对 $b=3/(16\pi^2)$、$\lambda_0>0$，求 Landau 极点尺度。
4. 若 $\lambda_0=0.1$，计算 $\ln(\mu_L/\mu_0)$ 的近似值。
5. 为什么选择 $\mu\sim Q$ 能改善含 $\ln(Q/\mu)$ 的微扰展开？
6. 写出多耦合固定点的定义和线性化矩阵。
7. 在本篇约定下，$\theta>0$ 的扰动朝 UV 增强还是衰减？
8. 用 $d=4-2\varepsilon$ 约定求一圈 Wilson–Fisher 固定点。
9. 比较微扰 RG 与 Wilsonian RG 的共同点和区别。
10. 为什么“irrelevant 算符”不等于“物理上永远没有影响”？
11. 解释世界面 RG 与时空 RG 为什么不能直接等同。
12. 判断：任意深度神经网络都可以严格解释为重整化群流。

## 答案与提示

1. $\mu$ 是参数定义中的任意尺度。参数随 $\mu$ 跑动，恰好抵消振幅中的显式 $\mu$ 依赖，使全阶物理预测不变。
2. 分离变量并积分：$-1/\lambda(\mu)+1/\lambda_0=b\ln(\mu/\mu_0)$，所以 $\lambda(\mu)=\lambda_0/[1-b\lambda_0\ln(\mu/\mu_0)]$。
3. 令分母为零，得 $\mu_L=\mu_0\exp[16\pi^2/(3\lambda_0)]$。
4. $16\pi^2/(0.3)\approx526.4$。
5. 这样显式对数接近零，同时通过运行参数把从 $\mu_0$ 到 $Q$ 的一串大对数组织进 RG 解；但不会自动补齐所有高阶常数项。
6. $\beta_i(g_*)=0$，$M_{ij}=\partial\beta_i/\partial g_j|_{g_*}$。
7. 朝 UV 增强，因为 $\delta g\propto(\mu/\mu_0)^\theta$；采用向 IR 增大的 RG 时间时标签符号会反转。
8. 解 $-2\varepsilon\lambda+3\lambda^2/(16\pi^2)=0$，得 $\lambda_*=0$ 或 $\lambda_*=32\pi^2\varepsilon/3+O(\varepsilon^2)$。
9. 两者都描述尺度流并给出 beta 函数；微扰 RG 从 $\mu$ 独立性出发，Wilsonian RG 显式积分高动量壳层并生成全部允许算符。
10. 它只表示该方向在指定固定点附近流向 IR 时受抑制；在更高精度、较高能量或阈值匹配中仍可能贡献。
11. 世界面 RG 改变二维世界面理论的尺度，时空 RG 改变时空有效理论的重整化尺度；二者可通过背景一致性联系，但作用对象不同。
12. 错误。严格对应需要定义粗粒化映射、尺度变量、组合律、保留量和固定点等结构；网络有很多层本身不是证据。

## 与其他概念的关系

- [[正规化与重整化]] 定义重整化参数、尺度 $\mu$ 和一圈反项，是本页 RG 方程的直接起点；
- [[beta 函数]] 专门展开怎样从 MS 极点提取 beta，并比较 $\lambda\phi^4$、QED、QCD 与世界面 sigma 模型的系数；
- [[有效场论]] 用 RG 演化 Wilson 系数，并补充算符基、matching、阈值与截断误差；
- [[Weyl 反常]] 说明世界面 RG 流与量子共形不变性的关系，以及 Weyl anomaly coefficient 与普通 beta 的边界；
- [[微扰论]] 说明大对数为何会破坏固定阶展开，RG 用运行参数组织这些对数；
- [[生成泛函]] 组织 Green 函数和 1PI 顶点，Callan–Symanzik 方程描述它们的尺度依赖；
- [[对数函数]] 解释 $\ln(\mu/\mu_0)$ 为什么必须作用于同量纲尺度之比；
- [[常微分方程]] 提供求解耦合流和固定点线性化的数学工具；
- [[弦理论与大模型训练的已知关系]] 记录变分 RG 与特定概率网络的已知研究及适用边界；
- [[弦理论完整学习路线图]] 将本节点放在正规化与重整化之后、beta 函数之前。

## 参考资料

- Michael E. Peskin and Daniel V. Schroeder, *An Introduction to Quantum Field Theory*, Westview Press, 1995, Chapters 12 and 16.
- Mark Srednicki, *Quantum Field Theory*, Cambridge University Press, 2007, Chapters 27–31.
- Michael D. Schwartz, *Quantum Field Theory and the Standard Model*, Cambridge University Press, 2014, Chapters 23 and 28.
- John C. Collins, *Renormalization*, Cambridge University Press, 1984.
- Kenneth G. Wilson and John Kogut, “The Renormalization Group and the $\varepsilon$ Expansion,” *Physics Reports* 12 (1974), 75–199.
- Joseph Polchinski, “Renormalization and Effective Lagrangians,” *Nuclear Physics B* 231 (1984), 269–295.
- David Tong, *Statistical Field Theory*, sections on the renormalization group: https://www.damtp.cam.ac.uk/user/tong/statphys.html
- David Tong, *String Theory*, sections on the worldsheet beta functions: https://www.damtp.cam.ac.uk/user/tong/string.html
- Pankaj Mehta and David J. Schwab, “An Exact Mapping Between the Variational Renormalization Group and Deep Learning,” arXiv:1410.3831.
