---
aliases: [向量检索, ANN, 近似最近邻, HNSW, IVF, 混合检索, Vector Retrieval, Approximate Nearest Neighbor, Hybrid Search]
tags: [概念, RAG, 检索, 向量, 大模型, 学习教程]
level: L7
prerequisites: ["[[标量向量矩阵与张量]]", "[[RAG检索重排序与引用]]", "[[维度灾难与高维直觉]]"]
---
# 向量检索与 ANN

## 一句话定义

向量检索在向量集合中寻找与查询最相似的向量；ANN（近似最近邻）牺牲少量检索精度换取大幅加速；HNSW 和 IVF 是两种主流 ANN 算法。

## 直觉理解

精确检索像在图书馆逐本翻书；ANN 像先看目录分类，再只翻相关书架。HNSW 像多层公路网：高速公路快速跳远，小路精细导航。IVF 先把书按主题分到不同房间，搜索时只进最相关的房间。

## 关键方法

- HNSW：多层邻接图，高层稀疏快速跳跃，低层密集精细搜索；
- IVF：聚类划分空间，搜索时先找最近聚类中心；
- 混合检索：向量相似度 + 关键词匹配（BM25）融合。

## 例子

假设有 100 万条向量。精确搜索要计算查询与全部向量的相似度；HNSW 等 ANN 索引只访问候选子集，通常能显著减少查询计算，但延迟和召回率没有跨系统固定数值。它们取决于向量维度、距离函数、索引参数、硬件、并发和数据分布，应在目标数据上画出“延迟—召回率”曲线。混合检索则可让关键词方法负责专有名词和编号，让向量方法补充语义近似。

## 边界

- ANN 不保证找到真正最近邻；
- 索引构建需要时间和显存/内存；
- 向量更新后索引可能需重建；
- 相似度不等于语义正确性。

## 最小自测

1. ANN 和精确检索的区别是什么？
2. 为什么混合检索比纯向量检索更稳定？

答案：1. ANN 用近似换取速度，不保证最优结果；2. 向量检索擅长语义，关键词检索擅长精确匹配，两者互补。

## 详细教程

- [[嵌入与向量数据库|嵌入与向量数据库]]

## 来源

- Malkov & Yashunin (2020), *Efficient and Robust Approximate Nearest Neighbor Search Using HNSW*：https://arxiv.org/abs/1603.09320 `[A]`