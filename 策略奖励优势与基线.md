---
aliases: [策略, 奖励, 优势函数, 基线, Policy, Reward, Advantage, Baseline]
tags: [概念, 强化学习, 对齐, 大模型, 学习教程]
level: L6
prerequisites: ["[[变量与函数]]", "[[随机事件与随机变量]]", "[[似然最大似然与负对数似然]]", "[[Logit概率与采样]]", "[[条件概率链式法则与期望公式|条件概率、链式法则与期望公式]]"]
---
# 策略、奖励、优势与基线

## 一句话定义

策略（Policy）规定模型在给定输入时如何选择输出；奖励（Reward）给一次输出打分；优势（Advantage）表示某次选择比参考水平好多少；基线（Baseline）就是用于比较的参考水平。

## 策略

在语言模型中，策略常写成：

$$
\pi_\theta(y\mid x)
$$

- $x$：提示或当前状态；
- $y$：完整回答或下一步动作；
- $\theta$：模型参数；
- $\pi_\theta(y\mid x)$：当前模型生成 $y$ 的条件概率。

策略不是一条固定回答，而是一套概率规则。同一个提示可能生成多个不同回答。

## 奖励

奖励函数把回答映射成一个数字：

$$
r(x,y)\in\mathbb R
$$

分数更高表示在这套规则下更受偏好。奖励可以来自人工训练的奖励模型，也可以来自代码测试、数学答案核验或格式检查。

奖励只反映评分规则。模型把奖励提高，不自动等于事实性、安全性和所有能力都提高。

## 基线与优势

最简单的优势写法是：

$$
A_i=r_i-b
$$

$b$ 是基线。若 $A_i>0$，候选 $i$ 比基线好；若 $A_i<0$，则比基线差。

GRPO 的简化例子使用同组平均奖励作为基线：

$$
b=\frac1G\sum_{j=1}^{G}r_j
$$

$$
A_i=r_i-b
$$

## 数值例子

同一题生成四个候选，奖励是：

$$
[1,0,-1,1]
$$

平均奖励：

$$
b=\frac{1+0-1+1}{4}=0.25
$$

优势为：

$$
[0.75,-0.25,-1.25,0.75]
$$

第一和第四个候选高于组内平均，第三个候选明显低于平均。

## 为什么使用基线

如果所有奖励都整体加 100，候选的相对好坏没有变化。减去基线可以把注意力放在“比通常表现好多少”上，并常用于降低梯度估计方差。

基线不能依赖当前要评价的动作到破坏无偏性；具体算法怎样选择和训练基线，需要看其推导与实现。

## 边界

- 优势不是奖励本身，而是奖励或回报相对基线的差；
- PPO 常用价值函数估计优势，GRPO 使用同组候选的相对统计量；
- 实际算法还可能使用折扣回报、广义优势估计、标准差归一化、裁剪和 KL 正则，不能用一条均值公式代替完整算法。

## 最小自测

题目：奖励为 $[2,4,0]$，以组内平均为基线，各候选优势是多少？

答案：平均值为 2，优势为 $[0,2,-2]$。

## 详细教程

- [[人类反馈强化学习|人类反馈强化学习]]
- [[组相对策略优化|组相对策略优化]]

## 来源

- Sutton & Barto (2018), *Reinforcement Learning: An Introduction*, 2nd ed.：http://incompleteideas.net/book/the-book-2nd.html `[A]`
- Schulman et al. (2016), *High-Dimensional Continuous Control Using Generalized Advantage Estimation*：https://arxiv.org/abs/1506.02438 `[A]`
- Shao et al. (2024), *DeepSeekMath*：https://arxiv.org/abs/2402.03300 `[A]`
