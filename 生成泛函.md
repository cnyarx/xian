---
aliases:
  - Generating Functional
  - 生成泛函
  - 源泛函
english_name: Generating Functional
tags:
  - 物理学
  - 量子场论
  - 弦理论先修
  - 弦理论
prerequisites:
  - "[[泛函]]"
  - "[[Gaussian 积分]]"
  - "[[路径积分]]"
  - "[[传播子]]"
  - "[[Wick 定理]]"
  - "[[时序乘积]]"
  - "[[微扰论]]"
used_by:
  - "[[Feynman 图]]"
  - "[[正规化与重整化]]"
  - "[[重整化群]]"
  - "[[弦理论完整学习路线图]]"
evidence_status: 教材事实
---

# 生成泛函（Generating Functional）

## 一句话解释

生成泛函 $Z[J]$ 是把所有时间有序关联函数打包成单个对象的工具：对源 $J(x)$ 逐次求泛函导数，就从 $Z[J]$ 中逐次取出真空 $n$ 点关联函数，就像普通生成函数通过求导生成矩一样。

## 为什么要学它

Wick 定理把 $n$ 点关联函数分解为传播子的配对组合，但 $n$ 增大时，配对项数按 $(2n-1)!!$ 增长，逐个写出越来越繁琐。生成泛函用一个指数结构同时编码所有配对——求导时自然从指数中按需提取收缩。

它也是连接微扰展开与 Feynman 图的枢纽：

- $Z[J]$ 是完整生成泛函；
- $W[J]=-i\ln Z[J]$ 生成连通关联函数；使用 $Z[J]/Z[0]$ 或 $W[J]-W[0]$ 才会同时去掉与源无关的真空泡泡；
- $\Gamma[\varphi]$ 做 Legendre 变换，生成单粒子不可约顶点。

弦世界面路径积分中，指数顶点算符可通过在自由场生成泛函中代入由插入点和动量组成的分布源来处理；这把 Gaussian 源泛函结构直接投射到弦散射振幅的构造中。

## 本篇约定

采用自然单位 $\hbar=c=1$，mostly-minus 度规
$\eta_{\mu\nu}=\operatorname{diag}(+1,-1,-1,-1)$。自由场源项约定为：

$$
S_{\mathrm{src}}[\phi,J]=\int d^Dx\,J(x)\phi(x).
$$

泛函导数约定：

$$
\frac{\delta J(x)}{\delta J(y)}=\delta^{(D)}(x-y).
$$

## 直观图景

想象一个工厂，产品是真空关联函数。工厂的“总机”是 $Z[J]$，给总机输入不同的“订单”（源 $J(x)$ 的取法），总机输出一个数。每次对订单的某个位置做微小变动并读取变化率，就得到在该位置插入一个场的关联函数。

更具体地说：$Z[J]$ 像一个“母函数”——把 $J$ 当作形式参数，对 $J$ 求导相当于在积分中插入场算符，求完导再令 $J=0$ 就得到真空期望值。

## 正式定义

### 完整生成泛函

$$
\boxed{
Z[J]=\int\mathcal D\phi\,
\exp\left\{
iS[\phi]+i\int d^Dx\,J(x)\phi(x)
\right\}
}
$$

$n$ 点函数由 $n$ 次泛函导数得到：

$$
\boxed{
\langle0|T\{\phi(x_1)\cdots\phi(x_n)\}|0\rangle
=\left.
\frac{1}{Z[0]}
\frac{1}{i^n}
\frac{\delta^n Z[J]}
{\delta J(x_1)\cdots\delta J(x_n)}
\right|_{J=0}
}
$$

除以 $Z[0]$ 保证 $\langle0|0\rangle=1$，同时消去无外线的真空泡泡。

### 连通生成泛函

$$
\boxed{
W[J]=-i\ln Z[J]
}
$$

连通 $n$ 点函数：

$$
\boxed{
\langle\phi(x_1)\cdots\phi(x_n)\rangle_c
=\left.
\frac{1}{i^{n-1}}
\frac{\delta^n W[J]}
{\delta J(x_1)\cdots\delta J(x_n)}
\right|_{J=0}
}
$$

对数把 $Z[J]$ 的各阶乘积拆成连通部分之和。例如两点函数：

$$
\langle\phi_1\phi_2\rangle
=\langle\phi_1\phi_2\rangle_c
+\langle\phi_1\rangle\langle\phi_2\rangle,
$$

右边第二项在 $\langle\phi\rangle=0$ 时为零；对更高阶，$W$ 自动选出所有含外源插入的连通分量。与源无关的连通真空图属于 $W[0]$，需再作归一化消去。

## 核心公式

生成泛函及其三层组织结构可以浓缩为：

$$
Z[J]=\int\mathcal D\phi\,e^{iS[\phi]+i\int J\phi},
\qquad
W[J]=-i\ln Z[J],
$$

$$
\varphi(x)=\frac{\delta W}{\delta J(x)},
\qquad
\Gamma[\varphi]=W[J]-\int d^Dx\,J(x)\varphi(x).
$$

它们依次生成全部相关函数、连通相关函数和 1PI 顶点；真空期望值还需以 $Z[0]$ 归一化。

### 单粒子不可约生成泛函（有效作用量）

定义经典场：

$$
\varphi(x)=\frac{\delta W[J]}{\delta J(x)}.
$$

Legendre 变换：

$$
\boxed{
\Gamma[\varphi]=W[J]-\int d^Dx\,J(x)\varphi(x),
\qquad
J=J[\varphi]
}
$$

其中 $J[\varphi]$ 是 $\varphi=\delta W/\delta J$ 的反函数。$\Gamma[\varphi]$ 的泛函导数给出 1PI 顶点函数。量子运动方程由 $\delta\Gamma/\delta\varphi=-J$ 描述；无外源时为 $\delta\Gamma/\delta\varphi=0$。

在微扰论中，$\Gamma$ 的树级近似回到经典作用量 $S[\varphi]$；圈图贡献是对经典作用量的量子修正。

## 自由标量场的生成泛函

自由场作用量在分部积分并忽略边界项后为：

$$
S_0[\phi]
=-\frac12\int d^Dx\,\phi(x)(\Box+m^2)\phi(x).
$$

记 $A=\Box+m^2$，则 $S_0=-\frac12\int\phi A\phi$。路径积分是 Gaussian 的：

$$
Z_0[J]
=\int\mathcal D\phi\,
\exp\left\{
i\int d^Dx\,
\left[
-\frac12\phi A\phi+J\phi
\right]
\right\}.
$$

平移 $\phi$ 完成平方。由于传播子满足：

$$
A_x\Delta_F(x-y)=-i\delta^{(D)}(x-y),
$$

所以 $A^{-1}=i\Delta_F$。选择：

$$
\phi(x)=\phi'(x)+i\int d^Dy\,\Delta_F(x-y)J(y)
$$

可消去 $\phi'$ 的线性项。代入并积分，得到：

$$
\boxed{
Z_0[J]
=Z_0[0]
\exp\left[
-\frac12
\int d^Dx\,d^Dy\,
J(x)\Delta_F(x-y)J(y)
\right]
}
$$

此时 $Z_0[0]$ 是与 $J$ 无关的 Gaussian 行列式归一化常数。两点函数：

$$
\langle0|T\{\phi(x)\phi(y)\}|0\rangle
=\left.\frac{1}{i^2}
\frac{\delta^2 Z_0[J]}{\delta J(x)\delta J(y)}
\right|_{J=0}
\times\frac{1}{Z_0[0]}
$$

$$
=\Delta_F(x-y).
$$

四点函数对 $J$ 求四次导，从指数中的二次型取两次源对，得到三种配对：

$$
\langle\phi_1\phi_2\phi_3\phi_4\rangle
=\Delta_{12}\Delta_{34}
+\Delta_{13}\Delta_{24}
+\Delta_{14}\Delta_{23}.
$$

这就是路径积分版 Wick 定理：Gaussian 指数中的二次源项 $J\Delta_F J$ 在多次求导时自动枚举所有两两配对。

## 相互作用理论

若完整作用量包含相互作用 $S_{\mathrm{int}}$：

$$
S[\phi]=S_0[\phi]+S_{\mathrm{int}}[\phi],
$$

则：

$$
Z[J]
=\int\mathcal D\phi\,
e^{iS_0[\phi]+i\int J\phi}
e^{iS_{\mathrm{int}}[\phi]}.
$$

由于 $e^{iS_{\mathrm{int}}}$ 是 $\phi$ 的函数，可以把它写成对源求导的算符。例如 $\phi^4$ 理论：

$$
S_{\mathrm{int}}[\phi]
=-\frac{\lambda}{4!}\int d^Dx\,\phi^4(x).
$$

注意到：

$$
\phi(x)
\to\frac{1}{i}\frac{\delta}{\delta J(x)}
$$

在 $Z_0[J]$ 的积分表示下成立。因此：

$$
\boxed{
Z[J]
=\exp\left[
iS_{\mathrm{int}}\!\left(\frac{1}{i}\frac{\delta}{\delta J}\right)
\right]
Z_0[J]
}
$$

展开指数：

$$
Z[J]
=Z_0[J]
+iS_{\mathrm{int}}\!\left(\frac1i\frac\delta{\delta J}\right)Z_0[J]
+\frac12
\left[iS_{\mathrm{int}}\!\left(\frac1i\frac\delta{\delta J}\right)\right]^2
Z_0[J]
+\cdots.
$$

每一项对 $Z_0[J]$ 求若干次泛函导数，再令 $J=0$，就得到微扰展开的 Feynman 图。$Z_0[J]$ 的指数结构确保每次求导产生一个传播子，$S_{\mathrm{int}}$ 中的 $\phi^4$ 通过四次求导对四个传播子端点在一点汇合，形成顶点。

## 每个符号是什么意思

- $\mathcal D\phi$：所有场配置的泛函积分测度；
- $S[\phi]$：场 $\phi$ 的经典作用量；
- $J(x)$：外源，是时空坐标的函数，不是物理动力学场；
- $Z[J]$：完整生成泛函，输出含源路径积分；
- $Z[0]$：$J=0$ 时的真空—真空振幅；
- $W[J]$：连通生成泛函，$W=-i\ln Z$；
- $\Gamma[\varphi]$：单粒子不可约生成泛函（有效作用量）；
- $\varphi(x)$：经典场，定义在场上取期望值；
- $\Delta_F(x-y)$：Feynman 传播子；
- $\Box=\partial_t^2-\nabla^2$：d'Alembert 算符；
- $\lambda$：$\phi^4$ 耦合常数；
- $S_{\mathrm{int}}$：相互作用部分的作用量。

## 具体例子

### 两点函数的生成

对自由场 $Z_0[J]$，求 $\delta^2/\delta J(x)\delta J(y)$：

$$
\frac{\delta^2}{\delta J(x)\delta J(y)}
\exp\left[-\frac12\int J\Delta_F J\right]
$$

第一步：对 $J(y)$ 求导，从指数中拉下 $-\int d^Dz\,\Delta_F(y-z)J(z)$。

第二步：对 $J(x)$ 再求导，产生两项：
- 一项来自对前一步因子中的 $J(z)$ 再次求导：$-\Delta_F(y-x)$；
- 另一项来自指数再拉下一个因子，含 $J$ 的平方项，$J=0$ 时为零。

令 $J=0$ 后只有第一项存活；因为 $1/i^2=-1$，再除以 $Z_0[0]$ 后得到 $\Delta_F(x-y)$。

### 四点函数的生成

四次求导从指数中拉下两个 $J$ 因子，每个 $J$ 因子再被求导一次产生一个传播子。两个 $J$ 因子之间有三种配对方式，对应三点对 $(12)(34)$、$(13)(24)$、$(14)(23)$。这正是 Wick 定理的三种完全收缩。

## 与 Feynman 图的关系

- $Z[J]$ 的微扰展开：所有图，包括不连通图。
- $W[J]$ 的微扰展开：只保留连通图。
- $\Gamma[\varphi]$ 的微扰展开：只保留单粒子不可约图。

树级散射振幅可直接从 $\Gamma$ 的树图部分读出；圈图则从 $\Gamma$ 的更高阶项通过圈展开逐阶计算。

## 与弦世界面的关系

弦世界面路径积分形式为：

$$
\mathcal A_n
=\int\mathcal D X\,\mathcal D g\,
e^{-S[X,g]}
\prod_{i=1}^n V_{k_i}(z_i,\bar z_i).
$$

把顶点算符写成正规序指数：

$$
V_{k_i}(z_i,\bar z_i)=:e^{ik_i\cdot X(z_i,\bar z_i)}:.
$$

一次普通的泛函导数 $\delta/\delta J$ 只插入一个线性场 $X$，不能直接生成指数顶点。指数插入应在 Euclidean 自由场生成泛函中代入分布源：

$$
J^\mu(z,\bar z)
=\sum_{i=1}^n k_i^\mu\,
\delta^{(2)}(z-z_i),
$$

使源指数成为：

$$
\exp\left[
i\int d^2z\,J(z)\cdot X(z)
\right]
=\prod_{i=1}^n e^{ik_i\cdot X(z_i,\bar z_i)}.
$$

对 Gaussian 的 $X^\mu$ 积分后，源二次型产生所有点对交叉收缩；去掉各顶点内部自收缩并处理零模后，得到：

$$
\prod_{i<j}|z_i-z_j|^{\alpha' k_i\cdot k_j}
$$

以及目标时空动量守恒 delta。精确指数符号、开闭弦系数和整体归一化取决于世界面作用量、二点函数与边界条件约定。

## 适用条件与事实边界

1. 上述生成泛函构造基于路径积分量子化，与算符形式等价。
2. 相互作用生成泛函的微扰展开是渐近级数，不保证对任意耦合收敛。
3. 自由场 Gaussian 生成泛函要求作用量是场的二次型；非二次型必须用微扰展开或非微扰方法。
4. $W[J]$ 生成连通图，但与 $J$ 无关的连通真空图可留在 $W[0]$ 中；使用 $W[J]-W[0]$ 或等价的 $Z[J]/Z[0]$ 才将其消去。
5. 1PI 生成泛函 $\Gamma[\varphi]$ 的 Legendre 变换要求 $\delta^2 W/\delta J\delta J$ 可逆。
6. 源导数约定（是否含 $1/i$）和 Fourier 相位约定必须整套一致。
7. 弦世界面生成泛函还需结合零模积分、鬼场、世界面拓扑、正规序和整体动量守恒。

## 常见误区

1. **$Z[J]$ 本身就是物理可观测量。** $Z[J]$ 是数学工具，物理量是对 $J$ 求导再令 $J=0$ 得到的关联函数。
2. **$W[J]$ 和 $Z[J]$ 只差一个常数因子。** 差的是对数，它把乘积结构变成求和结构，剔除不连通分量。
3. **$\Gamma[\varphi]$ 就是经典作用量。** 它包含所有量子修正，树级近似才回到经典作用量。
4. **生成泛函只能用于微扰论。** 它也是非微扰定义（如格点场论）和对称性 Ward 恒等式推导的起点。
5. **源 $J$ 必须有物理意义。** 它是数学辅助量，求导后令为零。
6. **自由场 $Z_0[J]$ 的指数结构只对实标量场成立。** 对复标量场、费米场、规范场，二次型结构相同，但配对规则和行列式因子不同。
7. **Legendre 变换 $\Gamma[\varphi]$ 在所有场论中都自动存在。** 在规范理论中需要先处理规范冗余；更一般地，还要检查 Legendre 变换所需的可逆性或凸性条件。

## 练习

1. 写出自由标量场 $Z_0[J]$ 的表达式，并说明指数中的 $J\Delta_F J$ 项从哪里来。
2. 用 $Z_0[J]$ 对 $J$ 求导，计算自由场的两点和四点函数。
3. 对相互作用 $S_{\mathrm{int}}=-\frac{\lambda}{4!}\int d^Dx\,\phi^4(x)$，写出一阶微扰中四外点连通函数的顶点因子。
4. 说明 $W[J]=-i\ln Z[J]$ 为什么生成连通图，以及还需怎样消去与源无关的真空图。
5. 写出 $\Gamma[\varphi]$ 的定义，并说明 Legendre 变换的动机。
6. 在弦世界面路径积分中，顶点算符怎样对应于源导数？
7. 若二点函数 $\Delta_F(x-y)$ 已知，$Z_0[J]$ 的指数因子决定了哪些关联函数非零？
8. 为什么 $\phi^4$ 理论的真空三点函数在树级为零？

## 答案与提示

1. $Z_0[J]=Z_0[0]\exp[-\frac12\int J(x)\Delta_F(x-y)J(y)]$。通过平移 $\phi=\phi'+i\Delta_F J$ 完成 Gaussian 平方；这里 $A\Delta_F=-i\delta$，所以 $A^{-1}=i\Delta_F$。
2. 两点：$\delta^2 Z_0/\delta J\delta J|_{J=0}=-\Delta_F Z_0[0]$，再乘 $1/i^2=-1$ 并除以 $Z_0[0]$，得到 $\Delta_F$。四点：四次求导后存活项为 $\Delta_{12}\Delta_{34}+\Delta_{13}\Delta_{24}+\Delta_{14}\Delta_{23}$，因为 $1/i^4=1$，结果不再改变符号。
3. 展开 $e^{iS_{\mathrm{int}}}$ 的一阶项得到 $-i\lambda/4!$。四次源导数有 $4!$ 种方式把四条外线接到同一顶点，抵消 $1/4!$，所以顶点因子为 $-i\lambda$；完整连通四点函数还要乘四条外部传播子并对顶点位置积分。
4. $\ln Z$ 的组合展开把图分解为连通分量，因此 $W=-i\ln Z$ 的源导数只生成连通关联函数。但 $W[0]$ 仍可包含连通真空图；改用 $W[J]-W[0]$，等价于先把 $Z[J]$ 除以 $Z[0]$，才会同时消去这些与源无关的项。
5. $\Gamma[\varphi]=W[J]-\int J\varphi$，其中 $\varphi=\delta W/\delta J$。动机：Legendre 变换把以源 $J$ 为自变量的连通生成泛函改写为以经典场 $\varphi$ 为自变量的 1PI 生成泛函。
6. 单次 $\delta/\delta J$ 只插入一个 $X$。指数顶点应把分布源 $J^\mu(z)=\sum_i k_i^\mu\delta^{(2)}(z-z_i)$ 代入 Euclidean 自由场 $Z_0[J]$，使 $e^{i\int J\cdot X}=\prod_i e^{ik_i\cdot X(z_i)}$；随后处理正规序自收缩与零模。
7. 只有偶数个 $J$ 求导时非零（Gaussian 的奇次矩为零）。因此自由场的奇数点真空关联函数为零。
8. 若真空和正则化保持未破缺的 $\mathbb Z_2: \phi\to-\phi$ 对称性，任何奇数点真空关联函数在微扰论各阶都为零；它不是到一圈才首次非零。

## 与其他概念的关系

- [[泛函]] 定义泛函和泛函导数；
- [[Gaussian 积分]] 提供有限维原型：源参数 $b$ 对 $Z(b)$ 求导生成矩；
- [[路径积分]] 定义场配置的泛函积分；
- [[传播子]] 作为 $A^{-1}$ 出现在 $Z_0[J]$ 的指数中；
- [[Wick 定理]] 由 $Z_0[J]$ 对 $J$ 多次求导自动产生；
- [[时序乘积]] 由 $i\varepsilon$ 处方和源导数约定保证；
- [[微扰论]] 用 $Z[J]$ 组织微扰展开的各阶贡献；
- [[正规化与重整化]] 用反项使 $Z[J]$、$W[J]$ 和 $\Gamma[\varphi]$ 生成的顶点函数有限；
- [[重整化群]] 用 Callan–Symanzik 方程描述重整化 Green 函数和顶点随尺度的变化；
- [[弦理论完整学习路线图]] 中阶段 2 的量子场论节点。

## 参考资料

- [David Tong, Quantum Field Theory, §3.4](https://www.damtp.cam.ac.uk/user/tong/qft.html)
- Peskin & Schroeder, *An Introduction to Quantum Field Theory*, Chapter 9, 11
- Srednicki, *Quantum Field Theory*, Chapters 8, 21
- Zinn-Justin, *Quantum Field Theory and Critical Phenomena*, Chapter 5