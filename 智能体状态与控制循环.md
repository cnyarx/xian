---
aliases: [智能体控制循环, 智能体状态, Agent Control Loop, Agent State, Planning, Reflection]
tags: [概念, 智能体, 大模型, 学习教程]
level: L6
prerequisites: ["[[集合与映射]]", "[[Token词表与Token-ID]]", "[[Logit概率与采样]]"]
---
# 智能体状态与控制循环

## 一句话定义

智能体控制循环（Agent Control Loop）反复读取当前状态、选择动作、执行工具、记录观察并检查终止条件；状态（State）是程序下一步决策所需且被明确保存的信息。

## 状态不只是聊天记录

状态可以写成一个结构：

```text
目标、待办、已知事实、工具结果、权限、剩余步数、错误记录
```

模型上下文只是状态的一种载体。外部数据库、文件或工作流系统也可保存状态。模型参数不会因一次工具调用自动改变。

## 基本状态转移

把第 $t$ 步状态记为 $s_t$，模型或策略选择动作 $a_t$，工具返回观察 $o_{t+1}$，程序更新状态：

$$
s_{t+1}=U(s_t,a_t,o_{t+1})
$$

$U$ 是程序规定的更新规则。模型可以建议怎样更新，但权限、审计和终止判断不应完全依赖模型自由文本。

## 一个完整例子

目标：查明“订单为什么退款失败”。

1. 状态记录订单号、只读权限和最大 6 步；
2. 模型选择“查询订单状态”；
3. 工具返回 `payment_captured=true`、`refund_id=null`；
4. 程序把真实返回写入状态，模型不能假装已有退款号；
5. 模型查询支付网关日志，发现参数校验失败；
6. 终止条件满足：已得到可引用错误和修复建议，不执行退款写操作。

该例子的关键不是模型“想得多”，而是每一步都以真实观察修正状态。

## 规划、记忆与反思

- 规划（Planning）：选择子目标和行动顺序；
- 工作记忆（Working Memory）：当前任务的短期状态；
- 长期记忆（Long-term Memory）：跨任务保存、可检索的信息；
- 反思（Reflection）：根据轨迹检查遗漏或矛盾。

反思只是一次新的模型判断。若没有新证据，它可能把原错误说得更坚定，因此必须与测试、规则或独立数据源配合。

## 多智能体

多个智能体可并行调查不同问题，但需要：

- 不重叠或明确加锁的写入范围；
- 统一证据格式；
- 一个最终综合者；
- 冲突时回到原始证据，而不是投票决定事实。

## 失败模式

- 循环没有可判定的终止条件；
- 工具报错却继续使用假想结果；
- 不可信网页内容被当成高优先级指令；
- 长期记忆过期但未重新验证；
- 多个智能体同时修改同一对象导致覆盖。

## 最小自测

题目：工具返回“记录不存在”后，智能体为什么不能继续假设该记录的字段值？

答案：因为状态更新必须以真实观察为依据；不存在的记录没有可验证字段，继续推断会把猜测伪装成事实。

## 详细教程

- [[工具调用|工具调用]]
- [[智能体基础|智能体基础]]

## 来源

- Yao et al. (2023), *ReAct*：https://arxiv.org/abs/2210.03629 `[A]`
- Shinn et al. (2023), *Reflexion*：https://arxiv.org/abs/2303.11366 `[A]`
- Wang et al. (2024), *A Survey on Large Language Model based Autonomous Agents*：https://arxiv.org/abs/2308.11432 `[B]`
