---
aliases:
  - Polynomial
  - Polynomials
english_name: Polynomial
tags:
  - 数学
  - 高中基础
  - 代数
  - 弦理论先修
  - 弦理论
prerequisites:
  - "[[实数]]"
  - "[[复数]]"
  - "[[方程]]"
used_by:
  - "[[函数]]"
  - "[[级数]]"
  - "[[Grassmann 变量]]"
  - "[[Laurent 级数]]"
evidence_status: 教材事实
---

# 多项式（Polynomial）

## 一句话解释

多项式是由常数和变量的非负整数次幂，经过有限次加法与乘法组成的代数式。

## 为什么要学它

多项式是代数中最基本的一类表达式。直线、抛物线和许多近似公式都可以用多项式表示。

物理中，人们常把难处理的函数在某点附近展开成多项式。量子力学中的 Hermite 多项式、场论中的相互作用势，以及 Grassmann 变量组成的有限表达式，都需要多项式语言。

## 直观图景

把多项式想成由若干“积木块”相加得到的表达式。例如：

$$
3x^2-2x+5
$$

由三块组成：

$$
3x^2,
\qquad
-2x,
\qquad
5.
$$

每一块叫一项。变量 $x$ 的指数只能是 $0,1,2,3,\ldots$ 这样的非负整数，而且总项数有限。

## 正式定义

单变量多项式的一般形式是：

$$
P(x)=a_0+a_1x+a_2x^2+\cdots+a_nx^n.
$$

其中：

- $P(x)$：以 $x$ 为变量的多项式；
- $x$：变量；
- $a_0,a_1,\ldots,a_n$：系数，可取实数、复数或其他指定数域中的元素；
- $n$：非负整数；
- $a_n\ne0$ 时，$n$ 是这个非零多项式的次数；
- $a_0$：常数项；
- $a_n$：最高次项系数，也叫首项系数。

求和符号可把同一公式写成：

$$
P(x)=\sum_{k=0}^{n}a_kx^k.
$$

这里 $k$ 只是给各项编号的求和指标。

## 什么表达式不是多项式

以下表达式不是普通的 $x$ 多项式：

$$
\frac1x,
\qquad
\sqrt{x},
\qquad
e^x,
\qquad
\sin x.
$$

原因分别是：

- $1/x=x^{-1}$ 含负整数次幂；
- $\sqrt{x}=x^{1/2}$ 含非整数次幂；
- $e^x$ 和 $\sin x$ 不是有限个幂次项的和。

不过，$e^x$ 和 $\sin x$ 可以在适当条件下展开成无穷幂级数。无穷级数不是有限多项式。

## 项、系数和次数

考虑：

$$
P(x)=4x^3-7x+2.
$$

它的各项是：

$$
4x^3,
\qquad
-7x,
\qquad
2.
$$

相应系数是 $4,-7,2$。没有写出的二次项可看成系数为零：

$$
P(x)=4x^3+0x^2-7x+2.
$$

最高非零幂是 $x^3$，所以它是三次多项式。

常数多项式 $P(x)=c$ 在 $c\ne0$ 时次数为 $0$。零多项式的所有系数都为零；其次数通常不定义，某些高级语境会约定为负无穷，使用时必须说明约定。

## 多项式的值与多项式函数

把一个数代入变量，就得到多项式在该点的值。

例如：

$$
P(x)=x^2-3x+2.
$$

在 $x=4$ 时：

$$
P(4)=4^2-3\times4+2=6.
$$

多项式表达式可以定义一个函数：输入 $x$，输出 $P(x)$。在通常的实数或复数范围内，同一个多项式表达式确定相应的多项式函数。

## 加法与减法

多项式相加时，把相同幂次的系数相加。

设：

$$
P(x)=2x^2+3x-1,
$$

$$
Q(x)=x^2-5x+4.
$$

则：

$$
P(x)+Q(x)=3x^2-2x+3.
$$

减法同理：

$$
P(x)-Q(x)=x^2+8x-5.
$$

## 乘法

多项式相乘使用分配律，并利用：

$$
x^m x^n=x^{m+n}.
$$

例如：

$$
(x+2)(x+3)
=x^2+3x+2x+6
=x^2+5x+6.
$$

若 $P$ 的次数为 $m$、$Q$ 的次数为 $n$，并且系数所在系统没有导致首项乘积变为零的异常情形，则：

$$
\deg(PQ)=m+n.
$$

对实系数或复系数多项式，这条关系成立。

## 展开与因式分解

把乘积写成各幂次之和叫展开。反过来，把多项式写成若干较简单因子的乘积叫因式分解。

例如：

$$
x^2+5x+6=(x+2)(x+3).
$$

因式分解有助于求根，因为乘积为零时，至少有一个因子为零：

$$
(x+2)(x+3)=0
$$

给出：

$$
x=-2
\quad\text{或}\quad
x=-3.
$$

## 根与因式定理

若：

$$
P(r)=0,
$$

就称 $r$ 是多项式 $P$ 的根或零点。

因式定理说：

$$
P(r)=0
$$

当且仅当：

$$
(x-r)
$$

是 $P(x)$ 的因式。

例如：

$$
P(x)=x^2-1.
$$

因为 $P(1)=0$、$P(-1)=0$，所以：

$$
x^2-1=(x-1)(x+1).
$$

## 实根与复根

多项式是否有根，取决于允许根属于哪一类数。

例如：

$$
x^2+1=0
$$

没有实数根，但有两个复数根：

$$
x=i,
\qquad
x=-i.
$$

代数学基本定理说明：每个次数至少为一的复系数多项式，在复数范围内至少有一个根；计入重数后，$n$ 次多项式共有 $n$ 个复根。

这是标准数学定理，但完整证明需要复分析，本篇不展开证明。

## 多变量多项式

多项式可以含多个变量。例如：

$$
P(x,y)=3x^2y+2xy^2-y+4.
$$

每一项中各变量的指数仍是非负整数。

项 $3x^2y$ 的总次数是：

$$
2+1=3.
$$

整个非零多变量多项式的总次数，是所有非零项总次数的最大值。

物理中的势能和场相互作用经常是多变量或多分量场的多项式。

## 多项式为什么容易计算

多项式只需有限次加法和乘法，因此：

- 容易代入数值；
- 容易求导和积分；
- 容易由计算机处理；
- 可用于近似更复杂的函数。

例如：

$$
P(x)=a_0+a_1x+\cdots+a_nx^n
$$

的导数为：

$$
P'(x)=a_1+2a_2x+\cdots+na_nx^{n-1}.
$$

每次求导仍得到多项式。

## 与幂级数的区别

多项式只有有限项：

$$
a_0+a_1x+\cdots+a_nx^n.
$$

幂级数可以有无限项：

$$
\sum_{n=0}^{\infty}a_nx^n.
$$

有限多项式对所有有限 $x$ 都有明确值。无穷幂级数还必须讨论收敛半径和边界行为。

多项式可以看成只有有限多个非零系数的幂级数，但不能因此忽略一般无穷级数的收敛问题。

## Grassmann 多项式为何会截断

普通变量 $x$ 的各次幂通常都可能不同：

$$
1,x,x^2,x^3,\ldots
$$

一个 Grassmann 变量 $\theta$ 满足：

$$
\theta^2=0.
$$

因此它的任意多项式都会自动化简为：

$$
f(\theta)=a+b\theta.
$$

所有二次及更高次项都为零。

若有两个 Grassmann 变量 $\theta_1,\theta_2$，一般多项式可写成：

$$
f(\theta_1,\theta_2)
=a+b_1\theta_1+b_2\theta_2+c\theta_1\theta_2.
$$

因为每个变量重复出现就会因平方为零而消失，而且交换两个变量会变号。这说明“多项式”的概念可以保留，但乘法规则不再是普通对易变量的规则。

## 一个计算例子

设：

$$
P(x)=2x^2-x+3,
\qquad
Q(x)=x-2.
$$

相乘：

$$
P(x)Q(x)
=(2x^2-x+3)(x-2).
$$

逐项展开：

$$
=2x^3-4x^2-x^2+2x+3x-6.
$$

合并同类项：

$$
P(x)Q(x)=2x^3-5x^2+5x-6.
$$

可用 $x=2$ 检查：左边因为 $Q(2)=0$ 而等于零；右边也有：

$$
2\times2^3-5\times2^2+5\times2-6=0.
$$

## 适用条件与事实边界

1. 普通多项式要求变量指数是非负整数，并且只有有限项。
2. 系数属于哪个数域或代数必须说明；实系数与复系数多项式的可用根范围不同。
3. $1/x$、$\sqrt{x}$、$e^x$ 和 $\sin x$ 不是有限多项式，即使其中一些能展开为幂级数。
4. 零多项式的次数通常不定义，不能直接套用普通次数运算规则。
5. 多项式近似只在指定范围和误差条件下可靠，不能把局部近似当成全局恒等式。
6. Grassmann 多项式仍是有限代数表达式，但变量反对易，不能擅自交换因子的顺序。
7. 代数学基本定理保证的是复数范围内的根；它不保证所有根都是实数。

## 常见误区

1. **任何含 $x$ 的代数式都是多项式。** 含负次幂、分数次幂或变量在分母中的表达式通常不是普通多项式。
2. **没有写出的幂次不存在。** 可以把它看成对应系数为零。
3. **多项式与方程是一回事。** 多项式是表达式；写成 $P(x)=0$ 后才形成方程。
4. **$n$ 次多项式一定有 $n$ 个不同实根。** 根可能是复数，也可能重复。
5. **无穷幂级数就是无限次多项式，可以不管收敛。** 无穷级数必须讨论是否收敛。
6. **$\theta^2=0$ 说明 Grassmann 变量等于零。** Grassmann 变量不是普通实数，它可以非零但幂零。
7. **多项式近似与原函数处处完全相等。** 截断近似通常只在指定区域内达到某个精度。

## 练习

1. 指出 $5x^4-2x^2+x-7$ 的次数、首项系数和常数项。
2. 判断 $x^{-1}+2$、$\sqrt{x}+1$、$3x^2-x$ 中哪些是 $x$ 的普通多项式。
3. 展开 $(x-3)(x+4)$。
4. 因式分解 $x^2-9$，并求其根。
5. 计算 $P(x)=x^3-2x+1$ 在 $x=2$ 时的值。
6. 说明有限多项式和无穷幂级数的主要区别。
7. 若 $\theta^2=0$，化简 $3+2\theta+5\theta^2-\theta^3$。

## 答案与提示

1. 次数为 $4$，首项系数为 $5$，常数项为 $-7$。
2. 只有 $3x^2-x$ 是普通多项式；前两个分别含负整数次幂和分数次幂。
3. $(x-3)(x+4)=x^2+x-12$。
4. $x^2-9=(x-3)(x+3)$，根为 $3$ 和 $-3$。
5. $P(2)=8-4+1=5$。
6. 多项式项数有限；无穷幂级数含无限项，还必须判断收敛范围。
7. 因为 $\theta^2=0$，更高次幂也为零，结果是 $3+2\theta$。

## 与其他概念的关系

- [[方程]] 可由令多项式等于某个值形成；
- [[函数]] 包含由多项式定义的多项式函数；
- [[复数]] 为多项式提供更完整的根所在范围；
- [[级数]] 把有限多项式推广到可能含无限多项的表达式；
- [[Laurent 级数]] 还允许变量出现负整数次幂；
- [[Grassmann 变量]] 的幂零性质使相关多项式自动在有限阶截断。

## 参考资料

- [OpenStax, Algebra and Trigonometry 2e, Polynomial and Rational Functions](https://openstax.org/details/books/algebra-and-trigonometry-2e)
- [OpenStax, College Algebra 2e](https://openstax.org/details/books/college-algebra-2e)
- Michael Artin, *Algebra*, 2nd ed., Pearson, 2011.
- Serge Lang, *Algebra*, Revised 3rd ed., Springer, 2002.
- Felix Berezin, *Introduction to Superanalysis*, D. Reidel, 1987.
