---
aliases: [注意力分数, 注意力权重, Attention Score, Attention Weight]
tags: [概念, Transformer, 大模型, 学习教程]
level: L7
prerequisites: ["[[标量向量矩阵与张量]]", "[[逐元素运算与广播]]", "[[Logit概率与采样]]"]
---
# 注意力分数与注意力权重

## 一句话定义

注意力分数（Attention Score）是 Query 与 Key 的匹配数值；注意力权重（Attention Weight）是分数经过缩放、掩码和 softmax 后得到的非负权重。

## 完整顺序

$$
S=\frac{QK^T}{\sqrt{d_k}}+M
$$

$$
A=\operatorname{softmax}(S)
$$

$$
O=AV
$$

- $S$：分数矩阵；
- $M$：掩码；
- $A$：每行总和为 1 的权重矩阵；
- $O$：对 Value 的加权和。

## 一个小例子

某个 Query 对两个 Key 的分数为 `[2,0]`，softmax 后约 `[0.881,0.119]`。若 Value 是 `[10,20]`，输出加权和约为：

$$
0.881\times10+0.119\times20=11.19
$$

注意力不是只“选中”一个 Value，而是通常混合多个 Value。

## 重要边界

- attention weights 是一次前向计算的中间量，不是持久参数；
- 权重高表示该层该头在当前计算中分配更多数值权重，不自动证明人类语义上的因果解释；
- 掩码后未来位置应得到 0 权重，但浮点实现细节要通过测试确认。

## 最小自测

题目：为什么掩码位置要加极大负数而不是 0？

答案：加 0 仍可能在 softmax 后得到正权重；极大负数的指数趋近 0。

## 详细教程

[[缩放点积注意力|缩放点积注意力]]

## 来源

- Vaswani et al. (2017), *Attention Is All You Need*：https://arxiv.org/abs/1706.03762 `[A]`
