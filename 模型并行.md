---
aliases: [模型并行, Tensor Parallelism, Pipeline Parallelism, Expert Parallelism]
tags: [大模型, 分布式训练, 教程, 学习教程]
prerequisites: ["[[训练系统概览]]", "[[数据并行]]", "[[模型并行与通信|模型并行与通信]]", "[[矩阵乘法与仿射层公式|矩阵乘法公式]]"]
next: ["[[混合精度训练]]"]
---
# 模型并行

## 为什么要拆模型

数据并行让每张 GPU 各放一份完整模型。如果模型参数、梯度、优化器状态和激活连一张卡都放不下，就要拆模型本身。模型并行（Model Parallelism）的核心问题不是“能不能切”，而是：**切开后需要传什么、多久传一次、哪台设备会等待。**

先读：[[模型并行与通信|模型并行与通信]]。

## 1. 张量并行

张量并行（Tensor Parallelism，TP）把同一层的矩阵运算分到多台设备。完整公式与通信含义见 [[并行切分通信与流水线效率公式|并行切分、通信与流水线效率公式]]。

### 列并行：拆输出维

线性层：

$$
Y=XW
$$

把 $W$ 按输出列切开：

$$
W=[W_1\;W_2]
$$

GPU0 算 $Y_1=XW_1$，GPU1 算 $Y_2=XW_2$，最后按列拼成 $Y=[Y_1\;Y_2]$。

例子：输入维 4、输出维 8，分到 2 张卡。每张卡保存形状 $4\times4$ 的权重，并产生 4 维输出分片。若下一层也能接收分片，可以延后 All-Gather；否则需要收集成完整 8 维向量。

### 行并行：拆输入维

把输入和权重沿输入维切开：

$$
X=[X_1\;X_2],\qquad
W=\begin{bmatrix}W_1\\W_2\end{bmatrix}
$$

每张卡算同一输出的一部分：

$$
Y=X_1W_1+X_2W_2
$$

因此需要求和型通信。Megatron-LM 将列并行和行并行配对，用于拆分注意力投影和 FFN，避免在每个小运算后都恢复完整张量 `[A]`。

### 为什么需要高速互联

张量并行通常在每层发生通信。粗略通信时间可理解为“固定启动延迟 + 数据量/有效带宽”。如果单次计算很短而通信频繁，多加 GPU 反而可能更慢。

## 2. 流水线并行

流水线并行（Pipeline Parallelism，PP）把连续层分给不同设备：

```text
阶段0: 第1-8层
阶段1: 第9-16层
阶段2: 第17-24层
阶段3: 第25-32层
```

如果整批一次通过，只有一台设备工作，其他设备等待。解决方法是把批量拆为微批次（Micro-batch）：

```text
时刻1: 阶段0处理微批1
时刻2: 阶段0处理微批2；阶段1处理微批1
时刻3: 阶段0处理微批3；阶段1处理微批2；阶段2处理微批1
```

### Pipeline Bubble

开始时后段无数据，结束时前段已完成，这些空闲叫流水线气泡（Pipeline Bubble）。在最简单的等耗时前向模型中，4 个阶段、8 个微批次的有效计算占比约为：

$$
\frac{8}{8+4-1}\approx72.7\%
$$

增加微批次可降低填充和排空所占比例，但会影响激活保存、调度和全局批量语义。

### 1F1B

1F1B（One Forward One Backward）在稳定阶段交替运行一个前向和一个反向，减少同时保留的激活数量。它仍有预热、排空和阶段不均衡，不是“气泡归零”。

## 3. 专家并行

专家并行（Expert Parallelism，EP）把 MoE 的不同专家放到不同设备。路由器为 token 选择专家后，要执行 All-to-All：每台设备把不同 token 发给对应专家，再把专家输出送回。

这与 [[MoE路由与专家负载|MoE 路由与专家负载]] 紧密相关。若 40% token 都选中同一设备上的专家，该设备会成为慢点；即使平均通信量不大，尾部不均衡也会让其他设备等待。

## 4. 组合并行

大型训练常组成多维设备网格。例如 64 张卡可拆成：

```text
数据并行 4 × 流水线并行 4 × 张量并行 4 = 64
```

若还有专家并行，设备映射会更复杂。一般倾向把通信最频繁的张量并行放在节点内高速链路，把较低频的数据并行通信跨节点安排，但具体最优方案取决于真实拓扑和测量结果 `[C]`。

## 5. 怎样选择

| 问题 | 更相关的并行方式 |
|---|---|
| 单层权重或激活太大 | 张量并行 |
| 层数多，整模型放不下 | 流水线并行 |
| MoE 专家总参数太大 | 专家并行 |
| 模型能放下，但训练吞吐不足 | 数据并行 |

选择前要测量：各层耗时、激活峰值、节点内外带宽、集合通信时间、序列长度分布和专家负载。

## 6. 常见失败

- 只按参数量平均分层，忽略不同层耗时；
- TP 跨越低带宽节点，通信压过计算；
- 微批次太少，流水线气泡很大；
- 微批次太小，矩阵乘法不能充分利用硬件；
- 专家负载不均，All-to-All 出现长尾；
- 只报告理论加速，不报告端到端吞吐和硬件拓扑。

## 概念与公式导航

- [[模型并行与通信|模型并行与通信]]
- [[并行切分通信与流水线效率公式|并行切分、通信与流水线效率公式]]

## 练习

1. 张量并行和流水线并行分别拆什么？
2. 为什么张量并行需要高速互联？
3. pipeline bubble 是什么？
4. MoE 为什么需要专家并行？

答案：[[训练系统与规模化答案#03-模型并行|模型并行答案]]

## 来源

- Shoeybi et al. (2019), *Megatron-LM*：https://arxiv.org/abs/1909.08053 `[A]`
- Huang et al. (2019), *GPipe*：https://arxiv.org/abs/1811.06965 `[A]`
- Narayanan et al. (2021), *Efficient Large-Scale Language Model Training on GPU Clusters Using Megatron-LM*：https://arxiv.org/abs/2104.04473 `[A]`
- Lepikhin et al. (2021), *GShard*：https://arxiv.org/abs/2006.16668 `[A]`
- 并行拓扑选择属于依赖具体硬件和工作负载的工程决策 `[C]`
