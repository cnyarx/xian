---
aliases: [注意力, Attention, Query, Key, Value]
tags: [Transformer, 注意力, 教程, 大模型, 学习教程]
prerequisites: ["[[语言模型的基本思想]]", "[[点积与矩阵乘法]]"]
next: ["[[缩放点积注意力]]"]
---
# 注意力直觉

## 一句话定义

注意力（Attention）让每个 token 根据当前需要，从其他 token 中挑选相关信息并按重要程度加权汇总。

## 一个句子

```text
小明把书递给小红，因为她要复习。
```

理解“她”时，需要重点关注“小红”，不必同等关注“书”或“递给”。注意力就是让模型动态决定关注谁。

## Query、Key、Value

可以类比图书馆搜索：

- Query（查询）：我正在找什么。
- Key（键）：每本书的索引标签，用来与查询比较。
- Value（值）：书中真正要取回的内容。

每个 token 的隐藏向量经过三个不同线性变换：

$$Q=XW_Q,\quad K=XW_K,\quad V=XW_V$$

模型用 Query 和所有 Key 比较相关程度，再按相关程度混合对应 Value。

## 三步过程

1. **匹配**：$QK^T$，每个 Query 与所有 Key 做点积。
2. **归一化**：softmax 把分数变成总和为 1 的权重。
3. **汇总**：权重乘 Value 并求和。

简写：

$$\operatorname{Attention}(Q,K,V)=\operatorname{softmax}(QK^T)V$$

完整公式还需要缩放因子，下一篇解释。

## 自注意力

Query、Key、Value 都来自同一段序列时，叫自注意力（Self-Attention）。“自”表示序列内部彼此关注，不表示模型具有自我意识。

## 为什么比固定窗口灵活

卷积或早期循环模型的信息传递路线较固定。自注意力可以让远距离 token 在一层内直接建立联系。但标准全注意力的计算和显存会随序列长度平方增长。

## 常见误区

- 注意力权重不是人类注意力的证明。
- 权重高说明该层该头在此次计算中贡献较大，不一定等于可解释的因果理由。
- Query、Key、Value 是向量角色，不是三个独立数据库。

## 概念与公式导航

- [[注意力分数与注意力权重|注意力分数与注意力权重]]

## 练习

1. 用图书馆比喻解释 Q、K、V。
2. 对“苹果公司发布了手机，它很昂贵”，哪个词可能关注“苹果公司”？
3. 自注意力中的“自”是什么意思？
4. 注意力为什么能处理长距离关系？代价是什么？

## 来源

- Bahdanau, D. et al. (2015). *Neural Machine Translation by Jointly Learning to Align and Translate*. ICLR https://arxiv.org/abs/1409.0473 `[A]`
- Vaswani, A. et al. (2017). *Attention Is All You Need*. https://arxiv.org/abs/1706.03762 `[A]`