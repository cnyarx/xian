---
aliases: [Limit]
english_name: Limit
tags: [数学, 微积分, 弦理论先修, 弦理论]
prerequisites:
  - "[[函数]]"
  - "[[区间]]"
  - "[[数列]]"
used_by:
  - "[[导数]]"
  - "[[积分]]"
  - "[[级数]]"
evidence_status: 教材事实
---

# 极限（Limit）

## 一句话解释

极限描述输入越来越接近某个位置时，函数值越来越接近什么结果。

## 为什么要学它

瞬时速度不能用真正为零的时间间隔相除，无穷级数也没有最后一项。极限让我们严谨处理“无限接近”，从而定义导数、积分、连续性和级数收敛。

## 函数极限

$$
\lim_{x\to a}f(x)=L
$$

表示：只要让 $x$ 足够接近 $a$，但不要求 $x=a$，就能让 $f(x)$ 任意接近 $L$。

例如：

$$
\lim_{x\to2}(3x+1)=7.
$$

这里函数连续，可以直接代入。但“直接代入”是计算方法，不是极限的定义。

## 极限可以与函数在该点的值不同

设：

$$
f(x)=\frac{x^2-1}{x-1},\qquad x\ne1.
$$

因式分解后，在 $x\ne1$ 时：

$$
f(x)=x+1.
$$

所以：

$$
\lim_{x\to1}\frac{x^2-1}{x-1}=2.
$$

原式在 $x=1$ 没有定义，但极限仍存在，因为极限研究附近趋势。

## 左极限和右极限

$$
\lim_{x\to a^-}f(x)
$$

表示从左侧接近，

$$
\lim_{x\to a^+}f(x)
$$

表示从右侧接近。只有两侧极限存在且相等，双侧极限才存在。

例如阶跃函数在跳跃点左右趋向不同数，因此双侧极限不存在。

## 无穷远处的极限

$$
\lim_{x\to\infty}f(x)=L
$$

表示 $x$ 越来越大时，$f(x)$ 越来越接近 $L$。这里 $x$ 不会“到达无穷”，因为 $\infty$ 不是实数。

例如：

$$
\lim_{x\to\infty}\frac1x=0.
$$

每个有限 $x>0$ 时 $1/x$ 都不等于零，但它可以任意接近零。

## 数列极限

$$
\lim_{n\to\infty}a_n=L
$$

表示编号 $n$ 足够大时，数列项 $a_n$ 任意接近 $L$。无穷级数则通过部分和数列的极限定义。

## 连续性

函数 $f$ 在 $a$ 处连续，要求：

$$
\lim_{x\to a}f(x)=f(a).
$$

这包含三件事：$f(a)$ 有定义、极限存在、二者相等。直观上，图像在该点没有跳跃或孔洞。

## 极限运算法则

若 $\lim f(x)=A$、$\lim g(x)=B$，则在条件满足时：

$$
\lim(f+g)=A+B,
$$

$$
\lim(fg)=AB,
$$

$$
\lim\frac fg=\frac AB\quad(B\ne0).
$$

遇到 $0/0$ 时不能把它当成答案；它是不定式，提示需要化简或使用其他方法。

## 精确含义

$\lim_{x\to a}f(x)=L$ 的标准定义是：对任意 $\varepsilon>0$，都存在 $\delta>0$，使得当

$$
0<|x-a|<\delta
$$

时必有：

$$
|f(x)-L|<\varepsilon.
$$

说人话：无论你要求输出离 $L$ 多近，我都能规定输入离 $a$ 足够近来保证它。

## 物理中的例子

瞬时速度：

$$
v(t)=\lim_{\Delta t\to0}
\frac{x(t+\Delta t)-x(t)}{\Delta t}.
$$

$\Delta t$ 始终先取非零值，再研究趋近零的结果，所以没有进行除以零。

## 常见误区

1. **趋近等于最终取到。** 错，$1/x$ 趋近零但有限 $x$ 时不取零。
2. **函数在一点没定义，极限就不存在。** 错，极限看附近。
3. **左右极限分别存在就够了。** 还必须相等。
4. **$0/0=0$。** 错，它是不定式。
5. **$x\to\infty$ 表示把无穷当作普通数代入。** 错。

## 练习

1. 求 $\lim_{x\to3}(2x-1)$。
2. 求 $\lim_{x\to2}(x^2-4)/(x-2)$。
3. 若左右极限分别为 $1$ 和 $2$，双侧极限是否存在？
4. 解释为什么瞬时速度定义没有除以零。
5. $a_n=1/n$ 是否在某个有限 $n$ 等于其极限？

## 答案与提示

1. $5$。
2. 化简为 $x+2$，极限为 $4$。
3. 不存在。
4. 先在 $\Delta t\ne0$ 时取商，再研究其趋近零的极限。
5. 不等于；每项为正，极限为零。

## 与其他概念的关系

- [[导数]] 是差商的极限；
- [[积分]] 可由小区间求和的极限定义；
- [[级数]] 的和是部分和数列的极限；
- 连续性和各种近似都依赖极限。

## 参考资料

- [OpenStax, Calculus Volume 1, Chapter 2](https://openstax.org/books/calculus-volume-1/pages/2-introduction)
- [MIT OCW 18.01SC, Limits and Continuity](https://ocw.mit.edu/courses/18-01sc-single-variable-calculus-fall-2010/)
