---
aliases:
  - Differential Geometry
english_name: Differential Geometry
tags:
  - 数学
  - 微分几何
  - 几何学
  - 弦理论先修
  - 弦理论
prerequisites:
  - "[[流形]]"
  - "[[切空间]]"
  - "[[余切空间]]"
  - "[[向量场]]"
  - "[[微分形式]]"
  - "[[外微分]]"
  - "[[度量]]"
  - "[[联络]]"
  - "[[协变导数]]"
  - "[[曲率]]"
used_by:
  - "[[黎曼曲面]]"
  - "[[群作用]]"
  - "[[模空间]]"
  - "[[广义相对论]]"
  - "[[规范理论]]"
  - "[[世界面几何]]"
  - "[[非线性 sigma model]]"
  - "[[紧致化]]"
evidence_status: 教材事实
---

# 微分几何（Differential Geometry）

## 一句话解释

微分几何用微积分研究光滑空间：先把弯曲空间拆成每一点附近可计算的线性近似，再用度量、联络、微分形式和曲率把局部信息拼成整体几何。

## 为什么要学它

普通微积分主要在 $\mathbb R^n$ 中工作。弦理论却需要处理：

- 弯曲目标时空；
- 二维弦世界面；
- 紧致额外维度；
- 规范场所在的向量丛与主丛；
- 自旋量和自旋联络；
- 曲率、拓扑与通量；
- 不同坐标中形式不同、物理意义相同的方程。

微分几何提供一套共同语言，使这些对象能在任意光滑坐标中一致描述。

## 直观图景

地球表面整体是弯的，但在一座城市的尺度内，地面可近似看成平面。

微分几何反复使用这个思想：

1. 在一点附近用切空间做线性近似；
2. 用张量记录与坐标无关的局部数据；
3. 用联络比较不同点的向量；
4. 用曲率测量比较规则的路径依赖；
5. 用积分和拓扑把局部数据汇总成整体结论。

切空间像每一点附带的一张“小平面草稿纸”。流形可能弯曲，但草稿纸是线性的，因此可以在上面使用线性代数。

## 正式研究对象

微分几何通常研究带有额外几何结构的光滑流形：

$$
(M,\text{smooth structure},\text{geometric structures}).
$$

其中几何结构可以包括：

- Riemannian 或 pseudo-Riemannian 度量 $g$；
- 线性联络 $\nabla$；
- 微分形式 $\omega$；
- 辛形式 $\omega_{\mathrm{symp}}$；
- 复结构 $J$；
- 向量丛或主丛上的联络；
- 群作用和对称性。

“微分几何”是一个学科总称，不是单一张量。本篇解释它的共同方法和核心结构，各具体概念由独立文件详细展开。

## 每个符号是什么意思

- $M$：光滑流形；
- $p\in M$：流形上的一点；
- $T_pM$：点 $p$ 处的切空间；
- $T_p^*M$：点 $p$ 处的余切空间；
- $TM$、$T^*M$：切丛与余切丛；
- $g$：度量张量；
- $\nabla$：联络或协变导数规则；
- $R$：联络的曲率；
- $\Omega^p(M)$：$M$ 上的光滑 $p$ 形式空间；
- $d$：外微分；
- $X,Y,Z$：向量场；
- $\phi:M\to N$：流形之间的光滑映射。

本文使用爱因斯坦求和约定：同一项中重复的一上一下指标表示求和。

## 第一层：拓扑告诉我们什么

拓扑先规定：

- 哪些集合是开集；
- 哪些点彼此邻近；
- 什么叫连续；
- 空间有几个连通分支；
- 是否存在不能连续收缩掉的环路或高维周期。

拓扑不自动给出长度、角度和曲率。

圆和椭圆在拓扑上同胚，因为可连续变形到彼此。作为平面中的嵌入曲线，它们的总长度和外蕴曲率分布通常不同；但作为一维 Riemannian 流形，它们的内蕴 Riemann 曲率都恒为零。

## 第二层：光滑结构允许做微积分

光滑流形由一组坐标图覆盖：

$$
\varphi_\alpha:U_\alpha\to\mathbb R^n.
$$

不同坐标图重叠时，过渡映射：

$$
\varphi_\beta\circ\varphi_\alpha^{-1}
$$

必须足够光滑。

这保证在不同坐标中求导可以通过链式法则一致转换。

光滑结构让我们定义：

- 光滑函数；
- 切向量和向量场；
- 微分形式；
- 光滑映射的微分；
- Lie 括号和外微分。

但它仍不自动给出距离和角度。

## 第三层：切空间做局部线性化

点 $p$ 附近的流形由切空间：

$$
T_pM
$$

作一阶近似。

曲线：

$$
\gamma(t),\qquad \gamma(0)=p
$$

的速度：

$$
\dot\gamma(0)\in T_pM
$$

是切向量。

在局部坐标 $x^i$ 中：

$$
\dot\gamma(0)
=\left.\frac{dx^i}{dt}\right|_{t=0}\partial_i.
$$

切空间是线性空间，所以同一点的切向量可以相加、数乘，并可使用矩阵表示线性映射。

## 余切空间记录线性测量

余切空间：

$$
T_p^*M
$$

由作用在切向量上的线性函数组成。

若 $f$ 是光滑函数，它在 $p$ 点的微分：

$$
df_p\in T_p^*M
$$

满足：

$$
df_p(v)=v[f].
$$

没有度量时，$df_p$ 自然是一形式，而不是自然的梯度向量。选定非退化度量后，才可定义：

$$
\operatorname{grad}f=(df)^\sharp.
$$

## 张量为什么重要

张量是在切向量和余切向量之间进行多线性运算的几何对象。

例如 $(r,s)$ 型张量在一点可看成：

$$
T_p:
\underbrace{T_p^*M\times\cdots\times T_p^*M}_{r\text{ 个}}
\times
\underbrace{T_pM\times\cdots\times T_pM}_{s\text{ 个}}
\to\mathbb R.
$$

张量分量会随坐标改变，但整个多线性对象不依赖坐标选择。

度量、微分形式、曲率和能量动量张量都属于张量体系。

## 坐标与几何对象的区别

坐标是给点编号的方法，几何对象是被坐标描述的对象。

例如同一 Euclidean 平面在直角坐标中：

$$
ds^2=dx^2+dy^2,
$$

在极坐标中：

$$
ds^2=dr^2+r^2d\theta^2.
$$

公式形式不同，但描述的是同一个平直度量。

因此不能只看分量是否变化来判断几何是否变化。

## 光滑映射的微分

给定光滑映射：

$$
\phi:M\to N,
$$

它在点 $p$ 诱导线性映射：

$$
d\phi_p:T_pM\to T_{\phi(p)}N.
$$

它把定义域中的速度送到值域中的速度：

$$
d\phi_p(\dot\gamma(0))
=\frac{d}{dt}\bigg|_{t=0}(\phi\circ\gamma)(t).
$$

在坐标中，$d\phi_p$ 由 Jacobi 矩阵表示。

## 推前与拉回

光滑映射自然把切向量向前推：

$$
\phi_*:T_pM\to T_{\phi(p)}N.
$$

微分形式则向后拉：

$$
\phi^*:\Omega^k(N)\to\Omega^k(M).
$$

对一形式 $\alpha$：

$$
(\phi^*\alpha)_p(v)
=\alpha_{\phi(p)}(d\phi_p(v)).
$$

拉回之所以重要，是因为它允许把目标空间的几何量搬到定义域上。

## 嵌入与诱导度量

若：

$$
X:\Sigma\to M
$$

把流形 $\Sigma$ 浸入带度量 $G$ 的流形 $M$，则可拉回度量：

$$
g=X^*G.
$$

局部分量为：

$$
g_{ab}
=G_{\mu\nu}(X)
\partial_aX^\mu\partial_bX^\nu.
$$

若 $X$ 是浸入且相应限制非退化，$g$ 给出 $\Sigma$ 的诱导度量。

弦世界面的诱导度量正是这个公式的物理应用。

## 度量加入长度和角度

Riemannian 度量在每一点给出正定内积：

$$
g_p:T_pM\times T_pM\to\mathbb R.
$$

它定义向量长度：

$$
\|v\|=\sqrt{g(v,v)},
$$

以及曲线长度：

$$
L(\gamma)
=\int_a^b\sqrt{g(\dot\gamma,\dot\gamma)}\,dt.
$$

pseudo-Riemannian 度量放弃正定，但保持对称和非退化。Lorentzian 时空度量属于这一类。

## 度量不等于距离函数

度量张量 $g_p$ 作用在同一点的切向量上。

距离函数：

$$
d(p,q)
$$

作用在两点上。

正定 Riemannian 度量可通过曲线长度的下确界诱导距离：

$$
d_g(p,q)
=\inf_{\gamma:p\to q}L(\gamma).
$$

Lorentzian 度量不能照搬这个公式得到普通度量空间意义的距离。

## 联络解决跨点比较

若 $p\ne q$，则：

$$
T_pM\ne T_qM
$$

是两个不同向量空间。没有附加结构时，$v_p-v_q$ 没有天然定义。

联络 $\nabla$ 规定向量场怎样沿方向变化：

$$
\nabla_XY.
$$

在坐标基中：

$$
\nabla_iY^k
=\partial_iY^k+\Gamma^k{}_{ij}Y^j.
$$

修正项补偿坐标基自身的变化。

## 协变导数扩张到张量

对一形式：

$$
\nabla_i\alpha_j
=\partial_i\alpha_j
-\Gamma^k{}_{ij}\alpha_k.
$$

对一般张量：

- 每个上标贡献一个 $+\Gamma$ 项；
- 每个下标贡献一个 $-\Gamma$ 项；
- 分量本身还要做普通偏导。

这个规则由联络、对偶配对、张量积 Leibniz 法则和缩并相容性唯一决定。

## Levi-Civita 联络

给定 Riemannian 或 pseudo-Riemannian 度量 $g$，存在唯一联络同时满足：

1. 无挠：$T=0$；
2. 度量相容：$\nabla g=0$。

它称为 Levi-Civita 联络。

坐标系数为：

$$
\Gamma^k{}_{ij}
=\frac12g^{k\ell}
\left(
\partial_i g_{j\ell}
+\partial_j g_{i\ell}
-\partial_\ell g_{ij}
\right).
$$

“度量决定联络”只在这两个附加条件下成立。

## 平行移动

沿曲线 $\gamma(t)$ 的向量场 $V(t)$ 若满足：

$$
\frac{DV}{dt}=0,
$$

就称 $V$ 沿曲线平行。

坐标中：

$$
\frac{DV^k}{dt}
=\frac{dV^k}{dt}
+\Gamma^k{}_{ij}\dot x^iV^j.
$$

平行移动由联络决定，通常依赖所走路径。

## 测地线

测地线是速度向量沿自身平行的曲线：

$$
\frac{D\dot\gamma}{dt}=0.
$$

坐标方程为：

$$
\frac{d^2x^k}{dt^2}
+\Gamma^k{}_{ij}
\frac{dx^i}{dt}
\frac{dx^j}{dt}=0.
$$

对 Levi-Civita 联络，适当参数化的测地线也是能量泛函的驻值曲线；在 Riemannian 情形，足够短的测地线局部最短，但一般测地线不保证全局最短。

## 挠率

联络的挠率为：

$$
T(X,Y)
=\nabla_XY-\nabla_YX-[X,Y].
$$

它测量协变导数反对称部分与 Lie 括号之间的差。

无挠不等于无曲率。标准球面的 Levi-Civita 联络无挠，但曲率非零。

## 曲率

联络曲率定义为：

$$
R(X,Y)Z
=\nabla_X\nabla_YZ
-\nabla_Y\nabla_XZ
-\nabla_{[X,Y]}Z.
$$

曲率测量协变导数顺序交换后剩下的差异，也控制小闭合回路平行移动的最低阶净变化。

坐标分量含：

$$
R\sim \partial\Gamma+\Gamma\Gamma.
$$

这只是结构简写；完整指标公式见 [[曲率]]。

## 平直不等于系数为零

在 Euclidean 平面的极坐标中：

$$
\Gamma^r{}_{\theta\theta}=-r,
\qquad
\Gamma^\theta{}_{r\theta}=\frac1r,
$$

但曲率仍为零。

联络系数依坐标，曲率是张量。某坐标中的 $\Gamma\ne0$ 不能单独证明空间弯曲。

## 微分形式

$p$ 形式是完全反对称的 $(0,p)$ 型张量。

一形式：

$$
\alpha=\alpha_i dx^i.
$$

二形式常写成：

$$
\omega=\frac12\omega_{ij}dx^i\wedge dx^j.
$$

楔积满足分次反交换：

$$
\alpha\wedge\beta
=(-1)^{pq}\beta\wedge\alpha
$$

当 $\alpha$ 是 $p$ 形式、$\beta$ 是 $q$ 形式时成立。

## 外微分

外微分把 $p$ 形式变成 $(p+1)$ 形式：

$$
d:\Omega^p(M)\to\Omega^{p+1}(M).
$$

它满足：

$$
d^2=0,
$$

以及分次 Leibniz 法则：

$$
d(\alpha\wedge\beta)
=d\alpha\wedge\beta
+(-1)^p\alpha\wedge d\beta.
$$

外微分只依赖光滑结构，不需要选择联络。

## Stokes 定理

微积分基本定理、Green 定理、Gauss 散度定理和经典 Stokes 定理可统一写成：

$$
\int_M d\omega
=\int_{\partial M}\omega.
$$

这里要求流形、边界、定向、形式次数和支撑或收敛条件合适。

它把区域内部的微分信息与边界积分联系起来。

## 局部与整体的差别

Poincaré 引理说明，在可缩的小区域内，闭形式通常是恰当形式：

$$
d\omega=0
\quad\Longrightarrow\quad
\omega=d\eta
$$

对正次数形式局部成立。

但全局可能失败。闭但不全局恰当的形式记录拓扑障碍，其等价类构成 de Rham 上同调：

$$
H^p_{\mathrm{dR}}(M)
=\frac{\ker(d:\Omega^p\to\Omega^{p+1})}
{\operatorname{im}(d:\Omega^{p-1}\to\Omega^p)}.
$$

这展示了微分几何与拓扑之间的联系。

## Lie 导数

Lie 导数沿向量场 $X$ 的流比较张量，记为：

$$
\mathcal L_X.
$$

它不需要联络。

对函数：

$$
\mathcal L_Xf=X[f].
$$

对向量场：

$$
\mathcal L_XY=[X,Y].
$$

对称性常用 Lie 导数表达。例如度量满足：

$$
\mathcal L_Xg=0
$$

时，$X$ 是 Killing 向量场。

## Lie 导数与协变导数不能混同

二者都描述“沿方向变化”，但比较方式不同：

- $\nabla_X$ 依赖联络，适合平行移动、测地线和曲率；
- $\mathcal L_X$ 依赖 $X$ 的流，不需要联络，适合对称性和不变性；
- 对函数两者都等于 $X[f]$；
- 对一般张量两者通常不同。

## 内蕴几何与外蕴几何

**内蕴几何**只使用流形上已经给定的度量，以及由它决定的 Levi-Civita 联络和 Riemann 曲率等数据。度量可以从嵌入和环境度量拉回得到，但“诱导”描述的是它的构造来源；度量一旦给定，仅依赖该度量计算的量就是内蕴的。

**外蕴几何**研究流形怎样嵌入更高维环境空间，例如法向量、第二基本形式和平均曲率。

二维圆柱面在三维空间中看起来弯曲，但它的内蕴 Gaussian 曲率为零。把纸卷成圆柱不会拉伸纸面，因此纸面上的小生物仅靠内在距离测量不能发现局部 Gaussian 曲率。

球面则不能无拉伸地摊平成平面，具有正的内蕴曲率。

## Gaussian 曲率的特殊意义

对 Euclidean 三维空间 $\mathbb R^3$ 中的正则曲面，Gaussian 曲率 $K$ 可由两个主曲率乘积定义：

$$
K=k_1k_2.
$$

若曲面位于一般弯曲的 Riemannian 环境空间中，Gauss 方程还会加入环境空间沿该切平面的截面曲率，不能只保留主曲率乘积。

Gauss 的卓越定理说明，在 $\mathbb R^3$ 的曲面情形中，$K$ 实际可以完全由曲面自身的度量及其导数计算，因此是内蕴量。

这说明某些看似依赖外部嵌入的弯曲信息，能够由内部距离测量恢复。

## 二维中的曲率简化

本节固定分量约定：

$$
R(\partial_c,\partial_d)\partial_b
=R^a{}_{bcd}\partial_a,
$$

$$
R_{abcd}=g_{ae}R^e{}_{bcd},
\qquad
R_{bd}=R^a{}_{bad}.
$$

在此约定下，二维 Riemannian 流形的 Riemann 曲率张量由 Gaussian 曲率完全决定：

$$
R_{abcd}
=K(g_{ac}g_{bd}-g_{ad}g_{bc}).
$$

Ricci 张量和标量曲率为：

$$
R_{ab}=Kg_{ab},
$$

$$
R=2K.
$$

若资料整体反转 Riemann 曲率定义，上述相关符号也会整体改变。二维世界面因此具有许多特殊简化。

## Gauss–Bonnet 的入口

对紧致、定向、无边界二维 Riemannian 流形：

$$
\int_M K\,dA
=2\pi\chi(M).
$$

左边是局部曲率的整体积分，右边是拓扑不变量 Euler 示性数。

有边界时还要加入测地曲率边界项；非紧致或有奇点时需要额外条件。

## 对称性怎样进入微分几何

群 $G$ 可通过光滑变换作用在流形 $M$ 上：

$$
G\times M\to M.
$$

若作用保持度量，就得到等距对称；若只保持角度结构，就得到共形对称。

连续群的一参数子群产生向量场。其无穷小对称条件通常由 Lie 导数表达。

对称性可：

- 简化坐标和方程；
- 产生守恒量；
- 帮助分类几何结构；
- 定义商空间和模空间。

## 坐标不变性不等于物理对称性

把同一几何对象改用另一套坐标描述，是描述方式变化。

物理对称性则把一个物理配置映到另一个在相关意义下等价或能量相同的配置。

在广义协变理论中二者关系细致，不能仅凭“公式在换坐标后形式相同”就断言存在新的可观测守恒量。

## Riemannian、Lorentzian 与其他几何

微分几何包含多种分支：

- **Riemannian geometry**：正定度量；
- **pseudo-Riemannian geometry**：非退化不定号度量；
- **Lorentzian geometry**：一个时间型方向与其余空间型方向；
- **symplectic geometry**：闭且非退化的二形式；
- **complex geometry**：复结构与全纯坐标；
- **Kähler geometry**：度量、辛结构和复结构彼此相容；
- **conformal geometry**：只关心度量的局部尺度等价类；
- **bundle geometry**：研究向量丛、主丛及其联络和曲率。

弦理论会同时使用其中多种结构。

## 一个完整计算流程：极坐标平面

从坐标变换开始：

$$
x=r\cos\theta,
\qquad
y=r\sin\theta.
$$

第一步，计算度量：

$$
ds^2=dr^2+r^2d\theta^2.
$$

第二步，计算非零 Levi-Civita 联络系数：

$$
\Gamma^r{}_{\theta\theta}=-r,
$$

$$
\Gamma^\theta{}_{r\theta}
=\Gamma^\theta{}_{\theta r}
=\frac1r.
$$

第三步，计算曲率，例如：

$$
R^r{}_{\theta r\theta}=0.
$$

结论：度量分量随位置变化、联络系数非零，但几何仍平直。

这体现微分几何的核心方法：区分坐标表达与坐标无关结论。

## 一个完整计算流程：二维球面

半径 $a$ 的球面度量为：

$$
ds^2=a^2d\theta^2
+a^2\sin^2\theta\,d\phi^2.
$$

其 Gaussian 曲率为：

$$
K=\frac1{a^2}.
$$

标量曲率为：

$$
R=\frac2{a^2}.
$$

半径越大，曲率越小；在很小局部区域内，大球面更接近平面。

## 微分几何中的典型证明策略

### 先在一点选方便坐标

对 Levi-Civita 联络，可在一点选正规坐标，使：

$$
g_{ij}(p)=
\begin{cases}
\delta_{ij},&\text{Riemannian 情形},\\
\eta_{ij},&\text{pseudo-Riemannian 情形},
\end{cases}
\qquad
\partial_k g_{ij}(p)=0,
\qquad
\Gamma^k{}_{ij}(p)=0.
$$

但 $\partial_\ell\Gamma^k{}_{ij}(p)$ 通常不为零，所以曲率仍可非零。

这种方法可在一点简化计算，但不能把只在该点成立的坐标性质误当成整个邻域的事实。

### 证明对象是张量后再选坐标

若一个等式两边都是张量，可在每一点选择最方便的坐标验证。一旦张量等式在该点的一组基中成立，它就在所有坐标中成立。

前提是必须先确认所比较的量确实是张量。

### 使用自然性

许多几何运算与拉回相容，例如：

$$
d(\phi^*\omega)=\phi^*(d\omega).
$$

这种关系称为自然性。它能减少坐标计算，并说明定义不依赖任意选择。

## 维数分析与坐标分析

复杂公式中可做两类快速检查：

1. **指标检查**：等式两边自由指标的数量、位置和类型必须匹配；
2. **量纲检查**：物理公式各项应具有相同物理量纲。

例如：

$$
\nabla_iY^k
=\partial_iY^k+\Gamma^k{}_{ij}Y^j
$$

两项都带自由指标 $i,k$。

指标检查不能证明公式正确，但能发现许多排版和推导错误。

## 世界面几何

弦扫过二维世界面 $\Sigma$，局部坐标常记为：

$$
\sigma^a=(\tau,\sigma).
$$

世界面可带独立度量：

$$
h_{ab}(\sigma).
$$

由它可定义：

- 世界面 Levi-Civita 联络；
- 世界面曲率 $R^{(2)}(h)$；
- 面积元 $\sqrt{|h|}\,d^2\sigma$；
- Laplace–Beltrami 算符；
- 共形结构和 Weyl 变换。

世界面指标通常用 $a,b,c$ 表示。

## 目标时空几何

弦嵌入映射：

$$
X:\Sigma\to M
$$

的分量写成：

$$
X^\mu(\sigma).
$$

目标时空 $M$ 可带度量：

$$
G_{\mu\nu}(X),
$$

以及 $B$ 场、dilaton 和其他背景场。

目标时空指标通常用 $\mu,\nu,\rho$ 表示，不能与世界面指标混用。

## 两套几何怎样同时出现

对象：

$$
\partial_aX^\mu
$$

既带世界面下标 $a$，又带目标时空上标 $\mu$。

其完整协变导数包含两套联络：

$$
\nabla_b(\partial_aX^\mu)
=\partial_b\partial_aX^\mu
-\Gamma^c{}_{ba}(h)\partial_cX^\mu
+\Gamma^\mu{}_{\nu\rho}(G)
\partial_bX^\nu\partial_aX^\rho.
$$

每套联络只修正属于相应空间的指标。

## Polyakov 作用量中的几何

玻色弦在背景度量中的 Polyakov 作用量常写为：

$$
S_P
=-\frac{T}{2}
\int d^2\sigma\,
\sqrt{-h}\,
h^{ab}G_{\mu\nu}(X)
\partial_aX^\mu\partial_bX^\nu
$$

在一种 Lorentzian 世界面约定下成立。

其中：

- $h_{ab}$ 测量世界面内部几何；
- $G_{\mu\nu}$ 测量目标时空几何；
- $\partial_aX^\mu$ 连接两套几何；
- 指标缩并使被积函数成为坐标标量密度的一部分。

整体符号和归一化依约定变化。

## 为什么二维共形几何重要

二维中，局部坐标变换与 Weyl 缩放能大幅简化世界面度量。

在经典 Polyakov 理论中：

$$
h_{ab}\mapsto e^{2\omega(\sigma)}h_{ab}
$$

是局部 Weyl 对称。

量子层面能否保持这一对称取决于共形反常。对平直背景中的临界玻色弦与 RNS 超弦，量子 Weyl 一致性要求把物质场和 ghost 场都计入后，总中心荷满足相应抵消条件，分别得到临界维数 $D=26$ 与 $D=10$。

在一般背景中，更完整的条件是各 Weyl 反常系数或 sigma model beta 函数消失，而不只是检查一个中心荷数字。非临界弦还可由 Liouville 模式、线性 dilaton 或额外世界面共形场论补偿中心荷亏损。

因此二维微分几何是进入共形场论和弦量子化的桥梁。

## 紧致化中的几何

弦理论常把高维时空近似写成非紧致时空与紧致内部空间的组合。

内部空间的：

- 度量和曲率；
- holonomy；
- 复结构与 Kähler 结构；
- 调和形式与上同调；
- 周期、通量和特征类；

会影响低维有效理论中的场、耦合和对称性。

但仅知道拓扑或 Betti 数通常不足以确定完整物理谱，还要结合动力学、背景场、边界条件和量子修正。

## 规范场的几何语言

规范势可视为主丛联络的局部表示：

$$
A=A_\mu dx^\mu.
$$

场强是联络曲率：

$$
F=dA+A\wedge A
$$

在一种常见矩阵约定下成立。

这表明规范理论中的“补偿普通导数”与时空联络的机制相似，但它们作用在不同向量丛和不同指标上。

## 适用条件与事实边界

1. 微分几何是研究光滑空间及其几何结构的学科，不是单一公式或单一张量。
2. 拓扑规定连续性与整体结构；光滑结构允许求导；度量进一步加入长度和角度。
3. 切空间是一点附近的一阶线性近似，不等于流形在有限区域内真的平坦。
4. 坐标分量依赖坐标，几何对象本身不依赖坐标描述。
5. 不同点的切向量没有天然比较；联络是附加结构。
6. 度量不自动唯一确定任意联络；无挠和度量相容共同确定 Levi-Civita 联络。
7. 联络系数非零不等于曲率非零，某一点系数为零也不等于曲率为零。
8. 外微分和 Lie 导数不需要联络；协变导数需要联络。
9. 内蕴曲率只依赖流形自身的几何数据，外蕴曲率还依赖嵌入方式。
10. 测地线是局部几何概念，一般不保证全局最短。
11. Gauss–Bonnet 等局部到整体定理需要紧致性、定向和边界等条件。
12. 世界面与目标时空是不同流形，各有自己的度量、联络和曲率。
13. 规范联络与时空联络共享几何框架，但作用对象和变换群不同。
14. 弦论背景方程常来自微扰量子一致性条件；具体表达依阶数、场重定义和背景内容。

## 常见误区

1. **微分几何只是研究画出来的曲面。** 它研究任意维光滑流形、向量丛、联络和多种几何结构。
2. **流形局部像 $\mathbb R^n$，所以局部曲率一定为零。** “像”指拓扑和光滑坐标，不表示度量平直。
3. **坐标变了，几何对象也变了。** 通常只是分量表达改变。
4. **度量张量就是两点距离函数。** 度量张量逐点作用在切向量上，正定时可进一步诱导距离。
5. **不同点的向量可以直接相减。** 需要联络、平行移动或其他识别规则。
6. **联络系数是张量。** 它有非齐次坐标变换项。
7. **$\Gamma\ne0$ 就有曲率。** 极坐标平面是反例。
8. **测地线永远是两点间最短路径。** 它只满足局部驻值方程，越过共轭点后可能不再最短。
9. **外微分就是协变导数。** 外微分只作用于微分形式且不需联络；协变导数作用于一般张量并依赖联络。
10. **内蕴弯曲与嵌入后的外观弯曲相同。** 圆柱面外观看起来弯，但内蕴 Gaussian 曲率为零。
11. **世界面度量等于目标时空度量。** 二者定义在不同流形上。
12. **所有微分几何结论都适用于任意号差、任意联络。** 正定性、无挠、度量相容和定向等条件必须逐条核对。

## 练习

1. 拓扑、光滑结构和度量分别增加了什么信息？
2. 为什么切空间称为流形的一阶线性近似？
3. 函数微分 $df_p$ 属于什么空间？
4. 没有度量时，为什么不能自然把 $df$ 当作梯度向量？
5. 光滑映射 $\phi:M\to N$ 怎样作用在切向量和微分形式上？
6. 写出诱导度量的局部公式。
7. 度量张量与两点距离函数有何区别？
8. 为什么不同点的切向量不能天然相减？
9. 写出向量与一形式的协变导数公式。
10. Levi-Civita 联络满足哪两个条件？
11. 写出测地线方程。
12. 曲率和挠率分别测量什么？
13. 为什么极坐标平面中 $\Gamma\ne0$ 但曲率为零？
14. 外微分满足哪两个核心代数性质？
15. Stokes 定理怎样统一内部微分与边界积分？
16. Lie 导数与协变导数的主要区别是什么？
17. 内蕴几何与外蕴几何有何区别？
18. 半径 $a$ 的二维球面的 $K$ 和 $R$ 分别是多少？
19. 为什么正规坐标中 $\Gamma(p)=0$ 仍不能推出 $R(p)=0$？
20. 世界面指标和目标时空指标为何需要两套联络？
21. Polyakov 作用量中的 $h_{ab}$ 与 $G_{\mu\nu}$ 分别测量什么？
22. 规范场强为什么可称为联络曲率？

## 答案与提示

1. 拓扑给连续性和整体邻接；光滑结构允许微积分；度量加入长度、角度和相应体积结构。
2. 曲线在一点的速度构成线性空间，并捕捉函数在该点的一阶变化。
3. $df_p\in T_p^*M$。
4. $df$ 是余切向量；把它转成切向量需要非退化度量提供升指标映射。
5. 切向量由 $d\phi_p$ 推前，微分形式由 $\phi^*$ 拉回。
6. $g_{ab}=G_{\mu\nu}(X)\partial_aX^\mu\partial_bX^\nu$。
7. 前者作用于同一点的切向量；后者作用于两点。Riemannian 度量可通过曲线长度诱导距离。
8. 它们属于不同的切空间，需要额外比较规则。
9. $\nabla_iY^k=\partial_iY^k+\Gamma^k{}_{ij}Y^j$；$\nabla_i\alpha_j=\partial_i\alpha_j-\Gamma^k{}_{ij}\alpha_k$。
10. 无挠和度量相容。
11. $\ddot x^k+\Gamma^k{}_{ij}\dot x^i\dot x^j=0$。
12. 挠率比较一阶反对称部分与 Lie 括号；曲率比较二阶协变导数顺序和平行移动路径依赖。
13. 非零系数来自极坐标基变化；代入曲率公式后导数项与二次项抵消。
14. $d^2=0$；$d(\alpha\wedge\beta)=d\alpha\wedge\beta+(-1)^p\alpha\wedge d\beta$。
15. $\int_Md\omega=\int_{\partial M}\omega$，在满足定向、次数和收敛条件时成立。
16. 协变导数依赖联络；Lie 导数依赖向量场的流而不需要联络。
17. 内蕴几何只用流形自身数据；外蕴几何还研究流形在环境空间中的嵌入。
18. $K=1/a^2$，$R=2/a^2$。
19. 曲率还含 $\partial\Gamma$，而正规坐标只让该点的 $\Gamma$ 消失。
20. 两类指标属于不同流形的切丛，各自按不同坐标变换，因此要分别补偿。
21. $h_{ab}$ 测量世界面内禀几何；$G_{\mu\nu}$ 测量目标时空几何。
22. 主丛或关联向量丛联络的平方给出曲率，局部写成 $F=dA+A\wedge A$。

## 与其他概念的关系

- [[拓扑空间]] 提供连续性、连通性和整体结构；
- [[流形]] 是微分几何的基本空间；
- [[切空间]] 与 [[余切空间]] 给出局部线性模型；
- [[张量积]] 构造度量、微分形式和曲率等张量；
- [[向量场]] 描述流动、方向导数和无穷小对称；
- [[微分形式]] 与 [[外微分]] 统一积分和局部到整体结构；
- [[上同调]] 记录闭形式不能全局写成恰当形式的障碍；
- [[度量]] 加入长度、角度、体积和指标升降；
- [[联络]] 与 [[协变导数]] 定义跨点比较和张量微分；
- [[曲率]] 测量联络的非平直性；
- [[黎曼曲面]] 把复分析、二维微分几何和拓扑联系起来；
- [[群作用]] 与 [[模空间]] 组织对称性和几何结构的等价类；
- [[广义相对论]] 用 Lorentzian 微分几何描述引力；
- [[规范理论]] 用主丛联络和曲率描述规范场；
- [[世界面几何]] 是弦作用量和量子化的二维几何基础；
- [[非线性 sigma model]] 把世界面场映入弯曲目标流形；
- [[紧致化]] 使用内部空间的度量、holonomy、复结构和上同调。

## 参考资料

- John M. Lee, *Introduction to Smooth Manifolds*, 2nd ed., Springer, 2013.
- John M. Lee, *Introduction to Riemannian Manifolds*, 2nd ed., Springer, 2018.
- Manfredo P. do Carmo, *Riemannian Geometry*, Birkhäuser, 1992.
- Barrett O'Neill, *Semi-Riemannian Geometry*, Academic Press, 1983.
- Michael Spivak, *A Comprehensive Introduction to Differential Geometry*, 3rd ed., Publish or Perish, 1999.
- Shoshichi Kobayashi and Katsumi Nomizu, *Foundations of Differential Geometry, Vol. 1*, Wiley, 1963.
- Loring W. Tu, *An Introduction to Manifolds*, 2nd ed., Springer, 2011.
- Mikio Nakahara, *Geometry, Topology and Physics*, 2nd ed., CRC Press, 2003.
- Theodore Frankel, *The Geometry of Physics*, 3rd ed., Cambridge University Press, 2012.
- Robert M. Wald, *General Relativity*, University of Chicago Press, 1984.
- Sean M. Carroll, *Spacetime and Geometry*, Cambridge University Press, 2019.
- Joseph Polchinski, *String Theory, Vol. 1*, Cambridge University Press, 1998.
- Barton Zwiebach, *A First Course in String Theory*, 2nd ed., Cambridge University Press, 2009.
- David Tong, *String Theory*, arXiv:0908.0333, [arXiv](https://arxiv.org/abs/0908.0333)
